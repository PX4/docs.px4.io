import{_ as e,a,b as t,c as r}from"./chunks/thermal_calibration_baro.BAbPqO8O.js";import{_ as o,c as i,o as n,ab as s}from"./chunks/framework.CUflZczI.js";const C=JSON.parse('{"title":"Thermal Calibration and Compensation","description":"","frontmatter":{},"headers":[],"relativePath":"en/advanced_config/sensor_thermal_calibration.md","filePath":"en/advanced_config/sensor_thermal_calibration.md"}'),l={name:"en/advanced_config/sensor_thermal_calibration.md"},c=s('<h1 id="thermal-calibration-and-compensation" tabindex="-1">Thermal Calibration and Compensation <a class="header-anchor" href="#thermal-calibration-and-compensation" aria-label="Permalink to &quot;Thermal Calibration and Compensation&quot;">​</a></h1><p>PX4 contains functionality to calibrate and compensate accelerometer, gyro, magnetometer, and barometric pressure sensors for the effect of changing sensor temperature on sensor bias.</p><p>This topic details the <a href="#test_setup">test environment</a> and <a href="#calibration_procedures">calibration procedures</a>. At the end there is a description of the <a href="#implementation">implementation</a>.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>After thermal calibration, the thermal calibration parameters (<code>TC_*</code>) are used for <em>all</em> calibration/compensation of the respective sensors. Any subsequent standard calibration will therefore update <code>TC_*</code> parameters and not the &quot;normal&quot; <code>SYS_CAL_*</code> calibration parameters (and in some cases these parameters may be reset).</p></div><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Releases up to PX4 v1.14, do not support thermal calibration of the magnetometer.</p></div><p><a id="test_setup"></a></p><h2 id="test-setup-best-practice" tabindex="-1">Test Setup/Best Practice <a class="header-anchor" href="#test-setup-best-practice" aria-label="Permalink to &quot;Test Setup/Best Practice&quot;">​</a></h2><p>The <a href="#calibration_procedures">calibration procedures</a> described in the following sections are ideally run in an <em>environmental chamber</em> (a temperature and humidity controlled environment) as the board is heated from the lowest to the highest operating/calibration temperature. Before starting the calibration, the board is first <em>cold soaked</em> (cooled to the minimum temperature and allowed to reach equilibrium).</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Active electric heating elements will affect the magnetometer calibration values. Ensure that heating elements are either inactive or sufficiently far from the sensor to avoid injecting noise into the magnetometer calibration.</p></div><p>For the cold soak you can use a regular home freezer to achieve -20C, and commercial freezers can achieve of the order of -40C. The board should be placed in a ziplock/anti-static bag containing a silica packet, with a power lead coming out through a sealed hole. After the cold soak the bag can be moved to the test environment and the test continued in the same bag.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The bag/silica is to prevent condensation from forming on the board.</p></div><p>It possible to perform the calibration without a commercial-grade environment chamber. A simple environment container can be created using a styrofoam box with a very small internal volume of air. This allows the autopilot to self-heat the air relatively quickly (be sure that the box has a small hole to equalize to ambient room pressure, but still be able to heat up inside).</p><p>Using this sort of setup it is possible to heat a board to ~70C. Anecdotal evidence suggests that many common boards can be heated to this temperature without adverse side effects. If in doubt, check the safe operating range with your manufacturer.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>To check the status of the onboard thermal calibration use the MAVlink console (or NuttX console) to check the reported internal temp from the sensor.</p></div><p><a id="calibration_procedures"></a></p><h2 id="calibration-procedures" tabindex="-1">Calibration Procedures <a class="header-anchor" href="#calibration-procedures" aria-label="Permalink to &quot;Calibration Procedures&quot;">​</a></h2><p>PX4 supports two calibration procedures:</p><ul><li><a href="#onboard_calibration">onboard</a> - calibration is run on the board itself. This method requires knowledge of the amount of temperature rise that is achievable with the test setup.</li><li><a href="#offboard_calibration">offboard</a> - compensation parameters are calculated on a development computer based on log information collected during the calibration procedure. This method allows users to visually check the quality of the data and curve-fit.</li></ul><p>The offboard approach is more complex and slower, but requires less knowledge of the test setup and is easier to validate.</p><p><a id="onboard_calibration"></a></p><h3 id="onboard-calibration-procedure" tabindex="-1">Onboard Calibration Procedure <a class="header-anchor" href="#onboard-calibration-procedure" aria-label="Permalink to &quot;Onboard Calibration Procedure&quot;">​</a></h3><p>Onboard calibration is run entirely on the device. It require knowledge of the amount of temperature rise that is achievable with the test setup.</p><p>To perform and onboard calibration:</p><ol><li>Ensure the frame type is set before calibration, otherwise calibration parameters will be lost when the board is setup.</li><li>Power the board and set the <code>SYS_CAL_*</code> parameters to 1 to enable calibration of the required sensors at the next startup. [^1]</li><li>Set the <a href="./../advanced_config/parameter_reference.html#SYS_CAL_TDEL">SYS_CAL_TDEL</a> parameter to the number of degrees of temperature rise required for the onboard calibrator to complete. If this parameter is too small, then the calibration will complete early and the temperature range for the calibration will not be sufficient to compensate when the board is fully warmed up. If this parameter is set too large, then the onboard calibrator will never complete. allowance should be made for the rise in temperature due to the boards self heating when setting this parameter. If the amount of temperature rise at the sensors is unknown, then the off-board method should be used.</li><li>Set the <a href="./../advanced_config/parameter_reference.html#SYS_CAL_TMIN">SYS_CAL_TMIN</a> parameter to the lowest temperature data that you want the calibrator to use. This enables a lower cold soak ambient temperature to be used to reduce the cold soak time whilst maintaining control over the calibration minimum temperature. The data for a sensor will not be used by the calibrator if it is below the value set by this parameter.</li><li>Set the <a href="./../advanced_config/parameter_reference.html#SYS_CAL_TMAX">SYS_CAL_TMAX</a> parameter to the highest starting sensor temperature that should be accepted by the calibrator. If the starting temperature is higher than the value set by this parameter, the calibration will exit with an error. Note that if the variation in measured temperature between different sensors exceeds the gap between <code>SYS_CAL_TMAX</code> and <code>SYS_CAL_TMIN</code>, then it will be impossible for the calibration to start.</li><li>Remove power and cold soak the board to below the starting temperature specified by the <code>SYS_CAL_TMIN</code> parameter. Note that there is a 10 second delay on startup before calibration starts to allow any sensors to stabilise and the sensors will warm internally during this period.</li><li>Keeping the board stationary[^2], apply power and warm to a temperature high enough to achieve the temperature rise specified by the <code>SYS_CAL_TDEL</code> parameter. The completion percentage is printed to the system console during calibration. [^3]</li><li>When the calibration completes, remove power, allow the board to cool to a temperature that is within the calibration range before performing the next step.</li><li>Perform a 6-point accel calibration via the system console using <code>commander calibrate accel</code> or via <em>QGroundControl</em>. If the board is being set-up for the first time, the gyro and magnetometer calibration will also need to be performed.</li><li>The board should always be re-powered before flying after any sensor calibration, because sudden offset changes from calibration can upset the navigation estimator and some parameters are not loaded by the algorithms that use them until the next startup.</li></ol><p><a id="offboard_calibration"></a></p><h3 id="offboard-calibration-procedure" tabindex="-1">Offboard Calibration Procedure <a class="header-anchor" href="#offboard-calibration-procedure" aria-label="Permalink to &quot;Offboard Calibration Procedure&quot;">​</a></h3><p>Offboard calibration is run on a development computer using data collected during the calibration test. This method provides a way to visually check the quality of data and curve fit.</p><p>To perform an offboard calibration:</p><ol><li><p>Ensure the frame type is set before calibration, otherwise calibration parameters will be lost when the board is setup.</p></li><li><p>Power up the board and set the <a href="./../advanced_config/parameter_reference.html#TC_A_ENABLE">TC_A_ENABLE</a>, <a href="./../advanced_config/parameter_reference.html#TC_B_ENABLE">TC_B_ENABLE</a>, <a href="./../advanced_config/parameter_reference.html#TC_G_ENABLE">TC_G_ENABLE</a>, and <a href="./../advanced_config/parameter_reference.html#TC_M_ENABLE">TC_M_ENABLE</a> parameters to <code>1</code>.</p></li><li><p>Set all <a href="./../advanced_config/parameter_reference.html#CAL_ACC0_ID">CAL_ACC*</a>, <a href="./../advanced_config/parameter_reference.html#CAL_GYRO0_ID">CAL_GYRO*</a>, <a href="./../advanced_config/parameter_reference.html#CAL_MAG0_ID">CAL_MAG*</a>, and <a href="./../advanced_config/parameter_reference.html#CAL_BARO0_ID">CAL_BARO*</a> parameters to defaults.</p></li><li><p>Set the <a href="./../advanced_config/parameter_reference.html#SDLOG_MODE">SDLOG_MODE</a> parameter to 2 to enable logging of data from boot.</p></li><li><p>Set the <a href="./../advanced_config/parameter_reference.html#SDLOG_PROFILE">SDLOG_PROFILE</a> checkbox for <em>thermal calibration</em> (bit 2) to log the raw sensor data required for calibration.</p></li><li><p>Cold soak the board to the minimum temperature it will be required to operate in.</p></li><li><p>Apply power and keeping the board still [^2], warm it slowly to the maximum required operating temperature. [^3]</p></li><li><p>Remove power and extract the .ulog file.</p></li><li><p>Open a terminal window in the <strong>Firmware/Tools</strong> directory and run the python calibration script:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> process_sensor_caldata.py</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">full</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> to</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .ulog</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fil</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>This will generate a <strong>.pdf</strong> file showing the measured data and curve fits for each sensor, and a <strong>.params</strong> file containing the calibration parameters.</p></li><li><p>Power the board, connect <em>QGroundControl</em> and load the parameter from the generated <strong>.params</strong> file onto the board using <em>QGroundControl</em>. Due to the number of parameters, loading them may take some time.</p></li><li><p>After parameters have finished loading, set <code>SDLOG_MODE</code> to 1 to re-enable normal logging and remove power.</p></li><li><p>Power the board and perform a normal accelerometer sensor calibration using <em>QGroundControl</em>. It is important that this step is performed when board is within the calibration temperature range. The board must be repowered after this step before flying as the sudden offset changes can upset the navigation estimator and some parameters are not loaded by the algorithms that use them until the next startup.</p></li></ol><p><a id="implementation"></a></p><h2 id="implementation-detail" tabindex="-1">Implementation Detail <a class="header-anchor" href="#implementation-detail" aria-label="Permalink to &quot;Implementation Detail&quot;">​</a></h2><p>Calibration refers to the process of measuring the change in sensor value across a range of internal temperatures, and performing a polynomial fit on the data to calculate a set of coefficients (stored as parameters) that can be used to correct the sensor data. Compensation refers to the process of using the internal temperature to calculate an offset that is subtracted from the sensor reading to correct for changing offset with temperature</p><p>The accelerometer, gyro, and magnetometer sensor offsets are calculated using a 3rd order polynomial, whereas the barometric pressure sensor offset is calculated using a 5th order polynomial. Example fits are show below:</p><p><img src="'+e+'" alt="Thermal calibration accel"></p><p><img src="'+a+'" alt="Thermal calibration gyro"></p><p><img src="'+t+'" alt="Thermal calibration mag"></p><p><img src="'+r+'" alt="Thermal calibration barometer"></p><h3 id="calibration-parameter-storage" tabindex="-1">Calibration Parameter Storage <a class="header-anchor" href="#calibration-parameter-storage" aria-label="Permalink to &quot;Calibration Parameter Storage&quot;">​</a></h3><p>With the existing parameter system implementation we are limited to storing each value in the struct as a separate entry. To work around this limitation the following logical naming convention is used for the <a href="./../advanced_config/parameter_reference.html#thermal-compensation">thermal compensation parameters</a>:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TC_[type][instance]_[cal_name]_[axis]</span></span></code></pre></div><p>Where:</p><ul><li><p><code>type</code>: is a single character indicating the type of sensor where <code>A</code> = accelerometer, <code>G</code> = rate gyroscope, <code>M</code> = magnetometer, and <code>B</code> = barometer.</p></li><li><p><code>instance</code>: is an integer 0,1 or 2 allowing for calibration of up to three sensors of the same <code>type</code>.</p></li><li><p><code>cal_name</code>: is a string identifying the calibration value. It has the following possible values:</p><ul><li><code>Xn</code>: Polynomial coefficient where n is the order of the coefficient, e.g. <code>X3 * (temperature - reference temperature)**3</code>.</li><li><code>SCL</code>: scale factor.</li><li><code>TREF</code>: reference temperature (deg C).</li><li><code>TMIN</code>: minimum valid temperature (deg C).</li><li><code>TMAX</code>: maximum valid temperature (deg C).</li></ul></li><li><p><code>axis</code>: is an integer 0,1 or 2 indicating that the calibration data is for X,Y or Z axis in the board frame of reference. For the barometric pressure sensor, the <code>axis</code> suffix is omitted.</p></li></ul><p>Examples:</p><ul><li><a href="./../advanced_config/parameter_reference.html#TC_A1_TREF">TC_A1_TREF</a> is the reference temperature for the second accelerometer.</li><li><a href="./../advanced_config/parameter_reference.html#TC_G0_X3_0">TC_G0_X3_0</a> is the <code>^3</code> coefficient for the first gyro x-axis.</li></ul><h3 id="calibration-parameter-usage" tabindex="-1">Calibration Parameter Usage <a class="header-anchor" href="#calibration-parameter-usage" aria-label="Permalink to &quot;Calibration Parameter Usage&quot;">​</a></h3><p>The correction for thermal offsets (using the calibration parameters) is performed in the <a href="./../modules/modules_system.html#sensors">sensors module</a>. The reference temperature is subtracted from the measured temperature to obtain a delta temperature where:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>delta = measured_temperature - reference_temperature</span></span></code></pre></div><p>The delta temperature is then used to calculate a offset, where:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>offset = X0 + X1*delta + X2*delta**2 + ... + Xn*delta**n</span></span></code></pre></div><p>The offset and temperature scale factor are then used to correct the sensor measurement where:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>corrected_measurement = (raw_measurement - offset) * scale_factor</span></span></code></pre></div><p>If the temperature is above the test range set by the <code>*_TMIN</code> and <code>*_TMAX</code> parameters, then the measured temperature will be clipped to remain within the limits.</p><p>Correction of the accelerometer, gyroscope, magnetometer, or barometer data is enabled by setting <a href="./../advanced_config/parameter_reference.html#TC_A_ENABLE">TC_A_ENABLE</a>, <a href="./../advanced_config/parameter_reference.html#TC_G_ENABLE">TC_G_ENABLE</a>, <a href="./../advanced_config/parameter_reference.html#TC_M_ENABLE">TC_M_ENABLE</a>, or <a href="./../advanced_config/parameter_reference.html#TC_B_ENABLE">TC_B_ENABLE</a> parameters to 1 respectively.</p><h3 id="compatibility-with-legacy-cal-parameters-and-commander-controlled-calibration" tabindex="-1">Compatibility with legacy <code>CAL_*</code> parameters and commander controlled calibration <a class="header-anchor" href="#compatibility-with-legacy-cal-parameters-and-commander-controlled-calibration" aria-label="Permalink to &quot;Compatibility with legacy `CAL_*` parameters and commander controlled calibration&quot;">​</a></h3><p>The legacy temperature-agnostic PX4 rate gyro and accelerometer sensor calibration is performed by the commander module and involves adjusting offset, and in the case of accelerometer calibration, scale factor calibration parameters. The offset and scale factor parameters are applied within the driver for each sensor. These parameters are found in the <a href="./../advanced_config/parameter_reference.html#sensor-calibration">CAL parameter group</a>.</p><p>Onboard temperature calibration is controlled by the events module and the corrections are applied within the sensors module before the sensor combined uORB topic is published. This means that if thermal compensation is being used, all of the corresponding legacy offset and scale factor parameters must be set to defaults of zero and unity before a thermal calibration is performed. If an on-board temperature calibration is performed, this will be done automatically, however if an offboard calibration is being performed it is important that the legacy <code>CAL*OFF</code> and <code>CAL*SCALE</code> parameters be reset before calibration data is logged.</p><p>If accel thermal compensation has been enabled by setting the <code>TC_A_ENABLE</code> parameter to 1, then the commander controlled 6-point accel calibration can still be performed. However, instead of adjusting the <code>*OFF</code> and <code>*SCALE</code> parameters in the <code>CAL</code> parameter group, these parameters are set to defaults and the thermal compensation <code>X0</code> and <code>SCL</code> parameters are adjusted instead.</p><p>If gyro thermal compensation has been enabled by setting the <code>TC_G_ENABLE</code> parameter to 1, then the commander controlled gyro calibration can still be performed, however it will be used to shift the compensation curve up or down by the amount required to zero the angular rate offset. It achieves this by adjusting the X0 coefficients.</p><p>If magnetometer thermal compensation has been enabled by setting the <code>TC_M_ENABLE</code> parameter to 1, then the commander controlled 6-point accel calibration can still be performed. However, instead of adjusting the <code>*OFF</code> and <code>*SCALE</code> parameters in the <code>CAL</code> parameter group, these parameters are set to defaults and the thermal compensation <code>X0</code> and <code>SCL</code> parameters are adjusted instead.</p><h3 id="limitations" tabindex="-1">Limitations <a class="header-anchor" href="#limitations" aria-label="Permalink to &quot;Limitations&quot;">​</a></h3><p>Scale factors are assumed to be temperature invariant due to the difficulty associated with measuring these at different temperatures. This limits the usefulness of the accelerometer calibration to those sensor models with stable scale factors. In theory with a thermal chamber or IMU heater capable of controlling IMU internal temperature to within a degree, it would be possible to perform a series of 6 sided accelerometer calibrations and correct the accelerometers for both offset and scale factor. Due to the complexity of integrating the required board movement with the calibration algorithm, this capability has not been included.</p><hr><p>[^1]: The <a href="./../advanced_config/parameter_reference.html#SYS_CAL_ACCEL">SYS_CAL_ACCEL</a>, <a href="./../advanced_config/parameter_reference.html#SYS_CAL_BARO">SYS_CAL_BARO</a> and <a href="./../advanced_config/parameter_reference.html#SYS_CAL_GYRO">SYS_CAL_GYRO</a> parameters are reset to 0 when the calibration is started. [^2]: Calibration of the barometric pressure sensor offsets requires a stable air pressure environment. The air pressure will change slowly due to weather and inside buildings can change rapidly due to external wind fluctuations and HVAC system operation. [^3]: Care must be taken when warming a cold soaked board to avoid formation of condensation on the board that can cause board damage under some circumstances.</p>',63),d=[c];function h(p,m,f,u,b,g){return n(),i("div",null,d)}const y=o(l,[["render",h]]);export{C as __pageData,y as default};
