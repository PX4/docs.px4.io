import{_ as e,c as a,o as s,ab as t}from"./chunks/framework.CUflZczI.js";const k=JSON.parse('{"title":"Gazebo Models Repository (PX4-gazebo-models)","description":"","frontmatter":{},"headers":[],"relativePath":"zh/sim_gazebo_gz/gazebo_models.md","filePath":"zh/sim_gazebo_gz/gazebo_models.md"}'),i={name:"zh/sim_gazebo_gz/gazebo_models.md"},o=t('<h1 id="gazebo-models-repository-px4-gazebo-models" tabindex="-1">Gazebo Models Repository (PX4-gazebo-models) <a class="header-anchor" href="#gazebo-models-repository-px4-gazebo-models" aria-label="Permalink to &quot;Gazebo Models Repository (PX4-gazebo-models)&quot;">​</a></h1><p>The <a href="https://github.com/PX4/PX4-gazebo-models" target="_blank" rel="noreferrer">PX4-gazebo-models</a> repository is used to store all <a href="./../sim_gazebo_gz/">Gazebo</a> models and worlds that are supported by PX4.</p><ul><li>Models are stored in the <code>/models</code> directory and worlds are stored in the <code>/worlds</code> directory.</li><li>The <a href="https://github.com/PX4/PX4-gazebo-models/blob/main/simulation-gazebo" target="_blank" rel="noreferrer">simulation-gazebo</a> Python script is used for <a href="./../sim_gazebo_gz/#standalone-mode">starting Gazebo in standalone mode</a>.</li></ul><p>The <code>PX4-gazebo-models</code> repository is included in PX4 as a submodule, and all models are available by default when using the &quot;normal&quot; <code>make</code> targets, such as <code>make px4_sitl gz_x500</code>.</p><p>For Gazebo <a href="./../sim_gazebo_gz/#standalone-mode">standalone simulations</a> you first have obtain the <a href="https://github.com/PX4/PX4-gazebo-models/blob/main/simulation-gazebo" target="_blank" rel="noreferrer">simulation-gazebo</a> Python script, and then it will fetch the models and worlds to <code>~/.simulation-gazebo</code> if that directory is not present.</p><h2 id="simulation-gazebo-standalone-simulation-start-up-script" tabindex="-1">simulation-gazebo (Standalone Simulation Start-up Script) <a class="header-anchor" href="#simulation-gazebo-standalone-simulation-start-up-script" aria-label="Permalink to &quot;simulation-gazebo (Standalone Simulation Start-up Script)&quot;">​</a></h2><p>The <a href="https://github.com/PX4/PX4-gazebo-models/blob/main/simulation-gazebo" target="_blank" rel="noreferrer">simulation-gazebo</a> Python script is used for starting Gazebo in <a href="./../sim_gazebo_gz/#standalone-mode">standalone mode</a>. The script can communicate with a PX4 SITL instance on the same host by default. If the script arguments are set correctly, it can also communicate with any PX4 instance on any machine within the same network.</p><p>The <code>simulation-gazebo</code> script does not require any additional libraries and should work out of the box.</p><h3 id="basic-usage" tabindex="-1">Basic Usage <a class="header-anchor" href="#basic-usage" aria-label="Permalink to &quot;Basic Usage&quot;">​</a></h3><p>The default <code>simulation-script</code> can be run with:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> simulation-gazebo</span></span></code></pre></div><p>This will fetch the models and worlds from the <a href="https://github.com/PX4/PX4-gazebo-models" target="_blank" rel="noreferrer">PX4 gazebo models repository</a> into subfolders of the <code>~/.simulation-gazebo</code> directory the first time it is called (or more precisely, if the directory is not detected). A <em>gz-server</em> instance will then be launched using the default grey plane world.</p><p>The build system won&#39;t automatically update the local copy again if the <code>.simulation-gazebo</code> folder is detected, but you can force it to update to the latest models and vehicles by passing the <code>overwrite</code> flag to the script. The resulting command will look something like:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> simulation-gazebo</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --overwrite</span></span></code></pre></div><p>You can connect a PX4-enabled vehicle to an instance of <em>gz-server</em> using several approaches:</p><ul><li><p>In a new terminal, run PX4 using <code>PX4_GZ_STANDALONE=1 make px4_sitl gz_&lt;vehicle&gt;</code> and you will observe a vehicle appearing in Gazebo.</p></li><li><p>Gazebo also has its own, built-in &quot;resource spawner&quot;. It can be called up by clicking on the three dots in the top right corner of the Gazebo GUI. There enter &quot;resource spawner&quot; and, under &quot;Fuel resources&quot;, add the owner &quot;px4&quot;. You can then drag and drop any PX4 model into your simulation.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>These models are taken from an web-server called <a href="https://app.gazebosim.org/dashboard" target="_blank" rel="noreferrer">Gazebo Fuel</a>, which essentially acts as an online database for all types of models and worlds that can be launched in Gazebo.</p></div></li></ul><p>:::</p><p>After dropping the vehicle of your choice into Gazebo, launch PX4 SITL with:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PX4_SYS_AUTOSTART</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">airframe-number-of-choic</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PX4_GZ_MODEL_NAME</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">vehicle-of-choic</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ./build/px4_sitl_default/bin/px4</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">`</span></span></code></pre></div><p>This will connect PX4 SITL to the running instance of Gazebo.</p><p>All the functionality and flexibility that exists for PX4 is applicable and directly works in the Gazebo instance.</p><h3 id="command-line-arguments" tabindex="-1">Command line arguments <a class="header-anchor" href="#command-line-arguments" aria-label="Permalink to &quot;Command line arguments&quot;">​</a></h3><p>The following arguments can be passed to the <code>simulation-gazebo</code> script:</p><ul><li><p><code>--world</code> A string variable that names the sdf file which runs the simulation world. Default argument is &quot;default&quot;, which links to the default world.</p></li><li><p><code>--gz_partition</code> A string variable that sets the gazebo partition to run in (more information <a href="https://gazebosim.org/api/transport/13/envvars.html" target="_blank" rel="noreferrer">here</a>)</p></li><li><p><code>--gz_ip</code> A string variable that sets the IP of the outgoing network interface (more information <a href="https://gazebosim.org/api/transport/13/envvars.html" target="_blank" rel="noreferrer">here</a>)</p></li><li><p><code>--interactive</code> A boolean variable that requires the ability to run the code in interactive mode, allowing for custom paths for <code>--model_download_source</code>. If this is not set, <code>--model_download_source</code> will only download from the default Github repo.</p></li><li><p><code>--model_download_source</code> A string variable setting the path to a directory from where models are to be imported. At the moment this can only be a local file directory or a http address. The source should end with the zipped model file (for example: <a href="https://path/to/simulation/models/models.zip" target="_blank" rel="noreferrer">https://path/to/simulation/models/models.zip</a>).</p></li><li><p><code>--model_store</code> A string variable setting the path to the model storage directory. This is where the zip file provided in <code>model_download_source</code> will be placed.</p></li><li><p><code>--overwrite</code> A boolean variable indicating that the <code>.simulation-gazebo</code> should be updated with world and vehicle data from the gazebo model repository.</p></li><li><p><code>--dryrun</code> A boolean variable that can be set when running test cases. It will not provide any interactivity and will not start a gz-server instance.</p></li></ul><p>None of these arguments are required for <code>simulation-gazebo</code> to work. They are needed when you want to provide custom model downloads, other worlds, or you want to run Gazebo and PX4 on separate hosts.</p><h2 id="example-running-one-host-with-multiple-terminals" tabindex="-1">Example: Running One Host with Multiple Terminals <a class="header-anchor" href="#example-running-one-host-with-multiple-terminals" aria-label="Permalink to &quot;Example: Running One Host with Multiple Terminals&quot;">​</a></h2><p>This example explains how you can run standalone mode PX4 via two terminals on one host.</p><ol><li><p>In one terminal run</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PX4_GZ_STANDALONE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PX4_SYS_AUTOSTART</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4001</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PX4_SIM_MODEL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">gz_x500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PX4_GZ_WORLD</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">windy</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ./build/px4_sitl_default/bin/px4</span></span></code></pre></div></li><li><p>In a second terminal window run:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /path/to/simulation-gazebo</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --world</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> windy</span></span></code></pre></div></li></ol><p>No additional parameters have to be passed to the simulation-gazebo script in order for this example to work, as all Gazebo nodes run on the same host. See the example below for a more involved scenario with different hosts.</p><h2 id="example-running-multiple-hosts" tabindex="-1">Example: Running Multiple Hosts <a class="header-anchor" href="#example-running-multiple-hosts" aria-label="Permalink to &quot;Example: Running Multiple Hosts&quot;">​</a></h2><p>The following example will illustrate how you can set up a distributed system, running PX4 on one host on a network (called &quot;PX4-host&quot;) and Gazebo on another (called &quot;Gazebo-host&quot;). This will result in two Gazebo nodes running on two different hosts in the same network, and communicating using the <code>gz-transport</code> library.</p><p>We first have to figure out what IP address we can use to send out messages on both hosts.</p><p>On the PX4-host, run the following commands:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> iproute2</span></span></code></pre></div><p>Then type:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> a</span></span></code></pre></div><p>If you are connected to a network via WiFi, then the desired address will usually have a name along the lines of <code>wlp12345</code>. Record the IPv4 address for this interface by noting the number listed next to <code>inet</code>. It should be four numbers, separated by points and followed by a slash and another number. A valid IPv4 address would be for example: <code>192.168.24.89/24</code>.</p><p>You only require the first four numbers. So in the example&#39;s case, the IP address of the PX4-host would be: <code>192.168.24.89</code>. Note that if you are connected via Ethernet the network interface might start with <code>eth</code> or <code>en</code>, however this is not standardized.</p><p>Repeat the same procedure on the Gazebo-host and note down the second IPv4 address. For this example we will take <code>192.168.24.107</code>.</p><p>We can now start setting up both hosts. We first set up the PX4-host:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GZ_PARTITION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">relay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GZ_RELAY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">192.168.24.107</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GZ_IP</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">192.168.24.89</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PX4_GZ_STANDALONE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PX4_SYS_AUTOSTART</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4001</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PX4_SIM_MODEL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">gz_x500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PX4_GZ_WORLD</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">baylands</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ./build/px4_sitl_default/bin/px4</span></span></code></pre></div><p>Explanation of the environment variables:</p><ul><li><code>GZ_PARTITION</code> declares the partition name that the two gazebo nodes will run in. This <strong>has</strong> to be same across all connected notes.</li><li><code>GZ_RELAY</code> points to the target IP address on the other host (in this case the Gazebo-host). This environment variable is necessary to connect the two nodes to each other. Note that this connection is bidirectional, so <code>GZ_RELAY</code> only has to be set on one host.</li><li><code>GZ_IP</code> tells the current host what network interface to use to send out messages. This is required when advertising topics or services.</li></ul><p>We can then set up the Gazebo-host. Note that the actual setup order (PX4-host first or Gazebo-host first) is not actually important. Both will continuously look for other Gazebo nodes until they find one. On your Gazebo-host, in a terminal, run:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /path/to/simulation-gazebo</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --gz_partition</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> relay</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --gz_ip</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.24.107</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --world</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> baylands</span></span></code></pre></div><p>Here we pass same environmental variables as arguments. Note that the <code>--world</code> values must be the same in order to be able to connect. If you accidentally set <code>baylands</code> in one host and say <code>default</code> in the other, then the two nodes will not be able to connect.</p><p>If everything worked correctly, then the two hosts should now be connected and you should be able to fly your vehicle in the command line on the PX4-host.</p><p>Furthermore, you could also set up QGC and fly your vehicle that way. In addition it is also possible to connect multiple PX4-hosts to the same Gazebo-host by setting the <code>-i</code> flag as shown on the <a href="./multi_vehicle_simulation.html">multi-vehicle simulation</a> page. For more information, concerning the environment variables, you can also refer to the <a href="https://gazebosim.org/api/transport/12/envvars.html" target="_blank" rel="noreferrer">gz-transport documentation</a>.</p><p>A connection over VPN (and hence multiple networks) is also possible, but is currently not documented.</p><h2 id="workflow" tabindex="-1">Workflow <a class="header-anchor" href="#workflow" aria-label="Permalink to &quot;Workflow&quot;">​</a></h2><p>When a branch gets merged onto the main branch (this could be a model addition, deletion or a something like a parameter change), all models that have received any sort of change will automatically be updated and uploaded to the PX4 account on <a href="https://app.gazebosim.org/PX4" target="_blank" rel="noreferrer">Gazebo Fuel</a>. Furthermore, there is also a workflow that can be used to check the validity of any provided sdf file.</p>',51),n=[o];function l(h,r,d,p,c,u){return s(),a("div",null,n)}const m=e(i,[["render",l]]);export{k as __pageData,m as default};
