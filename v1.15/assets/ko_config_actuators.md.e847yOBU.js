import{_ as a,a as r,b as i,c as l,d as s,e as n,f as c,g as d,h,i as u,j as e,k as p,l as m,m as f,n as g,o as v,p as b,q as y,r as w,s as _,t as T}from"./chunks/reverse_dshot.DOJjvoGl.js";import{_ as q,E as C,c as P,J as S,m as t,a as A,ab as k,o as x}from"./chunks/framework.CUflZczI.js";const U=JSON.parse('{"title":"액추에이터 설정 및 테스트","description":"","frontmatter":{},"headers":[],"relativePath":"ko/config/actuators.md","filePath":"ko/config/actuators.md"}'),M={name:"ko/config/actuators.md"},O=t("h1",{id:"액추에이터-설정-및-테스트",tabindex:"-1"},[A("액추에이터 설정 및 테스트 "),t("a",{class:"header-anchor",href:"#액추에이터-설정-및-테스트","aria-label":'Permalink to "액추에이터 설정 및 테스트"'},"​")],-1),R=k('<p>The <em>Actuators Setup</em> view is used to customize the specific geometry of the vehicle, assign actuators and motors to flight controller outputs, and test the actuator and motor response.</p><h2 id="개요" tabindex="-1">개요 <a class="header-anchor" href="#개요" aria-label="Permalink to &quot;개요&quot;">​</a></h2><p>Open the view in <em>QGroundControl</em> here: <strong>&quot;Q&quot; (app menu) &gt; Vehicle Setup &gt; Actuators</strong> (tab). The displayed elements depend on the <a href="./../config/airframe.html">selected frame</a>, with outputs mapped by default as shown in the <a href="./../airframes/airframe_reference.html">Airframe Reference</a>.</p><p>The view has three sections:</p><ul><li><a href="#geometry">기하</a>: <a href="./../config/airframe.html">선택한 기체</a>의 기하 도형을 설정합니다. 여기에는 <a href="#motor-geometry">모터</a>의 수, 위치 및 속성과 <a href="#control-surfaces-geometry">제어 표면</a> 및 <a href="#motor-tilt-servo-geometry">모터 틸트 서보</a>의 수와 속성이 포함됩니다.</li><li><a href="#actuator-outputs">액추에이터 출력</a>: 모터, 제어 표면 및 기타 액추에이터를 특정 출력에 할당합니다.</li><li><a href="#actuator-testing">액추에이터 테스트</a>: 모터와 액추에이터가 예상대로 방향과 속도로 움직이는 지 테스트합니다.</li></ul><p>A quadcopter might have an setup screen similar to the one shown below. This defines a 4-rotor copter with X-geometry. It maps the 4 motors to the AUX1 to AUX4 outputs, and specifies that the connect to DShot1200 ESC. It also maps PWM400 AUX outputs for controlling a parachute and landing gear.</p><p><img src="'+a+'" alt="Actuators MC (QGC)"></p><p>지오메트리 섹션은 선택한 <a href="./../config/airframe.html">기체</a>에 대해 구성 가능한 지오메트리 관련 매개변수를 설정하는 데 사용됩니다. 여기에는 <a href="#motor-geometry">모터</a>의 수와 위치, <a href="#control-surfaces-geometry">제어 표면</a>의 수, 기능 및 속성이 포함됩니다. VTOL 틸트로터 차량의 경우 <a href="#motor-tilt-servo-geometry">틸트 서보</a>의 수와 속성도 포함됩니다.</p><h2 id="지오메트리" tabindex="-1">지오메트리 <a class="header-anchor" href="#지오메트리" aria-label="Permalink to &quot;지오메트리&quot;">​</a></h2><p>The geometry section is used to set any configurable geometry-related parameters for the selected <a href="./../config/airframe.html">airframe</a>. This includes the number and position of <a href="#motor-geometry">motors</a>, and the number, function, and properties of <a href="#control-surfaces-geometry">control surfaces</a>. For VTOL tiltrotor vehicles, it will also include the number and properties of <a href="#motor-tilt-servo-geometry">tilt servos</a></p><p>모터 지오메트리 섹션에서는 모터 수, 상대 위치 및 각 모터의 기타 속성을 설정합니다.</p><ul><li>선택한 기체 유형에 대한 <em>구성 가능</em> 필드만 표시됩니다. 기체에 대해 구성할 수 없는 필드는 숨겨져 있습니다.</li><li>모터 위치 다이어그램은 현재 멀티콥터 프레임에만 표시됩니다. :::</li></ul><h3 id="모터-지오메트리" tabindex="-1">모터 지오메트리 <a class="header-anchor" href="#모터-지오메트리" aria-label="Permalink to &quot;모터 지오메트리&quot;">​</a></h3><p>The motor geometry section lets you set the number of motors, the relative position, and other properties of each motor.</p><p>멀티콥터 기체의 기하학적 구성은 각 모터의 상대적 x,y 위치를 보여주는 다이어그램을 제공합니다. 다른 프레임의 모터 위치에 대한 광범위한 이해는 <a href="./../airframes/airframe_reference.html">기체 참조</a>를 참고하십시오. For example, <code>Tilted-by</code> and <code>axis</code> are only relevant for <a href="#motor-geometry-vtol-tiltrotor">Tiltrotor VTOL</a> and <a href="#motor-geometry-standard-vtol">Standard VTOL</a> vehicles, respectively.</p><p>The geometry configuration for multicopter airframes provides a diagram showing the relative x,y positions for each of the motors. See the <a href="./../airframes/airframe_reference.html">Airframe Reference</a> for an broad understanding of the motor positions for other frames.</p><p>Core geometry concepts and the configuration for a number of different frames are provided in the following sections.</p><h4 id="모터-지오메트리-멀티콥터" tabindex="-1">모터 지오메트리: 멀티콥터 <a class="header-anchor" href="#모터-지오메트리-멀티콥터" aria-label="Permalink to &quot;모터 지오메트리: 멀티콥터&quot;">​</a></h4><p>The image below shows the geometry setup for a quadrotor multicopter frame with and without advanced settings.</p><p><img src="'+r+'" alt="Geometry MC (QGC)"></p><p>각 모터에 대해 다음을 설정할 수 있습니다:</p><p>For each motor you can then set:</p><ul><li><code>위치 X</code>: <a href="#motor-position-coordinate-system">X 위치</a>, 미터 단위.</li><li><code>위치 Y</code>: <a href="#motor-position-coordinate-system">Y 위치</a>, 미터 단위.</li><li><code>위치 Z</code>: <a href="#motor-position-coordinate-system">Z 위치</a>, 미터 단위.</li><li>(고급) <code>CCW 방향</code>: 모터가 시계 반대 방향으로 회전하는 것을 나타내는 확인란입니다(시계 방향의 경우 선택 취소).</li><li>(고급) <code>양방향</code>: 모터가 <a href="#bidirectional-motors">양방향</a>임을 나타내는 확인란</li><li>(고급) <code>슬루율</code>: 자세한 내용은 <a href="#control-surfaces-geometry">제어 표면 기하학</a> 섹션을 참조하십시오.</li></ul><div class="info custom-block"><p class="custom-block-title">The <code>X</code>, <code>Y</code>, <code>Z</code> positions are in <a href="#motor-position-coordinate-system">FRD coordinate frame, relative to the <em>centre of gravity</em></a>. Note, this may not be the same as the position of the flight controller!</p></div><h4 id="모터-지오메트리-vtol-quadrotor-tailsitter" tabindex="-1">모터 지오메트리: VTOL Quadrotor Tailsitter <a class="header-anchor" href="#모터-지오메트리-vtol-quadrotor-tailsitter" aria-label="Permalink to &quot;모터 지오메트리: VTOL Quadrotor Tailsitter&quot;">​</a></h4><p>모터는 <a href="#motor-geometry-multicopter">멀티콥터 지오메트리</a>와 동일한 구성 필드를 갖습니다.</p><p>Motors have the same configuration fields as for the <a href="#motor-geometry-multicopter">multicopter geometry</a>.</p><p><img src="'+i+'" alt="Geometry motor: tailsitter vtol"></p><h4 id="모터-지오메트리-vtol-틸트로터" tabindex="-1">모터 지오메트리: VTOL 틸트로터 <a class="header-anchor" href="#모터-지오메트리-vtol-틸트로터" aria-label="Permalink to &quot;모터 지오메트리: VTOL 틸트로터&quot;">​</a></h4><p>The motor geometry for a <a href="./../airframes/airframe_reference.html#vtol_vtol_tiltrotor_generic_quadplane_vtol_tiltrotor">Generic Quadplane VTOL Tiltrotor</a> is shown below (the approach for configuring other <a href="./../airframes/airframe_reference.html#vtol_vtol_tiltrotor_generic_quadplane_vtol_tiltrotor">VTOL tiltrotors</a> will be similar).</p><p><img src="'+l+'" alt="Geometry motor: tiltrotor vtol"></p><ul><li><code>기울기</code>: 모터를 기울이는 데 사용되는 관련 서보입니다. 이 서보의 속성은 <a href="#motor-tilt-servo-geometry">모터 틸트 서보 지오메트리</a>에 정의되어 있습니다.</li></ul><h4 id="모터-형상-표준-vtol" tabindex="-1">모터 형상: 표준 VTOL <a class="header-anchor" href="#모터-형상-표준-vtol" aria-label="Permalink to &quot;모터 형상: 표준 VTOL&quot;">​</a></h4><p>The motor geometry for a <a href="./../airframes/airframe_reference.html#vtol_standard_vtol_generic_standard_vtol">Generic Standard VTOL</a> is shown below (the approach for configuring other &quot;Standard VTOL&quot; will be similar).</p><p><img src="'+s+'" alt="Geometry motor: standard vtol"></p><p>다른 기체 유형은 프레임 유형에 적합한 모터 형상을 정의합니다. 다시 한 번 이러한 모터는 일반적으로 위에 표시된 것과 같은 종류의 속성을 가집니다.</p><ul><li><code>축</code>: <code>위로</code>, <code>아래로</code>, <code>앞으로</code>, <code>뒤로</code>, <code> 중 하나 왼쪽으로</code>, <code>오른쪽으로</code>, <code>사용자 지정</code><ul><li><code>사용자 지정</code>을 선택하면 UI에 모터 방향 설정을 위한 세 개의 추가 필드가 표시됩니다.</li></ul></li></ul><h4 id="모터-지오메트리-기타-기체" tabindex="-1">모터 지오메트리: 기타 기체 <a class="header-anchor" href="#모터-지오메트리-기타-기체" aria-label="Permalink to &quot;모터 지오메트리: 기타 기체&quot;">​</a></h4><p>Other vehicle types will define an appropriate motor geometry for their frame type. Once again these motors will generally have the same kinds of properties as shown above.</p><p>모터 위치를 나타내는 좌표계는 FRD(몸체 프레임 내)이며, 여기서 X축은 앞쪽, Y축은 오른쪽, Z축은 아래쪽을 가르킵니다.</p><h4 id="모터-위치-좌표계" tabindex="-1">모터 위치 좌표계 <a class="header-anchor" href="#모터-위치-좌표계" aria-label="Permalink to &quot;모터 위치 좌표계&quot;">​</a></h4><p>The coordinate system for motor positions is FRD (in body frame), where the X axis points forward, the Y axis to the right and the Z axis down.</p><p>The origin is the vehicle&#39;s <strong>centre-of-gravity (COG)</strong>. This may <strong>NOT</strong> be the same position as the location of the autopilot.</p><p><img src="'+n+'" alt="Actuators CG reference diagram"></p><h4 id="양방향-모터" tabindex="-1">양방향 모터 <a class="header-anchor" href="#양방향-모터" aria-label="Permalink to &quot;양방향 모터&quot;">​</a></h4><p>Some vehicles may use bidirectional motors (i.e. motors that support spinning in both directions). For example, ground vehicles that want to move forwards and backwards, or VTOL vehicles that have pusher motors that can turn in either direction.</p><p>If bidiectional motors are used, make sure to select the <strong>Reversible</strong> checkbox for those motors (the checkbox is displayed as an &quot;advanced&quot; option).</p><p><img src="'+c+'" alt="Reversible"></p><p>Note that you will need to also ensure that the ESC associated with bidirectional motors is configured appropriately (e.g. 3D mode enabled for DShot ESCs, which can be achieved via <a href="./../peripherals/dshot.html#commands">DShot commands</a>).</p><h3 id="조종면-지오메트리" tabindex="-1">조종면 지오메트리 <a class="header-anchor" href="#조종면-지오메트리" aria-label="Permalink to &quot;조종면 지오메트리&quot;">​</a></h3><p>The control surfaces section of the geometry panel lets you set the number and types of control surfaces that are present on the vehicle. You may also need to set trim and slew rate values in some cases. More advanced users can also configure the roll scale, yaw scale, and pitch scale (generally the defaults are acceptable, and this is not needed). An &quot;example&quot; control surface section for a vehicle with two ailerons is shown below. Note that ailerons only affect roll, so the pitch and yaw fields are disabled.</p><p><img src="'+d+'" alt="Control Surface Setup Example"></p><div class="info custom-block"><p class="custom-block-title">Only the most common settings are displayed by default. Select the <strong>Advanced</strong> checkbox in the top right corner of the view to display all settings.</p></div><p>The fields are:</p><ul><li><code>조종면</code>: 조종면의 갯수(먼저 설정하십시오!)</li><li><code>유형</code>: 각 조종면의 유형: <code>LeftAileron</code>, <code>RightAileron</code>, <code>Elevator</code>, <code>Rudder&lt;/0 &gt;, &lt;code&gt;왼쪽 엘레본</code>, <code>오른쪽 엘레본</code>, <code>왼쪽 V-Tail</code>, <code>오른쪽 V-Tail</code>, <code>왼쪽 플랩</code>, <code>오른쪽 플랩</code>, <code>에어브레이크</code>, <code>맞춤형</code>.</li><li><code>Roll Torque</code>: Effectiveness of actuator around roll axis (normalised: -1 to 1). <a href="#actuator-roll-pitch-and-yaw-scaling">일반적으로 기본 액추에이터 값을 사용하여야 합니다</a>.</li><li><code>Pitch Torque</code>: Effectiveness of actuator around pitch axis (normalised: -1 to 1). <a href="#actuator-roll-pitch-and-yaw-scaling">일반적으로 기본 액추에이터 값을 사용하여야 합니다</a>.</li><li><code>Yaw Torque</code>: Effectiveness of actuator around yaw axis (normalised: -1 to 1). <a href="#actuator-roll-pitch-and-yaw-scaling">일반적으로 기본 액추에이터 값을 사용합니다</a>.</li><li><code>트림</code>: 입력 없이 중앙에 오도록 액추에이터에 추가된 오프셋입니다. 이것은 시행착오를 거쳐 결정될 수 있습니다.</li><li>(Advanced) <code>Slew Rate</code>: Minimum time allowed for the motor/servo signal to pass through the full output range, in seconds. <ul><li>The setting limits the rate of change of an actuator (if not specified then no rate limit is applied). It is intended for actuators that may be damaged if they move too fast — such as the tilting actuators on a tiltrotor VTOL vehicle.</li><li>For example, a setting of 2.0 means that the motor/servo will not be commanded to move from 0 to 1 at a rate that completes the operation in less than 2 seconds (in case of reversible motors, the range is -1 to 1).</li></ul></li><li>(Advanced) <code>Flap Scale</code>: How much this actuator is deflected at the &quot;full flaps configuration&quot; [0, 1] (see <a href="#flap-scale-and-spoiler-scale-configuration">Flap Scale and Spoiler Scale Configuration</a> below). Can be used to configure aerodynamic surface as flap or to compensate for generated torque through main flaps.</li><li>(Advanced) <code>Spoiler Scale</code>: How much this actuator is deflected at the &quot;full spoiler configuration&quot; [0, 1] (see <a href="#flap-scale-and-spoiler-scale-configuration">Flap Scale and Spoiler Scale Configuration</a> below). Can be used to configure aerodynamic surface as spoiler or to compensate for generated torque through main spoiler.</li><li>(VTOL only) <code>Lock control surfaces in hover</code>: <ul><li><code>사용</code>: 대부분의 차량은 호버링 시 조종면을 사용하지 않습니다. 이 설정을 사용하여 기체 역학에 영향을 미치지 않도록 잠그십시오.</li><li><code>비활성화</code>: 듀오 테일시터(피치 및 요 제어에 엘레본 사용)와 같이 호버링에서 조종면을 사용하는 기체를 설정합니다. 또한 조종면을 사용하여 고속으로 이동할 때 호버 모드에서 추가 안정화를 제공하거나 강한 바람이 부는 기체에 대하여 설정합니다.</li></ul></li></ul><h4 id="flap-scale-and-spoiler-scale-configuration" tabindex="-1">Flap Scale and Spoiler Scale Configuration <a class="header-anchor" href="#flap-scale-and-spoiler-scale-configuration" aria-label="Permalink to &quot;Flap Scale and Spoiler Scale Configuration&quot;">​</a></h4><p>&quot;Flap-control&quot; and &quot;Spoiler-control&quot; are aerodynamic configurations that can either be commanded manually by the pilot (using RC, say), or are set automatically by the controller. For example, a pilot or the landing system might engage &quot;Spoiler-control&quot; in order to reduce the airspeed before landing.</p><p>The configurations are an <em>abstract</em> way for the controller to tell the allocator how much it should adjust the aerodynamic properties of the wings relative to the &quot;full flaps&quot; or &quot;full spoiler&quot; configuration (between <code>[0,1]</code>, where &quot;1&quot; indicates the full range). The allocator then uses any of the available control surfaces it wants in order to achieve the requested configuration: usually flaps, ailerons, and elevator.</p><p>The <code>flap scale</code> and <code>spoiler scale</code> settings in the actuator UI inform the allocator how much ailerons, elevators, flaps, spoilers, and other control surfaces, contribute to a requested &quot;Flap-control&quot; and/or &quot;Spoiler-control&quot; value. Specifically, they indicate how much each control surface should be deflected when the controller is demanding &quot;full flaps&quot; or &quot;full spoiler&quot;.</p><p>In the following example, the vehicle has two ailerons, one elevator, one rudder and two flaps as control surfaces:</p><p><img src="'+h+'" alt="Flaps and spoiler actuator configuration example"></p><ul><li>The flaps have both <code>Flap Scale</code> set to 1, meaning that they will be fully deflected with the flap-control at 1. They also have a slew rate of 0.5/s, meaning that it will take 2s to fully deflect them (a slew rate on the flaps is generally recommended to reduce the disturbances their movement creates).</li><li>The ailerons are primarily tasked to provide the commanded roll torque. They also have <code>Spoiler Scale</code> set to 0.5, and will additionally be deflected upwards 50% if the controller demands full spoiler configuration. The aileron deflection is thus the sum of the (asymmetrical) deflection for the roll torque, plus the (symmetrical) deflection for the spoiler setpoint.</li><li>The elevator is primarily tasked to provide pitch torque. It also has non-zero entries in the <code>Flap Scale</code> and <code>Spoiler Scale</code> fields. These are the elevator deflections added to compensate for the pitching moments generated by the flaps and spoiler actuators. In the case here the elevator would be deflected 0.3 up when the flaps are fully deployed to counteract the pitching down moment caused by the flaps.</li></ul><h4 id="액추에이터-롤-피치-및-요-스케일링" tabindex="-1">액추에이터 롤, 피치 및 요 스케일링 <a class="header-anchor" href="#액추에이터-롤-피치-및-요-스케일링" aria-label="Permalink to &quot;액추에이터 롤, 피치 및 요 스케일링&quot;">​</a></h4><div class="info custom-block"><p class="custom-block-title">INFO</p><p>For the majority of airframe setups the default values for each control surface types should not be changed.</p></div><p>The <code>Roll scale</code>, <code>Pitch scale</code> and <code>Yaw scale</code> values indicate the normalized effectiveness of the actuator around the corresponding axis.</p><p>Tuning the values is a low/level/advanced topic, and is generally only needed when tuning coupled control surfaces (like an elevon, that controls both pitch and roll). In this case the things you need to know are:</p><ul><li>입력된 숫자는 할당 매트릭스에 직접 입력되며, 원하는 순간(정규화)에서 제어 신호를 얻기 위해 반전됩니다.</li><li>배율을 높이면 제어 표면의 처짐이 _감소_됩니다(역전될 때).</li></ul><h4 id="조종면-처짐-규칙" tabindex="-1">조종면 처짐 규칙 <a class="header-anchor" href="#조종면-처짐-규칙" aria-label="Permalink to &quot;조종면 처짐 규칙&quot;">​</a></h4><p>The diagram below shows the convention for deflections:</p><p><img src="'+u+'" alt="Control Surface Deflections"></p><p>In summary:</p><ul><li><strong>수평 조종면:</strong> 위쪽으로 이동하면 양의 편향이 발생합니다. 에일러론 등 포함</li><li><strong>수직 조종면:</strong> 오른쪽으로 이동하면 양의 편향이 발생합니다. 러더 등이 포함됩니다.</li><li><strong>혼합 조종면:</strong> 위쪽/오른쪽 방향 움직임은 양수입니다(위와 같이). V-Tail 등이 포함됩니다.</li></ul><h3 id="모터-틸트-서보-지오메트리" tabindex="-1">모터 틸트 서보 지오메트리 <a class="header-anchor" href="#모터-틸트-서보-지오메트리" aria-label="Permalink to &quot;모터 틸트 서보 지오메트리&quot;">​</a></h3><p><a href="./../frames_vtol/tiltrotor.html">VTOL tiltrotor vehicles</a> can tilt their motors to transition between hover and forward flight. This section defines the properties of the tilting servos. These are mapped to specific motors in the motor geometry for a tiltrotor.</p><p>The example below shows the tilt servo setup for the <a href="./../config/actuators.html#motor-geometry-vtol-tiltrotor">tiltrotor motor geometry shown above</a>.</p><p><img src="'+e+'" alt="Tilt Servo Geometry Setup Example"></p><p>The values that can be set are:</p><ul><li><code>틸트 서보</code>: 서보(틸트 가능한 모터)의 수입니다.</li><li><code>최소 기울기 각도</code>: z축을 기준으로 한 <a href="#tilt-servo-coordinate-system">최대 기울기 각도</a>(도)</li><li><code>최대 기울기 각도</code>: Z축을 기준으로 한 <a href="#tilt-servo-coordinate-system">최소 기울기 각도</a>(도)</li><li><code>기울기 방향</code>: <code>앞쪽으로</code>(양의 x 방향) 또는 <code>오른쪽으로</code>(양의 y 방향).</li><li><code>제어에 사용</code>: <a href="#tilt-servos-for-yaw-pitch-control">요/피치에 사용되는 틸트 서보</a><ul><li><code>없음</code>: 토크 제어를 사용하지 않습니다.</li><li><code>요</code>: 요를 제어하는 데 사용되는 틸트 서보입니다.</li><li><code>피치</code>: 피치를 제어하는 데 사용되는 틸트 서보입니다.</li><li><code>요 및 피치 모두</code>: 틸트 서보는 요와 피치를 모두 제어하는 데 사용됩니다.</li></ul></li></ul><h4 id="틸트-서보-좌표계" tabindex="-1">틸트 서보 좌표계 <a class="header-anchor" href="#틸트-서보-좌표계" aria-label="Permalink to &quot;틸트 서보 좌표계&quot;">​</a></h4><p>The coordinate system for tilt rotor angles is shown below. The reference direction for tilt angles is straight upwards (0 degrees). Tilt angles towards the front or right of the vehicle are positive, and towards the back or to the left are negative.</p><p><img src="'+p+'" alt="Tilt Axis"></p><p>The <code>Angle at min tilt</code> and <code>Angle at max tilt</code> indicate the range of movement for the tilt servo. The minimum tilt is the smaller <em>numerical value</em> (not absolute) of the two angles.</p><p>If the max/min tilt vectors are <strong>P<sub>0</sub></strong> and <strong>P<sub>1</sub></strong> as shown above, both tilt angles are positive but <strong>θ<sub>0</sub></strong> is smaller:</p><ul><li><code>최소 기울기 각도</code> = <strong>θ<sub>0</sub></strong></li><li><code>최대 기울기 각도</code> = <strong>θ<sub>1</sub></strong></li></ul><div class="info custom-block"><p class="custom-block-title">If the diagram was mirrored so that <strong>P<sub>0</sub></strong> and <strong>P<sub>1</sub></strong> were tilting into the -x, -y quadrant, then both the tilt angles would be negative. Because <strong>θ<sub>1</sub></strong> would more negative (smaller) than <strong>θ<sub>0</sub></strong>, it would be the <code>Angle at min tilt</code>.</p><p>Similarly, a servo that moves:</p><ul><li>between the upright and forward positions would have <code>min=0</code> and <code>max=90</code>.</li><li>수직 위치를 중심으로 대칭적으로 45도는 <code>최소=-45</code> 및 <code>최대=45</code>입니다.</li><li>수직 위치와 후방 위치 사이의 값은 <code>min=-90</code> 및 <code>max=0</code>입니다.</li></ul></div><p>The <code>Tilt direction</code> indicates whether the servo tilts in the plane towards the <code>Front</code> or <code>Right</code> of the vehicle. On the diagram this would be represented by <strong>α</strong> that can only take values of 0 (front) or 90 (right).</p><h4 id="요-피치-제어용-틸트-서보" tabindex="-1">요/피치 제어용 틸트 서보 <a class="header-anchor" href="#요-피치-제어용-틸트-서보" aria-label="Permalink to &quot;요/피치 제어용 틸트 서보&quot;">​</a></h4><p>이 기능의 사용 여부는 <code>제어에 사용</code>에서 설정합니다.</p><ul><li>Yaw는 일반적으로 이러한 방식으로 설정되지만 모터는 4개 이상의 모터가 있는 기체에서 대신 사용되는 경우가 많습니다.</li><li>피치는 일반적으로 차동 모터 추력을 사용하여 제어합니다. 틸트 서보를 사용한 제어는 <a href="https://www.youtube.com/watch?v=hfss7nCN40A" target="_blank" rel="noreferrer">Bicopter</a>와 같이 차동 추력을 사용할 수 없는 기체에 유용합니다.</li></ul><p><em>액추에이터 출력</em> 섹션은 모터, 제어 표면 서보 및 특정 프레임에서 사용하는 기타 액츄에이터를 비행 컨트롤러의 물리적 출력에 할당하고 해당 출력에 대한 매개변수를 설정합니다.</p><h2 id="액추에이터-출력" tabindex="-1">액추에이터 출력 <a class="header-anchor" href="#액추에이터-출력" aria-label="Permalink to &quot;액추에이터 출력&quot;">​</a></h2><p>The <em>Actuator Outputs</em> section is used to assign motors, control surface servos, and other actuators used by the particular frame to the physical outputs on the flight controller, and to set parameters for those outputs.</p><p><img src="'+m+'" alt="Actuator Outputs - Multicopter diagram"></p><p>Separate tabs are displayed for each output bus supported by the connected flight controller: PWM MAIN (I/O Board output), PWM AUX (FMU Board output), UAVCAN.</p><p>Motors and actuators (which are referred to as &quot;<a href="#output-functions">functions</a>&quot;) can be assigned to any physical output on any of the available buses.</p><p>PWM 출력은 하드웨어 타이머 그룹을 기반으로 그룹화됩니다. 즉, 한 그룹의 모든 출력은 동일한 프로토콜에서 동일한 속도로 작동하여야 합니다(예: 한 그룹의 모든 출력에 대해 400Hz의 PWM 신호).</p><p>PWM AUX 탭에는 일반적으로 <a href="./../peripherals/camera.html#trigger-configuration">카메라 캡처/트리거 입력</a>으로 사용되는 CAP 출력이 있습니다. 그러나, CAP 출력을 다른 출력 기능에 매핑할 수 있으며 다른 AUX 출력을 카메라 캡처/트리거 입력으로 사용 가능합니다. Therefore it is not possible to map Servo and a Motor in the same output group, as they usually operate at a different rate.</p><p>The PWM AUX tab has CAP outputs that are generally used as the <a href="./../camera/fc_connected_camera.html#trigger-configuration">camera capture/trigger input</a>. However you can map the CAP outputs to other output functions, and other AUX outputs can be used as camera capture/triggering input.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Configuring the Camera Capture / Trigger input requires a reboot to take effect</p></div><p>You should assign functions to the outputs that match your physical wiring of motors and servos, and use the <a href="#actuator-testing">Actuator Testing</a> section described below to determine appropriate output parameter values. These steps are covered in <a href="#output-assignment-and-configuration">Output Assignment and Configuration</a>.</p><h3 id="output-functions" tabindex="-1">Output Functions <a class="header-anchor" href="#output-functions" aria-label="Permalink to &quot;Output Functions&quot;">​</a></h3><p>Output functions are used to map the &quot;logical functions&quot; of an airframe, such as <code>Motor 1</code> or <code>Landing gear</code>, to physical outputs like FMU output pin 2. This makes it easy to use a particular output pin for almost any purpose.</p><p>Some functions are only relevant to particular frames or output types, and will not be offered on others.</p><p>Functions include:</p><ul><li><code>Disabled</code>: Output has no assigned function.</li><li><code>Constant_Min</code>: Output set to constant minimum value (-1).</li><li><code>Constant_Max</code>: Output is set to constant maximum value (+1).</li><li><code>Motor 1</code> to <code>Motor 12</code>: Output is indicated motor. Only motors allowed for airframe are displayed.</li><li><code>Servo 1</code> to <code>Servo 8</code>: Servo output. These are further assigned a specific meaning based on airframe, such as &quot;tilt servo&quot;, &quot;left aileron&quot;.</li><li><code>Peripheral via Acutator Set 1</code> to <code>Peripheral via Acutator Set 6</code>: <a href="./../payloads/generic_actuator_control.html#generic-actuator-control-with-mavlink">Generic Actuator Control with MAVLink</a>.</li><li><code>Landing Gear</code>: Output is landing gear.</li><li><code>Parachute</code>: Output is parachute. The minimum value is sent in normal use and the maximum value is emitted when a failsafe is triggered.</li><li><code>RC Roll</code>: Output is passthrough roll from RC (<a href="./../advanced_config/parameter_reference.html#RC_MAP_ROLL">RC_MAP_ROLL</a> maps an RC channel to this output). An RC channel is mapped to the output using .</li><li><code>RC Pitch</code>: Output is passthrough pitch from RC (<a href="./../advanced_config/parameter_reference.html#RC_MAP_PITCH">RC_MAP_PITCH</a> maps an RC channel to this output).</li><li><code>RC Throttle</code>: Output is passthrough throttle from RC (<a href="./../advanced_config/parameter_reference.html#RC_MAP_THROTTLE">RC_MAP_THROTTLE</a> maps an RC channel to this output).</li><li><code>RC Yaw</code>: Output is yaw from RC (<a href="./../advanced_config/parameter_reference.html#RC_MAP_YAW">RC_MAP_YAW</a> maps an RC channel to this output).</li><li><code>RC Flaps</code>: Output is flaps from RC (<a href="./../advanced_config/parameter_reference.html#RC_MAP_FLAPS">RC_MAP_FLAPS</a> maps an RC channel to this output).</li><li><code>RC AUXn</code> to <code>RC AUX1</code>: Outputs used for <a href="./../payloads/generic_actuator_control.html#generic-actuator-control-with-rc">arbitrary payloads triggered by RC passthrough</a>.</li><li><code>Gimbal Roll</code>: Output controls gimbal roll.</li><li><code>Gimbal Pitch</code>: Output controls Gimbal pitch.</li><li><code>Gimbal Yaw</code>: Output controls Gimbal yaw.</li></ul><p>The following functions can only be applied to FMU outputs:</p><ul><li><code>Camera_Trigger</code>: Output to trigger camera. Enabled when <a href="./../advanced_config/parameter_reference.html#TRIG_MODE"><code>TRIG_MODE==0</code></a>. Configured via <code>TRIG_*</code> parameters.</li><li><code>Camera_Capture</code>: Input to get image capture notification. Enabled when <a href="./../advanced_config/parameter_reference.html#CAM_CAP_FBACK">CAM_CAP_FBACK==0</a>. Configured via <code>CAM_CAP_*</code> parameters.</li><li><code>PPS_Input</code>: Pulse-per-second input capture. Used for GPS synchronisation. Enabled when <a href="./../advanced_config/parameter_reference.html#PPS_CAP_ENABLE"><code>PPS_CAP_ENABLE==0</code></a></li></ul><p>오른쪽 하단의 <em>액추에이터 테스트</em> 섹션에는 액츄에이터 및 모터 설정을 테스트(및 결정)하는 데 사용할 수 있는 슬라이더가 있습니다. <a href="#actuator-outputs">액추에이터 출력</a> 섹션에 정의된 각 출력에 대해 슬라이더가 제공됩니다. 아래 슬라이더 예는 일반적인 VTOL Tiltrotor 기체에 대한 섹션을 나타냅니다.</p><h2 id="액추에이터-테스트" tabindex="-1">액추에이터 테스트 <a class="header-anchor" href="#액추에이터-테스트" aria-label="Permalink to &quot;액추에이터 테스트&quot;">​</a></h2><p>The <em>Actuator Testing</em> section in lower-right corner provides sliders that can be used to test (and determine) actuator and motor settings. A slider is provided for each output defined in the <a href="#actuator-outputs">Actuator Outputs</a> section. The slider example below shows the section for a typical VTOL Tiltrotor airframe.</p><p><img src="'+f+'" alt="Actuator Testing Slider"></p><p>:::note <strong>슬라이더 활성화</strong> 스위치를 토글한 후 액추에이터/모터는 해당 슬라이더가 _이동_될 때까지 아무 작업도 하지 않습니다. 이것은 스위치가 활성화된 후 갑작스러운 모터 움직임을 방지하기 위한 안전 장치입니다.</p><div class="info custom-block"><p class="custom-block-title">After you toggle the <strong>Enable sliders</strong> switch, actuators/motors won&#39;t do anything until the corresponding slider is <em>moved</em>. This is a safety feature to prevent sudden motor movements after switch is enabled.</p></div><p>Sliders can be used to verify the following:</p><ol><li>액추에이터(모터, 제어 표면 등)는 예상 출력에 할당됩니다.</li><li>PWM 출력 값이 <code>해제</code>일 때 모터가 회전하지 않습니다.</li><li>모터는 <code>최소</code> PWM 출력 값에서 거의 회전하지 않습니다.</li><li>모터가 예상 방향으로 <strong>양의 추력</strong>을 제공합니다.</li><li>조종면은 출력 값 <code>시동</code>에 대하여 올바른 유휴 위치에 있습니다.</li><li>Control Surfaces move in the direction as defined in the <a href="#control-surface-deflection-convention">Control Surface Convention</a></li><li>모터 틸트 서보가 출력 값 <code>해제</code>에 대해 올바른 유휴 위치에 있습니다.</li><li>Motor Tilt Servos move in the direction as defined in the <a href="#tilt-servo-coordinate-system">Tilt Servo Convention</a></li></ol><h2 id="출력-할당-및-설정" tabindex="-1">출력 할당 및 설정 <a class="header-anchor" href="#출력-할당-및-설정" aria-label="Permalink to &quot;출력 할당 및 설정&quot;">​</a></h2><p><strong>식별 &amp; 모터 할당</strong> 버튼은 반자동 프로세스를 사용하여 모터를 PWM 출력에 할당합니다.</p><ul><li>MC vehicles that have connected motors to PWM outputs can use the <a href="#multicopter-pwm-motor-assignment">Identify &amp; Assign Motors</a> button to perform motor assignment &quot;semi-automatically&quot;.</li><li>모터와 액추에이터의 출력 할당은 슬라이더를 사용하여 수행/확인할 수 있습니다(<a href="#output-assignment-manual">출력 할당(수동)</a> 참조).</li><li>모든 출력에 대한 해제, 최소 및 최대 설정도 슬라이더를 사용하여 결정할 수 있습니다. This is shown as part of <a href="#motor-configuration">Motor Configuration</a>, <a href="#control-surface-setup">Control Surface Setup</a>, <a href="#tilt-servo-setup">Tilt servo setup</a></li></ul><h3 id="멀티콥터-pwm-모터-할당" tabindex="-1">멀티콥터 PWM: 모터 할당 <a class="header-anchor" href="#멀티콥터-pwm-모터-할당" aria-label="Permalink to &quot;멀티콥터 PWM: 모터 할당&quot;">​</a></h3><p>You can use the <strong>Identify &amp; Assign Motors</strong> button to assign motors to PWM outputs using a semi-automated process.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>출력이나 테스트를 할당하기 전에 모터에서 프로펠러를 제거하십시오. On other frames you can follow the instructions in <a href="#output-assignment-manual">Output Assignment (Manual)</a>.</p></div><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Remove the propellers from motors before assigning outputs or any testing.</p></div><p><img src="'+g+'" alt="Identify motor button"></p><p>When you click the button, QGC sends a command to a motor, causing it to spin. To assign that motor to an output you simply select the corresponding motor displayed in the screen. QGC will then spin the next motor for you to assign, and so on.</p><p>Instructions:</p><ol><li><p>프레임의 모터와 일치하도록 모터 형상을 설정합니다.</p></li><li><p>모터를 할당할 PWM 탭을 선택합니다.</p></li><li><p><strong>식별 &amp; 모터 할당</strong> 버튼을 클릭합니다.</p></li><li><p>하나의 모터가 회전하기 시작합니다(참고할 수 없을 정도로 회전이 너무 빨리 멈추면 <strong>모터 다시 회전</strong> 클릭).</p><p>지오메트리 섹션에서 해당 모터를 선택합니다.</p><p><img src="'+v+'" alt=""></p></li><li><p>모든 모터를 할당한 후 도구는 출력에 대한 올바른 모터 매핑을 설정한 다음 종료됩니다.</p></li></ol><h3 id="출력-할당-수동" tabindex="-1">출력 할당(수동) <a class="header-anchor" href="#출력-할당-수동" aria-label="Permalink to &quot;출력 할당(수동)&quot;">​</a></h3><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Remove the propellers from motors before assigning outputs or any testing.</p></div><p>액추에이터에 할당하려면:</p><p>To assign an actuator:</p><ol><li>먼저 <em>액추에이터 출력</em> 섹션에서 정확할 것으로 <em>가능성이 있는</em> 출력에 기능을 할당합니다.</li><li><em>액추에이터 테스트</em> 섹션에서 <strong>슬라이더 사용</strong> 스위치를 전환합니다.</li><li>테스트하려는 액추에이터의 슬라이더를 이동합니다. <ul><li>모터는 최소 추력 위치로 이동하여야 합니다.</li><li>서보는 중간 위치 근처로 이동하여야 합니다.</li></ul></li><li>어떤 액츄에이터가 차량에서 움직이는 지 확인하십시오. 이것은 지오메트리에 대한 액추에이터 위치와 일치해야 합니다(<a href="./../airframes/airframe_reference.html">기체 참조</a>는 여러 표준 기체에 대한 모터 위치를 나타냅니다). <ul><li>올바른 액츄에이터가 움직이면 다음 단계로 진행합니다.</li><li>잘못된 액츄에이터가 움직이면 출력 할당을 변경합니다.</li><li>아무 것도 움직이지 않으면, 슬라이더를 범위 중간에 늘리고 필요한 경우 더 높입니다. 그 후 아무 것도 움직이지 않으면, 출력이 연결되지 않거나 모터에 전원이 공급되지 않거나 출력 설정에 오류가 있을 수 있습니다. 문제를 해결하여야 합니다(&quot;무엇이든&quot;이 움직이는지 확인하기 위하여 다른 액추에이터 출력을 시도할 수 있음).</li></ul></li><li>슬라이더를 &quot;무장 해제&quot; 위치로 되돌립니다(모터의 경우 슬라이더 하단, 서보의 경우 슬라이더 중앙).</li><li>모든 액추에이터에 대하여 반복합니다.</li></ol><h3 id="모터-설정" tabindex="-1">모터 설정 <a class="header-anchor" href="#모터-설정" aria-label="Permalink to &quot;모터 설정&quot;">​</a></h3><div class="info custom-block"><p class="custom-block-title">If using PWM or OneShot ESCs, you should first perform <a href="./../advanced_config/esc_calibration.html">ESC Calibration</a> (this topic also covers PWM specific motor configuration).</p><p><a href="./../peripherals/dshot.html">DShot</a> ESCs do not require configuration of the command limits but only rotation direction.</p></div><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Remove propellers!</p></div><p>각각의 모터에 대하여:</p><ul><li>don&#39;t spin when disarmed (at the <code>disarmed</code> PWM output value).</li><li>barely but reliably spin up at the <code>minimum</code> PWM output value.</li><li>have the <em>lowest</em> <code>maximum</code> PWM output value that spins the motor at its <em>highest</em> rate.</li><li>give <strong>positive thrust</strong> in the expected direction.</li></ul><p>For each motor:</p><ol><li><p>모터 슬라이더를 아래로 당겨서 아래쪽에 찰칵 소리가 나도록 합니다. In this position the motor is set to the outputs <code>disarmed</code> value.</p><ul><li>모터가 이 위치에서 회전하지 않는 지 확인하십시오.</li><li>If the motor spins, reduce the corresponding PWM <code>disarmed</code> value in the <a href="#actuator-outputs">Actuator Outputs</a> section to below the level at which it still spins.</li></ul></li><li><p>슬라이더가 <em>최소</em> 위치에 고정될 때까지 슬라이더를 천천히 위로 이동합니다. In this position the motor is set to the outputs <code>minimum</code> value.</p><ul><li><p>이 위치에서 모터가 매우 느리게 회전하는 지 확인합니다.</p></li><li><p>If the motor is not spinning, or spinning too fast you will need to adjust the corresponding PWM <code>minimum</code> value in the <a href="#actuator-outputs">Actuator Outputs</a> such that the motors barely spin.</p><p><img src="'+b+'" alt="PWM 최소 출력"> ::: info For DShot output, this is not required.</p></li></ul></li></ol><p>:::</p><ol start="3"><li><p>Increase the slider value to a level where you can verify that the motor is spinning in the correct direction and that it would give a positive thrust in the expected direction.</p><ul><li>The expected thrust direction can vary by vehicle type. For example in multicopters the thrust should always point upwards, while in a fixed-wing vehicle the thrust will push the vehicle forwards.</li><li>For VTOL, thrust should point upwards when the Tilt Servo is at 0 degrees as defined the <a href="#tilt-servo-coordinate-system">Tilt Servo Convention</a>. Testing of the <a href="#tilt-servo-setup">Tilt Servo</a> is covered below as well.</li><li>If thrust is in the wrong direction, you may need to <a href="#reversing-motors">reverse the motors</a>.</li></ul></li><li><p>Increase the slider value to the maximum value, so the motor is spinning quickly. Reduce the value of the PWM output&#39;s <code>maximum</code> value just below the default. Listen to the tone of the motors as you increase the value in small (25us) increments. The &quot;optimal&quot; maximum value is the value at which you last hear a change in the tone.</p></li></ol><h3 id="조종면-설정" tabindex="-1">조종면 설정 <a class="header-anchor" href="#조종면-설정" aria-label="Permalink to &quot;조종면 설정&quot;">​</a></h3><p>First set the <em>frame rate</em> for the servos used in each group of outputs. This would normally be set to the maximum value supported by your servo. Below we show how you would set it to PWM50 (the most common value).</p><p><img src="'+y+'" alt="Control Surface Disarmed 1500 Setting"></p><div class="info custom-block"><p class="custom-block-title">You will almost certainly need to change the pulse rate from the default of 400Hz because support is rare (if not supported the servo will usually make an &quot;odd&quot; noise). If you&#39;re using PWM servos, PWM50 is far more common. If a high rate servo is <em>really</em> needed, DShot offers better value.</p></div><p>For each of the control surfaces:</p><ol><li><p>무장 해제 시 표면이 중립 위치를 유지하도록 <code>비시동 해제</code> 값을 설정합니다. 이것은 일반적으로 PWM 서보의 경우 약 <code>1500</code>입니다.</p></li><li><p>Move the slider for the surface upwards (positive command) and verify that it moves in the direction defined in the <a href="#control-surface-deflection-convention">Control Surface Convention</a>.</p><ul><li>조종면이 반대 방향으로 이동하는 경우 <code>Rev Range</code> 확인란을 클릭하여 범위를 반대로 설정합니다.</li></ul></li><li><p>Move the slider again to the middle and check if the Control Surfaces are aligned in the neutral position of the wing</p><ul><li><p>If it is not aligned, you can set the <strong>Trim</strong> value for the control surface. ::: info This is done in the <code>Trim</code> setting of the Geometry panel, usually by &quot;trial and error&quot;. <img src="'+w+'" alt="Control Surface Trimming"> :::</p></li><li><p>After setting the trim for a control surface, move its slider away from the center, release, and then back into disarmed (middle) position. Confirm that surface is in the neutral position.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span></span></span></code></pre></div></li></ul></li></ol><div class="info custom-block"><p class="custom-block-title">Another way to test without using the sliders would be to set the <a href="./../advanced_config/parameter_reference.html#COM_PREARM_MODE"><code>COM_PREARM_MODE</code></a> parameter to <code>Always</code>:</p><ul><li>이렇게 하면 기체가 무장 해제된 경우에도 서보 제어가 가능하며 제어 표면에 트림 설정이 지속적으로 적용됩니다.</li><li>Trim에 대해 다른 값을 설정하고 정렬을 확인한 다음 만족하는 값으로 설정할 수 있습니다.</li></ul></div><h3 id="틸트-서보-설정" tabindex="-1">틸트 서보 설정 <a class="header-anchor" href="#틸트-서보-설정" aria-label="Permalink to &quot;틸트 서보 설정&quot;">​</a></h3><p>First set the <em>frame rate</em> for the servos used in each group of outputs. This would normally be set to the maximum value supported by your servo. Below it is set to PWM50 (the most common value). Note, this part of the setup is the same as for control surfaces above.</p><p><img src="'+_+'" alt="Tilt Servo Setup"></p><p>For each of the tilt servos:</p><ol><li><p><code>Disarmed</code> 값(예: PWM Servo의 경우 <code>1000</code> 또는 <code>2000</code>)을 설정하여 설정 <em>해제</em> 시 서보가 예상 방향으로 위치하도록 합니다.</p></li><li><p>서보의 슬라이더를 가장 낮은 위치에 놓고 양수 값 증가가 <code>최소 기울기 각도</code>(기하학 섹션에 정의됨)를 가리키는 지 확인합니다.</p><p><img src="'+e+'" alt="틸트 서보 지오메트리 설정"></p></li><li><p>서보의 슬라이더를 가장 높은 위치에 놓고 양의 모터 추력이 <code>최대 기울기 각도</code>를 가리키는 지 확인합니다(기하학 섹션에 정의됨).</p></li></ol><h3 id="기타-참고-사항" tabindex="-1">기타 참고 사항 <a class="header-anchor" href="#기타-참고-사항" aria-label="Permalink to &quot;기타 참고 사항&quot;">​</a></h3><ul><li>안전 버튼을 쿨러야 액츄에이트를 테스트 할 수 있습니다.</li><li>중지 스위치를 사용하면 모터를 즉시 중지할 수 있습니다.</li><li>해당 슬라이더가 변경될 때까지 서보는 실제로 이동하지 않습니다.</li><li>매개 변수 <a href="./../advanced_config/parameter_reference.html#COM_MOT_TEST_EN">COM_MOT_TEST_EN</a>을 사용하여 액츄에이터 테스트를 완전히 비활성화 할 수 있습니다.</li><li>셸에서 <a href="./../modules/modules_command.html#actuator-test">actuator_test</a>는 액추에이터를 테스트할 수 있습니다.</li><li>VTOL은 <strong>고정익 비행</strong> 중에 위쪽을 가리키는 모터를 자동으로 끕니다. <ul><li>표준 VTOL: 멀티콥터 모터로 정의된 모터가 꺼집니다.</li><li>틸트로터: 연결된 틸트 서보가 없는 모터는 꺼집니다.</li><li>테이시터는 고정익 비행시에도 모터를 끄지 않습니다.</li></ul></li></ul><h3 id="모터-역전" tabindex="-1">모터 역전 <a class="header-anchor" href="#모터-역전" aria-label="Permalink to &quot;모터 역전&quot;">​</a></h3><p>The motors must turn in the direction defined in configured geometry (&quot;<strong>Direction CCW</strong>&quot; checkboxes). If any motors do not turn in the correct direction they must be reversed.</p><p>There are several options:</p><ul><li><p>If the ESCs are configured as <a href="./../peripherals/dshot.html">DShot</a> you can permanently reverse the direction via UI. The <strong>Set Spin Direction</strong> buttons are displayed below the Actuator sliders (if DShot motors are used). These popup a dialog in which you select the motor for which you want to apply the direction.</p><p><img src="'+T+'" alt="Set spin direction buttons"></p><p>Note that the current direction cannot be queried, so you may need to try both options.</p></li><li><p>모터 케이블 3개 중 2개를 변경하면됩니다(어떤 케이블이든 상관 없음).</p><p>:::note 모터가 총알 커넥터를 사용하지 않은 경우에는 납땜을 다시 하여야 합니다 (이러한 이유로 DShot ESC를 선호합니다). :::</p></li></ul>',158);function I(F,L,W,E,V,G){const o=C("Badge");return x(),P("div",null,[O,S(o,{type:"tip",text:"PX4 v1.14"}),R])}const X=q(M,[["render",I]]);export{U as __pageData,X as default};
