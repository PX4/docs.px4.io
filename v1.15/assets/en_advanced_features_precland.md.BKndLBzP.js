import{_ as e}from"./chunks/precland-flow-diagram.DoSa_RZP.js";import{_ as a,c as i,o as t,ab as n}from"./chunks/framework.CUflZczI.js";const _=JSON.parse('{"title":"Precision Landing","description":"","frontmatter":{},"headers":[],"relativePath":"en/advanced_features/precland.md","filePath":"en/advanced_features/precland.md"}'),o={name:"en/advanced_features/precland.md"},r=n(`<h1 id="precision-landing" tabindex="-1">Precision Landing <a class="header-anchor" href="#precision-landing" aria-label="Permalink to &quot;Precision Landing&quot;">​</a></h1><p>PX4 supports precision landing for <em>multicopters</em> on either stationary or moving targets. The target may be provided by an onboard IR sensor and a landing beacon, or by an offboard positioning system.</p><p>Precision landing can be <a href="#initiating-a-precision-landing">started/initiated</a> as part of a <a href="#mission">mission</a>, in a <a href="#return-mode-precision-landing">Return mode</a> landing, or by entering the <a href="#precision-landing-flight-mode"><em>Precision Land</em> flight mode</a>.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Precision landing is only possible with a valid global position (due to a limitation in the current implementation of the position controller).</p></div><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><h3 id="land-modes" tabindex="-1">Land Modes <a class="header-anchor" href="#land-modes" aria-label="Permalink to &quot;Land Modes&quot;">​</a></h3><p>A precision landing can be configured to either be &quot;required&quot; or &quot;opportunistic&quot;. The choice of mode affects how a precision landing is performed.</p><h4 id="required-mode" tabindex="-1">Required Mode <a class="header-anchor" href="#required-mode" aria-label="Permalink to &quot;Required Mode&quot;">​</a></h4><p>In <em>Required Mode</em> the vehicle will search for a target if none is visible when landing is initiated. The vehicle will perform a precision landing if a target is located.</p><p>The search procedure consists of climbing to the search altitude (<a href="./../advanced_config/parameter_reference.html#PLD_SRCH_ALT">PLD_SRCH_ALT</a>). If the target is still not visible at the search altitude and after a search timeout (<a href="./../advanced_config/parameter_reference.html#PLD_SRCH_TOUT">PLD_SRCH_TOUT</a>), a normal landing is initiated at the current position.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>If using an offboard positioning system PX4 assumes that the target is visible when it is receiving MAVLink <a href="https://mavlink.io/en/messages/common.html#LANDING_TARGET" target="_blank" rel="noreferrer">LANDING_TARGET</a> messages.</p></div><h4 id="opportunistic-mode" tabindex="-1">Opportunistic Mode <a class="header-anchor" href="#opportunistic-mode" aria-label="Permalink to &quot;Opportunistic Mode&quot;">​</a></h4><p>In <em>Opportunistic Mode</em> the vehicle will use precision landing <em>if</em> (and only if) the target is visible when landing is initiated. If it is not visible the vehicle immediately performs a <em>normal</em> landing at the current position.</p><h3 id="landing-phases" tabindex="-1">Landing Phases <a class="header-anchor" href="#landing-phases" aria-label="Permalink to &quot;Landing Phases&quot;">​</a></h3><p>A precision landing has three phases:</p><ol><li><p><strong>Horizontal approach:</strong> The vehicle approaches the target horizontally while keeping its current altitude. Once the position of the target relative to the vehicle is below a threshold (<a href="./../advanced_config/parameter_reference.html#PLD_HACC_RAD">PLD_HACC_RAD</a>), the next phase is entered. If the target is lost during this phase (not visible for longer than <a href="./../advanced_config/parameter_reference.html#PLD_BTOUT">PLD_BTOUT</a>), a search procedure is initiated (during a required precision landing) or the vehicle does a normal landing (during an opportunistic precision landing).</p></li><li><p><strong>Descent over target:</strong> The vehicle descends, while remaining centered over the target. If the target is lost during this phase (not visible for longer than <code>PLD_BTOUT</code>), a search procedure is initiated (during a required precision landing) or the vehicle does a normal landing (during an opportunistic precision landing).</p></li><li><p><strong>Final approach:</strong> When the vehicle is close to the ground (closer than <a href="./../advanced_config/parameter_reference.html#PLD_FAPPR_ALT">PLD_FAPPR_ALT</a>), it descends while remaining centered over the target. If the target is lost during this phase, the descent is continued independent of the kind of precision landing.</p></li></ol><p>Search procedures are initiated in the first and second steps, and will run at most <a href="./../advanced_config/parameter_reference.html#PLD_MAX_SRCH">PLD_MAX_SRCH</a> times. Landing Phases Flow Diagram</p><p>A flow diagram showing the phases can be found in <a href="#landing-phases-flow-diagram">landing phases flow Diagram</a> below.</p><h2 id="initiating-a-precision-landing" tabindex="-1">Initiating a Precision Landing <a class="header-anchor" href="#initiating-a-precision-landing" aria-label="Permalink to &quot;Initiating a Precision Landing&quot;">​</a></h2><p>Precision landing can be used in missions, during the landing phase in <em>Return mode</em>, or by entering the <em>Precision Land</em> mode.</p><p><a id="mission"></a></p><h3 id="mission-precision-landing" tabindex="-1">Mission Precision Landing <a class="header-anchor" href="#mission-precision-landing" aria-label="Permalink to &quot;Mission Precision Landing&quot;">​</a></h3><p>Precision landing can be initiated as part of a <a href="./../flying/missions.html">mission</a> using <a href="https://mavlink.io/en/messages/common.html#MAV_CMD_NAV_LAND" target="_blank" rel="noreferrer">MAV_CMD_NAV_LAND</a> with <code>param2</code> set appropriately:</p><ul><li><code>0</code>: Normal landing without using the target.</li><li><code>1</code>: <a href="#opportunistic-mode">Opportunistic</a> precision landing.</li><li><code>2</code>: <a href="#required-mode">Required</a> precision landing.</li></ul><h3 id="return-mode-precision-landing" tabindex="-1">Return Mode Precision Landing <a class="header-anchor" href="#return-mode-precision-landing" aria-label="Permalink to &quot;Return Mode Precision Landing&quot;">​</a></h3><p>Precision landing can be used in the <a href="./../flight_modes_mc/return.html">Return mode</a> landing phase.</p><p>This is enabled using the parameter <a href="./../advanced_config/parameter_reference.html#RTL_PLD_MD">RTL_PLD_MD</a>, which takes the following values:</p><ul><li><code>0</code>: Precision landing disabled (land as normal).</li><li><code>1</code>: <a href="#opportunistic-mode">Opportunistic</a> precision landing.</li><li><code>2</code>: <a href="#required-mode">Required</a> precision landing.</li></ul><h3 id="precision-landing-flight-mode" tabindex="-1">Precision Landing Flight Mode <a class="header-anchor" href="#precision-landing-flight-mode" aria-label="Permalink to &quot;Precision Landing Flight Mode&quot;">​</a></h3><p>Precision landing can be enabled by switching to the <em>Precision Landing</em> flight mode.</p><p>You can verify this using the <a href="./../debug/mavlink_shell.html#qgroundcontrol-mavlink-console"><em>QGroundControl</em> MAVLink Console</a> to enter the following command:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">commander</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mode</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> auto:precland</span></span></code></pre></div><div class="info custom-block"><p class="custom-block-title">INFO</p><p>When switching to the mode in this way, the precision landing is always &quot;required&quot;; there is no way to specify the type of landing.</p></div><div class="info custom-block"><p class="custom-block-title">INFO</p><p>At time of writing is no <em>convenient</em> way to directly invoke precision landing (other than commanding return mode):</p><ul><li><em>QGroundControl</em> does not provide it as a UI option.</li><li><a href="https://mavlink.io/en/messages/common.html#MAV_CMD_NAV_LAND" target="_blank" rel="noreferrer">MAV_CMD_NAV_LAND</a> only works in missions.</li><li><a href="https://mavlink.io/en/messages/common.html#MAV_CMD_DO_SET_MODE" target="_blank" rel="noreferrer">MAV_CMD_DO_SET_MODE</a> should work, but you will need to determine the appropriate base and custom modes used by PX4 to represent the precision landing mode.</li></ul></div><h2 id="hardware-setup" tabindex="-1">Hardware Setup <a class="header-anchor" href="#hardware-setup" aria-label="Permalink to &quot;Hardware Setup&quot;">​</a></h2><h3 id="ir-sensor-beacon-setup" tabindex="-1">IR Sensor/Beacon Setup <a class="header-anchor" href="#ir-sensor-beacon-setup" aria-label="Permalink to &quot;IR Sensor/Beacon Setup&quot;">​</a></h3><p>The IR sensor/landing beacon solution requires an <a href="https://irlock.com/products/ir-lock-sensor-precision-landing-kit" target="_blank" rel="noreferrer">IR-LOCK Sensor</a> and downward facing <a href="./../sensor/rangefinders.html">distance sensor</a> connected to the flight controller, and an IR beacon as a target (e.g. <a href="https://irlock.com/collections/markone" target="_blank" rel="noreferrer">IR-LOCK MarkOne</a>). This enables landing with a precision of roughly 10 cm (GPS precision, by contrast, may be as large as several meters).</p><p>Install the IR-LOCK sensor by following the <a href="https://irlock.readme.io/v2.0/docs" target="_blank" rel="noreferrer">official guide</a>. Ensure that the sensor&#39;s x axis is aligned with the vehicle&#39;s y axis and the sensor&#39;s y axis aligned with the vehicle&#39;s -x direction (this is the case if the camera is pitched down 90 degrees from facing forward).</p><p>Install a <a href="./../sensor/rangefinders.html">range/distance sensor</a> (the <em>LidarLite v3</em> has been found to work well).</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Many infrared based range sensors do not perform well in the presence of the IR-LOCK beacon. Refer to the IR-LOCK guide for other compatible sensors.</p></div><h2 id="offboard-positioning" tabindex="-1">Offboard Positioning <a class="header-anchor" href="#offboard-positioning" aria-label="Permalink to &quot;Offboard Positioning&quot;">​</a></h2><p>The offboard solution requires a positioning system that implements the MAVLink <a href="https://mavlink.io/en/services/landing_target.html" target="_blank" rel="noreferrer">Landing Target Protocol</a>. This can use any positioning mechanism to determine the landing target, for example computer vision and a visual marker.</p><p>The system must publish the coordinates of the target in the <a href="https://mavlink.io/en/messages/common.html#LANDING_TARGET" target="_blank" rel="noreferrer">LANDING_TARGET</a> message. Note that PX4 <em>requires</em> <code>LANDING_TARGET.frame</code> to be <a href="https://mavlink.io/en/messages/common.html#MAV_FRAME_LOCAL_NED" target="_blank" rel="noreferrer">MAV_FRAME_LOCAL_NED</a> and only populates the fields <code>x</code>, <code>y</code>, and <code>z</code>. The origin of the local NED frame [0,0] is the home position (you can map this home position to global coordinates using <a href="https://mavlink.io/en/messages/common.html#GPS_GLOBAL_ORIGIN" target="_blank" rel="noreferrer">GPS_GLOBAL_ORIGIN</a>).</p><p>PX4 does not explicitly require a <a href="./../sensor/rangefinders.html">distance sensor</a> or other sensors, but will perform better if it can more precisely determine its own position.</p><h2 id="firmware-configuration" tabindex="-1">Firmware Configuration <a class="header-anchor" href="#firmware-configuration" aria-label="Permalink to &quot;Firmware Configuration&quot;">​</a></h2><p>Precision landing requires the modules <code>irlock</code> and <code>landing_target_estimator</code>. These are included in PX4 firmware by default, for most flight controllers.</p><p>They are not included by default on FMUv2-based controllers. On these, and other boards where they are not included, you can add them by setting the following keys to &#39;y&#39; in the relevant configuration file for your flight controller (e.g. as done here for FMUv5: <a href="https://github.com/PX4/PX4-Autopilot/blob/release/1.15/boards/px4/fmu-v5/default.px4board" target="_blank" rel="noreferrer">PX4-Autopilot/boards/px4/fmu-v5/default.px4board</a>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>CONFIG_DRIVERS_IRLOCK=y</span></span>
<span class="line"><span>CONFIG_MODULES_LANDING_TARGET_ESTIMATOR=y</span></span></code></pre></div><h2 id="px4-configuration-parameters" tabindex="-1">PX4 Configuration (Parameters) <a class="header-anchor" href="#px4-configuration-parameters" aria-label="Permalink to &quot;PX4 Configuration (Parameters)&quot;">​</a></h2><p>The IR-Lock sensor is disabled by default. Enable it by setting <a href="./../advanced_config/parameter_reference.html#SENS_EN_IRLOCK">SENS_EN_IRLOCK</a> to <code>1</code> (true).</p><p><a href="./../advanced_config/parameter_reference.html#LTEST_MODE">LTEST_MODE</a> determines if the target is assumed to be stationary or moving. If <code>LTEST_MODE</code> is set to moving (e.g. it is installed on a vehicle on which the multicopter is to land), target measurements are only used to generate position setpoints in the precision landing controller. If <code>LTEST_MODE</code> is set to stationary, the target measurements are also used by the vehicle position estimator (EKF2 or LPE).</p><p>Other relevant parameters are listed in the parameter reference under <a href="./../advanced_config/parameter_reference.html#landing-target-estimator">Landing_target estimator</a> and <a href="./../advanced_config/parameter_reference.html#precision-land">Precision land</a> parameters. Some of the most useful ones are listed below.</p><table><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><a id="SENS_EN_IRLOCK"></a><a href="./../advanced_config/parameter_reference.html#SENS_EN_IRLOCK">SENS_EN_IRLOCK</a></td><td>IR-LOCK Sensor (external I2C). Disable: <code>0</code> (default): Enable: <code>1</code>).</td></tr><tr><td><a id="LTEST_MODE"></a><a href="./../advanced_config/parameter_reference.html#LTEST_MODE">LTEST_MODE</a></td><td>Landing target is moving (<code>0</code>) or stationary (<code>1</code>). Default is moving.</td></tr><tr><td><a id="PLD_HACC_RAD"></a><a href="./../advanced_config/parameter_reference.html#PLD_HACC_RAD">PLD_HACC_RAD</a></td><td>Horizontal acceptance radius, within which the vehicle will start descending. Default is 0.2m.</td></tr><tr><td><a id="PLD_BTOUT"></a><a href="./../advanced_config/parameter_reference.html#PLD_BTOUT">PLD_BTOUT</a></td><td>Landing Target Timeout, after which the target is assumed lost. Default is 5 seconds.</td></tr><tr><td><a id="PLD_FAPPR_ALT"></a><a href="./../advanced_config/parameter_reference.html#PLD_FAPPR_ALT">PLD_FAPPR_ALT</a></td><td>Final approach altitude. Default is 0.1 metres.</td></tr><tr><td><a id="PLD_MAX_SRCH"></a><a href="./../advanced_config/parameter_reference.html#PLD_MAX_SRCH">PLD_MAX_SRCH</a></td><td>Maximum number of search attempts in an required landing.</td></tr><tr><td><a id="RTL_PLD_MD"></a><a href="./../advanced_config/parameter_reference.html#RTL_PLD_MD">RTL_PLD_MD</a></td><td>RTL precision land mode. <code>0</code>: disabled, <code>1</code>: <a href="#opportunistic-mode">Opportunistic</a>, <code>2</code>: <a href="#required-mode">Required</a>.</td></tr></tbody></table><h3 id="ir-beacon-scaling" tabindex="-1">IR Beacon Scaling <a class="header-anchor" href="#ir-beacon-scaling" aria-label="Permalink to &quot;IR Beacon Scaling&quot;">​</a></h3><p>Measurement scaling may be necessary due to lens distortions of the IR-LOCK sensor.</p><p><a href="./../advanced_config/parameter_reference.html#LTEST_SCALE_X">LTEST_SCALE_X</a> and <a href="./../advanced_config/parameter_reference.html#LTEST_SCALE_Y">LTEST_SCALE_Y</a> can be used to scale beacon measurements before they are used to estimate the beacon&#39;s position and velocity relative to the vehicle. Note that <code>LTEST_SCALE_X</code> and <code>LTEST_SCALE_Y</code> are considered in the sensor frame, not the vehicle frame.</p><p>To calibrate these scale parameters, set <code>LTEST_MODE</code> to moving, fly your multicopter above the beacon and perform forward-backward and left-right motions with the vehicle, while <a href="./../dev_log/logging.html#configuration">logging</a> <code>landing_target_pose</code> and <code>vehicle_local_position</code>. Then, compare <code>landing_target_pose.vx_rel</code> and <code>landing_target_pose.vy_rel</code> to <code>vehicle_local_position.vx</code> and <code>vehicle_local_position.vy</code>, respectively (both measurements are in NED frame). If the estimated beacon velocities are consistently smaller or larger than the vehicle velocities, adjust the scale parameters to compensate.</p><p>If you observe slow sideways oscillations of the vehicle while doing a precision landing with <code>LTEST_MODE</code> set to stationary, the beacon measurements are likely scaled too high and you should reduce the scale parameter in the relevant direction.</p><h2 id="simulation" tabindex="-1">Simulation <a class="header-anchor" href="#simulation" aria-label="Permalink to &quot;Simulation&quot;">​</a></h2><p>Precision landing with the IR-LOCK sensor and beacon can be simulated in <a href="./../sim_gazebo_classic/">Gazebo Classic</a>.</p><p>To start the simulation with the world that contains a IR-LOCK beacon and a vehicle with a range sensor and IR-LOCK camera, run:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> px4_sitl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gazebo-classic_iris_irlock</span></span></code></pre></div><p>You can change the location of the beacon either by moving it in the Gazebo Classic GUI or by changing its location in the <a href="https://github.com/PX4/PX4-SITL_gazebo-classic/blob/main/worlds/iris_irlock.world#L42" target="_blank" rel="noreferrer">Gazebo world</a>.</p><h2 id="operating-principles" tabindex="-1">Operating Principles <a class="header-anchor" href="#operating-principles" aria-label="Permalink to &quot;Operating Principles&quot;">​</a></h2><h3 id="landing-target-estimator" tabindex="-1">Landing Target Estimator <a class="header-anchor" href="#landing-target-estimator" aria-label="Permalink to &quot;Landing Target Estimator&quot;">​</a></h3><p>The <code>landing_target_estimator</code> takes measurements from the <code>irlock</code> driver as well as the estimated terrain height to estimate the beacon&#39;s position relative to the vehicle.</p><p>The measurements in <code>irlock_report</code> contain the tangent of the angles from the image center to the beacon. In other words, the measurements are the x and y components of the vector pointing towards the beacon, where the z component has length &quot;1&quot;. This means that scaling the measurement by the distance from the camera to the beacon results in the vector from the camera to the beacon. This relative position is then rotated into the north-aligned, level body frame using the vehicle&#39;s attitude estimate. Both x and y components of the relative position measurement are filtered in separate Kalman Filters, which act as simple low-pass filters that also produce a velocity estimate and allow for outlier rejection.</p><p>The <code>landing_target_estimator</code> publishes the estimated relative position and velocity whenever a new <code>irlock_report</code> is fused into the estimate. Nothing is published if the beacon is not seen or beacon measurements are rejected. The landing target estimate is published in the <code>landing_target_pose</code> uORB message.</p><h3 id="enhanced-vehicle-position-estimation" tabindex="-1">Enhanced Vehicle Position Estimation <a class="header-anchor" href="#enhanced-vehicle-position-estimation" aria-label="Permalink to &quot;Enhanced Vehicle Position Estimation&quot;">​</a></h3><p>If the target is specified to be stationary using the parameter <code>LTEST_MODE</code>, the vehicle&#39;s position/velocity estimate can be improved with the help of the target measurements. This is done by fusing the target&#39;s velocity as a measurement of the negative velocity of the vehicle.</p><h3 id="landing-phases-flow-diagram" tabindex="-1">Landing Phases Flow Diagram <a class="header-anchor" href="#landing-phases-flow-diagram" aria-label="Permalink to &quot;Landing Phases Flow Diagram&quot;">​</a></h3><p>This image shows the <a href="#landing-phases">landing phases</a> as a flow diagram.</p><p><img src="`+e+'" alt="Precision Landing Flow Diagram"></p>',73),s=[r];function d(l,c,h,p,m,g){return t(),i("div",null,s)}const b=a(o,[["render",d]]);export{_ as __pageData,b as default};
