import{_ as e,a as t}from"./chunks/gimbal-simulation.BEN-KceN.js";import{_ as a,c as i,o,ab as n}from"./chunks/framework.CUflZczI.js";const _=JSON.parse('{"title":"Gimbal Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"en/advanced/gimbal_control.md","filePath":"en/advanced/gimbal_control.md"}'),l={name:"en/advanced/gimbal_control.md"},r=n('<h1 id="gimbal-configuration" tabindex="-1">Gimbal Configuration <a class="header-anchor" href="#gimbal-configuration" aria-label="Permalink to &quot;Gimbal Configuration&quot;">​</a></h1><p>This page explains how to configure and control a gimbal that has an attached camera (or any other payload).</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>PX4 contains a generic mount/gimbal control driver that supports different input and output methods:</p><ul><li>The input method defines the protocol used to command a gimbal mount that is managed by PX4. This might be an RC controller, a MAVLink command sent by a GCS, or both — automatically switching between them.</li><li>The output method defines how PX4 communicates with the connected gimbal. The recommended protocol is MAVLink v2, but you can also connect directly to a flight controller PWM output port.</li></ul><p>PX4 takes the input signal and routes/translates it to be sent through to the output. Any input method can be selected to drive any output.</p><p>Both the input and output are configured using parameters. The input is set using the parameter <a href="./../advanced_config/parameter_reference.html#MNT_MODE_IN">MNT_MODE_IN</a>. By default this is set to <code>Disabled (-1)</code> and the driver does not run. After selecting the input mode, reboot the vehicle to start the mount driver.</p><p>You should set <code>MNT_MODE_IN</code> to one of: <code>RC (1)</code>, <code>MAVlink gimbal protocol v2 (4)</code> or <code>Auto (0)</code> (the other options are deprecated). If you select <code>Auto (0)</code>, the gimbal will automatically select either RC or or MAVLink input based on the latest input. Note that the auto-switch from MAVLink to RC requires a large stick motion!</p><p>The output is set using the <a href="./../advanced_config/parameter_reference.html#MNT_MODE_OUT">MNT_MODE_OUT</a> parameter. By default the output is set to a PXM port (<code>AUX (0)</code>). If the <a href="https://mavlink.io/en/services/gimbal_v2.html" target="_blank" rel="noreferrer">MAVLink Gimbal Protocol v2</a> is supported by your gimbal, you should instead select <code>MAVLink gimbal protocol v2 (2)</code>.</p><p>The full list of parameters for setting up the mount driver can be found in <a href="./../advanced_config/parameter_reference.html#mount">Parameter Reference &gt; Mount</a>. The relevant settings for a number of common gimbal configurations are described below.</p><h2 id="mavlink-gimbal-mnt-mode-out-mavlink" tabindex="-1">MAVLink Gimbal (MNT_MODE_OUT=MAVLINK) <a class="header-anchor" href="#mavlink-gimbal-mnt-mode-out-mavlink" aria-label="Permalink to &quot;MAVLink Gimbal (MNT_MODE_OUT=MAVLINK)&quot;">​</a></h2><p>Each physical gimbal device on the system must have its own high level gimbal manager, which is discoverable by a ground station using the MAVLink gimbal protocol. The ground station sends high level <a href="https://mavlink.io/en/services/gimbal_v2.html#gimbal-manager-messages" target="_blank" rel="noreferrer">MAVLink Gimbal Manager</a> commands to the manager of the gimbal it wants to control, and the manager will in turn send appropriate lower level &quot;gimbal device&quot; commands to control the gimbal.</p><p>PX4 can be configured as the gimbal manager to control a single gimbal device (which can either be physically connected or be a MAVLink gimbal that implements the <a href="https://mavlink.io/en/services/gimbal_v2.html#gimbal-device-messages" target="_blank" rel="noreferrer">gimbal device interface</a>).</p><p>To enable a MAVLink gimbal, first set parameter <a href="./../advanced_config/parameter_reference.html#MNT_MODE_IN">MNT_MODE_IN</a> to <code>MAVlink gimbal protocol v2</code> and <a href="./../advanced_config/parameter_reference.html#MNT_MODE_OUT">MNT_MODE_OUT</a> to <code>MAVLink gimbal protocol v2</code>.</p><p>The gimbal can be connected to <em>any free serial port</em> using the instructions in <a href="./../peripherals/mavlink_peripherals.html">MAVLink Peripherals (GCS/OSD/Companion)</a> (also see <a href="./../peripherals/serial_configuration.html#serial-port-configuration">Serial Port Configuration</a>). For example, if the <code>TELEM2</code> port on the flight controller is unused you can connect it to the gimbal and set the following PX4 parameters:</p><ul><li><a href="./../advanced_config/parameter_reference.html#MAV_1_CONFIG">MAV_1_CONFIG</a> to <strong>TELEM2</strong> (if <code>MAV_1_CONFIG</code> is already used for a companion computer (say), use <code>MAV_2_CONFIG</code>).</li><li><a href="./../advanced_config/parameter_reference.html#MAV_1_MODE">MAV_1_MODE</a> to <strong>NORMAL</strong></li><li><a href="./../advanced_config/parameter_reference.html#SER_TEL2_BAUD">SER_TEL2_BAUD</a> to manufacturer recommended baud rate.</li></ul><h3 id="multiple-gimbal-support" tabindex="-1">Multiple Gimbal Support <a class="header-anchor" href="#multiple-gimbal-support" aria-label="Permalink to &quot;Multiple Gimbal Support&quot;">​</a></h3><p>PX4 can automatically create a gimbal manager for a connected PWM gimbal or the first MAVLink gimbal device with the same system id it detects on any interface. It does not automatically create gimbal manager for any other MAVLink gimbal devices that it detects.</p><p>You can support additional gimbals provided that they:</p><ul><li>implement the gimbal <em>manager</em> protocol</li><li>Are visible to the ground station and PX4 on the MAVLink network. This may require that traffic forwarding be configured between PX4, the GCS, and the gimbal.</li><li>Each gimbal must have a unique component id. For a PWM connected gimbal this will be the component ID of the autopilot</li></ul><h2 id="gimbal-on-fc-pwm-output-mnt-mode-out-aux" tabindex="-1">Gimbal on FC PWM Output (MNT_MODE_OUT=AUX) <a class="header-anchor" href="#gimbal-on-fc-pwm-output-mnt-mode-out-aux" aria-label="Permalink to &quot;Gimbal on FC PWM Output (MNT_MODE_OUT=AUX)&quot;">​</a></h2><p>The gimbal can also be controlled by connecting it to up to three flight controller PWM ports and setting the output mode to <code>MNT_MODE_OUT=AUX</code>.</p><p>The output pins that are used to control the gimbal are set in the <a href="./../config/actuators.html#actuator-outputs">Acuator Configuration &gt; Outputs</a> by selecting any three unused Actuator Outputs and assigning them the following output functions:</p><ul><li><code>Gimbal Roll</code>: Output controls gimbal roll.</li><li><code>Gimbal Pitch</code>: Output controls Gimbal pitch.</li><li><code>Gimbal Yaw</code>: Output controls Gimbal yaw.</li></ul><p>For example, you might have the following settings to assign the gimbal roll, pitch and yaw to AUX1-3 outputs.</p><p><img src="'+e+'" alt="Gimbal Actuator config"></p><p>The PWM values to use for the disarmed, maximum and minimum values can be determined in the same way as other servo, using the <a href="./../config/actuators.html#actuator-testing">Actuator Test sliders</a> to confirm that each slider moves the appropriate axis, and changing the values so that the gimbal is in the appropriate position at the disarmed, low and high position in the slider. The values may also be provided in gimbal documentation.</p><h2 id="sitl" tabindex="-1">SITL <a class="header-anchor" href="#sitl" aria-label="Permalink to &quot;SITL&quot;">​</a></h2><p>The <a href="./../sim_gazebo_classic/">Gazebo Classic</a> simulation <a href="./../sim_gazebo_classic/vehicles.html#typhoon-h480-hexrotor">Typhoon H480 model</a> comes with a preconfigured simulated gimbal.</p><p>To run it, use:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> px4_sitl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gazebo-classic_typhoon_h480</span></span></code></pre></div><p>To just test the <a href="./../modules/modules_driver.html#gimbal">gimbal driver</a> on other models or simulators, make sure the driver runs (using <code>gimbal start</code>), then configure its parameters.</p><h2 id="testing" tabindex="-1">Testing <a class="header-anchor" href="#testing" aria-label="Permalink to &quot;Testing&quot;">​</a></h2><p>The driver provides a simple test command. The following describes testing in SITL, but the commands also work on a real device.</p><p>Start the simulation with (no parameter needs to be changed for that):</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> px4_sitl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gazebo-classic_typhoon_h480</span></span></code></pre></div><p>Make sure it&#39;s armed, eg. with <code>commander takeoff</code>, then use the following command to control the gimbal (for example):</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gimbal</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> yaw</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 30</span></span></code></pre></div><p>Note that the simulated gimbal stabilizes itself, so if you send MAVLink commands, set the <code>stabilize</code> flags to <code>false</code>.</p><p><img src="'+t+'" alt="Gazebo Gimbal Simulation"></p>',40),s=[r];function h(c,m,d,p,u,g){return o(),i("div",null,s)}const v=a(l,[["render",h]]);export{_ as __pageData,v as default};
