import{_ as a,a as r,b as i,c as s,d as n,e as l,f as c,g as u,h as d,i as h,j as e,k as p,l as m,m as f,n as g,o as v,p as y,q as b,r as w,s as T,t as _}from"./chunks/reverse_dshot.DOJjvoGl.js";import{_ as S,E as C,c as q,J as A,m as t,a as M,ab as P,o as k}from"./chunks/framework.CUflZczI.js";const U=JSON.parse('{"title":"Actuator Configuration and Testing","description":"","frontmatter":{},"headers":[],"relativePath":"en/config/actuators.md","filePath":"en/config/actuators.md"}'),O={name:"en/config/actuators.md"},x=t("h1",{id:"actuator-configuration-and-testing",tabindex:"-1"},[M("Actuator Configuration and Testing "),t("a",{class:"header-anchor",href:"#actuator-configuration-and-testing","aria-label":'Permalink to "Actuator Configuration and Testing"'},"​")],-1),I=P('<p>The <em>Actuators Setup</em> view is used to customize the specific geometry of the vehicle, assign actuators and motors to flight controller outputs, and test the actuator and motor response.</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Open the view in <em>QGroundControl</em> here: <strong>&quot;Q&quot; (app menu) &gt; Vehicle Setup &gt; Actuators</strong> (tab). The displayed elements depend on the <a href="./../config/airframe.html">selected frame</a>, with outputs mapped by default as shown in the <a href="./../airframes/airframe_reference.html">Airframe Reference</a>.</p><p>The view has three sections:</p><ul><li><a href="#geometry">Geometry</a>: Configure the geometry for the <a href="./../config/airframe.html">selected airframe</a>. This includes number, position and properties of <a href="#motor-geometry">motors</a> and also the number and properties of <a href="#control-surfaces-geometry">control surfaces</a> and <a href="#motor-tilt-servo-geometry">motor tilt servos</a>.</li><li><a href="#actuator-outputs">Actuator Outputs</a>: Assign motors, control surfaces, and other actuators to specific output.</li><li><a href="#actuator-testing">Actuator Testing</a>: Test that motors and actuators move in the direction / speed as expected.</li></ul><p>A quadcopter might have an setup screen similar to the one shown below. This defines a 4-rotor copter with X-geometry. It maps the 4 motors to the AUX1 to AUX4 outputs, and specifies that the connect to DShot1200 ESC. It also maps PWM400 AUX outputs for controlling a parachute and landing gear.</p><p><img src="'+a+'" alt="Actuators MC (QGC)"></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Only the most common settings are displayed by default. Select the <strong>Advanced</strong> checkbox in the top right corner to display all settings.</p></div><h2 id="geometry" tabindex="-1">Geometry <a class="header-anchor" href="#geometry" aria-label="Permalink to &quot;Geometry&quot;">​</a></h2><p>The geometry section is used to set any configurable geometry-related parameters for the selected <a href="./../config/airframe.html">airframe</a>. This includes the number and position of <a href="#motor-geometry">motors</a>, and the number, function, and properties of <a href="#control-surfaces-geometry">control surfaces</a>. For VTOL tiltrotor vehicles, it will also include the number and properties of <a href="#motor-tilt-servo-geometry">tilt servos</a></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The UI is customised for the selected airframe:</p><ul><li>Only <em>configurable</em> fields for the selected airframe type are displayed; fields that aren&#39;t configurable for the airframe are hidden.</li><li>The motor position diagram is currently only displayed for multicopter frames.</li></ul></div><h3 id="motor-geometry" tabindex="-1">Motor Geometry <a class="header-anchor" href="#motor-geometry" aria-label="Permalink to &quot;Motor Geometry&quot;">​</a></h3><p>The motor geometry section lets you set the number of motors, the relative position, and other properties of each motor.</p><p>Most motor properties apply to all frames. A few properties apply to specific frames. For example, <code>Tilted-by</code> and <code>axis</code> are only relevant for <a href="#motor-geometry-vtol-tiltrotor">Tiltrotor VTOL</a> and <a href="#motor-geometry-standard-vtol">Standard VTOL</a> vehicles, respectively.</p><p>The geometry configuration for multicopter airframes provides a diagram showing the relative x,y positions for each of the motors. See the <a href="./../airframes/airframe_reference.html">Airframe Reference</a> for an broad understanding of the motor positions for other frames.</p><p>Core geometry concepts and the configuration for a number of different frames are provided in the following sections.</p><h4 id="motor-geometry-multicopter" tabindex="-1">Motor Geometry: Multicopter <a class="header-anchor" href="#motor-geometry-multicopter" aria-label="Permalink to &quot;Motor Geometry: Multicopter&quot;">​</a></h4><p>The image below shows the geometry setup for a quadrotor multicopter frame with and without advanced settings.</p><p><img src="'+r+'" alt="Geometry MC (QGC)"></p><p>First, the <strong>Motors</strong> drop-down setting lets you choose the number of motors (4 for the example above).</p><p>For each motor you can then set:</p><ul><li><code>Position X</code>: <a href="#motor-position-coordinate-system">X-position</a>, in metres.</li><li><code>Position Y</code>: <a href="#motor-position-coordinate-system">Y-position</a>, in metres.</li><li><code>Position Z</code>: <a href="#motor-position-coordinate-system">Z-position</a>, in metres.</li><li>(Advanced) <code>Direction CCW</code>: Checkbox to indicate motor spins counter-clockwise (uncheck for clockwise).</li><li>(Advanced) <code>Bidirectional</code>: Checkbox to indicate motor is <a href="#bidirectional-motors">bidirectional</a></li><li>(Advanced) <code>Slew Rate</code>: Refer to the <a href="#control-surfaces-geometry">Control Surfaces Geometry</a> section for more information</li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The <code>X</code>, <code>Y</code>, <code>Z</code> positions are in <a href="#motor-position-coordinate-system">FRD coordinate frame, relative to the <em>centre of gravity</em></a>. Note, this may not be the same as the position of the flight controller!</p></div><h4 id="motor-geometry-vtol-quadrotor-tailsitter" tabindex="-1">Motor Geometry: VTOL Quadrotor Tailsitter <a class="header-anchor" href="#motor-geometry-vtol-quadrotor-tailsitter" aria-label="Permalink to &quot;Motor Geometry: VTOL Quadrotor Tailsitter&quot;">​</a></h4><p>The motor geometry for a <a href="./../airframes/airframe_reference.html#vtol-tailsitter">VTOL Quad Tailsitter</a> is shown below (the approach for configuring other tailsitter VTOL vehicles will be similar).</p><p>Motors have the same configuration fields as for the <a href="#motor-geometry-multicopter">multicopter geometry</a>.</p><p><img src="'+i+'" alt="Geometry motor: tailsitter vtol"></p><h4 id="motor-geometry-vtol-tiltrotor" tabindex="-1">Motor Geometry: VTOL Tiltrotor <a class="header-anchor" href="#motor-geometry-vtol-tiltrotor" aria-label="Permalink to &quot;Motor Geometry: VTOL Tiltrotor&quot;">​</a></h4><p>The motor geometry for a <a href="./../airframes/airframe_reference.html#vtol_vtol_tiltrotor_generic_quadplane_vtol_tiltrotor">Generic Quadplane VTOL Tiltrotor</a> is shown below (the approach for configuring other <a href="./../airframes/airframe_reference.html#vtol_vtol_tiltrotor_generic_quadplane_vtol_tiltrotor">VTOL tiltrotors</a> will be similar).</p><p><img src="'+s+'" alt="Geometry motor: tiltrotor vtol"></p><ul><li><code>Tilted by</code>: The associated servo used for tilting the motor. The properties of this servo are defined in the <a href="#motor-tilt-servo-geometry">Motor Tilt Servo Geometry</a>.</li></ul><h4 id="motor-geometry-standard-vtol" tabindex="-1">Motor Geometry: Standard VTOL <a class="header-anchor" href="#motor-geometry-standard-vtol" aria-label="Permalink to &quot;Motor Geometry: Standard VTOL&quot;">​</a></h4><p>The motor geometry for a <a href="./../airframes/airframe_reference.html#vtol_standard_vtol_generic_standard_vtol">Generic Standard VTOL</a> is shown below (the approach for configuring other &quot;Standard VTOL&quot; will be similar).</p><p><img src="'+n+'" alt="Geometry motor: standard vtol"></p><p>Motors have most of the same configuration fields as for the <a href="#motor-geometry-multicopter">multicopter geometry</a>. There is an additional field to indicate the direction in which the motor moves the vehicle (for a standard VTOL, the hover motors are usually set &quot;upwards&quot; and the pusher motor is set to &quot;forwards&quot;).</p><ul><li><code>Axis</code>: One of <code>Upwards</code>, <code>Downwards</code>, <code>Forwards</code>, <code>Backwards</code>, <code>Leftwards</code>, <code>Rightwards</code>, <code>Custom</code><ul><li>If <code>Custom</code> is selected, then the UI displays three additional fields for setting the motor orientation.</li></ul></li></ul><h4 id="motor-geometry-other-vehicles" tabindex="-1">Motor Geometry: Other Vehicles <a class="header-anchor" href="#motor-geometry-other-vehicles" aria-label="Permalink to &quot;Motor Geometry: Other Vehicles&quot;">​</a></h4><p>Other vehicle types will define an appropriate motor geometry for their frame type. Once again these motors will generally have the same kinds of properties as shown above.</p><p>For example, a fixed-wing vehicle may just have a single pusher moter, while a rover with differential steering will have a motor for throttle and for steering.</p><h4 id="motor-position-coordinate-system" tabindex="-1">Motor Position Coordinate System <a class="header-anchor" href="#motor-position-coordinate-system" aria-label="Permalink to &quot;Motor Position Coordinate System&quot;">​</a></h4><p>The coordinate system for motor positions is FRD (in body frame), where the X axis points forward, the Y axis to the right and the Z axis down.</p><p>The origin is the vehicle&#39;s <strong>centre-of-gravity (COG)</strong>. This may <strong>NOT</strong> be the same position as the location of the autopilot.</p><p><img src="'+l+'" alt="Actuators CG reference diagram"></p><h4 id="bidirectional-motors" tabindex="-1">Bidirectional Motors <a class="header-anchor" href="#bidirectional-motors" aria-label="Permalink to &quot;Bidirectional Motors&quot;">​</a></h4><p>Some vehicles may use bidirectional motors (i.e. motors that support spinning in both directions). For example, ground vehicles that want to move forwards and backwards, or VTOL vehicles that have pusher motors that can turn in either direction.</p><p>If bidiectional motors are used, make sure to select the <strong>Reversible</strong> checkbox for those motors (the checkbox is displayed as an &quot;advanced&quot; option).</p><p><img src="'+c+'" alt="Reversible"></p><p>Note that you will need to also ensure that the ESC associated with bidirectional motors is configured appropriately (e.g. 3D mode enabled for DShot ESCs, which can be achieved via <a href="./../peripherals/dshot.html#commands">DShot commands</a>).</p><h3 id="control-surfaces-geometry" tabindex="-1">Control Surfaces Geometry <a class="header-anchor" href="#control-surfaces-geometry" aria-label="Permalink to &quot;Control Surfaces Geometry&quot;">​</a></h3><p>The control surfaces section of the geometry panel lets you set the number and types of control surfaces that are present on the vehicle. You may also need to set trim and slew rate values in some cases. More advanced users can also configure the roll scale, yaw scale, and pitch scale (generally the defaults are acceptable, and this is not needed). An &quot;example&quot; control surface section for a vehicle with two ailerons is shown below. Note that ailerons only affect roll, so the pitch and yaw fields are disabled.</p><p><img src="'+u+'" alt="Control Surface Setup Example"></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Only the most common settings are displayed by default. Select the <strong>Advanced</strong> checkbox in the top right corner of the view to display all settings.</p></div><p>The fields are:</p><ul><li><code>Control Surfaces</code>: The number of control surfaces (set this first!)</li><li><code>Type</code>: The type of each control surface: <code>LeftAileron</code>, <code>RightAileron</code>, <code>Elevator</code>, <code>Rudder</code>, <code>Left Elevon</code>, <code>Right Elevon</code>, <code>Left V-Tail</code>, <code>Right V-Tail</code>, <code>Left Flap</code>, <code>Right Flap</code>, <code>Airbrakes</code>, <code>Custom</code>.</li><li><code>Roll Torque</code>: Effectiveness of actuator around roll axis (normalised: -1 to 1). <a href="#actuator-roll-pitch-and-yaw-scaling">Generally you should use the default actuator value</a>.</li><li><code>Pitch Torque</code>: Effectiveness of actuator around pitch axis (normalised: -1 to 1). <a href="#actuator-roll-pitch-and-yaw-scaling">Generally you should use the default actuator value</a>.</li><li><code>Yaw Torque</code>: Effectiveness of actuator around yaw axis (normalised: -1 to 1). <a href="#actuator-roll-pitch-and-yaw-scaling">Generally you should use the default actuator value</a>.</li><li><code>Trim</code>: An offset added to the actuator so that it is centered without input. This might be determined by trial and error.</li><li>(Advanced) <code>Slew Rate</code>: Minimum time allowed for the motor/servo signal to pass through the full output range, in seconds. <ul><li>The setting limits the rate of change of an actuator (if not specified then no rate limit is applied). It is intended for actuators that may be damaged if they move too fast — such as the tilting actuators on a tiltrotor VTOL vehicle.</li><li>For example, a setting of 2.0 means that the motor/servo will not be commanded to move from 0 to 1 at a rate that completes the operation in less than 2 seconds (in case of reversible motors, the range is -1 to 1).</li></ul></li><li>(Advanced) <code>Flap Scale</code>: How much this actuator is deflected at the &quot;full flaps configuration&quot; [0, 1] (see <a href="#flap-scale-and-spoiler-scale-configuration">Flap Scale and Spoiler Scale Configuration</a> below). Can be used to configure aerodynamic surface as flap or to compensate for generated torque through main flaps.</li><li>(Advanced) <code>Spoiler Scale</code>: How much this actuator is deflected at the &quot;full spoiler configuration&quot; [0, 1] (see <a href="#flap-scale-and-spoiler-scale-configuration">Flap Scale and Spoiler Scale Configuration</a> below). Can be used to configure aerodynamic surface as spoiler or to compensate for generated torque through main spoiler.</li><li>(VTOL only) <code>Lock control surfaces in hover</code>: <ul><li><code>Enabled</code>: Most vehicles do not use control surfaces in hover. Use this setting to lock them so that they don&#39;t affect vehicle dynamics.</li><li><code>Disabled</code>: Set this for vehicles that use control surfaces in hover, such as the duo tailsitter (which uses elevons for pitch and yaw control). It should also be set for vehicles that use control surfaces to provide additional stabilization in hover mode when moving at speed or in high winds.</li></ul></li></ul><h4 id="flap-scale-and-spoiler-scale-configuration" tabindex="-1">Flap Scale and Spoiler Scale Configuration <a class="header-anchor" href="#flap-scale-and-spoiler-scale-configuration" aria-label="Permalink to &quot;Flap Scale and Spoiler Scale Configuration&quot;">​</a></h4><p>&quot;Flap-control&quot; and &quot;Spoiler-control&quot; are aerodynamic configurations that can either be commanded manually by the pilot (using RC, say), or are set automatically by the controller. For example, a pilot or the landing system might engage &quot;Spoiler-control&quot; in order to reduce the airspeed before landing.</p><p>The configurations are an <em>abstract</em> way for the controller to tell the allocator how much it should adjust the aerodynamic properties of the wings relative to the &quot;full flaps&quot; or &quot;full spoiler&quot; configuration (between <code>[0,1]</code>, where &quot;1&quot; indicates the full range). The allocator then uses any of the available control surfaces it wants in order to achieve the requested configuration: usually flaps, ailerons, and elevator.</p><p>The <code>flap scale</code> and <code>spoiler scale</code> settings in the actuator UI inform the allocator how much ailerons, elevators, flaps, spoilers, and other control surfaces, contribute to a requested &quot;Flap-control&quot; and/or &quot;Spoiler-control&quot; value. Specifically, they indicate how much each control surface should be deflected when the controller is demanding &quot;full flaps&quot; or &quot;full spoiler&quot;.</p><p>In the following example, the vehicle has two ailerons, one elevator, one rudder and two flaps as control surfaces:</p><p><img src="'+d+'" alt="Flaps and spoiler actuator configuration example"></p><ul><li>The flaps have both <code>Flap Scale</code> set to 1, meaning that they will be fully deflected with the flap-control at 1. They also have a slew rate of 0.5/s, meaning that it will take 2s to fully deflect them (a slew rate on the flaps is generally recommended to reduce the disturbances their movement creates).</li><li>The ailerons are primarily tasked to provide the commanded roll torque. They also have <code>Spoiler Scale</code> set to 0.5, and will additionally be deflected upwards 50% if the controller demands full spoiler configuration. The aileron deflection is thus the sum of the (asymmetrical) deflection for the roll torque, plus the (symmetrical) deflection for the spoiler setpoint.</li><li>The elevator is primarily tasked to provide pitch torque. It also has non-zero entries in the <code>Flap Scale</code> and <code>Spoiler Scale</code> fields. These are the elevator deflections added to compensate for the pitching moments generated by the flaps and spoiler actuators. In the case here the elevator would be deflected 0.3 up when the flaps are fully deployed to counteract the pitching down moment caused by the flaps.</li></ul><h4 id="actuator-roll-pitch-and-yaw-scaling" tabindex="-1">Actuator Roll, Pitch, and Yaw Scaling <a class="header-anchor" href="#actuator-roll-pitch-and-yaw-scaling" aria-label="Permalink to &quot;Actuator Roll, Pitch, and Yaw Scaling&quot;">​</a></h4><div class="info custom-block"><p class="custom-block-title">INFO</p><p>For the majority of airframe setups the default values for each control surface types should not be changed.</p></div><p>The <code>Roll scale</code>, <code>Pitch scale</code> and <code>Yaw scale</code> values indicate the normalized effectiveness of the actuator around the corresponding axis.</p><p>Tuning the values is a low/level/advanced topic, and is generally only needed when tuning coupled control surfaces (like an elevon, that controls both pitch and roll). In this case the things you need to know are:</p><ul><li>The numbers that are entered are directly put into the allocation matrix, that is then inverted to get from desired moments (normalized) to control signals.</li><li>Increasing the scale will <em>reduce</em> the deflection of the control surfaces (as it gets inverted).</li></ul><h4 id="control-surface-deflection-convention" tabindex="-1">Control Surface Deflection Convention <a class="header-anchor" href="#control-surface-deflection-convention" aria-label="Permalink to &quot;Control Surface Deflection Convention&quot;">​</a></h4><p>The diagram below shows the convention for deflections:</p><p><img src="'+h+'" alt="Control Surface Deflections"></p><p>In summary:</p><ul><li><strong>Horizontal Control Surfaces:</strong> Upwards movement equals positive deflection. Includes Ailerons, etc</li><li><strong>Vertical Control Surfaces:</strong> Rightwards movement is positive deflection. Includes rudders etc.</li><li><strong>Mixed Control Surfaces:</strong> Upwards/rightwards movement is positive (as above). Includes V-Tail etc.</li></ul><h3 id="motor-tilt-servo-geometry" tabindex="-1">Motor Tilt Servo Geometry <a class="header-anchor" href="#motor-tilt-servo-geometry" aria-label="Permalink to &quot;Motor Tilt Servo Geometry&quot;">​</a></h3><p><a href="./../frames_vtol/tiltrotor.html">VTOL tiltrotor vehicles</a> can tilt their motors to transition between hover and forward flight. This section defines the properties of the tilting servos. These are mapped to specific motors in the motor geometry for a tiltrotor.</p><p>The example below shows the tilt servo setup for the <a href="./../config/actuators.html#motor-geometry-vtol-tiltrotor">tiltrotor motor geometry shown above</a>.</p><p><img src="'+e+'" alt="Tilt Servo Geometry Setup Example"></p><p>The values that can be set are:</p><ul><li><code>Tilt servos</code>: The number of servos (tiltable motors).</li><li><code>Angle at min tilt</code>: <a href="#tilt-servo-coordinate-system">Maximum tilt angle</a> in degrees, relative to the z axis.</li><li><code>Angle at max tilt</code>: <a href="#tilt-servo-coordinate-system">Minimum tilt angle</a> in degrees, relative to the z-axis.</li><li><code>Tilt direction</code>: <code>Towards front</code> (positive x direction) or <code>Towards right</code> (positive y direction).</li><li><code>Use for control</code>: <a href="#tilt-servos-for-yaw-pitch-control">Tilt servo used for yaw/pitch</a><ul><li><code>None</code>: Torque control is not used.</li><li><code>Yaw</code>: Tilt servos used to control yaw.</li><li><code>Pitch</code>: Tilt servos used to control pitch.</li><li><code>Both Yaw and Pitch</code>: Tilt servos are used to control both yaw and pitch.</li></ul></li></ul><h4 id="tilt-servo-coordinate-system" tabindex="-1">Tilt Servo Coordinate System <a class="header-anchor" href="#tilt-servo-coordinate-system" aria-label="Permalink to &quot;Tilt Servo Coordinate System&quot;">​</a></h4><p>The coordinate system for tilt rotor angles is shown below. The reference direction for tilt angles is straight upwards (0 degrees). Tilt angles towards the front or right of the vehicle are positive, and towards the back or to the left are negative.</p><p><img src="'+p+'" alt="Tilt Axis"></p><p>The <code>Angle at min tilt</code> and <code>Angle at max tilt</code> indicate the range of movement for the tilt servo. The minimum tilt is the smaller <em>numerical value</em> (not absolute) of the two angles.</p><p>If the max/min tilt vectors are <strong>P<sub>0</sub></strong> and <strong>P<sub>1</sub></strong> as shown above, both tilt angles are positive but <strong>θ<sub>0</sub></strong> is smaller:</p><ul><li><code>Angle at min tilt</code> = <strong>θ<sub>0</sub></strong></li><li><code>Angle at max tilt</code> = <strong>θ<sub>1</sub></strong></li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>If the diagram was mirrored so that <strong>P<sub>0</sub></strong> and <strong>P<sub>1</sub></strong> were tilting into the -x, -y quadrant, then both the tilt angles would be negative. Because <strong>θ<sub>1</sub></strong> would more negative (smaller) than <strong>θ<sub>0</sub></strong>, it would be the <code>Angle at min tilt</code>.</p><p>Similarly, a servo that moves:</p><ul><li>between the upright and forward positions would have <code>min=0</code> and <code>max=90</code>.</li><li>symmetrically 45 degrees around the upright position would have <code>min=-45</code> and <code>max=45</code></li><li>between the upright and backward positions would have <code>min=-90</code> and <code>max=0</code>.</li></ul></div><p>The <code>Tilt direction</code> indicates whether the servo tilts in the plane towards the <code>Front</code> or <code>Right</code> of the vehicle. On the diagram this would be represented by <strong>α</strong> that can only take values of 0 (front) or 90 (right).</p><h4 id="tilt-servos-for-yaw-pitch-control" tabindex="-1">Tilt Servos for Yaw/Pitch Control <a class="header-anchor" href="#tilt-servos-for-yaw-pitch-control" aria-label="Permalink to &quot;Tilt Servos for Yaw/Pitch Control&quot;">​</a></h4><p>Tilt servos can provide torque on one or more axes, which may be used to yaw or pitch the vehicle:</p><ul><li>Yaw is commonly set in this way, though motors are often used instead on vehicles with four or more motors.</li><li>Pitch is more commonly controlled using differential motors thrust. Control using tilt servos is useful on airframes that can&#39;t use differential thrust, such as a <a href="https://www.youtube.com/watch?v=hfss7nCN40A" target="_blank" rel="noreferrer">Bicopter</a>.</li></ul><p>Whether this feature is used is configured in the <code>Use for control</code> setting.</p><h2 id="actuator-outputs" tabindex="-1">Actuator Outputs <a class="header-anchor" href="#actuator-outputs" aria-label="Permalink to &quot;Actuator Outputs&quot;">​</a></h2><p>The <em>Actuator Outputs</em> section is used to assign motors, control surface servos, and other actuators used by the particular frame to the physical outputs on the flight controller, and to set parameters for those outputs.</p><p><img src="'+m+'" alt="Actuator Outputs - Multicopter diagram"></p><p>Separate tabs are displayed for each output bus supported by the connected flight controller: PWM MAIN (I/O Board output), PWM AUX (FMU Board output), UAVCAN.</p><p>Motors and actuators (which are referred to as &quot;<a href="#output-functions">functions</a>&quot;) can be assigned to any physical output on any of the available buses.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>PWM AUX outputs are preferred over the PWM MAIN outputs for controlling motors (they have lower latency).</p></div><p>PWM outputs are grouped based on the hardware timer groups. Meaning all the outputs in one group must operate under the same protocol at the same rate (e.g. PWM signal at 400Hz for all the outputs in one group). Therefore it is not possible to map Servo and a Motor in the same output group, as they usually operate at a different rate.</p><p>The PWM AUX tab has CAP outputs that are generally used as the <a href="./../camera/fc_connected_camera.html#trigger-configuration">camera capture/trigger input</a>. However you can map the CAP outputs to other output functions, and other AUX outputs can be used as camera capture/triggering input.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Configuring the Camera Capture / Trigger input requires a reboot to take effect</p></div><p>You should assign functions to the outputs that match your physical wiring of motors and servos, and use the <a href="#actuator-testing">Actuator Testing</a> section described below to determine appropriate output parameter values. These steps are covered in <a href="#output-assignment-and-configuration">Output Assignment and Configuration</a>.</p><h3 id="output-functions" tabindex="-1">Output Functions <a class="header-anchor" href="#output-functions" aria-label="Permalink to &quot;Output Functions&quot;">​</a></h3><p>Output functions are used to map the &quot;logical functions&quot; of an airframe, such as <code>Motor 1</code> or <code>Landing gear</code>, to physical outputs like FMU output pin 2. This makes it easy to use a particular output pin for almost any purpose.</p><p>Some functions are only relevant to particular frames or output types, and will not be offered on others.</p><p>Functions include:</p><ul><li><code>Disabled</code>: Output has no assigned function.</li><li><code>Constant_Min</code>: Output set to constant minimum value (-1).</li><li><code>Constant_Max</code>: Output is set to constant maximum value (+1).</li><li><code>Motor 1</code> to <code>Motor 12</code>: Output is indicated motor. Only motors allowed for airframe are displayed.</li><li><code>Servo 1</code> to <code>Servo 8</code>: Servo output. These are further assigned a specific meaning based on airframe, such as &quot;tilt servo&quot;, &quot;left aileron&quot;.</li><li><code>Peripheral via Acutator Set 1</code> to <code>Peripheral via Acutator Set 6</code>: <a href="./../payloads/generic_actuator_control.html#generic-actuator-control-with-mavlink">Generic Actuator Control with MAVLink</a>.</li><li><code>Landing Gear</code>: Output is landing gear.</li><li><code>Parachute</code>: Output is parachute. The minimum value is sent in normal use and the maximum value is emitted when a failsafe is triggered.</li><li><code>RC Roll</code>: Output is passthrough roll from RC (<a href="./../advanced_config/parameter_reference.html#RC_MAP_ROLL">RC_MAP_ROLL</a> maps an RC channel to this output). An RC channel is mapped to the output using .</li><li><code>RC Pitch</code>: Output is passthrough pitch from RC (<a href="./../advanced_config/parameter_reference.html#RC_MAP_PITCH">RC_MAP_PITCH</a> maps an RC channel to this output).</li><li><code>RC Throttle</code>: Output is passthrough throttle from RC (<a href="./../advanced_config/parameter_reference.html#RC_MAP_THROTTLE">RC_MAP_THROTTLE</a> maps an RC channel to this output).</li><li><code>RC Yaw</code>: Output is yaw from RC (<a href="./../advanced_config/parameter_reference.html#RC_MAP_YAW">RC_MAP_YAW</a> maps an RC channel to this output).</li><li><code>RC Flaps</code>: Output is flaps from RC (<a href="./../advanced_config/parameter_reference.html#RC_MAP_FLAPS">RC_MAP_FLAPS</a> maps an RC channel to this output).</li><li><code>RC AUXn</code> to <code>RC AUX1</code>: Outputs used for <a href="./../payloads/generic_actuator_control.html#generic-actuator-control-with-rc">arbitrary payloads triggered by RC passthrough</a>.</li><li><code>Gimbal Roll</code>: Output controls gimbal roll.</li><li><code>Gimbal Pitch</code>: Output controls Gimbal pitch.</li><li><code>Gimbal Yaw</code>: Output controls Gimbal yaw.</li></ul><p>The following functions can only be applied to FMU outputs:</p><ul><li><code>Camera_Trigger</code>: Output to trigger camera. Enabled when <a href="./../advanced_config/parameter_reference.html#TRIG_MODE"><code>TRIG_MODE==0</code></a>. Configured via <code>TRIG_*</code> parameters.</li><li><code>Camera_Capture</code>: Input to get image capture notification. Enabled when <a href="./../advanced_config/parameter_reference.html#CAM_CAP_FBACK">CAM_CAP_FBACK==0</a>. Configured via <code>CAM_CAP_*</code> parameters.</li><li><code>PPS_Input</code>: Pulse-per-second input capture. Used for GPS synchronisation. Enabled when <a href="./../advanced_config/parameter_reference.html#PPS_CAP_ENABLE"><code>PPS_CAP_ENABLE==0</code></a></li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>This list is correct at PX4 v1.13. The functions are defined in source at <a href="https://github.com/PX4/PX4-Autopilot/blob/release/1.15/src/lib/mixer_module/output_functions.yaml" target="_blank" rel="noreferrer">/src/lib/mixer_module/output_functions.yaml</a>.</p></div><h2 id="actuator-testing" tabindex="-1">Actuator Testing <a class="header-anchor" href="#actuator-testing" aria-label="Permalink to &quot;Actuator Testing&quot;">​</a></h2><p>The <em>Actuator Testing</em> section in lower-right corner provides sliders that can be used to test (and determine) actuator and motor settings. A slider is provided for each output defined in the <a href="#actuator-outputs">Actuator Outputs</a> section. The slider example below shows the section for a typical VTOL Tiltrotor airframe.</p><p><img src="'+f+'" alt="Actuator Testing Slider"></p><p>The section has an <strong>Enable Sliders</strong> switch that must be toggled before sliders can be used. The sliders can power the motors/servos across their full range of motion, and &quot;snap&quot; to the disarmed and minimum positions.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>After you toggle the <strong>Enable sliders</strong> switch, actuators/motors won&#39;t do anything until the corresponding slider is <em>moved</em>. This is a safety feature to prevent sudden motor movements after switch is enabled.</p></div><p>Sliders can be used to verify the following:</p><ol><li>Actuators (Motors, Control surfaces, etc.) are assigned to the expected output.</li><li>Motors don&#39;t spin when at the <code>disarmed</code> PWM output value</li><li>Motors barely spin at the <code>minimum</code> PWM output value.</li><li>Motors give <strong>positive thrust</strong> in the expected direction</li><li>Control Surfaces are in the correct idle position for <code>disarmed</code> output value</li><li>Control Surfaces move in the direction as defined in the <a href="#control-surface-deflection-convention">Control Surface Convention</a></li><li>Motor Tilt Servos are in the correct idle position for <code>disarmed</code> output value</li><li>Motor Tilt Servos move in the direction as defined in the <a href="#tilt-servo-coordinate-system">Tilt Servo Convention</a></li></ol><h2 id="output-assignment-and-configuration" tabindex="-1">Output Assignment and Configuration <a class="header-anchor" href="#output-assignment-and-configuration" aria-label="Permalink to &quot;Output Assignment and Configuration&quot;">​</a></h2><p>Outputs are assigned to functions and configured in the <a href="#actuator-outputs">Actuator Outputs</a> section, while the <a href="#actuator-testing">Actuator Testing</a> sliders are commonly used to determine appropriate configuration values to enter:</p><ul><li>MC vehicles that have connected motors to PWM outputs can use the <a href="#multicopter-pwm-motor-assignment">Identify &amp; Assign Motors</a> button to perform motor assignment &quot;semi-automatically&quot;.</li><li>Output assignment of both motors and actuators can be done/checked using sliders (see <a href="#output-assignment-manual">Output Assignment (Manual)</a>).</li><li>Disarmed, minimum, and maximum settings, for all outputs can also be also determined using sliders. This is shown as part of <a href="#motor-configuration">Motor Configuration</a>, <a href="#control-surface-setup">Control Surface Setup</a>, <a href="#tilt-servo-setup">Tilt servo setup</a></li></ul><h3 id="multicopter-pwm-motor-assignment" tabindex="-1">Multicopter PWM: Motor Assignment <a class="header-anchor" href="#multicopter-pwm-motor-assignment" aria-label="Permalink to &quot;Multicopter PWM: Motor Assignment&quot;">​</a></h3><p>You can use the <strong>Identify &amp; Assign Motors</strong> button to assign motors to PWM outputs using a semi-automated process.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>This is the easiest way to assign motors, but is currently only supported for motors on <strong>multicopter vehicles</strong> that are connected to PWM outputs (UAVCAN outputs and other frame types do not support this feature). On other frames you can follow the instructions in <a href="#output-assignment-manual">Output Assignment (Manual)</a>.</p></div><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Remove the propellers from motors before assigning outputs or any testing.</p></div><p><img src="'+g+'" alt="Identify motor button"></p><p>When you click the button, QGC sends a command to a motor, causing it to spin. To assign that motor to an output you simply select the corresponding motor displayed in the screen. QGC will then spin the next motor for you to assign, and so on.</p><p>Instructions:</p><ol><li><p>Setup the motor geometry to match the motors on your frame.</p></li><li><p>Select the PWM tab where you want to assign the motors.</p></li><li><p>Click the <strong>Identify &amp; Assign Motors</strong> button.</p></li><li><p>One motor will start spinning (click <strong>Spin Motor Again</strong> if it stops spinning too quickly to note.)</p><p>Select the corresponding motor in the geometry section.</p><p><img src="'+v+'" alt=""></p></li><li><p>After assigning all motors, the tool will set the correct motor mapping for the outputs and then exit.</p></li></ol><h3 id="output-assignment-manual" tabindex="-1">Output Assignment (Manual) <a class="header-anchor" href="#output-assignment-manual" aria-label="Permalink to &quot;Output Assignment (Manual)&quot;">​</a></h3><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Remove the propellers from motors before assigning outputs or any testing.</p></div><p>Actuator outputs for both motors and servos can be <em>manually</em> assigned using sliders in the <a href="#actuator-testing">Actuator Testing</a> section.</p><p>To assign an actuator:</p><ol><li>First assign functions to the outputs that you think are <em>likely</em> to be correct in the <em>Actuator Outputs</em> section.</li><li>Toggle the <strong>Enable sliders</strong> switch in <em>Actuator Testing</em> section.</li><li>Move the slider for the actuator you want to test: <ul><li>Motors should be moved to the minimum thrust position.</li><li>Servos should be moved near the middle position.</li></ul></li><li>Check which actuator moves on the vehicle. This should match the actuator positions for your geometry (the <a href="./../airframes/airframe_reference.html">airframe reference</a> shows motor positions for a number of standard airframes). <ul><li>If the correct actuator moves, then proceed to the next step.</li><li>If a wrong actuator moves, swap the output assignment over.</li><li>If nothing moves then increase the slider mid-way though the range, then higher if needed. If nothing moves after that the output might not be connected, the motor might not be powered, or the output might be misconfigured. You will need to troubleshoot (perhaps try other actuator outputs to see if &quot;anything&quot; moves).</li></ul></li><li>Return the slider to the &quot;disarmed&quot; position (bottom of slider for motors, centre of slider for servos).</li><li>Repeat for all actuators</li></ol><h3 id="motor-configuration" tabindex="-1">Motor Configuration <a class="header-anchor" href="#motor-configuration" aria-label="Permalink to &quot;Motor Configuration&quot;">​</a></h3><div class="info custom-block"><p class="custom-block-title">INFO</p><p>If using PWM or OneShot ESCs, you should first perform <a href="./../advanced_config/esc_calibration.html">ESC Calibration</a> (this topic also covers PWM specific motor configuration).</p><p><a href="./../peripherals/dshot.html">DShot</a> ESCs do not require configuration of the command limits but only rotation direction.</p></div><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Remove propellers!</p></div><p>The motor configuration sets output values such that motors:</p><ul><li>don&#39;t spin when disarmed (at the <code>disarmed</code> PWM output value).</li><li>barely but reliably spin up at the <code>minimum</code> PWM output value.</li><li>have the <em>lowest</em> <code>maximum</code> PWM output value that spins the motor at its <em>highest</em> rate.</li><li>give <strong>positive thrust</strong> in the expected direction.</li></ul><p>For each motor:</p><ol><li><p>Pull the motor slider down so that it snaps to the bottom. In this position the motor is set to the outputs <code>disarmed</code> value.</p><ul><li>Verify that the motor doesn&#39;t spin in this position.</li><li>If the motor spins, reduce the corresponding PWM <code>disarmed</code> value in the <a href="#actuator-outputs">Actuator Outputs</a> section to below the level at which it still spins.</li></ul></li><li><p>Slowly move the slider up until it snaps to the <em>minimum</em> position. In this position the motor is set to the outputs <code>minimum</code> value.</p><ul><li><p>Verify that the motor is spinning very slowly in this position.</p></li><li><p>If the motor is not spinning, or spinning too fast you will need to adjust the corresponding PWM <code>minimum</code> value in the <a href="#actuator-outputs">Actuator Outputs</a> such that the motors barely spin.</p><p><img src="'+y+'" alt="PWM Minimum Output"></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>For DShot output, this is not required.</p></div></li></ul></li><li><p>Increase the slider value to a level where you can verify that the motor is spinning in the correct direction and that it would give a positive thrust in the expected direction.</p><ul><li>The expected thrust direction can vary by vehicle type. For example in multicopters the thrust should always point upwards, while in a fixed-wing vehicle the thrust will push the vehicle forwards.</li><li>For VTOL, thrust should point upwards when the Tilt Servo is at 0 degrees as defined the <a href="#tilt-servo-coordinate-system">Tilt Servo Convention</a>. Testing of the <a href="#tilt-servo-setup">Tilt Servo</a> is covered below as well.</li><li>If thrust is in the wrong direction, you may need to <a href="#reversing-motors">reverse the motors</a>.</li></ul></li><li><p>Increase the slider value to the maximum value, so the motor is spinning quickly. Reduce the value of the PWM output&#39;s <code>maximum</code> value just below the default. Listen to the tone of the motors as you increase the value in small (25us) increments. The &quot;optimal&quot; maximum value is the value at which you last hear a change in the tone.</p></li></ol><h3 id="control-surface-setup" tabindex="-1">Control Surface Setup <a class="header-anchor" href="#control-surface-setup" aria-label="Permalink to &quot;Control Surface Setup&quot;">​</a></h3><p>First set the <em>frame rate</em> for the servos used in each group of outputs. This would normally be set to the maximum value supported by your servo. Below we show how you would set it to PWM50 (the most common value).</p><p><img src="'+b+'" alt="Control Surface Disarmed 1500 Setting"></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>You will almost certainly need to change the pulse rate from the default of 400Hz because support is rare (if not supported the servo will usually make an &quot;odd&quot; noise). If you&#39;re using PWM servos, PWM50 is far more common. If a high rate servo is <em>really</em> needed, DShot offers better value.</p></div><p>For each of the control surfaces:</p><ol><li><p>Set the <code>Disarmed</code> value so that the surfaces will stay at neutral position when disarmed. This is usually around <code>1500</code> for PWM servos.</p></li><li><p>Move the slider for the surface upwards (positive command) and verify that it moves in the direction defined in the <a href="#control-surface-deflection-convention">Control Surface Convention</a>.</p><ul><li>If the control surface moves in the opposite direction, click on the <code>Rev Range</code> checkbox to reverse the range.</li></ul></li><li><p>Move the slider again to the middle and check if the Control Surfaces are aligned in the neutral position of the wing</p><ul><li><p>If it is not aligned, you can set the <strong>Trim</strong> value for the control surface.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>This is done in the <code>Trim</code> setting of the Geometry panel, usually by &quot;trial and error&quot;. <img src="'+w+'" alt="Control Surface Trimming"></p></div></li><li><p>After setting the trim for a control surface, move its slider away from the center, release, and then back into disarmed (middle) position. Confirm that surface is in the neutral position.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span></span></span></code></pre></div></li></ul></li></ol><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Another way to test without using the sliders would be to set the <a href="./../advanced_config/parameter_reference.html#COM_PREARM_MODE"><code>COM_PREARM_MODE</code></a> parameter to <code>Always</code>:</p><ul><li>This will enable the control of servos even when the vehicle is disarmed, and will constantly be applying the Trim setting to the Control Surfaces</li><li>You can try setting different values for the Trim and check the alignment, and then settle on the value you are happy with.</li></ul></div><h3 id="tilt-servo-setup" tabindex="-1">Tilt Servo Setup <a class="header-anchor" href="#tilt-servo-setup" aria-label="Permalink to &quot;Tilt Servo Setup&quot;">​</a></h3><p>First set the <em>frame rate</em> for the servos used in each group of outputs. This would normally be set to the maximum value supported by your servo. Below it is set to PWM50 (the most common value). Note, this part of the setup is the same as for control surfaces above.</p><p><img src="'+T+'" alt="Tilt Servo Setup"></p><p>For each of the tilt servos:</p><ol><li><p>Set the <code>Disarmed</code> value (e.g. <code>1000</code> or <code>2000</code> for PWM Servos) so that the servo will be positioned in expected direction when <em>disarmed</em>.</p></li><li><p>Position the slider for the servo in the lowest position, and verify that a positive value increase will point towards the <code>Angle at Min Tilt</code> (defined in the Geometry section).</p><p><img src="'+e+'" alt="Tilt Servo Geometry Setup"></p></li><li><p>Position the slider for the servo in the highest position, and verify that positive motor thrust will point towards the <code>Angle at Max Tilt</code> (as defined in the Geometry section).</p></li></ol><h3 id="other-notes" tabindex="-1">Other Notes <a class="header-anchor" href="#other-notes" aria-label="Permalink to &quot;Other Notes&quot;">​</a></h3><ul><li>If a safety button is used, it must be pressed before actuator testing is allowed.</li><li>The kill-switch can still be used to stop motors immediately.</li><li>Servos do not actually move until the corresponding slider is changed.</li><li>The parameter <a href="./../advanced_config/parameter_reference.html#COM_MOT_TEST_EN">COM_MOT_TEST_EN</a> can be used to completely disable actuator testing.</li><li>On the shell, <a href="./../modules/modules_command.html#actuator-test">actuator_test</a> can be used as well for actuator testing.</li><li>VTOLs will automatically turn off motors pointing upwards during <strong>fixed-wing flight</strong>: <ul><li>Standard VTOL : Motors defined as multicopter motors will be turned off</li><li>Tiltrotors : Motors that have no associated tilt servo will turn off</li><li>Tailsitters do not turn off any motors in fixed-wing flight</li></ul></li></ul><h3 id="reversing-motors" tabindex="-1">Reversing Motors <a class="header-anchor" href="#reversing-motors" aria-label="Permalink to &quot;Reversing Motors&quot;">​</a></h3><p>The motors must turn in the direction defined in configured geometry (&quot;<strong>Direction CCW</strong>&quot; checkboxes). If any motors do not turn in the correct direction they must be reversed.</p><p>There are several options:</p><ul><li><p>If the ESCs are configured as <a href="./../peripherals/dshot.html">DShot</a> you can permanently reverse the direction via UI. The <strong>Set Spin Direction</strong> buttons are displayed below the Actuator sliders (if DShot motors are used). These popup a dialog in which you select the motor for which you want to apply the direction.</p><p><img src="'+_+'" alt="Set spin direction buttons"></p><p>Note that the current direction cannot be queried, so you may need to try both options.</p></li><li><p>Swap 2 of the 3 motor cables (it does not matter which ones).</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>If motors are not connected via bullet-connectors, re-soldering is required (this is a reason, among others, to prefer DShot ESCs).</p></div></li></ul>',155);function R(F,G,N,L,E,V){const o=C("Badge");return k(),q("div",null,[x,A(o,{type:"tip",text:"PX4 v1.14"}),I])}const Y=S(O,[["render",R]]);export{U as __pageData,Y as default};
