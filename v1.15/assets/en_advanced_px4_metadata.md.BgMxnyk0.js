import{_ as a}from"./chunks/actuator_metadata_processing.C1EC5ZrI.js";import{_ as e,c as t,o,ab as r}from"./chunks/framework.CUflZczI.js";const b=JSON.parse('{"title":"PX4 Metadata","description":"","frontmatter":{},"headers":[],"relativePath":"en/advanced/px4_metadata.md","filePath":"en/advanced/px4_metadata.md"}'),n={name:"en/advanced/px4_metadata.md"},i=r('<h1 id="px4-metadata" tabindex="-1">PX4 Metadata <a class="header-anchor" href="#px4-metadata" aria-label="Permalink to &quot;PX4 Metadata&quot;">​</a></h1><p>PX4 uses and generates data that has associated human- and machine- readable metadata:</p><ul><li><a href="./../advanced_config/parameters.html">Parameters</a> configure PX4 behaviour. <ul><li>A parameter is represented by an ID string that maps to a value stored in PX4.</li><li>The associated metadata includes a description of the setting, its possible values, information about how the value might be presented (say for bitmasks).</li></ul></li><li><a href="./../concept/events_interface.html">Events</a> provide notification of events, such as reasons for a failsafe, low battery warnings, end of calibration, and so on. <ul><li>An event is represented by an id, and is sent with a log level and arguments.</li><li>The associated metadata includes a message, a description and a list of arguments (including their type) of each event.</li></ul></li><li><a href="./../config/actuators.html">Actuators</a> configuration customizes the specific geometry of the vehicle, assigns actuators and motors to flight controller outputs, and tests the actuator and motor response. <ul><li>The metadata contains information about supported vehicle geometries, a list of output drivers, and how to configure them.</li><li><em>QGroundControl</em> uses that information to dynamically build a configuration UI.</li></ul></li></ul><p>The metadata and metadata translations are shared with external systems, such as QGroundControl, allowing them to display information about parameters and events, and to configure vehicle geometry and actuator output mappings.</p><p>This topic explains how you can define metadata and help translate strings (and &quot;just for your information&quot;, how it all works).</p><h2 id="metadata-translation" tabindex="-1">Metadata Translation <a class="header-anchor" href="#metadata-translation" aria-label="Permalink to &quot;Metadata Translation&quot;">​</a></h2><p>Translations for PX4 metadata are done using Crowdin in the <a href="https://crowdin.com/project/px4-metadata-translations" target="_blank" rel="noreferrer">PX4-Metadata-Translations</a> project. For more information about working with PX4 and Crowdin see <a href="./../contribute/translation.html">Translation</a>.</p><h2 id="defining-metadata" tabindex="-1">Defining Metadata <a class="header-anchor" href="#defining-metadata" aria-label="Permalink to &quot;Defining Metadata&quot;">​</a></h2><p>PX4 metadata is defined in PX4 source code alongside its associated data. This is done either in a C/C++ comment with special markup to indicate metadata fields and their values, or using YAML files.</p><p>For more information see the topics for each data type:</p><ul><li><a href="./../advanced/parameters_and_configurations.html#creating-defining-parameters">Parameters &amp; Configurations &gt; Creating/Defining Parameters</a></li><li><a href="./../concept/events_interface.html">Events Interface</a></li><li><a href="#actuator-metadata">Actuator Metadata</a> (below)</li></ul><h2 id="metadata-toolchain" tabindex="-1">Metadata Toolchain <a class="header-anchor" href="#metadata-toolchain" aria-label="Permalink to &quot;Metadata Toolchain&quot;">​</a></h2><p>The process for handling metadata is the same for all metadata types.</p><p>Metadata is collected into JSON files every time PX4 is built.</p><p>For most flight controllers (as most have enough FLASH available), the JSON file is xz-compressed and stored within the generated binary. The file is then shared to ground stations using the MAVLink <a href="https://mavlink.io/en/services/component_information.html" target="_blank" rel="noreferrer">Component Metadata Protocol</a>. Using the component metadata protocol ensures that the recipient can always fetch up-to-date metadata for the code running on the vehicle. Events metadata is also added to the log files, allowing log analysis tools (such as Flight Review) to use the correct metadata to display events.</p><p>Binaries for flight controller targets with constrained memory do not store the parameter metadata in the binary, but instead reference the same data stored on <code>px4-travis.s3.amazonaws.com</code>. This applies, for example, to the <a href="./../flight_controller/omnibus_f4_sd.html">Omnibus F4 SD</a>. The metadata is uploaded via <a href="https://github.com/PX4/PX4-Autopilot/blob/release/1.15/.github/workflows/metadata.yml" target="_blank" rel="noreferrer">github CI</a> for all build targets (and hence will only be available once parameters have been merged into main).</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>You can identify memory constrained boards because they specify <code>CONFIG_BOARD_CONSTRAINED_FLASH=y</code> in their <a href="https://github.com/PX4/PX4-Autopilot/blob/release/1.15/boards/omnibus/f4sd/default.px4board" target="_blank" rel="noreferrer">px4board definition file</a>.</p><p>If doing custom development on a FLASH-constrained board you can adjust the URL <a href="https://github.com/PX4/PX4-Autopilot/blob/release/1.15/src/lib/component_information/CMakeLists.txt#L41" target="_blank" rel="noreferrer">here</a> to point to another server.</p></div><p>The metadata on <code>px4-travis.s3.amazonaws.com</code> is used if parameter metadata is not present on the vehicle. It may also be used as a fallback to avoid a very slow download over a low-rate telemetry link.</p><p>The metadata JSON files for CI builds of <code>main</code> are also copied to the github repo: <a href="https://github.com/PX4/PX4-Metadata-Translations/" target="_blank" rel="noreferrer">PX4/PX4-Metadata-Translations</a>. This integrates with Crowdin to get translations, which are stored in the <a href="https://github.com/PX4/PX4-Metadata-Translations/tree/main/translated" target="_blank" rel="noreferrer">translated</a> folder as xz-compressed translation files for each language. These are referenced by the vehicle component metadata, and are downloaded when needed. For more information see <a href="https://github.com/PX4/PX4-Metadata-Translations/" target="_blank" rel="noreferrer">PX4-Metadata-Translations</a> and <a href="https://mavlink.io/en/services/component_information.html#translation" target="_blank" rel="noreferrer">Component Metadata Protocol &gt; Translation</a>.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The parameter XML file of the main branch is copied into the QGC source tree via CI and is used as a fallback in cases where no metadata is available via the component metadata protocol (this approach predates the existence of the component metadata protocol).</p></div><h3 id="actuator-metadata" tabindex="-1">Actuator Metadata <a class="header-anchor" href="#actuator-metadata" aria-label="Permalink to &quot;Actuator Metadata&quot;">​</a></h3><p>The following diagram shows how actuator metadata is assembled from the source code and used by QGroundControl:</p><p><img src="'+a+'" alt="Actuators Metadata"></p><ul><li><strong>Left</strong>: the metadata is defined in <code>module.yml</code> files in different modules. The <code>control_allocator</code> modules defines the geometries, while each output driver defines its set of channels and configuration parameters. <a href="https://github.com/PX4/PX4-Autopilot/blob/release/1.15/validation/module_schema.yaml" target="_blank" rel="noreferrer">The schema file</a> documents the structure of these yaml files.</li><li><strong>Middle</strong>: At build time, the <code>module.yml</code> files for all enabled modules for the currently built target are parsed and turned into an <code>actuators.json</code> file using the <a href="https://github.com/PX4/PX4-Autopilot/blob/release/1.15/Tools/module_config/generate_actuators_metadata.py" target="_blank" rel="noreferrer">Tools/module_config/generate_actuators_metadata.py</a> script. There is also <a href="https://github.com/mavlink/mavlink/blob/master/component_metadata/actuators.schema.json" target="_blank" rel="noreferrer">schema file</a> for this.</li><li><strong>Right</strong>: At runtime, the JSON file is requested by QGroundControl via MAVLink Component Metadata API (described above).</li></ul><h2 id="further-information" tabindex="-1">Further Information <a class="header-anchor" href="#further-information" aria-label="Permalink to &quot;Further Information&quot;">​</a></h2><ul><li><a href="./../advanced/parameters_and_configurations.html">Parameters &amp; Configurations</a></li><li><a href="./../concept/events_interface.html">Events Interface</a></li><li><a href="./../contribute/translation.html">Translation</a></li><li><a href="https://mavlink.io/en/services/component_information.html" target="_blank" rel="noreferrer">Component Metadata Protocol</a> (mavlink.io)</li><li><a href="https://github.com/PX4/PX4-Metadata-Translations/" target="_blank" rel="noreferrer">PX4-Metadata-Translations</a> (Github)</li></ul>',26),s=[i];function l(d,h,c,m,f,u){return o(),t("div",null,s)}const _=e(n,[["render",l]]);export{b as __pageData,_ as default};
