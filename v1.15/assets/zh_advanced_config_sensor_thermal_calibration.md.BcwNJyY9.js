import{_ as e,a,b as t,c as r}from"./chunks/thermal_calibration_baro.BAbPqO8O.js";import{_ as o,c as i,o as n,ab as s}from"./chunks/framework.CUflZczI.js";const v=JSON.parse('{"title":"热校准和补偿","description":"","frontmatter":{},"headers":[],"relativePath":"zh/advanced_config/sensor_thermal_calibration.md","filePath":"zh/advanced_config/sensor_thermal_calibration.md"}'),c={name:"zh/advanced_config/sensor_thermal_calibration.md"},l=s('<h1 id="热校准和补偿" tabindex="-1">热校准和补偿 <a class="header-anchor" href="#热校准和补偿" aria-label="Permalink to &quot;热校准和补偿&quot;">​</a></h1><p>PX4 contains functionality to calibrate and compensate accelerometer, gyro, magnetometer, and barometric pressure sensors for the effect of changing sensor temperature on sensor bias.</p><p>本主题详细介绍了 <a href="#test_setup">测试环境</a> 和 <a href="#calibration_procedures">校准过程</a>。 最后是 <a href="#implementation">实施过程</a> 的描述。</p><div class="info custom-block"><p class="custom-block-title">After thermal calibration, the thermal calibration parameters (<code>TC_*</code>) are used for <em>all</em> calibration/compensation of the respective sensors. Any subsequent standard calibration will therefore update <code>TC_*</code> parameters and not the &quot;normal&quot; <code>SYS_CAL_*</code> calibration parameters (and in some cases these parameters may be reset).</p></div><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Releases up to PX4 v1.14, do not support thermal calibration of the magnetometer.</p></div><p><a id="test_setup"></a></p><h2 id="测试设置-最佳实践" tabindex="-1">测试设置/最佳实践 <a class="header-anchor" href="#测试设置-最佳实践" aria-label="Permalink to &quot;测试设置/最佳实践&quot;">​</a></h2><p>The <a href="#calibration_procedures">calibration procedures</a> described in the following sections are ideally run in an <em>environmental chamber</em> (a temperature and humidity controlled environment) as the board is heated from the lowest to the highest operating/calibration temperature. Before starting the calibration, the board is first <em>cold soaked</em> (cooled to the minimum temperature and allowed to reach equilibrium).</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Active electric heating elements will affect the magnetometer calibration values. Ensure that heating elements are either inactive or sufficiently far from the sensor to avoid injecting noise into the magnetometer calibration.</p></div><p>对于冷却，您可以使用普通的家用冰箱达到 -20C，商用冰箱可以达到 -40C 的量级。 The board should be placed in a ziplock/anti-static bag containing a silica packet, with a power lead coming out through a sealed hole. 冷却后，可将袋子移至测试环境，并在同一袋中继续测试。</p><p>:::note 防静电袋和硅胶干燥剂是为了防止水蒸气在电路板上冷凝。 :::</p><p>它可以在没有商业级环境房间的情况下进行校准。 可以使用内部空间很小的泡沫塑料盒来创造一个简单的环境容器。 这允许自驾仪将空气相对快速地自加热（确保盒子有一个小孔以平衡容器内外压力，但仍然能够在容器内加热）。</p><p>使用这种设置可以将电路板加热到约 70C 。 经验表明，许多普通电路板可以加热到这个温度不会产生不良副作用。 如有疑问，请与制造商核实安全操作的温度范围。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>要检查板上的热校准状态，请使用 MAVlink 控制台(或NuttX 控制台) 检查来自传感器的报告的内部温度。</p></div><p><a id="calibration_procedures"></a></p><h2 id="校准过程" tabindex="-1">校准过程 <a class="header-anchor" href="#校准过程" aria-label="Permalink to &quot;校准过程&quot;">​</a></h2><p>PX4 支持两种校准过程：</p><ul><li><a href="#onboard_calibration">板载校准</a> - 校准在电路板上运行。 该方法需要知道测试设置中可实现的温升量。</li><li><a href="#offboard_calibration">板外校准</a> - 基于在校准过程期间收集的日志信息在计算机上计算补偿参数。 该方法允许用户可视地检查数据和曲线拟合的质量。</li></ul><p>The offboard approach is more complex and slower, but requires less knowledge of the test setup and is easier to validate.</p><p><a id="onboard_calibration"></a></p><h3 id="板载校准过程" tabindex="-1">板载校准过程 <a class="header-anchor" href="#板载校准过程" aria-label="Permalink to &quot;板载校准过程&quot;">​</a></h3><p>Onboard calibration is run entirely on the device. It require knowledge of the amount of temperature rise that is achievable with the test setup.</p><p>To perform and onboard calibration:</p><ol><li>确保在校准前设置机架类型，否则在设置飞控板时校准参数将丢失。</li><li>为电路板供电并将 <code>SYS_CAL _ *</code>参数设置为 1，以便在下次启动时启用所需传感器的校准。 [^1]</li><li>将<a href="./../advanced_config/parameter_reference.html#SYS_CAL_TDEL"> SYS_CAL_TDEL </a> 参数设置为板载校准器完成所需的温升度数。 如果此参数太小，则校准将提前完成，并且校准的温度范围将不足以在电路板完全预热时进行补偿。 如果此参数设置得太大，则板载校准器将永远不会完成。 在设置此参数时，应考虑到电路板自加热导致的温度升高。 如果传感器的温升量未知，则应使用板外校准方法。</li><li>将 <a href="./../advanced_config/parameter_reference.html#SYS_CAL_TMIN"> SYS_CAL_TMIN </a> 参数设置为您希望校准器发挥作用的最低温度数据。 更低的冷却温度能够用于减少冷却时间，同时保持对校准最低温度的控制。 如果校准器温度低于此参数设置的值，则不会使用传感器的数据。</li><li>将 <a href="./../advanced_config/parameter_reference.html#SYS_CAL_TMAX"> SYS_CAL_TMAX </a> 参数设置为校准器起作用的最高起始传感器温度。 如果起始温度高于此参数设置的值，校准将退出并报告错误。 注意，如果不同传感器测量的温度的差异超过 <code>SYS_CAL_TMAX </code> 和 <code>SYS_CAL_TMIN</code>的差值 ，则校准将不可能启动。</li><li>断开电源并将电路板冷却至低于<code>SYS_CAL_TMIN</code>参数指定的起始温度。 请注意，在校准开始之前启动过程有10秒的延迟，以允许所有传感器稳定，并且传感器在此期间会内部发热。</li><li>保持电路板静止[^2]，接通电源并加热到足够高的温度，以达到由<code>SYS_CAL_TDEL</code>参数指定的温升。 校准期间，完成百分比将打印到系统控制台。 [^3]</li><li>校准完成后，断开电源，让电路板冷却到校准范围内的温度，然后再执行下一步。</li><li>Perform a 6-point accel calibration via the system console using <code>commander calibrate accel</code> or via <em>QGroundControl</em>. 如果首次设置电路板，则还需要执行陀螺仪和磁力计校准。</li><li>在任何传感器校准之后的首次飞行之前，电路板必须重新上电，因为校准带来的突然的偏移变化可能会扰乱导航估计器，并且某些参数直到下次启动时才会被使用它们的算法加载。</li></ol><p><a id="offboard_calibration"></a></p><h3 id="板外校准过程" tabindex="-1">板外校准过程 <a class="header-anchor" href="#板外校准过程" aria-label="Permalink to &quot;板外校准过程&quot;">​</a></h3><p>Offboard calibration is run on a development computer using data collected during the calibration test. This method provides a way to visually check the quality of data and curve fit.</p><p>To perform an offboard calibration:</p><ol><li><p>确保在校准前设置机架类型，否则在设置飞控板时校准参数将丢失。</p></li><li><p>Power up the board and set the <a href="./../advanced_config/parameter_reference.html#TC_A_ENABLE">TC_A_ENABLE</a>, <a href="./../advanced_config/parameter_reference.html#TC_B_ENABLE">TC_B_ENABLE</a>, <a href="./../advanced_config/parameter_reference.html#TC_G_ENABLE">TC_G_ENABLE</a>, and <a href="./../advanced_config/parameter_reference.html#TC_M_ENABLE">TC_M_ENABLE</a> parameters to <code>1</code>.</p></li><li><p>Set all <a href="./../advanced_config/parameter_reference.html#CAL_ACC0_ID">CAL_ACC*</a>, <a href="./../advanced_config/parameter_reference.html#CAL_GYRO0_ID">CAL_GYRO*</a>, <a href="./../advanced_config/parameter_reference.html#CAL_MAG0_ID">CAL_MAG*</a>, and <a href="./../advanced_config/parameter_reference.html#CAL_BARO0_ID">CAL_BARO*</a> parameters to defaults.</p></li><li><p>将 <a href="./../advanced_config/parameter_reference.html#SDLOG_MODE"> SDLOG_MODE </a> 参数设置为 2 以从系统启动时就开始记录日志。</p></li><li><p>Set the <a href="./../advanced_config/parameter_reference.html#SDLOG_PROFILE">SDLOG_PROFILE</a> checkbox for <em>thermal calibration</em> (bit 2) to log the raw sensor data required for calibration.</p></li><li><p>将电路板冷却到操作所需的最低温度。</p></li><li><p>接通电源并保持电路板静止[^2]，将其缓慢加热至所需的最高工作温度。 [^3]</p></li><li><p>断开电源并取出 .ulog 文件。</p></li><li><p>Open a terminal window in the <strong>Firmware/Tools</strong> directory and run the python calibration script:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> process_sensor_caldata.py</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">full</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> to</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .ulog</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fil</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>This will generate a <strong>.pdf</strong> file showing the measured data and curve fits for each sensor, and a <strong>.params</strong> file containing the calibration parameters.</p></li><li><p>Power the board, connect <em>QGroundControl</em> and load the parameter from the generated <strong>.params</strong> file onto the board using <em>QGroundControl</em>. 由于参数的数量，加载它们可能需要一些时间。</p></li><li><p>参数完成加载后，将<code>SDLOG_MODE</code>设置为 1 以重新启用常规日志并断开电源。</p></li><li><p>Power the board and perform a normal accelerometer sensor calibration using <em>QGroundControl</em>. 重要的是，此步骤在飞控板处于校准温度范围内进行。 此步骤后的首次飞行之前，应重新启动电路板，因为突然的偏置变化会扰乱导航估计器，并且某些参数直到下次启动时才会被使用它们的算法加载。</p></li></ol><p><a id="implementation"></a></p><h2 id="实施细节" tabindex="-1">实施细节 <a class="header-anchor" href="#实施细节" aria-label="Permalink to &quot;实施细节&quot;">​</a></h2><p>Calibration refers to the process of measuring the change in sensor value across a range of internal temperatures, and performing a polynomial fit on the data to calculate a set of coefficients (stored as parameters) that can be used to correct the sensor data. Compensation refers to the process of using the internal temperature to calculate an offset that is subtracted from the sensor reading to correct for changing offset with temperature</p><p>The accelerometer, gyro, and magnetometer sensor offsets are calculated using a 3rd order polynomial, whereas the barometric pressure sensor offset is calculated using a 5th order polynomial. Example fits are show below:</p><p><img src="'+e+'" alt="Thermal calibration accel"></p><p><img src="'+a+'" alt="Thermal calibration gyro"></p><p><img src="'+t+'" alt="Thermal calibration mag"></p><p><img src="'+r+'" alt="Thermal calibration barometer"></p><h3 id="校准参数存储" tabindex="-1">校准参数存储 <a class="header-anchor" href="#校准参数存储" aria-label="Permalink to &quot;校准参数存储&quot;">​</a></h3><p>With the existing parameter system implementation we are limited to storing each value in the struct as a separate entry. To work around this limitation the following logical naming convention is used for the <a href="./../advanced_config/parameter_reference.html#thermal-compensation">thermal compensation parameters</a>:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TC_[type][instance]_[cal_name]_[axis]</span></span></code></pre></div><p>Where:</p><ul><li><p><code>type</code>: is a single character indicating the type of sensor where <code>A</code> = accelerometer, <code>G</code> = rate gyroscope, <code>M</code> = magnetometer, and <code>B</code> = barometer.</p></li><li><p><code>instance</code>：是一个整数 0、1或2 ，允许至多校准三个相同 <code>type</code> 的传感器。</p></li><li><p><code>cal_name</code>：是标识校准值的字符串。 它具有可能的值如下：</p><ul><li><code>Xn</code>：多项式系数，其中n是系数的阶数，例如 <code>X3* (temperature - reference temperature)**3</code> 。</li><li><code>SCL</code>：比例（缩放）系数</li><li><code>TREF</code>：参考温度(deg C)。</li><li><code>TMIN</code>：最低有效温度(deg C)。</li><li><code>TMAX</code>：最高有效温度(deg C)。</li></ul></li><li><p><code>axis</code>：是一个整数0，1或2，指示校准数据为飞控板参照系的 X，Y 或 Z 轴。 对于气压传感器，省略 <code>axis</code> 后缀。</p></li></ul><p>Examples:</p><ul><li><a href="./../advanced_config/parameter_reference.html#TC_A1_TREF">TC_A1_TREF</a> 是第二个加速度计的参考温度。</li><li><a href="./../advanced_config/parameter_reference.html#TC_G0_X3_0">TC_G0_X3_0</a> 是第一个陀螺 x 轴的 <code>^3</code> 系数。</li></ul><h3 id="校准参数使用" tabindex="-1">校准参数使用 <a class="header-anchor" href="#校准参数使用" aria-label="Permalink to &quot;校准参数使用&quot;">​</a></h3><p>The correction for thermal offsets (using the calibration parameters) is performed in the <a href="./../modules/modules_system.html#sensors">sensors module</a>. The reference temperature is subtracted from the measured temperature to obtain a delta temperature where:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>delta = measured_temperature - reference_temperature</span></span></code></pre></div><p>The delta temperature is then used to calculate a offset, where:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>offset = X0 + X1*delta + X2*delta**2 + ... + Xn*delta**n</span></span></code></pre></div><p>The offset and temperature scale factor are then used to correct the sensor measurement where:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>corrected_measurement = (raw_measurement - offset) * scale_factor</span></span></code></pre></div><p>If the temperature is above the test range set by the <code>*_TMIN</code> and <code>*_TMAX</code> parameters, then the measured temperature will be clipped to remain within the limits.</p><p>Correction of the accelerometer, gyroscope, magnetometer, or barometer data is enabled by setting <a href="./../advanced_config/parameter_reference.html#TC_A_ENABLE">TC_A_ENABLE</a>, <a href="./../advanced_config/parameter_reference.html#TC_G_ENABLE">TC_G_ENABLE</a>, <a href="./../advanced_config/parameter_reference.html#TC_M_ENABLE">TC_M_ENABLE</a>, or <a href="./../advanced_config/parameter_reference.html#TC_B_ENABLE">TC_B_ENABLE</a> parameters to 1 respectively.</p><h3 id="与遗留-cal-参数和-commander-控制校准的兼容性" tabindex="-1">与遗留 <code>CAL*</code> 参数和 commander 控制校准的兼容性 <a class="header-anchor" href="#与遗留-cal-参数和-commander-控制校准的兼容性" aria-label="Permalink to &quot;与遗留 `CAL*` 参数和 commander 控制校准的兼容性&quot;">​</a></h3><p>The legacy temperature-agnostic PX4 rate gyro and accelerometer sensor calibration is performed by the commander module and involves adjusting offset, and in the case of accelerometer calibration, scale factor calibration parameters. The offset and scale factor parameters are applied within the driver for each sensor. These parameters are found in the <a href="./../advanced_config/parameter_reference.html#sensor-calibration">CAL parameter group</a>.</p><p>Onboard temperature calibration is controlled by the events module and the corrections are applied within the sensors module before the sensor combined uORB topic is published. This means that if thermal compensation is being used, all of the corresponding legacy offset and scale factor parameters must be set to defaults of zero and unity before a thermal calibration is performed. If an on-board temperature calibration is performed, this will be done automatically, however if an offboard calibration is being performed it is important that the legacy <code>CAL*OFF</code> and <code>CAL*SCALE</code> parameters be reset before calibration data is logged.</p><p>If accel thermal compensation has been enabled by setting the <code>TC_A_ENABLE</code> parameter to 1, then the commander controlled 6-point accel calibration can still be performed. However, instead of adjusting the <code>*OFF</code> and <code>*SCALE</code> parameters in the <code>CAL</code> parameter group, these parameters are set to defaults and the thermal compensation <code>X0</code> and <code>SCL</code> parameters are adjusted instead.</p><p>If gyro thermal compensation has been enabled by setting the <code>TC_G_ENABLE</code> parameter to 1, then the commander controlled gyro calibration can still be performed, however it will be used to shift the compensation curve up or down by the amount required to zero the angular rate offset. It achieves this by adjusting the X0 coefficients.</p><p>If magnetometer thermal compensation has been enabled by setting the <code>TC_M_ENABLE</code> parameter to 1, then the commander controlled 6-point accel calibration can still be performed. However, instead of adjusting the <code>*OFF</code> and <code>*SCALE</code> parameters in the <code>CAL</code> parameter group, these parameters are set to defaults and the thermal compensation <code>X0</code> and <code>SCL</code> parameters are adjusted instead.</p><h3 id="局限" tabindex="-1">局限 <a class="header-anchor" href="#局限" aria-label="Permalink to &quot;局限&quot;">​</a></h3><p>Scale factors are assumed to be temperature invariant due to the difficulty associated with measuring these at different temperatures. This limits the usefulness of the accelerometer calibration to those sensor models with stable scale factors. In theory with a thermal chamber or IMU heater capable of controlling IMU internal temperature to within a degree, it would be possible to perform a series of 6 sided accelerometer calibrations and correct the accelerometers for both offset and scale factor. Due to the complexity of integrating the required board movement with the calibration algorithm, this capability has not been included.</p><hr><p>[^1]: 当校准开始时，<a href="./../advanced_config/parameter_reference.html#SYS_CAL_ACCEL">SYS_CAL_Accel</a>、<a href="./../advanced_config/parameter_reference.html#SYS_CAL_BARO">SYS_CAL_Baro</a> 和 <a href="./../advanced_config/parameter_reference.html#SYS_CAL_GYRO">SYS_CAL_GYRO</a> 参数重置为 0。 [^2]: 气压传感器偏置的校准需要一个稳定的气压环境。 由于天气的原因，空气压力变化缓慢，建筑物内部的气压会因室外风的波动和暖通空调系统的运行而迅速变化。 [^3]: 在加热冷却板时必须小心，以避免在某些情况下在板上形成凝结物，导致电路板损坏。</p>',63),d=[l];function p(h,m,f,_,u,b){return n(),i("div",null,d)}const A=o(c,[["render",p]]);export{v as __pageData,A as default};
