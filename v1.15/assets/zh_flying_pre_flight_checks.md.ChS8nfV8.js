import{_ as e,c as a,o as t,ab as i}from"./chunks/framework.CUflZczI.js";const u=JSON.parse('{"title":"飞行前传感器 / 估算器检查","description":"","frontmatter":{},"headers":[],"relativePath":"zh/flying/pre_flight_checks.md","filePath":"zh/flying/pre_flight_checks.md"}'),r={name:"zh/flying/pre_flight_checks.md"},l=i('<h1 id="飞行前传感器-估算器检查" tabindex="-1">飞行前传感器 / 估算器检查 <a class="header-anchor" href="#飞行前传感器-估算器检查" aria-label="Permalink to &quot;飞行前传感器 / 估算器检查&quot;">​</a></h1><p>PX4 performs a number of preflight sensor quality and estimator checks to determine if there is a good enough position estimate to arm and fly the vehicle (these checks are controlled by the <a href="./../advanced_config/parameter_reference.html#commander">COM_ARM_</a> parameters).</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Any preflight errors are reported in <em>QGroundControl</em> as <code>PREFLIGHT FAIL</code> messages. The <code>estimator_status.gps_check_fail_flags</code> message <a href="./../getting_started/flight_reporting.html">in the logs</a> also shows which GPS quality checks are failing.</p></div><p>The sections below list the errors, their likely causes and solutions, and any parameters that affect how the preflight checks are run.</p><h2 id="ekf-飞行前检查-错误" tabindex="-1">EKF 飞行前检查 / 错误 <a class="header-anchor" href="#ekf-飞行前检查-错误" aria-label="Permalink to &quot;EKF 飞行前检查 / 错误&quot;">​</a></h2><p>The following errors (with associated checks and parameters) are reported by the <a href="./../advanced_config/tuning_the_ecl_ekf.html">EKF</a> (and propagate to <em>QGroundControl</em>):</p><h4 id="preflight-fail-ekf-hgt-error" tabindex="-1">PREFLIGHT FAIL: EKF HGT ERROR <a class="header-anchor" href="#preflight-fail-ekf-hgt-error" aria-label="Permalink to &quot;PREFLIGHT FAIL: EKF HGT ERROR&quot;">​</a></h4><ul><li>当 IMU 和高度测量数据不一致时会产生此错误。</li><li>执行加速度计和陀螺仪校准并重新启动飞行器。 如果错误仍然存在，请检查高度传感器数据是否存在问题。</li><li>检查由<a href="./../advanced_config/parameter_reference.html#COM_ARM_EKF_HGT">COM_ARM_EKF_HGT</a>参数控制。</li></ul><h4 id="preflight-fail-ekf-vel-error" tabindex="-1">PREFLIGHT FAIL: EKF VEL ERROR <a class="header-anchor" href="#preflight-fail-ekf-vel-error" aria-label="Permalink to &quot;PREFLIGHT FAIL: EKF VEL ERROR&quot;">​</a></h4><ul><li>当 IMU 和 GPS 速度测量数据不一致时会产生此错误。</li><li>检查 GPS 速度数据是否存在不真实的数据跳转。 如果 GPS 质量看起来没有问题，请执行加速度计和陀螺仪校准并重新启动飞行器。</li><li>检查由<a href="./../advanced_config/parameter_reference.html#COM_ARM_EKF_VEL">COM_ARM_EKF_VEL</a>参数控制。</li></ul><h4 id="preflight-fail-ekf-horiz-pos-error" tabindex="-1">PREFLIGHT FAIL: EKF HORIZ POS ERROR <a class="header-anchor" href="#preflight-fail-ekf-horiz-pos-error" aria-label="Permalink to &quot;PREFLIGHT FAIL: EKF HORIZ POS ERROR&quot;">​</a></h4><ul><li>当 IMU 和位置测量数据（GPS 或外部视觉）不一致时会产生此问题。</li><li>检查位置传感器数据是否存在不真实的数据跳转。 如果数据质量看起来不错，请执行加速度计和陀螺仪校准并重新启动飞行器。</li><li>检查由<a href="./../advanced_config/parameter_reference.html#COM_ARM_EKF_POS">COM_ARM_EKF_POS</a>参数控制。</li></ul><h4 id="preflight-fail-ekf-yaw-error" tabindex="-1">PREFLIGHT FAIL: EKF YAW ERROR <a class="header-anchor" href="#preflight-fail-ekf-yaw-error" aria-label="Permalink to &quot;PREFLIGHT FAIL: EKF YAW ERROR&quot;">​</a></h4><ul><li>当使用陀螺仪数据估计的偏航角和来自磁力计或外部视觉系统的偏航角不一致时，产生该误差。</li><li>检查 IMU 数据是否存在较大的偏航率漂洗，并检查磁力计的对准和校准。</li><li>The check is controlled by the <a href="./../advanced_config/parameter_reference.html#COM_ARM_EKF_YAW">COM_ARM_EKF_YAW</a> parameter</li><li>The default value of 0.5 allows the differences between the navigation yaw angle and magnetic yaw angle (magnetometer or external vision) to be no more than 50% of the maximum tolerated by the EKF and provides some margin for error increase when flight commences.</li><li>如果偏航陀螺仪有较大的偏移量，或者飞行器在存在磁干扰或者磁力计校准的情况下移动或者旋转，则可能会校准失败。</li></ul><h4 id="preflight-fail-ekf-high-imu-accel-bias" tabindex="-1">PREFLIGHT FAIL: EKF HIGH IMU ACCEL BIAS <a class="header-anchor" href="#preflight-fail-ekf-high-imu-accel-bias" aria-label="Permalink to &quot;PREFLIGHT FAIL: EKF HIGH IMU ACCEL BIAS&quot;">​</a></h4><p>The EKF IMU acceleration bias is the difference between the measured acceleration reported by the IMU sensor and the expected acceleration reported by the EKF2 estimator (which fuses position and/or velocity data from a number of sources, including the IMU, GNSS, flow sensors etc.). This bias may change when the sensor is turned on (“turn-on bias”) and over time due to noise and temperature differences (“in-run bias”). The number should generally be very small (near zero), indicating that measurements from different sources all agree on the acceleration.</p><p>The warning indicates that the bias is higher than some arbitrary threshold (the vehicle will not be allowed to take off). It is most likely a sign that accelerometer or thermal calibration are required:</p><ul><li>If you <em>sometimes</em> get the warning: <a href="./../config/accelerometer.html">re-calibrate the accelerometer</a>.</li><li>If you get <em>regularly</em> get the warning: Perform a <a href="./../advanced_config/sensor_thermal_calibration.html">thermal calibration</a>.</li><li>If you still get the warning after thermal calibration (or you can&#39;t perform thermal calibration): <ul><li>Verify that the issues do not come from the sensor or autopilot hardware: <ul><li>The easiest way to do this is to test the same frame/sensors with another autopilot.</li><li>Alternatively, <a href="./../dev_log/logging.html#configuration">log and compare</a> all accelerometers across a number of bench test runs with <code>6: Sensor comparison</code> enabled in <a href="./../advanced_config/parameter_reference.html#SDLOG_PROFILE">SDLOG_PROFILE</a>.</li></ul></li><li>Attempt to change the accelerometer bias learning tuning parameters.</li></ul></li></ul><p>Increasing the parameters will make the autopilot less likely to detect an anomaly and can modify the stability of the estimator. However it may be required if there are problems with the sensor that cannot be fixed by other means (i.e you can tune the EKF for better performance, but there is no way you can calibrate the accelerometer &quot;better&quot;).</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Tuning these parameters is a last resort. It should only be attempted if you have data showing it will improve the performance of the estimator.</p></div><table><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><a id="EKF2_ABL_LIM"></a><a href="./../advanced_config/parameter_reference.html#EKF2_ABL_LIM">EKF2_ABL_LIM</a></td><td>The maximum bias value that the EKF is allowed to estimate (above this value the bias will be clipped and EKF will attempt to reset itself, possibly even switching to a more healthy EKF with a working IMU in a multi-EKF system). The autopilot will report a “high accel bias” if the estimated bias exceeds 75% of this parameter during a preflight check and prevent takeoff. The current value of 0.4m/s2 is already quite high and increasing it would make the autopilot less likely to detect an issue.</td></tr><tr><td><a id="EKF2_ABIAS_INIT"></a><a href="./../advanced_config/parameter_reference.html#EKF2_ABIAS_INIT">EKF2_ABIAS_INIT</a></td><td>Initial bias uncertainty (if perfectly calibrated, this is related to the “turn-on bias” of the sensor). Some users might want to reduce that value if they know that the sensor is well calibrated and that the turn-on bias is small.</td></tr><tr><td><a id="EKF2_ACC_B_NOISE"></a><a href="./../advanced_config/parameter_reference.html#EKF2_ACC_B_NOISE">EKF2_ACC_B_NOISE</a></td><td>The expected “in-run bias” of the accelerometer or “how fast do we expect the bias to change per second”. By default, this value is large enough to include the drift due to a temperature change. If the IMU is temperature calibrated, the user might want to reduce this parameter.</td></tr><tr><td><a id="EKF2_ABL_ACCLIM"></a><a href="./../advanced_config/parameter_reference.html#EKF2_ABL_ACCLIM">EKF2_ABL_ACCLIM</a></td><td>The maximum acceleration at which the estimator will try to learn an acceleration bias. This is to prevent the estimator from learning a bias due to non-linearity and scale factor errors. (Almost no user should need to change that parameter except if they really know what they are doing).</td></tr></tbody></table><h4 id="preflight-fail-ekf-high-imu-gyro-bias" tabindex="-1">PREFLIGHT FAIL: EKF HIGH IMU GYRO BIAS <a class="header-anchor" href="#preflight-fail-ekf-high-imu-gyro-bias" aria-label="Permalink to &quot;PREFLIGHT FAIL: EKF HIGH IMU GYRO BIAS&quot;">​</a></h4><ul><li>当由 EKF 估计的 IMU 陀螺仪偏差过大时会产生该错误。</li><li>Excessive in this case means that the bias estimate exceeds 10deg/s (half the configured limit, which is hardcoded to 20deg/s).</li></ul><h4 id="preflight-fail-accel-sensors-inconsistent-check-calibration" tabindex="-1">PREFLIGHT FAIL: ACCEL SENSORS INCONSISTENT - CHECK CALIBRATION <a class="header-anchor" href="#preflight-fail-accel-sensors-inconsistent-check-calibration" aria-label="Permalink to &quot;PREFLIGHT FAIL: ACCEL SENSORS INCONSISTENT - CHECK CALIBRATION&quot;">​</a></h4><ul><li>当来自不同 IMU 单元的加速度测量值不一致时，会产生此错误消息。</li><li>此检查仅适用于具有多个 IMU 的板。</li><li>检查由<a href="./../advanced_config/parameter_reference.html#COM_ARM_IMU_ACC">COM_ARM_IMU_ACC</a>参数控制。</li></ul><h4 id="preflight-fail-gyro-sensors-inconsistent-check-calibration" tabindex="-1">PREFLIGHT FAIL: GYRO SENSORS INCONSISTENT - CHECK CALIBRATION <a class="header-anchor" href="#preflight-fail-gyro-sensors-inconsistent-check-calibration" aria-label="Permalink to &quot;PREFLIGHT FAIL: GYRO SENSORS INCONSISTENT - CHECK CALIBRATION&quot;">​</a></h4><ul><li>当来自不同 IMU 单元的角速率测量值不一致时，会产生此错误消息。</li><li>此检查仅适用于具有多个 IMU 的板。</li><li>检查由<a href="./../advanced_config/parameter_reference.html#COM_ARM_IMU_GYR">COM_ARM_IMU_GYR</a>参数控制。</li></ul><h4 id="preflight-fail-compass-sensors-inconsistent-check-calibration" tabindex="-1">PREFLIGHT FAIL: COMPASS SENSORS INCONSISTENT - CHECK CALIBRATION <a class="header-anchor" href="#preflight-fail-compass-sensors-inconsistent-check-calibration" aria-label="Permalink to &quot;PREFLIGHT FAIL: COMPASS SENSORS INCONSISTENT - CHECK CALIBRATION&quot;">​</a></h4><ul><li>This error message is produced when the difference in measurements from different compass sensors is too great.</li><li>It indicates bad calibration, orientation or magnetic interference.</li><li>This check only applies to when more than one compass/magnetometer is connected.</li><li>The check is controlled by the <a href="./../advanced_config/parameter_reference.html#COM_ARM_MAG_ANG">COM_ARM_MAG_ANG</a> parameter.</li></ul><h4 id="preflight-fail-ekf-internal-checks" tabindex="-1">PREFLIGHT FAIL: EKF INTERNAL CHECKS <a class="header-anchor" href="#preflight-fail-ekf-internal-checks" aria-label="Permalink to &quot;PREFLIGHT FAIL: EKF INTERNAL CHECKS&quot;">​</a></h4><ul><li>如果水平 GPS 速度、偏航角、垂直 GPS 速度或者垂直位置传感器（气压计默认情况下可以使测距仪或 GPS ，如果使用非标准参数）其中之一新息过多，会产生此错误消息。 新息指的是惯性导航计算预测值与传感器测量值之间的差异。</li><li>用户应检查日志文件中新息级别以确定原因。 这些可以在<code>ekf2_innovations</code>消息下找到。 常见问题 / 解决方案包括： <ul><li>IMU 启动时漂移。 可以通过重启自驾仪来解决。 可能需要 IMU 加速度计和陀螺仪校准。</li><li>相邻磁干扰在飞行器运动中。 通过等待或者重新上电解决。</li><li>磁力计校准不良在飞行器运动中。。 通过重新校准解决。</li><li>启动时的初始冲击或快速移动导致惯性导航失败。 通过重新启动飞行器并在前 5 秒内最大限度地减少移动来解决此问题。</li></ul></li></ul><h2 id="其他参数" tabindex="-1">其他参数 <a class="header-anchor" href="#其他参数" aria-label="Permalink to &quot;其他参数&quot;">​</a></h2><p>The following parameters also affect preflight checks.</p><h4 id="com-arm-wo-gps" tabindex="-1">COM_ARM_WO_GPS <a class="header-anchor" href="#com-arm-wo-gps" aria-label="Permalink to &quot;COM_ARM_WO_GPS&quot;">​</a></h4><p>The <a href="./../advanced_config/parameter_reference.html#COM_ARM_WO_GPS">COM_ARM_WO_GPS</a> parameter controls whether or not arming is allowed without a global position estimate.</p><ul><li><code>1</code> (default): Arming <em>is</em> allowed without a position estimate for flight modes that do not require position information (only).</li><li><code>0</code>：只有当 EKF 提供全球位置估计并且 EKF GPS 质量检查正在通过时，才允许解锁。</li></ul>',36),o=[l];function n(h,s,c,d,f,m){return t(),a("div",null,o)}const p=e(r,[["render",n]]);export{u as __pageData,p as default};
