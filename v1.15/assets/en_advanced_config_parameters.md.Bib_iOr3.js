import{_ as e,a,b as t,c as r,d as o}from"./chunks/parameters_tools_menu.By8W2DKY.js";import{_ as i,c as n,o as s,ab as l}from"./chunks/framework.CUflZczI.js";const y=JSON.parse('{"title":"Finding/Updating Parameters","description":"","frontmatter":{},"headers":[],"relativePath":"en/advanced_config/parameters.md","filePath":"en/advanced_config/parameters.md"}'),c={name:"en/advanced_config/parameters.md"},h=l('<h1 id="finding-updating-parameters" tabindex="-1">Finding/Updating Parameters <a class="header-anchor" href="#finding-updating-parameters" aria-label="Permalink to &quot;Finding/Updating Parameters&quot;">​</a></h1><p>PX4 behaviour can be configured/tuned using <a href="./../advanced_config/parameter_reference.html">parameters</a> (e.g. <a href="./../config_mc/pid_tuning_guide_multicopter.html">Multicopter PID gains</a>, calibration information, etc.).</p><p>The <em>QGroundControl Parameters</em> screen allows you to find and modify <strong>any</strong> of the parameters associated with the vehicle. The screen is accessed by clicking the <strong>Q</strong> application icon &gt; <strong>Vehicle Setup</strong>, and then <em>Parameters</em> in the sidebar.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Most of the more commonly used parameters are more conveniently set using the dedicated setup screens, as described in the <a href="./../config/">Standard Configuration</a> section. The <em>Parameters</em> screen is needed when modifying less commonly modified parameters - for example while tuning a new vehicle.</p></div><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>While some parameters can be changed in flight, this is not recommended (except where explicitly stated in the guide).</p></div><p><a id="finding"></a></p><h2 id="finding-a-parameter" tabindex="-1">Finding a Parameter <a class="header-anchor" href="#finding-a-parameter" aria-label="Permalink to &quot;Finding a Parameter&quot;">​</a></h2><p>You can search for a parameter by entering a term in the <em>Search</em> field. This will show you a list of all parameter names and descriptions that contain the entered substring (press <strong>Clear</strong> to reset the search, and use the <strong>Show modified only</strong> checkbox to filter out unchanged parameters).</p><p><img src="'+e+'" alt="Parameters Search"></p><p>You can also browse the parameters by type and group by clicking on the buttons to the left (in the image below the <em>DShot</em> group in the <em>Standard</em> parameters is selected).</p><p><img src="'+a+'" alt="Parameters Screen"></p><p>You can expand/collapse the &quot;type&quot; groupings as shown. Note that the groups at the bottom named <em>Component X</em> are attached <a href="./../dronecan/#qgc-cannode-parameter-configuration">DroneCAN peripherals</a> (&quot;X&quot; is the node id). <a href="./../dronecan/#qgc-cannode-parameter-configuration">QGC can set the parameters</a> of these peripherals if they are attached to the Flight Controller when QGC is started.</p><p><img src="'+t+'" alt="Parameters Types - collapsed"></p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>If you can&#39;t find an expected parameter, see the <a href="#missing">next section</a>.</p></div><p><a id="missing"></a></p><h2 id="missing-parameters" tabindex="-1">Missing Parameters <a class="header-anchor" href="#missing-parameters" aria-label="Permalink to &quot;Missing Parameters&quot;">​</a></h2><p>Parameters are usually not visible because either they are conditional on other parameters, or they are not present in the firmware (see below).</p><h3 id="conditional-parameters" tabindex="-1">Conditional Parameters <a class="header-anchor" href="#conditional-parameters" aria-label="Permalink to &quot;Conditional Parameters&quot;">​</a></h3><p>A parameter may not be displayed if it is conditional on another parameter that is not enabled.</p><p>You can usually find out what parameters are conditional by searching the <a href="./../advanced_config/parameter_reference.html">full parameter reference</a> and other documentation. In particular <a href="./../peripherals/serial_configuration.html">serial port configuration parameters</a> depend on what service is assigned to a serial port.</p><h3 id="parameter-not-in-firmware" tabindex="-1">Parameter Not In Firmware <a class="header-anchor" href="#parameter-not-in-firmware" aria-label="Permalink to &quot;Parameter Not In Firmware&quot;">​</a></h3><p>A parameter may not be present in the firmware because you&#39;re using a different version of PX4 or because you&#39;re using a build in which the associated module is not included.</p><p>New parameters are added in each PX4 version, and existing parameters are sometimes removed or renamed. You can check whether a parameter <em>should</em> be present by reviewing the <a href="./../advanced_config/parameter_reference.html">full parameter reference</a> for the version you&#39;re targeting. You can also search for the parameter in the source tree and in the release notes.</p><p>The other reason that a parameter might not be in firmware is if its associated module has not been included. This is a problem (in particular) for <em>FMUv2 firmware</em>, which omits many modules so that PX4 can fit into the 1MB of available flash. There are two options to solve this problem:</p><ul><li><p>Check if you can update your board to run FMUv3 firmware, which includes all modules: <a href="./../config/firmware.html#bootloader">Firmware &gt; FMUv2 Bootloader Update</a></p></li><li><p>If your board can only run FMUv2 firmware you will need to <a href="./../dev_setup/building_px4.html">rebuild PX4</a> with the missing modules enabled. You need reconfigure the PX4 firmware itself through make px4_fmuv2_default boardconfig where you can enabled/disable modules.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>You may also need to disable other modules in order to fit the rebuilt firmware into 1MB flash. Finding modules to remove requires some trial/error and depends on what use cases you need the vehicle to meet.</p></div></li></ul><p><a id="changing"></a></p><h2 id="changing-a-parameter" tabindex="-1">Changing a Parameter <a class="header-anchor" href="#changing-a-parameter" aria-label="Permalink to &quot;Changing a Parameter&quot;">​</a></h2><p>To change the value of a parameter click on the parameter row in a group or search list. This will open a side dialog in which you can update the value (this dialog also provides additional detailed information about the parameter - including whether a reboot is required for the change to take effect).</p><p><img src="'+r+'" alt="Changing a parameter value"></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>When you click <strong>Save</strong> the parameter is automatically and silently uploaded to the connected vehicle. Depending on the parameter, you may then need to reboot the flight controller for the change to take effect.</p></div><h2 id="tools" tabindex="-1">Tools <a class="header-anchor" href="#tools" aria-label="Permalink to &quot;Tools&quot;">​</a></h2><p>You can select additional options from the <strong>Tools</strong> menu on the top right hand side of the screen.</p><p><img src="'+o+'" alt="Tools menu"></p><p><strong>Refresh</strong><br>Refresh the parameter values by re-requesting all of them from the vehicle.</p><p><strong>Reset all to firmware defaults</strong><br>Reset all parameters to their original default values in the firmware.</p><p><strong>Reset to vehicle&#39;s configuration defaults</strong><br>Reset all parameters to their original default values for the particular selected airfame configuration.</p><p><strong>Load from file / Save to file</strong><br>Load parameters from an existing file or save your current parameter settings to a file.</p><p><strong>Clear all RC to Param</strong><br>This clears all associations between RC transmitter controls and parameters. For more information see: <a href="./../config/radio.html#param-tuning-channels">Radio Setup &gt; Param Tuning Channels</a>.</p><p><strong>Reboot Vehicle</strong><br>Reboot the vehicle (required after changing some parameters).</p>',39),m=[h];function d(p,u,g,f,b,_){return s(),n("div",null,m)}const P=i(c,[["render",d]]);export{y as __pageData,P as default};
