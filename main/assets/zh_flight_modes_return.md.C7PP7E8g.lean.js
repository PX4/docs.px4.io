import{_ as t}from"./chunks/position_fixed.t2m3lWVy.js";import{_ as i}from"./chunks/rtl_cone.DihiHN2J.js";import{_ as a,c as n,a8 as o,o as r}from"./chunks/framework.BDnHobkS.js";const T=JSON.parse('{"title":"返航模式","description":"","frontmatter":{},"headers":[],"relativePath":"zh/flight_modes/return.md","filePath":"zh/flight_modes/return.md"}'),l={name:"zh/flight_modes/return.md"};function s(d,e,h,m,c,u){return r(),n("div",null,e[0]||(e[0]=[o('<h1 id="返航模式" tabindex="-1">返航模式 <a class="header-anchor" href="#返航模式" aria-label="Permalink to &quot;返航模式&quot;">​</a></h1><img src="'+t+'" title="Position fix required (e.g. GPS)" width="30px"><p>The <em>Return</em> flight mode is used to <em>fly a vehicle to safety</em> on an unobstructed path to a safe destination, where it should land.</p><p>PX4 提供了几种机制来选择安全的返航路径，返航目的地和着陆，包括使用其实位置，集结（“安全”）点，任务路径和任务着陆顺序。</p><ul><li><a href="./../flight_modes_mc/return.html">Multicopter</a></li><li><a href="./../flight_modes_fw/return.html">Fixed-wing (Plane)</a></li><li><a href="./../flight_modes_vtol/return.html">VTOL</a></li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><ul><li>Mode is automatic - no user intervention is <em>required</em> to control the vehicle.</li><li>Mode requires a global 3d position estimate (from GPS or inferred from a <a href="./../ros/external_position_estimation.html#enabling-auto-modes-with-a-local-position">local position</a>). <ul><li>Flying vehicles can&#39;t switch to this mode without global position.</li><li>Flying vehicles will failsafe if they lose the position estimate.</li></ul></li><li>Mode requires home position is set.</li><li>Mode prevents arming (vehicle must be armed when switching to this mode).</li><li>遥控开关可以在任何无人机上更改飞行模式。</li><li>RC stick movement in a multicopter (or VTOL in multicopter mode) will <a href="#COM_RC_OVERRIDE">by default</a> change the vehicle to <a href="./../flight_modes_mc/position.html">Position mode</a> unless handling a critical battery failsafe.</li><li>A VTOL will return as MC or FW based on its mode at the point the return mode was triggered. In MC mode it will respect multicopter parameters, such as the landing &quot;cone&quot;. In FW mode it will respect fixed-wing parameters (ignore the cone), but unless using a mission landing, will transition to MC mode and land at the destination after loitering at the descent altitude.</li></ul></div><h2 id="综述" tabindex="-1">综述 <a class="header-anchor" href="#综述" aria-label="Permalink to &quot;综述&quot;">​</a></h2><p>PX4 provides several mechanisms for choosing a safe return path, destination and landing, including using home location, rally (&quot;safe&quot;) points, mission paths, and landing sequences defined in a mission.</p><p>All vehicles <em>nominally</em> support all of these mechanisms, but not all of them make as much sense for particular vehicles. For example, a multicopter can land virtually anywhere, so it doesn&#39;t make sense for it to use a landing sequence except in rare cases. Similarly, a fixed-wing vehicle needs to fly a safe landing path: it can use the home location as a return point, but it won&#39;t try and land on it by default.</p><p>This topic covers all the possible return types that any vehicle <em>might</em> be configured to use — the vehicle-specific return mode topics cover the default/recommended return type and configuration for each vehicle.</p><p>The following sections explain how to configure the <a href="#return_types">return type</a>, <a href="#minimum-return-altitude">minimum return altitude</a> and <a href="#loiter-landing-at-destination">landing/arrival behaviour</a>.</p><p><a id="return_types"></a></p><h2 id="返航类型-rtl-type" tabindex="-1">返航类型（RTL_TYPE） <a class="header-anchor" href="#返航类型-rtl-type" aria-label="Permalink to &quot;返航类型（RTL_TYPE）&quot;">​</a></h2><p>PX4 provides four alternative approaches for finding an unobstructed path to a safe destination and/or landing, which are set using the <a href="#RTL_TYPE">RTL_TYPE</a> parameter.</p><p>At high level these are:</p><ul><li><a href="#home_return">Home/rally point return</a> (<code>RTL_TYPE=0</code>): Ascend to safe altitude and return via a direct path to the closest rally point or home location.</li><li><a href="#mission-landing-rally-point-return-type-rtl-type-1">Mission landing/rally point return</a> (<code>RTL_TYPE=1</code>): Ascend to a safe altitude, fly direct to the closest destination <em>other than home</em>: rally point or start of mission landing. 如果未定义任务着陆点或集结点，请通过直接路径返回起始位置。</li><li><a href="#mission-path-return-type-rtl-type-2">Mission path return</a> (<code>RTL_TYPE=2</code>): Use mission path and fast-continue to mission landing (if defined). If no mission <em>landing</em> defined, fast-reverse mission to home. If no <em>mission</em> defined, return direct to home (rally points are ignored).</li><li><a href="#closest-safe-destination-return-type-rtl-type-3">Closest safe destination return</a> (<code>RTL_TYPE=3</code>): Ascend to a safe altitude and return via direct path to closest destination: home, start of mission landing pattern, or rally point. 如果目的地是飞行任务着陆模式，则按照该模式降落。</li></ul><p>以下各节提供了每种类型的更详细说明。</p><p><a id="home_return"></a></p><h3 id="起始位置-集结点返航类型-rtl-type-0" tabindex="-1">起始位置/集结点返航类型（RTL_TYPE=0） <a class="header-anchor" href="#起始位置-集结点返航类型-rtl-type-0" aria-label="Permalink to &quot;起始位置/集结点返航类型（RTL_TYPE=0）&quot;">​</a></h3><p>This is the default return type for a <a href="./../flight_modes_mc/return.html">multicopter</a> (see topic for more information).</p><p>无人机在该返航类型中：</p><ul><li>Ascends to a safe <a href="#minimum-return-altitude">minimum return altitude</a> (above any expected obstacles).</li><li>通过直接路径飞往起始位置或集结点（以最近者为准）。</li><li>On <a href="#loiter-landing-at-destination">arrival</a> descends to &quot;descent altitude&quot; and waits for a configurable time. This time may be used to deploy landing gear.</li><li>Lands or waits (this depends on landing parameters), By default an MC or VTOL in MC mode will land and a fixed-wing vehicle circles at the descent altitude. A VTOL in FW mode aligns its heading to the destination point, transitions to MC mode, and then lands.</li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>If no rally points are defined, this is the same as a <em>Return to Launch</em> (RTL)/<em>Return to Home</em> (RTH).</p></div><h3 id="任务着陆-集结点返航类型-rtl-type-1" tabindex="-1">任务着陆/集结点返航类型 (RTL_TYPE=1) <a class="header-anchor" href="#任务着陆-集结点返航类型-rtl-type-1" aria-label="Permalink to &quot;任务着陆/集结点返航类型 (RTL_TYPE=1)&quot;">​</a></h3><p>This is the default return type for a <a href="./../flight_modes_fw/return.html">fixed-wing</a> or <a href="./../flight_modes_vtol/return.html">VTOL</a> vehicle (see topics for more information).</p><p>无人机在该返航类型中：</p><ul><li>Ascends to a safe <a href="#minimum-return-altitude">minimum return altitude</a> (above any expected obstacles) if needed. The vehicle maintains its initial altitude if that is higher than the minimum return altitude.</li><li>Flies via direct constant-altitude path to a rally point or the start of a <a href="#mission-landing-pattern">mission landing pattern</a> (whichever is closest). 如果未定义任务降落或集结点，无人机通过直接路径返回到起始位置。</li><li>如果目的地是飞行任务着陆模式，则按照该模式降落。</li><li>If the destination is a rally point or home it will <a href="#loiter-landing-at-destination">land or wait</a> at descent altitude (depending on landing parameters). By default an MC or VTOL in MC mode will land, and a fixed-wing vehicle circles at the descent altitude. A VTOL in FW mode aligns its heading to the destination point, transitions to MC mode, and then lands.</li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Fixed wing vehicles commonly also set <a href="#MIS_TKO_LAND_REQ">MIS_TKO_LAND_REQ</a> to <em>require</em> a mission landing pattern.</p></div><h3 id="任务路径返航类型-rtl-type-2" tabindex="-1">任务路径返航类型（RTL_TYPE=2） <a class="header-anchor" href="#任务路径返航类型-rtl-type-2" aria-label="Permalink to &quot;任务路径返航类型（RTL_TYPE=2）&quot;">​</a></h3><p>This return type uses the mission (if defined) to provide a safe return <em>path</em>, and the <a href="#mission-landing-pattern">mission landing pattern</a> (if defined) to provide landing behaviour. If there is a mission but no mission landing pattern, the mission is flown <em>in reverse</em>. 集结点，如果有的话，将被忽略。</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The behaviour is fairly complex because it depends on the flight mode, and whether a mission and mission landing are defined.</p></div><p>Mission <em>with</em> landing pattern:</p><ul><li><strong>Mission mode:</strong> Mission is continued in &quot;fast-forward mode&quot; (jumps, delay and any other non-position commands ignored, loiter and other position waypoints converted to simple waypoints) and then lands.</li><li><strong>Auto mode other than mission mode:</strong><ul><li>Ascend to a safe <a href="#minimum-return-altitude">minimum return altitude</a> above any expected obstacles.</li><li>直接飞到最近的航点（对固定翼而言，不是着陆航点），然后降落到航点高度。</li><li>从该航点以快速模式继续执行任务。</li></ul></li><li><strong>Manual modes:</strong><ul><li>Ascend to a safe <a href="#minimum-return-altitude">minimum return altitude</a> above any expected obstacles.</li><li>直接飞到降落序列位置并下降到航点高度。</li><li>使用任务降落模式着陆。</li></ul></li></ul><p>Mission <em>without</em> landing pattern defined:</p><ul><li><strong>Mission mode:</strong><ul><li>从上一个航点开始以“快退”（反向）飞行的任务 <ul><li>跳，延迟和其他任何非定位命令都会被忽略，悬停和其他位置航点将转换为简单航点。</li><li>VTOL 无人机在反向飞行任务之前切换到固定翼模式（如果需要）。</li></ul></li><li>On reaching waypoint 1, the vehicle ascends to the <a href="#minimum-return-altitude">minimum return altitude</a> and flies to the home position (where it <a href="#loiter-landing-at-destination">lands or waits</a>).</li></ul></li><li><strong>Auto mode other than mission mode:</strong><ul><li>直接飞到最近的航点（对固定翼而言，不是着陆航点），然后降落到航点高度。</li><li>反向执行任务，就像在任务模式中触发返航模式一样（上图）。</li></ul></li><li><strong>Manual modes:</strong> Fly directly to home location and land.</li></ul><p>如果未定义任务，PX4 将直接飞回起始位置并着陆（集结点将被忽略）。</p><p>如果任务在返航模式期间发生更改，则将按照与上述相同的规则根据新任务重新评估行为（例如，如果新任务没有降落顺序并且你在一个任务中，则任务将被逆转）。</p><h3 id="最近的安全目的地返回类型-rtl-type-3" tabindex="-1">最近的安全目的地返回类型（RTL_TYPE=3） <a class="header-anchor" href="#最近的安全目的地返回类型-rtl-type-3" aria-label="Permalink to &quot;最近的安全目的地返回类型（RTL_TYPE=3）&quot;">​</a></h3><p>无人机在该返航类型中：</p><ul><li>Ascends to a safe <a href="#minimum-return-altitude">minimum return altitude</a> (above any expected obstacles).</li><li>飞到最近目的地的直接路径：起始位置，任务着陆模式或集结点。</li><li>If the destination is a <a href="#mission-landing-pattern">mission landing pattern</a> the vehicle will follow the pattern to land.</li><li>If the destination is a home location or rally point, the vehicle will descend to the descent altitude (<a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a>) and then <a href="#loiter-landing-at-destination">lands or waits</a>. By default an MC or VTOL in MC mode will land, and a fixed-wing vehicle circles at the descent altitude. A VTOL in FW mode aligns its heading to the destination point, transitions to MC mode, and then lands.</li></ul><h2 id="悬停-降落在目的地" tabindex="-1">悬停/降落在目的地 <a class="header-anchor" href="#悬停-降落在目的地" aria-label="Permalink to &quot;悬停/降落在目的地&quot;">​</a></h2><p>For most <a href="#return_types">return types</a> a vehicle will ascend to a <em>minimum safe altitude</em> before returning (unless already above that altitude), in order to avoid any obstacles between it and the destination.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The exception is when executing a <a href="#mission-path-return-type-rtl-type-2">mission path return</a> from <em>within a mission</em>. In this case the vehicle follows mission waypoints, which we assume are planned to avoid any obstacles.</p></div><p>The return altitude for a fixed-wing vehicle or a VTOL in fixed-wing mode is configured using the parameter <a href="#RTL_RETURN_ALT">RTL_RETURN_ALT</a> (does not use the code described in the next paragraph).</p><p>The return altitude for a multicopter or a VTOL vehicles in MC mode is configured using the parameters <a href="#RTL_RETURN_ALT">RTL_RETURN_ALT</a> and <a href="#RTL_CONE_ANG">RTL_CONE_ANG</a>, which define a half cone centered around the destination (home location or safety point).</p><p><img src="'+i+'" alt="Return mode cone"></p><p>如果无人机是：</p><ul><li>Above <a href="#RTL_RETURN_ALT">RTL_RETURN_ALT</a> (1) it will return at its current altitude.</li><li>Below the cone it will return where it intersects the cone (2) or <a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a> (whichever is higher).</li><li>Outside the cone (3) it will first climb until it reaches <a href="#RTL_RETURN_ALT">RTL_RETURN_ALT</a>.</li><li>在圆锥内： <ul><li>Above <a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a> (4) it will return at its current altitude.</li><li>Below <a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a> (5) it will first ascend to <code>RTL_DESCEND_ALT</code>.</li></ul></li></ul><p>注意：</p><ul><li>If <a href="#RTL_CONE_ANG">RTL_CONE_ANG</a> is 0 degrees there is no &quot;cone&quot;: <ul><li>the vehicle returns at <code>RTL_RETURN_ALT</code> (or above).</li></ul></li><li>If <a href="#RTL_CONE_ANG">RTL_CONE_ANG</a> is 90 degrees the vehicle will return at the greater of <code>RTL_DESCEND_ALT</code> and the current altitude.</li><li>The vehicle will always ascend at least <a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a> for the return.</li></ul><h2 id="无人机默认行为" tabindex="-1">无人机默认行为 <a class="header-anchor" href="#无人机默认行为" aria-label="Permalink to &quot;无人机默认行为&quot;">​</a></h2><p>Unless executing a <a href="#mission-landing-pattern">mission landing pattern</a> as part of the return mode, the vehicle will arrive at its destination, and rapidly descend to the <a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a> altitude, where it will loiter for <a href="#RTL_LAND_DELAY">RTL_LAND_DELAY</a> before landing. If <code>RTL_LAND_DELAY=-1</code> it will loiter indefinitely.</p><p>The default landing configuration is vehicle dependent:</p><ul><li>如果定义了任务降落，直接飞往任务降落起点，然后着陆。</li><li>Fixed-wing vehicles use a return mode with a <a href="#mission-landing-pattern">mission landing pattern</a>, as this enables automated landing. If not using a mission landing, the default configuration is to loiter indefinitely, so the user can take over and manually land.</li><li>VTOLs in MC mode fly and land exactly as a multicopter.</li><li>VTOLS in FW mode head towards the landing point, transition to MC mode, and then land on the destination.</li></ul><h2 id="mission-landing-pattern" tabindex="-1">Mission Landing Pattern <a class="header-anchor" href="#mission-landing-pattern" aria-label="Permalink to &quot;Mission Landing Pattern&quot;">​</a></h2><p>A mission landing pattern is a landing pattern defined as part of a mission plan. This consists of a <a href="https://mavlink.io/en/messages/common.html#MAV_CMD_DO_LAND_START" target="_blank" rel="noreferrer">MAV_CMD_DO_LAND_START</a>, one or more position waypoints, and a <a href="https://mavlink.io/en/messages/common.html#MAV_CMD_NAV_LAND" target="_blank" rel="noreferrer">MAV_CMD_NAV_LAND</a> (or <a href="https://mavlink.io/en/messages/common.html#MAV_CMD_NAV_VTOL_LAND" target="_blank" rel="noreferrer">MAV_CMD_NAV_VTOL_LAND</a> for a VTOL Vehicle).</p><p>Landing patterns defined in missions are the safest way to automatically land a <em>fixed-wing</em> vehicle on PX4. For this reason fixed-wing vehicles are configured to use <a href="#mission-landing-rally-point-return-type-rtl-type-1">Mission landing/really point return</a> by default.</p><h2 id="参数" tabindex="-1">参数 <a class="header-anchor" href="#参数" aria-label="Permalink to &quot;参数&quot;">​</a></h2><p>The RTL parameters are listed in <a href="./../advanced_config/parameter_reference.html#return-mode">Parameter Reference &gt; Return Mode</a> (and summarised below).</p><table tabindex="0"><thead><tr><th>参数</th><th>描述</th></tr></thead><tbody><tr><td><a id="RTL_TYPE"></a><a href="./../advanced_config/parameter_reference.html#RTL_TYPE">RTL_TYPE</a></td><td>Return mechanism (path and destination).<br><code>0</code>: Return to a rally point or home (whichever is closest) via direct path.<br><code>1</code>: Return to a rally point or the mission landing pattern start point (whichever is closest), via direct path. 如果未定义任务着陆点或集结点，通过直接路径返回起始位置。 If the destination is a mission landing pattern, follow the pattern to land.<br><code>2</code>: Use mission path fast-forward to landing if a landing pattern is defined, otherwise fast-reverse to home. 忽略集结点。 Fly direct to home if no mission plan is defined.<br><code>3</code>: Return via direct path to closest destination: home, start of mission landing pattern or safe point. 如果目的地是飞行任务着陆模式，则按照该模式降落。</td></tr><tr><td><a id="RTL_RETURN_ALT"></a><a href="./../advanced_config/parameter_reference.html#RTL_RETURN_ALT">RTL_RETURN_ALT</a></td><td>Return altitude in meters (default: 60m) when <a href="./../advanced_config/parameter_reference.html#RTL_CONE_ANG">RTL_CONE_ANG</a> is 0. 如果已经超过这个值, 飞机将返回当前的高度。</td></tr><tr><td><a id="RTL_DESCEND_ALT"></a><a href="./../advanced_config/parameter_reference.html#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a></td><td>最小返航高度和无人机从较高的返航高度到减速或者停止的初始下降高度（默认： 30米）。</td></tr><tr><td><a id="RTL_LAND_DELAY"></a><a href="./../advanced_config/parameter_reference.html#RTL_LAND_DELAY">RTL_LAND_DELAY</a></td><td>Time to wait at <code>RTL_DESCEND_ALT</code> before landing (default: 0.5s) -by default this period is short so that the vehicle will simply slow and then land immediately. If set to -1 the system will loiter at <code>RTL_DESCEND_ALT</code> rather than landing. 延迟能够使你为起落架的展开部署配置时间（自动触发）。</td></tr><tr><td><a id="RTL_MIN_DIST"></a><a href="./../advanced_config/parameter_reference.html#RTL_MIN_DIST">RTL_MIN_DIST</a></td><td>能够触发无人机上升到返航高度，距离起始位置的最小水平距离由那个&quot;锥形&quot;指定。 If the vehicle is horizontally closer than this distance to home, it will return at its current altitude or <code>RTL_DESCEND_ALT</code> (whichever is higher) instead of first ascending to RTL_RETURN_ALT.</td></tr><tr><td><a id="RTL_CONE_ANG"></a><a href="./../advanced_config/parameter_reference.html#RTL_CONE_ANG">RTL_CONE_ANG</a></td><td>圆锥半角决定无人机的 RTL 返航高度。 数值(度数)：0、25、45、65、80、90。 Note that 0 is &quot;no cone&quot; (always return at <code>RTL_RETURN_ALT</code> or higher), while 90 indicates that the vehicle must return at the current altitude or <code>RTL_DESCEND_ALT</code> (whichever is higher).</td></tr><tr><td><a id="COM_RC_OVERRIDE"></a><a href="./../advanced_config/parameter_reference.html#COM_RC_OVERRIDE">COM_RC_OVERRIDE</a></td><td>Controls whether stick movement on a multicopter (or VTOL in MC mode) causes a mode change to <a href="./../flight_modes_mc/position.html">Position mode</a> (except when vehicle is handling a critical battery failsafe). 可以分别为自动模式和 offboard 模式启用此功能，默认情况下在自动模式下启用此功能。</td></tr><tr><td><a id="COM_RC_STICK_OV"></a><a href="./../advanced_config/parameter_reference.html#COM_RC_STICK_OV">COM_RC_STICK_OV</a></td><td>The amount of stick movement that causes a transition to <a href="./../flight_modes_mc/position.html">Position mode</a> (if <a href="#COM_RC_OVERRIDE">COM_RC_OVERRIDE</a> is enabled).</td></tr><tr><td><a id="RTL_LOITER_RAD"></a><a href="./../advanced_config/parameter_reference.html#RTL_LOITER_RAD">RTL_LOITER_RAD</a></td><td>[Fixed-wing Only] The radius of the loiter circle (at <a href="#RTL_LAND_DELAY">RTL_LAND_DELAY</a>).</td></tr><tr><td><a id="MIS_TKO_LAND_REQ"></a><a href="./../advanced_config/parameter_reference.html#MIS_TKO_LAND_REQ">MIS_TKO_LAND_REQ</a></td><td>Specify whether a mission landing or takeoff pattern is <em>required</em>. Generally fixed-wing vehicles set this to require a landing pattern but VTOL do not.</td></tr></tbody></table>',60)]))}const g=a(l,[["render",s]]);export{T as __pageData,g as default};
