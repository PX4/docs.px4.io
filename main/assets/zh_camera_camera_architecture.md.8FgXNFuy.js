import{_ as a,c as r,a8 as t,o}from"./chunks/framework.BDnHobkS.js";const p=JSON.parse('{"title":"PX4 Camera Architecture/Integration","description":"","frontmatter":{},"headers":[],"relativePath":"zh/camera/camera_architecture.md","filePath":"zh/camera/camera_architecture.md"}'),i={name:"zh/camera/camera_architecture.md"};function c(s,e,n,m,d,l){return o(),r("div",null,e[0]||(e[0]=[t('<h1 id="px4-camera-architecture-integration" tabindex="-1">PX4 Camera Architecture/Integration <a class="header-anchor" href="#px4-camera-architecture-integration" aria-label="Permalink to &quot;PX4 Camera Architecture/Integration&quot;">​</a></h1><p>This topic provides a brief overview of how PX4 camera support is <strong>implemented</strong>.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>See <a href="./../camera/">Camera</a> for information about <em>using</em> cameras.</p></div><h2 id="综述" tabindex="-1">综述 <a class="header-anchor" href="#综述" aria-label="Permalink to &quot;综述&quot;">​</a></h2><p>PX4 integrates with three types of cameras:</p><ul><li><a href="./../camera/mavlink_v2_camera.html">MAVLink cameras</a> that support the <a href="https://mavlink.io/en/services/camera.html" target="_blank" rel="noreferrer">Camera Protocol v2</a> (<strong>RECOMMENDED</strong>).</li><li><a href="./../camera/mavlink_v1_camera.html">Simple MAVLink cameras</a> that support the older <a href="https://mavlink.io/en/services/camera.html" target="_blank" rel="noreferrer">Camera Protocol v1</a>.</li><li><a href="./../camera/fc_connected_camera.html">Cameras attached to flight controller outputs</a>, which are controlled using the <a href="https://mavlink.io/en/services/camera.html" target="_blank" rel="noreferrer">Camera Protocol v1</a>.</li></ul><p>All of these cameras need to respond to MAVLink commands received over MAVLink or found in missions (the specific protocol depends on the camera).</p><p>The broad architecture used is described below.</p><h2 id="mavlink-cameras-camera-protocol-v2" tabindex="-1">MAVLink Cameras (Camera Protocol v2) <a class="header-anchor" href="#mavlink-cameras-camera-protocol-v2" aria-label="Permalink to &quot;MAVLink Cameras (Camera Protocol v2)&quot;">​</a></h2><p>PX4 does not have specific handling for <a href="./../camera/mavlink_v2_camera.html">MAVLink cameras</a> that support the <a href="https://mavlink.io/en/services/camera.html" target="_blank" rel="noreferrer">Camera Protocol v2</a>, other than <a href="#camera-commands-in-missions">re-emitting camera items in missions as commands</a></p><p>Ground stations are expected to communicate with these cameras directly in order to send commands. PX4 must be configured to route MAVLink traffic between the camera and ground stations if needed.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The <code>camera_trigger</code>, <code>camera_capture</code> and <code>camera_feedback</code> modules are not used with this camera.</p></div><h2 id="fc-connected-cameras" tabindex="-1">FC-connected Cameras <a class="header-anchor" href="#fc-connected-cameras" aria-label="Permalink to &quot;FC-connected Cameras&quot;">​</a></h2><p><a href="./../camera/fc_connected_camera.html">Cameras attached to flight controller outputs</a> need PX4 to activate the outputs to trigger the camera, and may need PX4 to detect when a <a href="./../camera/fc_connected_camera.html#camera-capture-configuration">camera capture pin</a> has been triggered by the camera hotshoe (in order to improve the logged camera-capture time).</p><p>This work is handled by three PX4 components: <a href="https://github.com/PX4/PX4-Autopilot/tree/main/src/drivers/camera_trigger" target="_blank" rel="noreferrer"><code>camera_trigger</code> driver</a>, <a href="https://github.com/PX4/PX4-Autopilot/tree/main/src/drivers/camera_capture" target="_blank" rel="noreferrer"><code>camera_capture</code> driver</a>, <a href="./../modules/modules_system.html#camera-feedback"><code>camera-feedback</code> module</a>.</p><p><code>camera_trigger</code> subscribes to the <a href="./../msg_docs/VehicleCommand.html">VehicleCommand</a> topic and monitors for updates to its <a href="./../camera/fc_connected_camera.html#mavlink-command-interface">supported commands</a>. Thes updates occur when either a command is received via MAVLink or when a <a href="#camera-commands-in-missions">camera item is reached in a mission</a>.</p><p>The commands enable and disable triggering, and configure triggering at time and distance intervals. The driver tracks these intervals, and when needed triggers the outputs. The driver publishes a <a href="./../msg_docs/CameraTrigger.html">CameraTrigger</a> topic (with <code>feedback</code> field set to <code>false</code>) that causes a <a href="https://mavlink.io/en/messages/common.html#CAMERA_TRIGGER" target="_blank" rel="noreferrer">CAMERA_TRIGGER</a> MAVLink message to be emitted.</p><p>The <code>camera_capture</code> driver, if enabled, monitors the camera capture pin and on triggering publishes a <a href="./../msg_docs/CameraTrigger.html">CameraTrigger</a> topic (with <code>feedback</code> field set to <code>true</code>) which also causes a <a href="https://mavlink.io/en/messages/common.html#CAMERA_TRIGGER" target="_blank" rel="noreferrer">CAMERA_TRIGGER</a> MAVLink message to be emitted.</p><p>The <code>camera_feedback</code> module monitors for updates to the <a href="./../msg_docs/CameraTrigger.html">CameraTrigger</a> topic, and publishes a <a href="./../msg_docs/CameraCapture.html">CameraCapture</a> topic for <code>CameraTrigger</code> updates from <em>either</em> <code>camera_trigger</code> or <code>camera_capture</code>. The information that is used depends on whether the camera capture pin is enabled and the value is of the <code>CameraTrigger.feedback</code> field. This <code>CameraCapture</code> topic is logged, and can be used to get the time of the capture.</p><h2 id="mavlink-cameras-camera-protocol-v1" tabindex="-1">MAVLink Cameras (Camera Protocol v1) <a class="header-anchor" href="#mavlink-cameras-camera-protocol-v1" aria-label="Permalink to &quot;MAVLink Cameras (Camera Protocol v1)&quot;">​</a></h2><p><a href="./../camera/mavlink_v1_camera.html">MAVLink cameras that support the older Camera Protocol v1</a> are integrated in much the same way as <a href="#fc-connected-cameras">FC-connected cameras</a>.</p><p><code>camera_trigger</code> subscribes to the <a href="./../msg_docs/VehicleCommand.html">VehicleCommand</a> topic and monitors for updates in the <a href="./../camera/fc_connected_camera.html#mavlink-command-interface">commands it supports</a>. This happens when either a command is received via MAVLink or when a <a href="#camera-commands-in-missions">camera item is found in a missions</a>.</p><p>The commands enable and disable triggering, and configure triggering at time and distance intervals. The driver tracks these intervals, but with the &quot;MAVLink backend&quot; does not need to actually trigger any outputs (since the commands are forwarded to the camera). When the camera would trigger the driver publishes a <a href="./../msg_docs/CameraTrigger.html">CameraTrigger</a> topic (with <code>feedback</code> field set to <code>false</code>) that causes a <a href="https://mavlink.io/en/messages/common.html#CAMERA_TRIGGER" target="_blank" rel="noreferrer">CAMERA_TRIGGER</a> MAVLink message to be emitted. The <code>camera_feedback</code> module should then log a corresponding <code>CameraCapture</code> topic.</p><h2 id="camera-commands-in-missions" tabindex="-1">Camera Commands in Missions <a class="header-anchor" href="#camera-commands-in-missions" aria-label="Permalink to &quot;Camera Commands in Missions&quot;">​</a></h2><p>PX4 re-emits camera items found in missions as MAVLink commands for all supported <a href="https://mavlink.io/en/services/camera.html" target="_blank" rel="noreferrer">Camera Protocol v2</a> and <a href="https://mavlink.io/en/services/camera.html" target="_blank" rel="noreferrer">Camera Protocol v1</a> commands. The system id of the emitted commands is the same as the ID of the autopilot. The component id of the commands can vary, but these are usually sent to either <a href="https://mavlink.io/en/messages/common.html#MAV_COMP_ID_CAMERA" target="_blank" rel="noreferrer">MAV_COMP_ID_CAMERA (100)</a> or <a href="https://mavlink.io/en/messages/common.html#MAV_COMP_ID_ALL" target="_blank" rel="noreferrer">MAV_COMP_ID_ALL</a> (see the individual camera documents for what ID is used in each case).</p><p>The commands are emitted irrespective of whether or not there is a connected camera of any type, provided there is a MAVLink channel to emit to.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>More generally PX4 re-emits all mission commands that may be consumed by external MAVLink components, such as gimbals. Commands for waypoints and conditional behaviour are not emitted.</p></div><p>The sections below highlight interesting parts of the codebase</p><h3 id="commands-supported-in-missions" tabindex="-1">Commands supported in missions <a class="header-anchor" href="#commands-supported-in-missions" aria-label="Permalink to &quot;Commands supported in missions&quot;">​</a></h3><p>Commands supported in missions, including camera commands, are shown in these methods:</p><ul><li><a href="https://github.com/PX4/PX4-Autopilot/blob/main/src/modules/navigator/MissionFeasibility/FeasibilityChecker.cpp#L257-L306" target="_blank" rel="noreferrer"><code>bool FeasibilityChecker::checkMissionItemValidity(mission_item_s &amp;mission_item, const int current_index)</code></a></li><li><a href="https://github.com/PX4/PX4-Autopilot/blob/main/src/modules/mavlink/mavlink_mission.cpp#L1672-L1693" target="_blank" rel="noreferrer"><code>format_mavlink_mission_item()</code></a></li></ul><h3 id="flow-for-re-emitting-camera-commands-found-in-missions" tabindex="-1">Flow for re-emitting camera commands found in missions <a class="header-anchor" href="#flow-for-re-emitting-camera-commands-found-in-missions" aria-label="Permalink to &quot;Flow for re-emitting camera commands found in missions&quot;">​</a></h3><ul><li><a href="https://github.com/PX4/PX4-Autopilot/blob/main/src/modules/navigator/mission.cpp#L187-L281" target="_blank" rel="noreferrer"><code>void Mission::setActiveMissionItems()</code></a><ul><li>Mission items are executed when set active.</li><li><code>issue_command(_mission_item)</code> is called at the end of this to send the current non-waypoint command <ul><li><a href="https://github.com/PX4/PX4-Autopilot/blob/main/src/modules/navigator/mission_block.cpp#L543-L562" target="_blank" rel="noreferrer"><code>MissionBlock::issue_command(const mission_item_s &amp;item)</code></a><ul><li>Creates a vehicle command for the mission item then calls <code>publish_vehicle_cmd</code> to publish it (<code>_navigator-&gt;publish_vehicle_cmd(&amp;vcmd);</code>) <ul><li><a href="https://github.com/PX4/PX4-Autopilot/blob/main/src/modules/navigator/navigator_main.cpp#L1358" target="_blank" rel="noreferrer"><code>void Navigator::publish_vehicle_cmd(vehicle_command_s *vcmd)</code></a><ul><li>For some camera commands it sets the component ID to the camera component id (<code>vcmd-&gt;target_component = 100; // MAV_COMP_ID_CAMERA</code>)</li><li>All others just get published to default component ID.</li><li>The <code>VehicleCommand</code> UORB topic is published.</li></ul></li></ul></li></ul></li></ul></li></ul></li></ul><p>The MAVLink streaming code monitors for changes to the <code>VehicleCommand</code> topic and publishes them over MAVLink. The MAVLink command is sent irrespective of whether the camera is a MAVLink camera, or connected to the flight controller.</p><p>The <code>camera_trigger</code> driver, if enabled, also monitors for changes to the <code>VehicleCommand</code>. If it is configured with a backend for a camera connected to the flight controller outputs, it will trigger those outputs appropriately.</p><h2 id="日志" tabindex="-1">日志 <a class="header-anchor" href="#日志" aria-label="Permalink to &quot;日志&quot;">​</a></h2><p><code>CameraCapture</code> topics are logged when there is a <code>CameraTrigger</code> update. The logged topic will depend on whether or not the camera capture pin is enabled.</p><p>Note that camera capture events are not logged when using the <a href="./../camera/mavlink_v2_camera.html">MAVLink cameras that support Camera Protocol v2</a>, because the corresponding trigger events are not generated within PX4.</p><h2 id="see-also" tabindex="-1">See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See Also&quot;">​</a></h2><ul><li>Camera trigger driver: <a href="https://github.com/PX4/PX4-Autopilot/tree/main/src/drivers/camera_trigger" target="_blank" rel="noreferrer">source code</a></li><li>Camera capture driver: <a href="https://github.com/PX4/PX4-Autopilot/tree/main/src/drivers/camera_capture" target="_blank" rel="noreferrer">source code</a></li></ul>',40)]))}const u=a(i,[["render",c]]);export{p as __pageData,u as default};
