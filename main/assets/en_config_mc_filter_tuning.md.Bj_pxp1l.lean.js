import{_ as t,a,b as o,c as r}from"./chunks/actuator_controls_fft_gyro_notch_32.DkA7UTgc.js";import{_ as i,c as n,a8 as s,o as l}from"./chunks/framework.BDnHobkS.js";const b=JSON.parse('{"title":"MC Filter Tuning & Control Latency","description":"","frontmatter":{},"headers":[],"relativePath":"en/config_mc/filter_tuning.md","filePath":"en/config_mc/filter_tuning.md"}'),c={name:"en/config_mc/filter_tuning.md"};function h(d,e,f,p,u,m){return l(),n("div",null,e[0]||(e[0]=[s('<h1 id="mc-filter-tuning-control-latency" tabindex="-1">MC Filter Tuning &amp; Control Latency <a class="header-anchor" href="#mc-filter-tuning-control-latency" aria-label="Permalink to &quot;MC Filter Tuning &amp; Control Latency&quot;">​</a></h1><p>Filters can be used to trade off <a href="#control-latency">control latency</a>, which affects flight performance, and noise filtering, which impacts both flight performance and motor health.</p><p>This topic provides an overview of control latency and PX4 filter tuning.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Before filter tuning you should do a first pass at <a href="./../config_mc/pid_tuning_guide_multicopter_basic.html">Basic MC PID tuning</a>. The vehicle needs to be undertuned (the <strong>P</strong> and <strong>D</strong> gains should be set too low), such that there are no oscillations from the controller that could be interpreted as noise (the default gains might be good enough).</p></div><h2 id="control-latency" tabindex="-1">Control Latency <a class="header-anchor" href="#control-latency" aria-label="Permalink to &quot;Control Latency&quot;">​</a></h2><p>The <em>control latency</em> is the delay from a physical disturbance of the vehicle until the motors react to the change.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Lowering latency allows you to increase the rate <strong>P</strong> gains, which results in better flight performance. Even one millisecond difference in the latency can have a significant impact.</p></div><p>The following factors affect control latency:</p><ul><li>A soft airframe or soft vibration mounting increases latency (they act as a filter).</li><li>Low-pass filters in software and on the sensor chip trade off increased latency for improved noise filtering.</li><li>PX4 software internals: the sensor signals need to be read in the driver and then pass through the controller to the output driver.</li><li>The maximum gyro publication rate (configured with <a href="./../advanced_config/parameter_reference.html#IMU_GYRO_RATEMAX">IMU_GYRO_RATEMAX</a>). A higher rate reduces latency but is computationally intensive/can starve other processes. 4 kHz or higher is only recommended for controllers with STM32H7 processor or newer (2 kHz value is near the limit for less capable processors).</li><li>The IO chip (MAIN pins) adds about 5.4 ms latency compared to using the AUX pins (this does not apply to a <em>Pixracer</em> or <em>Omnibus F4</em>, but does apply to a Pixhawk). To avoid the IO delay attach the motors to the AUX pins instead.</li><li>PWM output signal: enable <a href="./../peripherals/dshot.html">Dshot</a> by preference to reduce latency (or One-Shot if DShot is not supported). The protocol is selected for a group of outputs during <a href="./../config/actuators.html">Actuator Configuration</a>.</li></ul><p>Below we look at the impact of the low pass filters.</p><h2 id="filters" tabindex="-1">Filters <a class="header-anchor" href="#filters" aria-label="Permalink to &quot;Filters&quot;">​</a></h2><p>The filtering pipeline for the controllers in PX4 is described below.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Sampling and filtering is always performed at the full raw sensor rate (commonly 8kHz, depending on the IMU).</p></div><h3 id="on-chip-dlpf-for-the-gyro-sensor" tabindex="-1">On-chip DLPF for the Gyro Sensor <a class="header-anchor" href="#on-chip-dlpf-for-the-gyro-sensor" aria-label="Permalink to &quot;On-chip DLPF for the Gyro Sensor&quot;">​</a></h3><p>This is disabled on all chips where it can be disabled (if not, cutoff frequency is set to the highest level of the chip).</p><h3 id="notch-filters" tabindex="-1">Notch Filters <a class="header-anchor" href="#notch-filters" aria-label="Permalink to &quot;Notch Filters&quot;">​</a></h3><p>Setups that have a significant lower-frequency noise spike (e.g. due to harmonics at the rotor blade pass frequency) can benefit from using the notch filter to clean the signal before it is passed to the low pass filter (these harmonics have a similar detrimental impact on motors as other sources of noise).</p><p>Without the notch filter you&#39;d have to set the low pass filter cutoff much lower (increasing the phase lag) in order to avoid passing this noise to the motors.</p><h4 id="static-notch-filters" tabindex="-1">Static Notch Filters <a class="header-anchor" href="#static-notch-filters" aria-label="Permalink to &quot;Static Notch Filters&quot;">​</a></h4><p>One or two static notch filters on the gyro sensor data that are used to filter out narrow band noise, for example a bending mode of the airframe.</p><p>The static notch filters can be configured using:</p><ul><li>First notch filter: <a href="./../advanced_config/parameter_reference.html#IMU_GYRO_NF0_BW">IMU_GYRO_NF0_BW</a> and <a href="./../advanced_config/parameter_reference.html#IMU_GYRO_NF0_FRQ">IMU_GYRO_NF0_FRQ</a>.</li><li>Second notch filter: <a href="./../advanced_config/parameter_reference.html#IMU_GYRO_NF1_BW">IMU_GYRO_NF1_BW</a> and <a href="./../advanced_config/parameter_reference.html#IMU_GYRO_NF1_FRQ">IMU_GYRO_NF1_FRQ</a>.</li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Only two notch filters are provided. Airframes with more than two frequency noise spikes typically clean the first two spikes with the notch filters, and subsequent spikes using the low pass filter.</p></div><h4 id="dynamic-notch-filters" tabindex="-1">Dynamic Notch Filters <a class="header-anchor" href="#dynamic-notch-filters" aria-label="Permalink to &quot;Dynamic Notch Filters&quot;">​</a></h4><p>Dynamic notch filters use ESC RPM feedback and/or the onboard FFT analysis. The ESC RPM feedback is used to track the rotor blade pass frequency and its harmonics, while the FFT analysis can be used to track a frequency of another vibration source, such a fuel engine.</p><p>ESC RPM feedback requires ESCs capable of providing RPM feedback such as <a href="./../peripherals/esc_motors.html#dshot">DShot</a> with telemetry connected, a bidirectional DShot set up (<a href="https://github.com/PX4/PX4-Autopilot/pull/23863" target="_blank" rel="noreferrer">work in progress</a>), or <a href="./../dronecan/escs.html">UAVCAN/DroneCAN ESCs</a>. Before enabling, make sure that the ESC RPM is correct. You might have to adjust the <a href="./../advanced_config/parameter_reference.html#MOT_POLE_COUNT">pole count of the motors</a>.</p><p>The following parameters should be set to enable and configure dynamic notch filters:</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><a href="IMU_GYRO_DNF_EN"></a><a href="./../advanced_config/parameter_reference.html#IMU_GYRO_DNF_EN">IMU_GYRO_DNF_EN</a></td><td>Enable IMU gyro dynamic notch filtering. <code>0</code>: ESK RPM, <code>1</code>: Onboard FFT.</td></tr><tr><td><a href="IMU_GYRO_FFT_EN"></a><a href="./../advanced_config/parameter_reference.html#IMU_GYRO_FFT_EN">IMU_GYRO_FFT_EN</a></td><td>Enable onboard FFT (required if <code>IMU_GYRO_DNF_EN</code> is set to <code>1</code>).</td></tr><tr><td><a href="IMU_GYRO_DNF_MIN"></a><a href="./../advanced_config/parameter_reference.html#IMU_GYRO_DNF_MIN">IMU_GYRO_DNF_MIN</a></td><td>Minimum dynamic notch frequency in Hz.</td></tr><tr><td><a href="IMU_GYRO_DNF_BW"></a><a href="./../advanced_config/parameter_reference.html#IMU_GYRO_DNF_BW">IMU_GYRO_DNF_BW</a></td><td>Bandwidth for each notch filter in Hz.</td></tr><tr><td><a href="IMU_GYRO_DNF_HMC"></a><a href="./../advanced_config/parameter_reference.html#IMU_GYRO_NF0_BW">IMU_GYRO_DNF_HMC</a></td><td>Number of harmonics to filter.</td></tr></tbody></table><h3 id="low-pass-filter" tabindex="-1">Low-pass Filter <a class="header-anchor" href="#low-pass-filter" aria-label="Permalink to &quot;Low-pass Filter&quot;">​</a></h3><p>A low pass filter on the gyro data can be configured with the <a href="./../advanced_config/parameter_reference.html#IMU_GYRO_CUTOFF">IMU_GYRO_CUTOFF</a> parameter.</p><p>To reduce the control latency, we want to increase the cutoff frequency for the low-pass filters. The effect on latency of increasing <code>IMU_GYRO_CUTOFF</code> is approximated below.</p><table tabindex="0"><thead><tr><th>Cuttoff (Hz)</th><th>Delay approx. (ms)</th></tr></thead><tbody><tr><td>30</td><td>8</td></tr><tr><td>60</td><td>3.8</td></tr><tr><td>120</td><td>1.9</td></tr></tbody></table><p>However this is a trade-off as increasing <code>IMU_GYRO_CUTOFF</code> will also increase the noise of the signal that is fed to the motors. Noise on the motors has the following consequences:</p><ul><li>Motors and ESCs can get hot, to the point where they get damaged.</li><li>Reduced flight time because the motors continuously change their speed.</li><li>Visible random small twitches.</li></ul><h3 id="low-pass-filter-on-d-term" tabindex="-1">Low-pass Filter on D-term <a class="header-anchor" href="#low-pass-filter-on-d-term" aria-label="Permalink to &quot;Low-pass Filter on D-term&quot;">​</a></h3><p>The D-term is most susceptible to noise while slightly increased latency does not negatively affect performance. For this reason the D-term has a separately-configurable low-pass filter, <a href="./../advanced_config/parameter_reference.html#IMU_DGYRO_CUTOFF">IMU_DGYRO_CUTOFF</a>.</p><h3 id="slew-rate-filter-on-motor-outputs" tabindex="-1">Slew-rate Filter on Motor Outputs <a class="header-anchor" href="#slew-rate-filter-on-motor-outputs" aria-label="Permalink to &quot;Slew-rate Filter on Motor Outputs&quot;">​</a></h3><p>An optional slew-rate filter on the motor outputs. This rate may be configured as part of the <a href="./../config/actuators.html#motor-geometry-multicopter">Multicopter Geometry</a> when configuring actuators (which in turn modifies the <a href="./../advanced_config/parameter_reference.html#CA_R0_SLEW">CA_Rn_SLEW</a> parameters for each motor <code>n</code>).</p><h2 id="filter-tuning" tabindex="-1">Filter Tuning <a class="header-anchor" href="#filter-tuning" aria-label="Permalink to &quot;Filter Tuning&quot;">​</a></h2><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The best filter settings depend on the vehicle. The defaults are set conservatively — such that they work on lower-quality setups as well.</p></div><p>First make sure to have the high-rate logging profile activated (<a href="./../advanced_config/parameter_reference.html#SDLOG_PROFILE">SDLOG_PROFILE</a> parameter). <a href="./../getting_started/flight_reporting.html">Flight Review</a> will then show an FFT plot for the roll, pitch and yaw controls.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><ul><li>Do not try to fix a vehicle that suffers from high vibrations with filter tuning! Instead fix the vehicle hardware setup.</li><li>Confirm that PID gains, in particular D, are not set too high as this can show up as vibrations.</li></ul></div><p>Filter tuning is best done by reviewing flight logs. You can do multiple flights right after each other with different parameters and then inspect all logs, but make sure to disarm in between so that separate log files are created.</p><p>The performed flight manoeuvre can simply be hovering in <a href="./../flight_modes_mc/manual_stabilized.html">Stabilized mode</a> with some rolling and pitching to all directions and some increased throttle periods. The total duration does not need to be more than 30 seconds. In order to better compare, the manoeuvre should be similar in all tests.</p><p>First tune the gyro filter <a href="./../advanced_config/parameter_reference.html#IMU_GYRO_CUTOFF">IMU_GYRO_CUTOFF</a> by increasing it in steps of 10 Hz while using a low D-term filter value (<a href="./../advanced_config/parameter_reference.html#IMU_DGYRO_CUTOFF">IMU_DGYRO_CUTOFF</a> = 30). Upload the logs to <a href="https://logs.px4.io" target="_blank" rel="noreferrer">Flight Review</a> and compare the <em>Actuator Controls FFT</em> plot. Set the cutoff frequency to a value before the noise starts to increase noticeably (for frequencies around and above 60 Hz).</p><p>Then tune the D-term filter (<code>IMU_DGYRO_CUTOFF</code>) in the same way. Note that there can be negative impacts on performance if <code>IMU_GYRO_CUTOFF</code> and <code>IMU_DGYRO_CUTOFF</code> are set too far apart (the differences have to be significant though - e.g. D=15, gyro=80).</p><p>Below is an example for three different <code>IMU_DGYRO_CUTOFF</code> filter values (40Hz, 70Hz, 90Hz). At 90 Hz the general noise level starts to increase (especially for roll), and thus a cutoff frequency of 70 Hz is a safe setting. <img src="'+t+'" alt="IMU_DGYRO_CUTOFF=40"><img src="'+a+'" alt="IMU_DGYRO_CUTOFF=70"><img src="'+o+'" alt="IMU_DGYRO_CUTOFF=90"></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The plot cannot be compared between different vehicles, as the y axis scale can be different. On the same vehicle it is consistent and independent of the flight duration.</p></div><p>If the flight plots shows significant low frequency spikes, like the one shown in the diagram below, you can remove it using a notch filter. In this case you might use the settings: <a href="./../advanced_config/parameter_reference.html#IMU_GYRO_NF0_FRQ">IMU_GYRO_NF0_FRQ=32</a> and <a href="./../advanced_config/parameter_reference.html#IMU_GYRO_NF0_BW">IMU_GYRO_NF0_BW=5</a> (note, this spike is narrower than usual). The low pass filters and the notch filter can be tuned independently (i.e. you don&#39;t need to set the notch filter before collecting the data for tuning the low pass filter).</p><p><img src="'+r+'" alt="IMU_GYRO_NF0_FRQ=32 IMU_GYRO_NF0_BW=5"></p><h2 id="additional-tips" tabindex="-1">Additional Tips <a class="header-anchor" href="#additional-tips" aria-label="Permalink to &quot;Additional Tips&quot;">​</a></h2><ol><li><p>Acceptable latency depends on vehicle size and expectations. FPV racers typically tune for the absolute minimal latency (as a ballpark <code>IMU_GYRO_CUTOFF</code> around 120, <code>IMU_DGYRO_CUTOFF</code> of 50 to 80). For bigger vehicles latency is less critical and <code>IMU_GYRO_CUTOFF</code> of around 80 might be acceptable.</p></li><li><p>You can start tuning at higher <code>IMU_GYRO_CUTOFF</code> values (e.g. 100Hz), which might be desirable because the default tuning of <code>IMU_GYRO_CUTOFF</code> is set very low (30Hz). The only caveat is that you must be aware of the risks:</p><ul><li>Don&#39;t fly for more than 20-30 seconds</li><li>Check that the motors are not getting to hot</li><li>Listen for odd sounds and symptoms of excessive noise, as discussed above.</li></ul></li></ol>',52)]))}const F=i(c,[["render",h]]);export{b as __pageData,F as default};
