import{_ as a,a as s}from"./chunks/PX4_High-Level_Flight-Stack.B3u3dIZd.js";import{_ as t,c as i,a8 as n,o}from"./chunks/framework.BDnHobkS.js";const g=JSON.parse('{"title":"PX4 아키텍처 개요","description":"","frontmatter":{},"headers":[],"relativePath":"ko/concept/architecture.md","filePath":"ko/concept/architecture.md"}'),r={name:"ko/concept/architecture.md"};function l(h,e,d,p,c,u){return o(),i("div",null,e[0]||(e[0]=[n('<h1 id="px4-아키텍처-개요" tabindex="-1">PX4 아키텍처 개요 <a class="header-anchor" href="#px4-아키텍처-개요" aria-label="Permalink to &quot;PX4 아키텍처 개요&quot;">​</a></h1><p>PX4 consists of two main layers: the <a href="#flight-stack">flight stack</a> is an estimation and flight control system, and the <a href="#middleware">middleware</a> is a general robotics layer that can support any type of autonomous robot, providing internal/external communications and hardware integration.</p><p>All PX4 <a href="./../airframes/">airframes</a> share a single codebase (this includes other robotic systems like boats, rovers, submarines etc.). The complete system design is <a href="http://www.reactivemanifesto.org" target="_blank" rel="noreferrer">reactive</a>, which means that:</p><ul><li>모든 기능은 대체 가능한 구성 요소와 재사용 가능한 구성 요소로 나누어 집니다.</li><li>통신은 비동기 메시지 전달에 의해 수행됩니다.</li><li>시스템은 다양한 작업 부하를 처리할 수 있습니다.</li></ul><p><a id="architecture"></a></p><h2 id="고급-소프트웨어-아키텍처" tabindex="-1">고급 소프트웨어 아키텍처 <a class="header-anchor" href="#고급-소프트웨어-아키텍처" aria-label="Permalink to &quot;고급 소프트웨어 아키텍처&quot;">​</a></h2><p>아래 다이어그램은 PX4의 빌딩 블록에 대한 개요를 나타냅니다. 다이어그램의 상단에는 미들웨어 블록이 포함되어 있고, 하단에는 플라이트 스택의 구성 요소가 표시되어 있습니다.</p><p><img src="'+a+'" alt="PX4 Architecture"></p><p>The source code is split into self-contained modules/programs (shown in <code>monospace</code> in the diagram). 일반적으로 빌딩 블록은 하나의 모듈을 나타냅니다.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>At runtime, you can inspect which modules are executed with the <code>top</code> command in shell, and each module can be started/stopped individually via <code>&lt;module_name&gt; start/stop</code>. While <code>top</code> command is specific to NuttX shell, the other commands can be used in the SITL shell (pxh&gt;) as well. For more information about each of these modules see the <a href="./../modules/modules_main.html">Modules &amp; Commands Reference</a>.</p></div><p>The arrows show the information flow for the <em>most important</em> connections between the modules. 표시된 것보다 더 많은 연결이 있으며, 일부 데이터(예: 매개변수)는 대부분의 모듈에서 액세스합니다.</p><p>Modules communicate with each other through a publish-subscribe message bus named <a href="./../middleware/uorb.html">uORB</a>. 발행-구독 체계의 사용은 다음을 의미합니다.</p><ul><li>시스템은 반응형입니다. 비동기식이며, 새 데이터를 사용할 수 있을 때 즉시 업데이트합니다.</li><li>모든 작업과 통신은 완전히 병렬화됩니다.</li><li>시스템 구성 요소는 스레드로부터 안전하며, 어디서나 데이터를 사용할 수 있습니다.</li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>This architecture allows every single one of these blocks to be rapidly and easily replaced, even at runtime.</p></div><h3 id="비행-스택" tabindex="-1">비행 스택 <a class="header-anchor" href="#비행-스택" aria-label="Permalink to &quot;비행 스택&quot;">​</a></h3><p>비행 스택은 자율 드론을 위한 안내, 탐색 및 제어 알고리즘의 모음입니다. It includes controllers for fixed-wing, multirotor and VTOL airframes as well as estimators for attitude and position.</p><p>다음 다이어그램은 비행 스택의 빌딩 블록에 대한 개요를 나타냅니다. 여기에는 센서, RC 입력 및 자율 비행 제어(내비게이터)에서 모터 또는 서보 제어(액추에이터)에 이르는 전체 파이프라인이 포함됩니다.</p><p><img src="'+s+`" alt="PX4 High-Level Flight Stack"></p><p>An <strong>estimator</strong> takes one or more sensor inputs, combines them, and computes a vehicle state (for example the attitude from IMU sensor data).</p><p>A <strong>controller</strong> is a component that takes a setpoint and a measurement or estimated state (process variable) as input. Its goal is to adjust the value of the process variable such that it matches the setpoint. The output is a correction to eventually reach that setpoint. For example the position controller takes position setpoints as inputs, the process variable is the currently estimated position, and the output is an attitude and thrust setpoint that move the vehicle towards the desired position.</p><p>A <strong>mixer</strong> takes force commands (such as &quot;turn right&quot;) and translates them into individual motor commands, while ensuring that some limits are not exceeded. This translation is specific for a vehicle type and depends on various factors, such as the motor arrangements with respect to the center of gravity, or the vehicle&#39;s rotational inertia.</p><p><a id="middleware"></a></p><h3 id="미들웨어" tabindex="-1">미들웨어 <a class="header-anchor" href="#미들웨어" aria-label="Permalink to &quot;미들웨어&quot;">​</a></h3><p>The <a href="./../middleware/">middleware</a> consists primarily of device drivers for embedded sensors, communication with the external world (companion computer, GCS, etc.) and the uORB publish-subscribe message bus.</p><p>In addition, the middleware includes a <a href="./../simulation/">simulation layer</a> that allows PX4 flight code to run on a desktop operating system and control a computer modeled vehicle in a simulated &quot;world&quot;.</p><h2 id="속도-업데이트" tabindex="-1">속도 업데이트 <a class="header-anchor" href="#속도-업데이트" aria-label="Permalink to &quot;속도 업데이트&quot;">​</a></h2><p>Since the modules wait for message updates, typically the drivers define how fast a module updates. Most of the IMU drivers sample the data at 1kHz, integrate it and publish with 250Hz. Other parts of the system, such as the <code>navigator</code>, don&#39;t need such a high update rate, and thus run considerably slower.</p><p>The message update rates can be <a href="./../middleware/uorb.html">inspected</a> in real-time on the system by running <code>uorb top</code>.</p><p><a id="runtime-environment"></a></p><h2 id="실행-환경" tabindex="-1">실행 환경 <a class="header-anchor" href="#실행-환경" aria-label="Permalink to &quot;실행 환경&quot;">​</a></h2><p>PX4 runs on various operating systems that provide a POSIX-API (such as Linux, macOS, NuttX or QuRT). It should also have some form of real-time scheduling (e.g. FIFO).</p><p>The inter-module communication (using <a href="./../middleware/uorb.html">uORB</a>) is based on shared memory. The whole PX4 middleware runs in a single address space, i.e. memory is shared between all modules.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The system is designed such that with minimal effort it would be possible to run each module in separate address space (parts that would need to be changed include <code>uORB</code>, <code>parameter interface</code>, <code>dataman</code> and <code>perf</code>).</p></div><p>There are 2 different ways that a module can be executed:</p><ul><li><p><strong>Tasks</strong>: The module runs in its own task with its own stack and process priority.</p></li><li><p><strong>Work queue tasks</strong>: The module runs on a shared work queue, sharing the same stack and work queue thread priority as other modules on the queue.</p><ul><li>모든 작업은 서로 방해할 수 없으므로, 협력적으로 작동해야 합니다.</li><li>Multiple <em>work queue tasks</em> can run on a queue, and there can be multiple queues.</li><li>A <em>work queue task</em> is scheduled by specifying a fixed time in the future, or via uORB topic update callback.</li></ul><p>작업 대기열에서 모듈을 실행할 때의 이점은 RAM을 덜 사용하고 잠재적으로 작업 전환이 더 적은 것입니다. The disadvantages are that <em>work queue tasks</em> are not allowed to sleep or poll on a message, or do blocking IO (such as reading from a file). 장기 실행 작업(과중한 계산 수행)은 잠재적으로 별도의 작업 또는 최소한 별도의 작업 대기열에서 실행되어야 합니다.</p></li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Tasks running on a work queue do not show up in <a href="./../modules/modules_command.html#top"><code>top</code></a> (only the work queues themselves can be seen - e.g. as <code>wq:lp_default</code>). Use <a href="./../modules/modules_system.html#work-queue"><code>work_queue status</code></a> to display all active work queue items.</p></div><h3 id="백그라운드-작업" tabindex="-1">백그라운드 작업 <a class="header-anchor" href="#백그라운드-작업" aria-label="Permalink to &quot;백그라운드 작업&quot;">​</a></h3><p><code>px4_task_spawn_cmd()</code> is used to launch new tasks (NuttX) or threads (POSIX - Linux/macOS) that run independently from the calling (parent) task:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">independent_task </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> px4_task_spawn_cmd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;commander&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    // Process name</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SCHED_DEFAULT,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                  // Scheduling type (RR or FIFO)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SCHED_PRIORITY_DEFAULT </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 40</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Scheduling priority</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    3600</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                           // Stack size of the new task or thread</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    commander_thread_main,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // Task (or thread) main function</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">argv[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // Void pointer to pass to the new task</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                    // (here the commandline arguments).</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    );</span></span></code></pre></div><h3 id="운영체제별-정보" tabindex="-1">운영체제별 정보 <a class="header-anchor" href="#운영체제별-정보" aria-label="Permalink to &quot;운영체제별 정보&quot;">​</a></h3><h4 id="nuttx" tabindex="-1">NuttX <a class="header-anchor" href="#nuttx" aria-label="Permalink to &quot;NuttX&quot;">​</a></h4><p><a href="https://nuttx.apache.org//" target="_blank" rel="noreferrer">NuttX</a> is the primary RTOS for running PX4 on a flight-control board. It is open source (BSD license), light-weight, efficient and very stable.</p><p>Modules are executed as tasks: they have their own file descriptor lists, but they share a single address space. A task can still start one or more threads that share the file descriptor list.</p><p>Each task/thread has a fixed-size stack, and there is a periodic task which checks that all stacks have enough free space left (based on stack coloring).</p><h4 id="linux-macos" tabindex="-1">Linux/macOS <a class="header-anchor" href="#linux-macos" aria-label="Permalink to &quot;Linux/macOS&quot;">​</a></h4><p>On Linux or macOS, PX4 runs in a single process, and the modules run in their own threads (there is no distinction between tasks and threads as on NuttX).</p>`,46)]))}const f=t(r,[["render",l]]);export{g as __pageData,f as default};
