import{_ as t,c as a,a8 as i,o as s}from"./chunks/framework.BDnHobkS.js";const u=JSON.parse('{"title":"System Startup","description":"","frontmatter":{},"headers":[],"relativePath":"en/concept/system_startup.md","filePath":"en/concept/system_startup.md"}'),o={name:"en/concept/system_startup.md"};function n(l,e,r,c,h,d){return s(),a("div",null,e[0]||(e[0]=[i(`<h1 id="system-startup" tabindex="-1">System Startup <a class="header-anchor" href="#system-startup" aria-label="Permalink to &quot;System Startup&quot;">​</a></h1><p>The PX4 startup is controlled by shell scripts. On NuttX they reside in the <a href="https://github.com/PX4/PX4-Autopilot/tree/main/ROMFS/px4fmu_common/init.d" target="_blank" rel="noreferrer">ROMFS/px4fmu_common/init.d</a> folder - some of these are also used on Posix (Linux/MacOS). The scripts that are only used on Posix are located in <a href="https://github.com/PX4/PX4-Autopilot/tree/main/ROMFS/px4fmu_common/init.d-posix" target="_blank" rel="noreferrer">ROMFS/px4fmu_common/init.d-posix</a>.</p><p>All files starting with a number and underscore (e.g. <code>10000_airplane</code>) are predefined airframe configurations. They are exported at build-time into an <code>airframes.xml</code> file which is parsed by <a href="http://qgroundcontrol.com" target="_blank" rel="noreferrer">QGroundControl</a> for the airframe selection UI. Adding a new configuration is covered <a href="./../dev_airframes/adding_a_new_frame.html">here</a>.</p><p>The remaining files are part of the general startup logic. The first executed file is the <a href="https://github.com/PX4/PX4-Autopilot/blob/main/ROMFS/px4fmu_common/init.d/rcS" target="_blank" rel="noreferrer">init.d/rcS</a> script (or <a href="https://github.com/PX4/PX4-Autopilot/blob/main/ROMFS/px4fmu_common/init.d-posix/rcS" target="_blank" rel="noreferrer">init.d-posix/rcS</a> on Posix), which calls all other scripts.</p><p>The following sections are split according to the operating system that PX4 runs on.</p><h2 id="posix-linux-macos" tabindex="-1">Posix (Linux/MacOS) <a class="header-anchor" href="#posix-linux-macos" aria-label="Permalink to &quot;Posix (Linux/MacOS)&quot;">​</a></h2><p>On Posix, the system shell is used as script interpreter (e.g. /bin/sh, being symlinked to dash on Ubuntu). For that to work, a few things are required:</p><ul><li><p>PX4 modules need to look like individual executables to the system. This is done via symbolic links. For each module a symbolic link <code>px4-&lt;module&gt; -&gt; px4</code> is created in the <code>bin</code> directory of the build folder. When executed, the binary path is checked (<code>argv[0]</code>), and if it is a module (starts with <code>px4-</code>), it sends the command to the main px4 instance (see below).</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>The <code>px4-</code> prefix is used to avoid conflicts with system commands (e.g. <code>shutdown</code>), and it also allows for simple tab completion by typing <code>px4-&lt;TAB&gt;</code>.</p></div></li><li><p>The shell needs to know where to find the symbolic links. For that the <code>bin</code> directory with the symbolic links is added to the <code>PATH</code> variable right before executing the startup scripts.</p></li><li><p>The shell starts each module as a new (client) process. Each client process needs to communicate with the main instance of px4 (the server), where the actual modules are running as threads. This is done through a <a href="http://man7.org/linux/man-pages/man7/unix.7.html" target="_blank" rel="noreferrer">UNIX socket</a>. The server listens on a socket, to which clients can connect and send a command. The server then sends the output and return code back to the client.</p></li><li><p>The startup scripts call the module directly, e.g. <code>commander start</code>, rather than using the <code>px4-</code> prefix. This works via aliases: for each module an alias in the form of <code>alias &lt;module&gt;=px4-&lt;module&gt;</code> is created in the file <code>bin/px4-alias.sh</code>.</p></li><li><p>The <code>rcS</code> script is executed from the main px4 instance. It does not start any modules, but first updates the <code>PATH</code> variable and then simply runs a shell with the <code>rcS</code> file as argument.</p></li><li><p>In addition to that, multiple server instances can be started for multi-vehicle simulations. A client selects the instance via <code>--instance</code>. The instance is available in the script via <code>$px4_instance</code> variable.</p></li></ul><p>The modules can be executed from any terminal when PX4 is already running on a system. For example:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">PX4-Autopilo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/build/px4_sitl_default/bin</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./px4-commander</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> takeoff</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./px4-listener</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sensor_accel</span></span></code></pre></div><h3 id="dynamic-modules" tabindex="-1">Dynamic Modules <a class="header-anchor" href="#dynamic-modules" aria-label="Permalink to &quot;Dynamic Modules&quot;">​</a></h3><p>Normally, all modules are compiled into a single PX4 executable. However, on Posix, there&#39;s the option of compiling a module into a separate file, which can be loaded into PX4 using the <code>dyn</code> command.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dyn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./test.px4mod</span></span></code></pre></div><h2 id="nuttx" tabindex="-1">NuttX <a class="header-anchor" href="#nuttx" aria-label="Permalink to &quot;NuttX&quot;">​</a></h2><p>NuttX has an integrated shell interpreter (<a href="https://cwiki.apache.org/confluence/pages/viewpage.action?pageId=139629410" target="_blank" rel="noreferrer">NuttShell (NSH)</a>), and thus scripts can be executed directly.</p><h3 id="debugging-the-system-boot" tabindex="-1">Debugging the System Boot <a class="header-anchor" href="#debugging-the-system-boot" aria-label="Permalink to &quot;Debugging the System Boot&quot;">​</a></h3><p>A failure of a driver of software component will not lead to an aborted boot. This is controlled via <code>set +e</code> in the startup script.</p><p>The boot sequence can be debugged by connecting the <a href="./../debug/system_console.html">system console</a> and power-cycling the board. The resulting boot log has detailed information about the boot sequence and should contain hints why the boot aborted.</p><h4 id="common-boot-failure-causes" tabindex="-1">Common boot failure causes <a class="header-anchor" href="#common-boot-failure-causes" aria-label="Permalink to &quot;Common boot failure causes&quot;">​</a></h4><ul><li>For custom applications: The system was out of RAM. Run the <code>free</code> command to see the amount of free RAM.</li><li>A software fault or assertion resulting in a stack trace</li></ul><h3 id="replacing-the-system-startup" tabindex="-1">Replacing the System Startup <a class="header-anchor" href="#replacing-the-system-startup" aria-label="Permalink to &quot;Replacing the System Startup&quot;">​</a></h3><p>The whole boot can be replaced by creating a file <code>/etc/rc.txt</code> on the microSD card with a new configuration (nothing in the old configuration will be auto-started, and if the file is empty, nothing at all will be started).</p><p>Customizing the default boot is almost always a better approach. This is documented below.</p><h3 id="customizing-the-system-startup" tabindex="-1">Customizing the System Startup <a class="header-anchor" href="#customizing-the-system-startup" aria-label="Permalink to &quot;Customizing the System Startup&quot;">​</a></h3><p>The best way to customize the system startup is to introduce a <a href="./../dev_airframes/adding_a_new_frame.html">new frame configuration</a>. The frame configuration file can be included in the firmware or on an SD Card.</p><p>If you only need to &quot;tweak&quot; the existing configuration, such as starting one more application or setting the value of a few parameters, you can specify these by creating two files in the <code>/etc/</code> directory of the SD Card:</p><ul><li><a href="#customizing-the-configuration-config-txt">/etc/config.txt</a>: modify parameter values</li><li><a href="#starting-additional-applications-extras-txt">/etc/extras.txt</a>: start applications</li></ul><p>The files are described below.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>The system boot files are UNIX FILES which require UNIX LINE ENDINGS. If editing on Windows use a suitable editor.</p></div><div class="info custom-block"><p class="custom-block-title">INFO</p><p>These files are referenced in PX4 code as <code>/fs/microsd/etc/config.txt</code> and <code>/fs/microsd/etc/extras.txt</code>, where the root folder of the microsd card is identified by the path <code>/fs/microsd</code>.</p></div><h4 id="customizing-the-configuration-config-txt" tabindex="-1">Customizing the Configuration (config.txt) <a class="header-anchor" href="#customizing-the-configuration-config-txt" aria-label="Permalink to &quot;Customizing the Configuration (config.txt)&quot;">​</a></h4><p>The <code>config.txt</code> file can be used to modify parameters. It is loaded after the main system has been configured and <em>before</em> it is booted.</p><p>For example, you could create a file on the SD card, <code>etc/config.txt</code> with that sets parameter values as shown:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set-default</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> PWM_MAIN_DIS3</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1000</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set-default</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> PWM_MAIN_MIN3</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1120</span></span></code></pre></div><h4 id="starting-additional-applications-extras-txt" tabindex="-1">Starting Additional Applications (extras.txt) <a class="header-anchor" href="#starting-additional-applications-extras-txt" aria-label="Permalink to &quot;Starting Additional Applications (extras.txt)&quot;">​</a></h4><p>The <code>extras.txt</code> can be used to start additional applications after the main system boot. Typically these would be payload controllers or similar optional custom components.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Calling an unknown command in system boot files may result in boot failure. Typically the system does not stream mavlink messages after boot failure, in this case check the error messages that are printed on the system console.</p></div><p>The following example shows how to start custom applications:</p><ul><li><p>Create a file on the SD card <code>etc/extras.txt</code> with this content:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">custom_app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span></span></code></pre></div></li><li><p>A command can be made optional by gating it with the <code>set +e</code> and <code>set -e</code> commands:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> +e</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">optional_app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # Will not result in boot failure if optional_app is unknown or fails</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">set</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mandatory_app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # Will abort boot if mandatory_app is unknown or fails</span></span></code></pre></div></li></ul>`,39)]))}const m=t(o,[["render",n]]);export{u as __pageData,m as default};
