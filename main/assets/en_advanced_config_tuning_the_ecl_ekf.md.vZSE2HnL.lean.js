import{_ as t,a,b as i,c as o,d as n,e as r,f as s}from"./chunks/gnd_effect.BxqYXZzq.js";import{_ as l,c as d,a8 as c,o as h}from"./chunks/framework.BDnHobkS.js";const y=JSON.parse('{"title":"Using the ECL EKF","description":"","frontmatter":{},"headers":[],"relativePath":"en/advanced_config/tuning_the_ecl_ekf.md","filePath":"en/advanced_config/tuning_the_ecl_ekf.md"}'),m={name:"en/advanced_config/tuning_the_ecl_ekf.md"};function f(p,e,u,g,_,b){return h(),d("div",null,e[0]||(e[0]=[c(`<h1 id="using-the-ecl-ekf" tabindex="-1">Using the ECL EKF <a class="header-anchor" href="#using-the-ecl-ekf" aria-label="Permalink to &quot;Using the ECL EKF&quot;">​</a></h1><p>This tutorial answers common questions about use of the ECL EKF algorithm.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>The <a href="https://youtu.be/HkYRJJoyBwQ" target="_blank" rel="noreferrer">PX4 State Estimation Overview</a> video from the <em>PX4 Developer Summit 2019</em> (Dr. Paul Riseborough) provides an overview of the estimator, and additionally describes both the major changes from 2018/2019, and the expected improvements through 2020.</p></div><h2 id="what-is-the-ecl-ekf" tabindex="-1">What is the ECL EKF? <a class="header-anchor" href="#what-is-the-ecl-ekf" aria-label="Permalink to &quot;What is the ECL EKF?&quot;">​</a></h2><p>The Estimation and Control Library (ECL) uses an Extended Kalman Filter (EKF) algorithm to process sensor measurements and provide an estimate of the following states:</p><ul><li>Quaternion defining the rotation from North, East, Down local earth frame to X, Y, Z body frame</li><li>Velocity at the IMU - North, East, Down (m/s)</li><li>Position at the IMU - North, East, Down (m)</li><li>IMU delta angle bias estimates - X, Y, Z (rad)</li><li>IMU delta velocity bias estimates - X, Y, Z (m/s)</li><li>Earth Magnetic field components - North, East, Down (gauss)</li><li>Vehicle body frame magnetic field bias - X, Y, Z (gauss)</li><li>Wind velocity - North, East (m/s)</li></ul><p>The EKF runs on a delayed &#39;fusion time horizon&#39; to allow for different time delays on each measurement relative to the IMU. Data for each sensor is FIFO buffered and retrieved from the buffer by the EKF to be used at the correct time. The delay compensation for each sensor is controlled by the <a href="./../advanced_config/parameter_reference.html#ekf2">EKF2_*_DELAY</a> parameters.</p><p>A complementary filter is used to propagate the states forward from the &#39;fusion time horizon&#39; to current time using the buffered IMU data. The time constant for this filter is controlled by the <a href="./../advanced_config/parameter_reference.html#EKF2_TAU_VEL">EKF2_TAU_VEL</a> and <a href="./../advanced_config/parameter_reference.html#EKF2_TAU_POS">EKF2_TAU_POS</a> parameters.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The &#39;fusion time horizon&#39; delay and length of the buffers is determined by the largest of the <code>EKF2_*_DELAY</code> parameters. If a sensor is not being used, it is recommended to set its time delay to zero. Reducing the &#39;fusion time horizon&#39; delay reduces errors in the complementary filter used to propagate states forward to current time.</p></div><p>The position and velocity states are adjusted to account for the offset between the IMU and the body frame before they are output to the control loops. The position of the IMU relative to the body frame is set by the <code>EKF2_IMU_POS_X,Y,Z</code> parameters.</p><p>The EKF uses the IMU data for state prediction only. IMU data is not used as an observation in the EKF derivation. The algebraic equations for the covariance prediction, state update and covariance update were derived using the Matlab symbolic toolbox and can be found here: <a href="https://github.com/PX4/PX4-ECL/blob/master/EKF/matlab/scripts/Terrain%20Estimator/GenerateEquationsTerrainEstimator.m" target="_blank" rel="noreferrer">Matlab Symbolic Derivation</a>.</p><h2 id="running-a-single-ekf-instance" tabindex="-1">Running a Single EKF Instance <a class="header-anchor" href="#running-a-single-ekf-instance" aria-label="Permalink to &quot;Running a Single EKF Instance&quot;">​</a></h2><p>The <em>default behaviour</em> is to run a single instance of the EKF. In this case sensor selection and failover is performed before data is received by the EKF. This provides protection against a limited number of sensor faults, such as loss of data, but does not protect against the sensor providing inaccurate data that exceeds the ability of the EKF and control loops to compensate.</p><p>The parameter settings for running a single EKF instance are:</p><ul><li><a href="./../advanced_config/parameter_reference.html#EKF2_MULTI_IMU">EKF2_MULTI_IMU</a> = 0</li><li><a href="./../advanced_config/parameter_reference.html#EKF2_MULTI_MAG">EKF2_MULTI_MAG</a> = 0</li><li><a href="./../advanced_config/parameter_reference.html#SENS_IMU_MODE">SENS_IMU_MODE</a> = 1</li><li><a href="./../advanced_config/parameter_reference.html#SENS_MAG_MODE">SENS_MAG_MODE</a> = 1</li></ul><h2 id="running-multiple-ekf-instances" tabindex="-1">Running Multiple EKF Instances <a class="header-anchor" href="#running-multiple-ekf-instances" aria-label="Permalink to &quot;Running Multiple EKF Instances&quot;">​</a></h2><p>Depending on the number of IMUs and magnetometers and the autopilot&#39;s CPU capacity, multiple instances of the EKF can be run. This provides protection against a wider range of sensor errors and is achieved by each EKF instance using a different sensor combination. By comparing the internal consistency of each EKF instance, the EKF selector is able to determine the EKF and sensor combination with the best data consistency. This enables faults such as sudden changes in IMU bias, saturation or stuck data to be detected and isolated.</p><p>The total number of EKF instances is the product of the number of IMU&#39;s and number of magnetometers selected by <a href="./../advanced_config/parameter_reference.html#EKF2_MULTI_IMU">EKF2_MULTI_IMU</a> and <a href="./../advanced_config/parameter_reference.html#EKF2_MULTI_MAG">EKF2_MULTI_MAG</a> and is given by the following formula:</p><blockquote><p>N_instances = MAX(<a href="./../advanced_config/parameter_reference.html#EKF2_MULTI_IMU">EKF2_MULTI_IMU</a> , 1) x MAX(<a href="./../advanced_config/parameter_reference.html#EKF2_MULTI_MAG">EKF2_MULTI_MAG</a> , 1)</p></blockquote><p>For example an autopilot with 2 IMUs and 2 magnetometers could run with EKF2_MULTI_IMU = 2 and EKF2_MULTI_MAG = 2 for a total of 4 EKF instances where each instance uses the following combination of sensors:</p><ul><li>EKF instance 1 : IMU 1, magnetometer 1</li><li>EKF instance 2 : IMU 1, magnetometer 2</li><li>EKF instance 3 : IMU 2, magnetometer 1</li><li>EKF instance 4 : IMU 2, magnetometer 2</li></ul><p>The maximum number of IMU or magnetometer sensors that can be handled is 4 of each for a theoretical maximum of 4 x 4 = 16 EKF instances. In practice this is limited by available computing resources. During development of this feature, testing with STM32F7 CPU based HW demonstrated 4 EKF instances with acceptable processing load and memory utilisation margin.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Ground based testing to check CPU and memory utilisation should be performed before flying.</p></div><p>If <a href="./../advanced_config/parameter_reference.html#EKF2_MULTI_IMU">EKF2_MULTI_IMU</a> &gt;= 3, then the failover time for large rate gyro errors is further reduced because the EKF selector is able to apply a median select strategy for faster isolation of the faulty IMU.</p><p>The setup for multiple EKF instances is controlled by the following parameters:</p><ul><li><p><a href="./../advanced_config/parameter_reference.html#SENS_IMU_MODE">SENS_IMU_MODE</a>: Set to 0 if running multiple EKF instances with IMU sensor diversity, ie <a href="./../advanced_config/parameter_reference.html#EKF2_MULTI_IMU">EKF2_MULTI_IMU</a> &gt; 1.</p><p>When set to 1 (default for single EKF operation) the sensor module selects IMU data used by the EKF. This provides protection against loss of data from the sensor but does not protect against bad sensor data. When set to 0, the sensor module does not make a selection.</p></li><li><p><a href="./../advanced_config/parameter_reference.html#SENS_MAG_MODE">SENS_MAG_MODE</a>: Set to 0 if running multiple EKF instances with magnetometer sensor diversity, ie <a href="./../advanced_config/parameter_reference.html#EKF2_MULTI_MAG">EKF2_MULTI_MAG</a> &gt; 1.</p><p>When set to 1 (default for single EKF operation) the sensor module selects Magnetometer data used by the EKF. This provides protection against loss of data from the sensor but does not protect against bad sensor data. When set to 0, the sensor module does not make a selection.</p></li><li><p><a href="./../advanced_config/parameter_reference.html#EKF2_MULTI_IMU">EKF2_MULTI_IMU</a>: This parameter specifies the number of IMU sensors used by the multiple EKF&#39;s. If <code>EKF2_MULTI_IMU</code> &lt;= 1, then only the first IMU sensor will be used. When <a href="./../advanced_config/parameter_reference.html#SENS_IMU_MODE">SENS_IMU_MODE</a> = 1, this will be the sensor selected by the sensor module. If <code>EKF2_MULTI_IMU</code> &gt;= 2, then a separate EKF instance will run for the specified number of IMU sensors up to the lesser of 4 or the number of IMU&#39;s present.</p></li><li><p><a href="./../advanced_config/parameter_reference.html#EKF2_MULTI_MAG">EKF2_MULTI_MAG</a>: This parameter specifies the number of magnetometer sensors used by the multiple EKF&#39;s If <code>EKF2_MULTI_MAG</code> &lt;= 1, then only the first magnetometer sensor will be used. When <a href="./../advanced_config/parameter_reference.html#SENS_MAG_MODE">SENS_MAG_MODE</a> = 1, this will be the sensor selected by the sensor module. If <code>EKF2_MULTI_MAG</code> &gt;= 2, then a separate EKF instance will run for the specified number of magnetometer sensors up to the lesser of 4 or the number of magnetometers present.</p></li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The recording and <a href="./../debug/system_wide_replay.html#ekf2-replay">EKF2 replay</a> of flight logs with multiple EKF instances is not supported. To enable recording for EKF replay you must set the parameters to enable a <a href="#running-a-single-ekf-instance">single EKF instance</a>.</p></div><h2 id="what-sensor-measurements-does-it-use" tabindex="-1">What sensor measurements does it use? <a class="header-anchor" href="#what-sensor-measurements-does-it-use" aria-label="Permalink to &quot;What sensor measurements does it use?&quot;">​</a></h2><p>The EKF has different modes of operation that allow for different combinations of sensor measurements. On start-up the filter checks for a minimum viable combination of sensors and after initial tilt, yaw and height alignment is completed, enters a mode that provides rotation, vertical velocity, vertical position, IMU delta angle bias and IMU delta velocity bias estimates.</p><p>This mode requires IMU data, a source of yaw (magnetometer or external vision) and a source of height data. This minimum data set is required for all EKF modes of operation. Other sensor data can then be used to estimate additional states.</p><h3 id="imu" tabindex="-1">IMU <a class="header-anchor" href="#imu" aria-label="Permalink to &quot;IMU&quot;">​</a></h3><ul><li>Three axis body fixed Inertial Measurement unit delta angle and delta velocity data at a minimum rate of 100Hz. Note: Coning corrections should be applied to the IMU delta angle data before it is used by the EKF.</li></ul><h3 id="magnetometer" tabindex="-1">Magnetometer <a class="header-anchor" href="#magnetometer" aria-label="Permalink to &quot;Magnetometer&quot;">​</a></h3><p>Three axis body fixed magnetometer data (or external vision system pose data) at a minimum rate of 5Hz is required.</p><p>Magnetometer data can be used in two ways:</p><ul><li>Magnetometer measurements are converted to a yaw angle using the tilt estimate and magnetic declination. The yaw angle is then used as an observation by the EKF. <ul><li>This method is less accurate and does not allow for learning of body frame field offsets, however it is more robust to magnetic anomalies and large start-up gyro biases.</li><li>It is the default method used during start-up and on ground.</li></ul></li><li>The XYZ magnetometer readings are used as separate observations. <ul><li>This method is more accurate but requires that the magnetometer biases are correctly estimated. <ul><li>The biases are observable while the drone is rotating and the true heading is observable when the vehicle is accelerating (linear acceleration).</li><li>Since the biases can change and are only observable when moving, it is safer to switch back to heading fusion when not moving.</li></ul></li><li>It assumes the earth magnetic field environment only changes slowly and performs less well when there are significant external magnetic anomalies.</li><li>This is the default method used when the vehicle is moving.</li></ul></li></ul><p>The logic used to select these modes is set by the <a href="./../advanced_config/parameter_reference.html#EKF2_MAG_TYPE">EKF2_MAG_TYPE</a> parameter. The default &#39;Automatic&#39; mode (<code>EKF2_MAG_TYPE=0</code>) is recommended as it uses the more robust magnetometer yaw on the ground, and more accurate 3-axis magnetometer when moving. Setting &#39;3-axis&#39; mode all the time (<code>EKF2_MAG_TYPE=2</code>) is more error-prone, and requires that all the IMUs are well calibrated.</p><p>The option is available to operate without a magnetometer, either by replacing it using <a href="#yaw-measurements">yaw from a dual antenna GPS</a> or using the IMU measurements and GPS velocity data to <a href="#yaw-from-gps-velocity">estimate yaw from vehicle movement</a>.</p><h3 id="height" tabindex="-1">Height <a class="header-anchor" href="#height" aria-label="Permalink to &quot;Height&quot;">​</a></h3><p>A source of height data - GPS, barometric pressure, range finder, external vision or a combination of those at a minimum rate of 5Hz is required.</p><p>If none of the selected measurements are present, the EKF will not start. When these measurements have been detected, the EKF will initialise the states and complete the tilt and yaw alignment. When tilt and yaw alignment is complete, the EKF can then transition to other modes of operation enabling use of additional sensor data:</p><p>Each height source can be enabled/disabled using its dedicated control parameter:</p><ul><li><a href="#gnss-gps">GNSS/GPS</a>: <a href="./../advanced_config/parameter_reference.html#EKF2_GPS_CTRL">EKF2_GPS_CTRL</a></li><li><a href="#barometer">Barometer</a>: <a href="./../advanced_config/parameter_reference.html#EKF2_BARO_CTRL">EKF2_BARO_CTRL</a></li><li><a href="#range-finder">Range finder</a>: <a href="./../advanced_config/parameter_reference.html#EKF2_RNG_CTRL">EKF2_RNG_CTRL</a></li><li><a href="#external-vision-system">External vision</a>: Enabled when <a href="./../advanced_config/parameter_reference.html#EKF2_HGT_REF">EKF2_HGT_REF</a> is set to &quot;Vision&quot;</li></ul><p>Over the long term the height estimate follows the &quot;reference source&quot; of height data. This reference is defined by the <a href="./../advanced_config/parameter_reference.html#EKF2_HGT_REF">EKF2_HGT_REF</a> parameter.</p><h4 id="typical-configurations" tabindex="-1">Typical configurations <a class="header-anchor" href="#typical-configurations" aria-label="Permalink to &quot;Typical configurations&quot;">​</a></h4><table tabindex="0"><thead><tr><th></th><th><a href="./../advanced_config/parameter_reference.html#EKF2_GPS_CTRL">EKF2_GPS_CTRL</a></th><th><a href="./../advanced_config/parameter_reference.html#EKF2_BARO_CTRL">EKF2_BARO_CTRL</a></th><th><a href="./../advanced_config/parameter_reference.html#EKF2_RNG_CTRL">EKF2_RNG_CTRL</a></th><th><a href="./../advanced_config/parameter_reference.html#EKF2_HGT_REF">EKF2_HGT_REF</a></th></tr></thead><tbody><tr><td>Outdoor (default)</td><td>7 (Lon/lat/alt/vel)</td><td>1 (enabled)</td><td>1 (<a href="#conditional-range-aiding">conditional</a>)</td><td>1 (GNSS)</td></tr><tr><td>Indoor (non-flat terrain)</td><td>0 (disabled)</td><td>1 (enabled)</td><td>1 (<a href="#conditional-range-aiding">conditional</a>)</td><td>2 (range)</td></tr><tr><td>Indoor (flat terrain)</td><td>0 (disabled)</td><td>1 (enabled)</td><td>2 (<a href="#range-height-fusion">always enabled</a>)</td><td>2 (range)</td></tr><tr><td>External vision</td><td>As required</td><td>As required</td><td>As required</td><td>3 (vision)</td></tr></tbody></table><h3 id="barometer" tabindex="-1">Barometer <a class="header-anchor" href="#barometer" aria-label="Permalink to &quot;Barometer&quot;">​</a></h3><p>Enable/disable using <a href="./../advanced_config/parameter_reference.html#EKF2_BARO_CTRL">EKF2_BARO_CTRL</a> as a source for <a href="#height">Height</a> data.</p><p>Note that data from only one barometer is fused, even if multiple barometers are available. The barometer with the highest <a href="./../advanced_config/parameter_reference.html#CAL_BARO0_PRIO">CAL_BAROx_PRIO</a> priority value is selected first, falling back to the next highest priority barometer if a sensor fault is detected. If barometers have equal-highest priorities, the first detected is used. A barometer can be completely disabled as a possible source by setting its <code>CAL_BAROx_PRIO</code> value to <code>0</code> (disabled).</p><p>See <a href="#height">Height</a> more details about the configuration of height sources.</p><h4 id="correction-for-static-pressure-position-error" tabindex="-1">Correction for Static Pressure Position Error <a class="header-anchor" href="#correction-for-static-pressure-position-error" aria-label="Permalink to &quot;Correction for Static Pressure Position Error&quot;">​</a></h4><p>Barometric pressure altitude is subject to errors generated by aerodynamic disturbances caused by vehicle wind relative velocity and orientation. This is known in aeronautics as <em>static pressure position error</em>. The EKF2 module that uses the ECL/EKF2 estimator library provides a method of compensating for these errors, provided wind speed state estimation is active.</p><p>For vehicles operating in a fixed-wing mode, wind speed state estimation requires either <a href="#airspeed">Airspeed</a> and/or <a href="#synthetic-sideslip">Synthetic Sideslip</a> fusion to be enabled.</p><p>For multi-rotors, fusion of <a href="#mc_wind_estimation_using_drag">Drag Specific Forces</a> can be enabled and tuned to provide the required wind velocity state estimates.</p><p>The EKF2 module models the error as a body fixed ellipsoid that specifies the fraction of dynamic pressure that is added to/subtracted from the barometric pressure - before it is converted to a height estimate.</p><p>A good tuning is obtained as follows:</p><ol><li><p>Fly once in <a href="./../flight_modes_mc/position.html">Position mode</a> repeatedly forwards/backwards/left/right/up/down between rest and maximum speed (best results are obtained when this testing is conducted in still conditions).</p></li><li><p>Extract the <code>.ulg</code> log file using, for example, <a href="https://docs.qgroundcontrol.com/master/en/qgc-user-guide/analyze_view/log_download.html" target="_blank" rel="noreferrer">QGroundControl: Analyze &gt; Log Download</a></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The same log file can be used to tune the <a href="#mc_wind_estimation_using_drag">multirotor wind estimator</a>.</p></div></li><li><p>Use the log with the <a href="https://github.com/PX4/PX4-Autopilot/tree/main/src/modules/ekf2/EKF/python/tuning_tools/baro_static_pressure_compensation" target="_blank" rel="noreferrer">baro_static_pressure_compensation_tuning.py</a> Python script to obtain the optimal set of parameters.</p></li></ol><p>Tuning parameters:</p><ul><li><a href="./../advanced_config/parameter_reference.html#EKF2_PCOEF_XP">EKF2_PCOEF_XP</a></li><li><a href="./../advanced_config/parameter_reference.html#EKF2_PCOEF_XN">EKF2_PCOEF_XN</a></li><li><a href="./../advanced_config/parameter_reference.html#EKF2_PCOEF_YP">EKF2_PCOEF_YP</a></li><li><a href="./../advanced_config/parameter_reference.html#EKF2_PCOEF_YN">EKF2_PCOEF_YN</a></li><li><a href="./../advanced_config/parameter_reference.html#EKF2_PCOEF_Z">EKF2_PCOEF_Z</a></li></ul><h4 id="barometer-bias-compensation" tabindex="-1">Barometer bias compensation <a class="header-anchor" href="#barometer-bias-compensation" aria-label="Permalink to &quot;Barometer bias compensation&quot;">​</a></h4><p>A barometer at a constant altitude is subject to drift in its measurements due to changes in the ambient pressure environment or variations of the sensor temperature. To compensate for this measurement error, EKF2 estimates the bias using GNSS height (if available) a &quot;non drifting&quot; reference. No tuning is required.</p><h3 id="gnss-gps" tabindex="-1">GNSS/GPS <a class="header-anchor" href="#gnss-gps" aria-label="Permalink to &quot;GNSS/GPS&quot;">​</a></h3><h4 id="position-and-velocity-measurements" tabindex="-1">Position and Velocity Measurements <a class="header-anchor" href="#position-and-velocity-measurements" aria-label="Permalink to &quot;Position and Velocity Measurements&quot;">​</a></h4><p>GPS measurements will be used for position and velocity if the following conditions are met:</p><ul><li>GPS use is enabled via setting of the <a href="./../advanced_config/parameter_reference.html#EKF2_GPS_CTRL">EKF2_GPS_CTRL</a> parameter.</li><li>GPS quality checks have passed. These checks are controlled by the <a href="./../advanced_config/parameter_reference.html#EKF2_GPS_CHECK">EKF2_GPS_CHECK</a> and <code>EKF2_REQ_*</code> parameters.</li></ul><p>For more details about the configuration of height sources, <a href="#height">click here</a>.</p><h4 id="yaw-measurements" tabindex="-1">Yaw Measurements <a class="header-anchor" href="#yaw-measurements" aria-label="Permalink to &quot;Yaw Measurements&quot;">​</a></h4><p>Some GPS receivers such as the <a href="https://www.trimble.com/Precision-GNSS/MB-Two-Board.aspx" target="_blank" rel="noreferrer">Trimble MB-Two RTK GPS receiver</a> can be used to provide a heading measurement that replaces the use of magnetometer data. This can be a significant advantage when operating in an environment where large magnetic anomalies are present, or at latitudes here the earth&#39;s magnetic field has a high inclination. Use of GPS yaw measurements is enabled by setting bit position 3 to 1 (adding 8) in the <a href="./../advanced_config/parameter_reference.html#EKF2_GPS_CTRL">EKF2_GPS_CTRL</a> parameter.</p><h4 id="yaw-from-gps-velocity" tabindex="-1">Yaw From GPS Velocity <a class="header-anchor" href="#yaw-from-gps-velocity" aria-label="Permalink to &quot;Yaw From GPS Velocity&quot;">​</a></h4><p>The EKF runs an additional multi-hypothesis filter internally that uses multiple 3-state Extended Kalman Filters (EKF&#39;s) whose states are NE velocity and yaw angle. These individual yaw angle estimates are then combined using a Gaussian Sum Filter (GSF). The individual 3-state EKF&#39;s use IMU and GPS horizontal velocity data (plus optional airspeed data) and do not rely on any prior knowledge of the yaw angle or magnetometer measurements. This provides a backup to the yaw from the main filter and is used to reset the yaw for the main 24-state EKF when a post-takeoff loss of navigation indicates that the yaw estimate from the magnetometer is bad. This will result in an <code>Emergency yaw reset - magnetometer use stopped</code> message information message at the GCS.</p><p>Data from this estimator is logged when ekf2 replay logging is enabled and can be viewed in the <code>yaw_estimator_status</code> message. The individual yaw estimates from the individual 3-state EKF yaw estimators are in the <code>yaw</code> fields. The GSF combined yaw estimate is in the <code>yaw_composite</code> field. The variance for the GSF yaw estimate is in the <code>yaw_variance</code> field. All angles are in radians. Weightings applied by the GSF to the individual 3-state EKF outputs are in the<code>weight</code> fields.</p><p>This also makes it possible to operate without any magnetometer data or dual antenna GPS receiver for yaw provided some horizontal movement after takeoff can be performed to enable the yaw to become observable. To use this feature, set <a href="./../advanced_config/parameter_reference.html#EKF2_MAG_TYPE">EKF2_MAG_TYPE</a> to <code>none</code> (5) to disable magnetometer use. Once the vehicle has performed sufficient horizontal movement to make the yaw observable, the main 24-state EKF will align it&#39;s yaw to the GSF estimate and commence use of GPS.</p><h4 id="dual-receivers" tabindex="-1">Dual Receivers <a class="header-anchor" href="#dual-receivers" aria-label="Permalink to &quot;Dual Receivers&quot;">​</a></h4><p>Data from GPS receivers can be blended using an algorithm that weights data based on reported accuracy (this works best if both receivers output data at the same rate and use the same accuracy). The mechanism also provides automatic failover if data from a receiver is lost (it allows, for example, a standard GPS to be used as a backup to a more accurate RTK receiver). This is controlled by the <a href="./../advanced_config/parameter_reference.html#SENS_GPS_MASK">SENS_GPS_MASK</a> parameter.</p><p>The <a href="./../advanced_config/parameter_reference.html#SENS_GPS_MASK">SENS_GPS_MASK</a> parameter is set by default to disable blending and always use the first receiver, so it will have to be set to select which receiver accuracy metrics are used to decide how much each receiver output contributes to the blended solution. Where different receiver models are used, it is important that the <a href="./../advanced_config/parameter_reference.html#SENS_GPS_MASK">SENS_GPS_MASK</a> parameter is set to a value that uses accuracy metrics that are supported by both receivers. For example do not set bit position 0 to <code>true</code> unless the drivers for both receivers publish values in the <code>s_variance_m_s</code> field of the <code>vehicle_gps_position</code> message that are comparable. This can be difficult with receivers from different manufacturers due to the different way that accuracy is defined, e.g. CEP vs 1-sigma, etc.</p><p>The following items should be checked during setup:</p><ul><li>Verify that data for the second receiver is present. This will be logged as <code>vehicle_gps_position_1</code> and can also be checked when connected via the <em>nsh console</em> using the command <code>listener vehicle_gps_position -i 1</code>. The <a href="./../advanced_config/parameter_reference.html#GPS_2_CONFIG">GPS_2_CONFIG</a> parameter will need to be set correctly.</li><li>Check the <code>s_variance_m_s</code>, <code>eph</code> and <code>epv</code> data from each receiver and decide which accuracy metrics can be used. If both receivers output sensible <code>s_variance_m_s</code> and <code>eph</code> data, and GPS vertical position is not being used directly for navigation, then setting <a href="./../advanced_config/parameter_reference.html#SENS_GPS_MASK">SENS_GPS_MASK</a> to 3 is recommended. Where only <code>eph</code> data is available and both receivers do not output <code>s_variance_m_s</code> data, set <a href="./../advanced_config/parameter_reference.html#SENS_GPS_MASK">SENS_GPS_MASK</a> to 2. Bit position 2 would only be set if the GPS had been selected as the reference height source with the <a href="./../advanced_config/parameter_reference.html#EKF2_HGT_REF">EKF2_HGT_REF</a> parameter and both receivers output sensible <code>epv</code> data.</li><li>The output from the blended receiver data is logged as <code>ekf_gps_position</code>, and can be checked whilst connect via the nsh terminal using the command <code>listener ekf_gps_position</code>.</li><li>Where receivers output at different rates, the blended output will be at the rate of slower receiver. Where possible receivers should be configured to output at the same rate.</li></ul><h4 id="gnss-performance-requirements" tabindex="-1">GNSS Performance Requirements <a class="header-anchor" href="#gnss-performance-requirements" aria-label="Permalink to &quot;GNSS Performance Requirements&quot;">​</a></h4><p>For the ECL to accept GNSS data for navigation, certain minimum requirements need to be satisfied over a period of time, defined by <a href="./../advanced_config/parameter_reference.html#EKF2_REQ_GPS_H">EKF2_REQ_GPS_H</a> (10 seconds by default).</p><p>Minima are defined in the <a href="./../advanced_config/parameter_reference.html#EKF2_REQ_EPH">EKF2<em>REQ</em>*</a> parameters and each check can be enabled/disabled using the <a href="./../advanced_config/parameter_reference.html#EKF2_GPS_CHECK">EKF2_GPS_CHECK</a> parameter.</p><p>The table below shows the different metrics directly reported or calculated from the GNSS data, and the minimum required values for the data to be used by ECL. In addition, the <em>Average Value</em> column shows typical values that might reasonably be obtained from a standard GNSS module (e.g. u-blox M8 series) - i.e. values that are considered good/acceptable.</p><table tabindex="0"><thead><tr><th>Metric</th><th>Minimum required</th><th>Average Value</th><th>Units</th><th>Notes</th></tr></thead><tbody><tr><td>eph</td><td>&lt; 3 (<a href="./../advanced_config/parameter_reference.html#EKF2_REQ_EPH">EKF2_REQ_EPH</a>)</td><td>0.8</td><td>m</td><td>Standard deviation of horizontal position error</td></tr><tr><td>epv</td><td>&lt; 5 (<a href="./../advanced_config/parameter_reference.html#EKF2_REQ_EPV">EKF2_REQ_EPV</a>)</td><td>1.5</td><td>m</td><td>Standard deviation of vertical position error</td></tr><tr><td>Number of satellites</td><td>≥6 (<a href="./../advanced_config/parameter_reference.html#EKF2_REQ_NSATS">EKF2_REQ_NSATS</a>)</td><td>14</td><td>-</td><td></td></tr><tr><td>sacc</td><td>&lt; 0.5 (<a href="./../advanced_config/parameter_reference.html#EKF2_REQ_SACC">EKF2_REQ_SACC</a>)</td><td>0.2</td><td>m/s</td><td>Standard deviation of horizontal speed error</td></tr><tr><td>fix type</td><td>≥ 3</td><td>4</td><td>-</td><td>0-1: no fix, 2: 2D fix, 3: 3D fix, 4: RTCM code differential, 5: Real-Time Kinematic, float, 6: Real-Time Kinematic, fixed, 8: Extrapolated</td></tr><tr><td>PDOP</td><td>&lt; 2.5 (<a href="./../advanced_config/parameter_reference.html#EKF2_REQ_PDOP">EKF2_REQ_PDOP</a>)</td><td>1.0</td><td>-</td><td>Position dilution of precision</td></tr><tr><td>hpos drift rate</td><td>&lt; 0.1 (<a href="./../advanced_config/parameter_reference.html#EKF2_REQ_HDRIFT">EKF2_REQ_HDRIFT</a>)</td><td>0.01</td><td>m/s</td><td>Drift rate calculated from reported GNSS position (when stationary).</td></tr><tr><td>vpos drift rate</td><td>&lt; 0.2 (<a href="./../advanced_config/parameter_reference.html#EKF2_REQ_VDRIFT">EKF2_REQ_VDRIFT</a>)</td><td>0.02</td><td>m/s</td><td>Drift rate calculated from reported GNSS altitude (when stationary).</td></tr><tr><td>hspd</td><td>&lt; 0.1 (<a href="./../advanced_config/parameter_reference.html#EKF2_REQ_HDRIFT">EKF2_REQ_HDRIFT</a>)</td><td>0.01</td><td>m/s</td><td>Filtered magnitude of reported GNSS horizontal velocity.</td></tr><tr><td>vspd</td><td>&lt; 0.2 (<a href="./../advanced_config/parameter_reference.html#EKF2_REQ_VDRIFT">EKF2_REQ_VDRIFT</a>)</td><td>0.02</td><td>m/s</td><td>Filtered magnitude of reported GNSS vertical velocity.</td></tr></tbody></table><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The <code>hpos_drift_rate</code>, <code>vpos_drift_rate</code> and <code>hspd</code> are calculated over a period of 10 seconds and published in the <code>ekf2_gps_drift</code> topic. Note that <code>ekf2_gps_drift</code> is not logged!</p></div><h3 id="range-finder" tabindex="-1">Range Finder <a class="header-anchor" href="#range-finder" aria-label="Permalink to &quot;Range Finder&quot;">​</a></h3><p><a href="./../sensor/rangefinders.html">Range finder</a> distance to ground is used by a single state filter to estimate the vertical position of the terrain relative to the height datum.</p><p>The fusion modes of operation are controlled by <a href="./../advanced_config/parameter_reference.html#EKF2_RNG_CTRL">EKF2_RNG_CTRL</a>:</p><ol><li><a href="#conditional-range-aiding">Conditional range aiding</a></li><li><a href="#range-height-fusion">Range height fusion</a></li></ol><p>For more details about the configuration of height sources, <a href="#height">click here</a>.</p><h4 id="conditional-range-aiding" tabindex="-1">Conditional range aiding <a class="header-anchor" href="#conditional-range-aiding" aria-label="Permalink to &quot;Conditional range aiding&quot;">​</a></h4><p>Conditional range finder fusion (a.k.a. <em>Conditional range aid</em>) activates the range finder fusion for height estimation during low speed/low altitude operation (in addition to the other active height sources). If the range finder is set as the reference height source (using <a href="./../advanced_config/parameter_reference.html#EKF2_HGT_REF">EKF2_HGT_REF</a>), the other active height sources such as baro and GNSS altitude will adjust their measurement to match the readings of the range finder over time. When the conditions are not met to start range aiding, a secondary reference is automatically selected.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Switching between height references causes the absolute altitude estimate to drift over time. This isn&#39;t an issue when flying in position mode but can be problematic if the drone is supposed to fly a mission at a specific GNSS altitude. If the absolute altitude drift is unwanted, it is recommended to set the GNSS altitude as the height reference, even when using conditional range aid.</p></div><p>It is primarily intended for <em>takeoff and landing</em>, in cases where the barometer setup is such that interference from rotor wash is excessive and can corrupt EKF state estimates.</p><p>Range aid may also be used to improve altitude hold when the vehicle is stationary.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p><a href="./../flying/terrain_following_holding.html#terrain_hold">Terrain Hold</a> is recommended over <em>Range Aid</em> for terrain holding. This is because terrain hold uses the normal ECL/EKF estimator for determining height, and this is generally more reliable than a distance sensor in most conditions.</p></div><p><em>Conditional range aid</em> is enabled by setting <a href="./../advanced_config/parameter_reference.html#EKF2_RNG_CTRL">EKF2_RNG_CTRL</a> = &quot;Enabled (conditional mode)&quot; (1).</p><p>It is further configured using the <code>EKF2_RNG_A_</code> parameters:</p><ul><li><a href="./../advanced_config/parameter_reference.html#EKF2_RNG_A_VMAX">EKF2_RNG_A_VMAX</a>: Maximum horizontal speed, above which range aid is disabled.</li><li><a href="./../advanced_config/parameter_reference.html#EKF2_RNG_A_HMAX">EKF2_RNG_A_HMAX</a>: Maximum height, above which range aid is disabled.</li><li><a href="./../advanced_config/parameter_reference.html#EKF2_RNG_A_IGATE">EKF2_RNG_A_IGATE</a>: Range aid consistency checks &quot;gate&quot; (a measure of the error before range aid is disabled).</li></ul><h4 id="range-height-fusion" tabindex="-1">Range height fusion <a class="header-anchor" href="#range-height-fusion" aria-label="Permalink to &quot;Range height fusion&quot;">​</a></h4><p>PX4 allows you to continuously fuse the range finder as a source of height (in any flight mode/vehicle type). This may be useful for applications when the vehicle is <em>guaranteed</em> to only fly over a near-flat surface (e.g. indoors).</p><p>When using a distance sensor as a height source, fliers should be aware:</p><ul><li><p>Flying over obstacles can lead to the estimator rejecting rangefinder data (due to internal data consistency checks), which can result in poor altitude holding while the estimator is relying purely on accelerometer estimates.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>This scenario might occur when a vehicle ascends a slope at a near-constant height above ground, because the rangefinder altitude does not change while that estimated from the accelerometer does. The EKF performs innovation consistency checks that take into account the error between measurement and current state as well as the estimated variance of the state and the variance of the measurement itself. If the checks fail the rangefinder data will be rejected, and the altitude will be estimated from the accelerometer and the other selected height sources (GNSS, baro, vision), if enabled and available After 5 seconds of inconsistent data if the distance sensor is the active source oh height data, the estimator resets the height state to match the current distance sensor data. If one or more other sources of height are active, the range finder is declared faulty and the estimator continues to estimate its height using the other sensors. The measurements might also become consistent again, for example, if the vehicle descends, or if the estimated height drifts to match the measured rangefinder height.</p></div></li><li><p>The local NED origin will move up and down with ground level.</p></li><li><p>Rangefinder performance over uneven surfaces (e.g. trees) can be very poor, resulting in noisy and inconsistent data. This again leads to poor altitude hold.</p></li></ul><p>The feature is enabled by setting <a href="./../advanced_config/parameter_reference.html#EKF2_RNG_CTRL">EKF2_RNG_CTRL</a> to &quot;Enabled&quot; (2). To make the range finder the height reference when active, set: <a href="./../advanced_config/parameter_reference.html#EKF2_HGT_REF">EKF2_HGT_REF</a> to &quot;Range sensor&quot;.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>To enable the range finder fusion only when the drone is stationary (in order to benefit from a better altitude estimate during takeoff and landing) but not fuse the range finder the rest of the time, use the <a href="#conditional-range-aiding">conditional mode</a> (1) of <a href="./../advanced_config/parameter_reference.html#EKF2_RNG_CTRL">EKF2_RNG_CTRL</a>.</p></div><h4 id="range-finder-obstruction-detection" tabindex="-1">Range Finder Obstruction Detection <a class="header-anchor" href="#range-finder-obstruction-detection" aria-label="Permalink to &quot;Range Finder Obstruction Detection&quot;">​</a></h4><p>The EKF can detect whether the rangefinder path-to-ground is obstructed (perhaps by a payload) using a kinematic consistency check between the vertical velocity estimate and the numerical derivative of the range finder data. If the range finder is statistically inconsistent with EKF2, the sensor is rejected for the rest of the flight unless the statistical test passes again for at least 1 second at a vertical speed of 0.5m/s or more.</p><p>The check is only enabled when the rangefinder is not used as the primary height source, and is only active while the vehicle is not moving horizontally (as it assumes a static ground height).</p><p>For effective obstruction detection, the range finder noise parameter needs to be tightly tuned using flight data. The kinematic consistency gate parameter can then be adjusted to obtain the desired fault detection sensitivity.</p><p>Tuning parameters:</p><ul><li><a href="./../advanced_config/parameter_reference.html#EKF2_RNG_NOISE">EKF2_RNG_NOISE</a></li><li><a href="./../advanced_config/parameter_reference.html#EKF2_RNG_K_GATE">EKF2_RNG_K_GATE</a></li></ul><h3 id="airspeed" tabindex="-1">Airspeed <a class="header-anchor" href="#airspeed" aria-label="Permalink to &quot;Airspeed&quot;">​</a></h3><p>Equivalent Airspeed (EAS) data can be used to estimate wind velocity and reduce drift when GPS is lost by setting <a href="./../advanced_config/parameter_reference.html#EKF2_ARSP_THR">EKF2_ARSP_THR</a> to a positive value. Airspeed data will be used when it exceeds the threshold set by a positive value for <a href="./../advanced_config/parameter_reference.html#EKF2_ARSP_THR">EKF2_ARSP_THR</a> and the vehicle type is not rotary wing.</p><h3 id="synthetic-sideslip" tabindex="-1">Synthetic Sideslip <a class="header-anchor" href="#synthetic-sideslip" aria-label="Permalink to &quot;Synthetic Sideslip&quot;">​</a></h3><p>Fixed-wing platforms can take advantage of an assumed sideslip observation of zero to improve wind speed estimation and also enable wind speed estimation without an airspeed sensor. This is enabled by setting the <a href="./../advanced_config/parameter_reference.html#EKF2_FUSE_BETA">EKF2_FUSE_BETA</a> parameter to 1.</p><p><a id="mc_wind_estimation_using_drag"></a></p><h3 id="multicopter-wind-estimation-using-drag-specific-forces" tabindex="-1">Multicopter Wind Estimation using Drag Specific Forces <a class="header-anchor" href="#multicopter-wind-estimation-using-drag-specific-forces" aria-label="Permalink to &quot;Multicopter Wind Estimation using Drag Specific Forces&quot;">​</a></h3><p>Multi-rotor platforms can take advantage of the relationship between airspeed and drag force along the X and Y body axes to estimate North/East components of wind velocity. This can be enabled using <a href="./../advanced_config/parameter_reference.html#EKF2_DRAG_CTRL">EKF2_DRAG_CTRL</a>.</p><p>The relationship between airspeed and specific force (IMU accelerometer measurements) along the X and Y body axes is controlled by the <a href="./../advanced_config/parameter_reference.html#EKF2_BCOEF_X">EKF2_BCOEF_X</a>, <a href="./../advanced_config/parameter_reference.html#EKF2_BCOEF_Y">EKF2_BCOEF_Y</a> and <a href="./../advanced_config/parameter_reference.html#EKF2_MCOEF">EKF2_MCOEF</a> parameters which set the ballistic coefficients for flight in the X and Y directions, and the momentum drag produced by the propellers, respectively. The amount of specific force observation noise is set by the <a href="./../advanced_config/parameter_reference.html#EKF2_DRAG_NOISE">EKF2_DRAG_NOISE</a> parameter.</p><p>A good tuning is obtained as follows:</p><ol><li>Fly once in <a href="./../flight_modes_mc/position.html">Position mode</a> repeatedly forwards/backwards/left/right/up/down between rest and maximum speed (best results are obtained when this testing is conducted in still conditions).</li><li>Extract the <strong>.ulg</strong> log file using, for example, <a href="https://docs.qgroundcontrol.com/master/en/qgc-user-guide/analyze_view/log_download.html" target="_blank" rel="noreferrer">QGroundControl: Analyze &gt; Log Download</a><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The same <strong>.ulg</strong> log file can also be used to tune the <a href="#correction-for-static-pressure-position-error">static pressure position error coefficients</a>.</p></div></li><li>Use the log with the <a href="https://github.com/PX4/PX4-Autopilot/tree/main/src/modules/ekf2/EKF/python/tuning_tools/mc_wind_estimator" target="_blank" rel="noreferrer">mc_wind_estimator_tuning.py</a> Python script to obtain the optimal set of parameters.</li></ol><h3 id="optical-flow" tabindex="-1">Optical Flow <a class="header-anchor" href="#optical-flow" aria-label="Permalink to &quot;Optical Flow&quot;">​</a></h3><p><a href="./../sensor/optical_flow.html">Optical flow</a> data will be used if the following conditions are met:</p><ul><li>Valid range finder data is available.</li><li><a href="./../advanced_config/parameter_reference.html#EKF2_OF_CTRL">EKF2_OF_CTRL</a> is set.</li><li>The quality metric returned by the flow sensor is greater than the minimum requirement set by the <a href="./../advanced_config/parameter_reference.html#EKF2_OF_QMIN">EKF2_OF_QMIN</a> parameter.</li></ul><h3 id="external-vision-system" tabindex="-1">External Vision System <a class="header-anchor" href="#external-vision-system" aria-label="Permalink to &quot;External Vision System&quot;">​</a></h3><p>Position, velocity or orientation measurements from an external vision system, e.g. Vicon, can be used.</p><p>The measurements that are fused are configured by setting the appropriate bits of <a href="./../advanced_config/parameter_reference.html#EKF2_EV_CTRL">EKF2_EV_CTRL</a> to <code>true</code>:</p><ul><li><code>0</code>: Horizontal position data</li><li><code>1</code>: Vertical position data. Height sources may additionally be configured using <a href="./../advanced_config/parameter_reference.html#EKF2_HGT_REF">EKF2_HGT_REF</a> (see section <a href="#height">Height</a>).</li><li><code>2</code>: Velocity data</li><li><code>3</code>: Yaw data</li></ul><p>Note that if yaw data is used (bit 3) the heading is with respect to the external vision frame; otherwise the heading is relative to North.</p><p>The EKF considers uncertainty in the visual pose estimate. This uncertainty information can be sent via the covariance fields in the MAVLink <a href="https://mavlink.io/en/messages/common.html#ODOMETRY" target="_blank" rel="noreferrer">ODOMETRY</a> message or it can be set through the parameters <a href="./../advanced_config/parameter_reference.html#EKF2_EVP_NOISE">EKF2_EVP_NOISE</a>, <a href="./../advanced_config/parameter_reference.html#EKF2_EVV_NOISE">EKF2_EVV_NOISE</a> and <a href="./../advanced_config/parameter_reference.html#EKF2_EVA_NOISE">EKF2_EVA_NOISE</a>. You can choose the source of the uncertainty with <a href="./../advanced_config/parameter_reference.html#EKF2_EV_NOISE_MD">EKF2_EV_NOISE_MD</a>.</p><h2 id="how-do-i-use-the-ecl-library-ekf" tabindex="-1">How do I use the &#39;ecl&#39; library EKF? <a class="header-anchor" href="#how-do-i-use-the-ecl-library-ekf" aria-label="Permalink to &quot;How do I use the &#39;ecl&#39; library EKF?&quot;">​</a></h2><p>EKF2 is enabled by default (for more information see <a href="./../advanced/switching_state_estimators.html">Switching State Estimators</a> and <a href="./../advanced_config/parameter_reference.html#EKF2_EN">EKF2_EN</a>).</p><h2 id="what-are-the-advantages-and-disadvantages-of-the-ecl-ekf-over-other-estimators" tabindex="-1">What are the advantages and disadvantages of the ecl EKF over other estimators? <a class="header-anchor" href="#what-are-the-advantages-and-disadvantages-of-the-ecl-ekf-over-other-estimators" aria-label="Permalink to &quot;What are the advantages and disadvantages of the ecl EKF over other estimators?&quot;">​</a></h2><p>Like all estimators, much of the performance comes from the tuning to match sensor characteristics. Tuning is a compromise between accuracy and robustness and although we have attempted to provide a tune that meets the needs of most users, there will be applications where tuning changes are required.</p><p>For this reason, no claims for accuracy relative to the legacy combination of <code>attitude_estimator_q</code> + <code>local_position_estimator</code> have been made and the best choice of estimator will depend on the application and tuning.</p><h3 id="disadvantages" tabindex="-1">Disadvantages <a class="header-anchor" href="#disadvantages" aria-label="Permalink to &quot;Disadvantages&quot;">​</a></h3><ul><li>The ecl EKF is a complex algorithm that requires a good understanding of extended Kalman filter theory and its application to navigation problems to tune successfully. It is therefore more difficult for users that are not achieving good results to know what to change.</li><li>The ecl EKF uses more RAM and flash space.</li><li>The ecl EKF uses more logging space.</li></ul><h3 id="advantages" tabindex="-1">Advantages <a class="header-anchor" href="#advantages" aria-label="Permalink to &quot;Advantages&quot;">​</a></h3><ul><li>The ecl EKF is able to fuse data from sensors with different time delays and data rates in a mathematically consistent way which improves accuracy during dynamic maneuvers once time delay parameters are set correctly.</li><li>The ecl EKF is capable of fusing a large range of different sensor types.</li><li>The ecl EKF detects and reports statistically significant inconsistencies in sensor data, assisting with diagnosis of sensor errors.</li><li>For fixed-wing operation, the ecl EKF estimates wind speed with or without an airspeed sensor and is able to use the estimated wind in combination with airspeed measurements and sideslip assumptions to extend the dead-reckoning time available if GPS is lost in flight.</li><li>The ecl EKF estimates 3-axis accelerometer bias which improves accuracy for tailsitters and other vehicles that experience large attitude changes between flight phases.</li><li>The federated architecture (combined attitude and position/velocity estimation) means that attitude estimation benefits from all sensor measurements. This should provide the potential for improved attitude estimation if tuned correctly.</li></ul><h2 id="how-do-i-check-the-ekf-performance" tabindex="-1">How do I check the EKF performance? <a class="header-anchor" href="#how-do-i-check-the-ekf-performance" aria-label="Permalink to &quot;How do I check the EKF performance?&quot;">​</a></h2><p>EKF outputs, states and status data are published to a number of uORB topics which are logged to the SD card during flight. The following guide assumes that data has been logged using the <em>.ulog file format</em>. The <strong>.ulog</strong> format data can be parsed in python by using the <a href="https://github.com/PX4/pyulog" target="_blank" rel="noreferrer">PX4 pyulog library</a>.</p><p>Most of the EKF data is found in the <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorInnovations.msg" target="_blank" rel="noreferrer">EstimatorInnovations</a> and <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a> uORB messages that are logged to the .ulog file.</p><p>A python script that automatically generates analysis plots and metadata can be found <a href="https://github.com/PX4/PX4-Autopilot/blob/main/Tools/ecl_ekf/process_logdata_ekf.py" target="_blank" rel="noreferrer">here</a>. To use this script file, cd to the <code>Tools/ecl_ekf</code> directory and enter <code>python process_logdata_ekf.py &lt;log_file.ulg&gt;</code>. This saves performance metadata in a csv file named <strong>&lt;log_file&gt;.mdat.csv</strong> and plots in a pdf file named <code>&lt;log_file&gt;.pdf</code>.</p><p>Multiple log files in a directory can be analysed using the <a href="https://github.com/PX4/PX4-Autopilot/blob/main/Tools/ecl_ekf/batch_process_logdata_ekf.py" target="_blank" rel="noreferrer">batch_process_logdata_ekf.py</a> script. When this has been done, the performance metadata files can be processed to provide a statistical assessment of the estimator performance across the population of logs using the <a href="https://github.com/PX4/PX4-Autopilot/blob/main/Tools/ecl_ekf/batch_process_metadata_ekf.py" target="_blank" rel="noreferrer">batch_process_metadata_ekf.py</a> script.</p><h3 id="output-data" tabindex="-1">Output Data <a class="header-anchor" href="#output-data" aria-label="Permalink to &quot;Output Data&quot;">​</a></h3><ul><li>Attitude output data is found in the <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/VehicleAttitude.msg" target="_blank" rel="noreferrer">VehicleAttitude</a> message.</li><li>Local position output data is found in the <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/VehicleLocalPosition.msg" target="_blank" rel="noreferrer">VehicleLocalPosition</a> message.</li><li>Global (WGS-84) output data is found in the <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/VehicleGlobalPosition.msg" target="_blank" rel="noreferrer">VehicleGlobalPosition</a> message.</li><li>Wind velocity output data is found in the <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/Wind.msg" target="_blank" rel="noreferrer">Wind.msg</a> message.</li></ul><h3 id="states" tabindex="-1">States <a class="header-anchor" href="#states" aria-label="Permalink to &quot;States&quot;">​</a></h3><p>Refer to states[24] in <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStates.msg" target="_blank" rel="noreferrer">EstimatorStates</a>. The index map for states[24] is as follows:</p><ul><li>[0 ... 3] Quaternions</li><li>[4 ... 6] Velocity NED (m/s)</li><li>[7 ... 9] Position NED (m)</li><li>[10 ... 12] IMU delta angle bias XYZ (rad)</li><li>[13 ... 15] IMU delta velocity bias XYZ (m/s)</li><li>[16 ... 18] Earth magnetic field NED (gauss)</li><li>[19 ... 21] Body magnetic field XYZ (gauss)</li><li>[22 ... 23] Wind velocity NE (m/s)</li></ul><h3 id="state-variances" tabindex="-1">State Variances <a class="header-anchor" href="#state-variances" aria-label="Permalink to &quot;State Variances&quot;">​</a></h3><p>Refer to covariances[24] in <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStates.msg" target="_blank" rel="noreferrer">EstimatorStates</a>. The index map for covariances[24] is as follows:</p><ul><li>[0 ... 3] Quaternions</li><li>[4 ... 6] Velocity NED (m/s)^2</li><li>[7 ... 9] Position NED (m^2)</li><li>[10 ... 12] IMU delta angle bias XYZ (rad^2)</li><li>[13 ... 15] IMU delta velocity bias XYZ (m/s)^2</li><li>[16 ... 18] Earth magnetic field NED (gauss^2)</li><li>[19 ... 21] Body magnetic field XYZ (gauss^2)</li><li>[22 ... 23] Wind velocity NE (m/s)^2</li></ul><h3 id="observation-innovations-innovation-variances" tabindex="-1">Observation Innovations &amp; Innovation Variances <a class="header-anchor" href="#observation-innovations-innovation-variances" aria-label="Permalink to &quot;Observation Innovations &amp; Innovation Variances&quot;">​</a></h3><p>The observation <code>estimator_innovations</code>, <code>estimator_innovation_variances</code>, and <code>estimator_innovation_test_ratios</code> message fields are defined in <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorInnovations.msg" target="_blank" rel="noreferrer">EstimatorInnovations.msg</a>. The messages all have the same field names/types (but different units).</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The messages have the same fields because they are generated from the same field definition. The <code># TOPICS</code> line (at the end of <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorInnovations.msg" target="_blank" rel="noreferrer">the file</a>) lists the names of the set of messages to be created):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># TOPICS estimator_innovations estimator_innovation_variances estimator_innovation_test_ratios</span></span></code></pre></div></div><p>Some of the observations are:</p><ul><li>Magnetometer XYZ (gauss, gauss^2) : <code>mag_field[3]</code></li><li>Yaw angle (rad, rad^2) : <code>heading</code></li><li>True Airspeed (m/s, (m/s)^2) : <code>airspeed</code></li><li>Synthetic sideslip (rad, rad^2) : <code>beta</code></li><li>Optical flow XY (rad/sec, (rad/s)^2) : <code>flow</code></li><li>Height above ground (m, m^2) : <code>hagl</code></li><li>Drag specific force ((m/s)^2): <code>drag</code></li><li>Velocity and position innovations : per sensor</li></ul><p>In addition, each sensor has its own fields for horizontal and vertical position and/or velocity values (where appropriate). These are largely self documenting, and are reproduced below:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># GPS</span></span>
<span class="line"><span>float32[2] gps_hvel	# horizontal GPS velocity innovation (m/sec) and innovation variance ((m/sec)**2)</span></span>
<span class="line"><span>float32    gps_vvel	# vertical GPS velocity innovation (m/sec) and innovation variance ((m/sec)**2)</span></span>
<span class="line"><span>float32[2] gps_hpos	# horizontal GPS position innovation (m) and innovation variance (m**2)</span></span>
<span class="line"><span>float32    gps_vpos	# vertical GPS position innovation (m) and innovation variance (m**2)</span></span>
<span class="line"><span></span></span>
<span class="line"><span># External Vision</span></span>
<span class="line"><span>float32[2] ev_hvel	# horizontal external vision velocity innovation (m/sec) and innovation variance ((m/sec)**2)</span></span>
<span class="line"><span>float32    ev_vvel	# vertical external vision velocity innovation (m/sec) and innovation variance ((m/sec)**2)</span></span>
<span class="line"><span>float32[2] ev_hpos	# horizontal external vision position innovation (m) and innovation variance (m**2)</span></span>
<span class="line"><span>float32    ev_vpos	# vertical external vision position innovation (m) and innovation variance (m**2)</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Fake Position and Velocity</span></span>
<span class="line"><span>float32[2] fake_hvel	# fake horizontal velocity innovation (m/s) and innovation variance ((m/s)**2)</span></span>
<span class="line"><span>float32    fake_vvel	# fake vertical velocity innovation (m/s) and innovation variance ((m/s)**2)</span></span>
<span class="line"><span>float32[2] fake_hpos	# fake horizontal position innovation (m) and innovation variance (m**2)</span></span>
<span class="line"><span>float32    fake_vpos	# fake vertical position innovation (m) and innovation variance (m**2)</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Height sensors</span></span>
<span class="line"><span>float32 rng_vpos	# range sensor height innovation (m) and innovation variance (m**2)</span></span>
<span class="line"><span>float32 baro_vpos	# barometer height innovation (m) and innovation variance (m**2)</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Auxiliary velocity</span></span>
<span class="line"><span>float32[2] aux_hvel	# horizontal auxiliary velocity innovation from landing target measurement (m/sec) and innovation variance ((m/sec)**2)</span></span>
<span class="line"><span>float32    aux_vvel	# vertical auxiliary velocity innovation from landing target measurement (m/sec) and innovation variance ((m/sec)**2)</span></span></code></pre></div><h3 id="output-complementary-filter" tabindex="-1">Output Complementary Filter <a class="header-anchor" href="#output-complementary-filter" aria-label="Permalink to &quot;Output Complementary Filter&quot;">​</a></h3><p>The output complementary filter is used to propagate states forward from the fusion time horizon to current time. To check the magnitude of the angular, velocity and position tracking errors measured at the fusion time horizon, refer to <code>output_tracking_error[3]</code> in the <code>ekf2_innovations</code> message.</p><p>The index map is as follows:</p><ul><li>[0] Angular tracking error magnitude (rad)</li><li>[1] Velocity tracking error magnitude (m/s). The velocity tracking time constant can be adjusted using the <a href="./../advanced_config/parameter_reference.html#EKF2_TAU_VEL">EKF2_TAU_VEL</a> parameter. Reducing this parameter reduces steady state errors but increases the amount of observation noise on the NED velocity outputs.</li><li>[2] Position tracking error magnitude (m). The position tracking time constant can be adjusted using the <a href="./../advanced_config/parameter_reference.html#EKF2_TAU_POS">EKF2_TAU_POS</a> parameter. Reducing this parameter reduces steady state errors but increases the amount of observation noise on the NED position outputs.</li></ul><h3 id="ekf-errors" tabindex="-1">EKF Errors <a class="header-anchor" href="#ekf-errors" aria-label="Permalink to &quot;EKF Errors&quot;">​</a></h3><p>The EKF contains internal error checking for badly conditioned state and covariance updates. Refer to the <code>filter_fault_flags</code> in <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.</p><h3 id="observation-errors" tabindex="-1">Observation Errors <a class="header-anchor" href="#observation-errors" aria-label="Permalink to &quot;Observation Errors&quot;">​</a></h3><p>There are two categories of observation faults:</p><ul><li>Loss of data. An example of this is a range finder failing to provide a return.</li><li>The innovation, which is the difference between the state prediction and sensor observation is excessive. An example of this is excessive vibration causing a large vertical position error, resulting in the barometer height measurement being rejected.</li></ul><p>Both of these can result in observation data being rejected for long enough to cause the EKF to attempt a reset of the states using the sensor observations. All observations have a statistical confidence checks applied to the innovations. The number of standard deviations for the check are controlled by the <code>EKF2_*_GATE</code> parameter for each observation type.</p><p>Test levels are available in <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a> as follows:</p><ul><li><code>mag_test_ratio</code>: ratio of the largest magnetometer innovation component to the innovation test limit</li><li><code>vel_test_ratio</code>: ratio of the largest velocity innovation component to the innovation test limit</li><li><code>pos_test_ratio</code>: ratio of the largest horizontal position innovation component to the innovation test limit</li><li><code>hgt_test_ratio</code>: ratio of the vertical position innovation to the innovation test limit</li><li><code>tas_test_ratio</code>: ratio of the true airspeed innovation to the innovation test limit</li><li><code>hagl_test_ratio</code>: ratio of the height above ground innovation to the innovation test limit</li></ul><p>For a binary pass/fail summary for each sensor, refer to innovation_check_flags in <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.</p><h3 id="gps-quality-checks" tabindex="-1">GPS Quality Checks <a class="header-anchor" href="#gps-quality-checks" aria-label="Permalink to &quot;GPS Quality Checks&quot;">​</a></h3><p>The EKF applies a number of GPS quality checks before commencing GPS aiding. These checks are controlled by the <a href="./../advanced_config/parameter_reference.html#EKF2_GPS_CHECK">EKF2_GPS_CHECK</a> and <code>EKF2_REQ_*</code> parameters. The pass/fail status for these checks is logged in the <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.gps_check_fail_flags message. This integer will be zero when all required GPS checks have passed. If the EKF is not commencing GPS alignment, check the value of the integer against the bitmask definition <code>gps_check_fail_flags</code> in <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.</p><h3 id="ekf-numerical-errors" tabindex="-1">EKF Numerical Errors <a class="header-anchor" href="#ekf-numerical-errors" aria-label="Permalink to &quot;EKF Numerical Errors&quot;">​</a></h3><p>The EKF uses single precision floating point operations for all of its computations and first order approximations for derivation of the covariance prediction and update equations in order to reduce processing requirements. This means that it is possible when re-tuning the EKF to encounter conditions where the covariance matrix operations become badly conditioned enough to cause divergence or significant errors in the state estimates.</p><p>To prevent this, every covariance and state update step contains the following error detection and correction steps:</p><ul><li>If the innovation variance is less than the observation variance (this requires a negative state variance which is impossible) or the covariance update will produce a negative variance for any of the states, then: <ul><li>The state and covariance update is skipped</li><li>The corresponding rows and columns in the covariance matrix are reset</li><li>The failure is recorded in the <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a> <code>filter_fault_flags</code> message</li></ul></li><li>State variances (diagonals in the covariance matrix) are constrained to be non-negative.</li><li>An upper limit is applied to state variances.</li><li>Symmetry is forced on the covariance matrix.</li></ul><p>After re-tuning the filter, particularly re-tuning that involve reducing the noise variables, the value of <code>estimator_status.gps_check_fail_flags</code> should be checked to ensure that it remains zero.</p><h2 id="what-should-i-do-if-the-height-estimate-is-diverging" tabindex="-1">What should I do if the height estimate is diverging? <a class="header-anchor" href="#what-should-i-do-if-the-height-estimate-is-diverging" aria-label="Permalink to &quot;What should I do if the height estimate is diverging?&quot;">​</a></h2><p>The most common cause of EKF height diverging away from GPS and altimeter measurements during flight is clipping and/or aliasing of the IMU measurements caused by vibration. If this is occurring, then the following signs should be evident in the data</p><ul><li><a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorInnovations.msg" target="_blank" rel="noreferrer">EstimatorInnovations</a>.vel_pos_innov[2] and <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorInnovations.msg" target="_blank" rel="noreferrer">EstimatorInnovations</a>.vel_pos_innov[5] will both have the same sign.</li><li><a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.hgt_test_ratio will be greater than 1.0</li></ul><p>The recommended first step is to ensure that the autopilot is isolated from the airframe using an effective isolation mounting system. An isolation mount has 6 degrees of freedom, and therefore 6 resonant frequencies. As a general rule, the 6 resonant frequencies of the autopilot on the isolation mount should be above 25Hz to avoid interaction with the autopilot dynamics and below the frequency of the motors.</p><p>An isolation mount can make vibration worse if the resonant frequencies coincide with motor or propeller blade passage frequencies.</p><p>The EKF can be made more resistant to vibration induced height divergence by making the following parameter changes:</p><ul><li>Double the value of the innovation gate for the primary height sensor. If using barometric height this is <a href="./../advanced_config/parameter_reference.html#EKF2_BARO_GATE">EKF2_BARO_GATE</a>.</li><li>Increase the value of <a href="./../advanced_config/parameter_reference.html#EKF2_ACC_NOISE">EKF2_ACC_NOISE</a> to 0.5 initially. If divergence is still occurring, increase in further increments of 0.1 but do not go above 1.0</li></ul><p>Note that the effect of these changes will make the EKF more sensitive to errors in GPS vertical velocity and barometric pressure.</p><h2 id="what-should-i-do-if-the-position-estimate-is-diverging" tabindex="-1">What should I do if the position estimate is diverging? <a class="header-anchor" href="#what-should-i-do-if-the-position-estimate-is-diverging" aria-label="Permalink to &quot;What should I do if the position estimate is diverging?&quot;">​</a></h2><p>The most common causes of position divergence are:</p><ul><li>High vibration levels. <ul><li>Fix by improving mechanical isolation of the autopilot.</li><li>Increasing the value of <a href="./../advanced_config/parameter_reference.html#EKF2_ACC_NOISE">EKF2_ACC_NOISE</a> and <a href="./../advanced_config/parameter_reference.html#EKF2_GYR_NOISE">EKF2_GYR_NOISE</a> can help, but does make the EKF more vulnerable to GPS glitches.</li></ul></li><li>Large gyro bias offsets. <ul><li>Fix by re-calibrating the gyro. Check for excessive temperature sensitivity (&gt; 3 deg/sec bias change during warm-up from a cold start and replace the sensor if affected of insulate to slow the rate of temperature change.</li></ul></li><li>Bad yaw alignment <ul><li>Check the magnetometer calibration and alignment.</li><li>Check the heading shown QGC is within 15 deg truth</li></ul></li><li>Poor GPS accuracy <ul><li>Check for interference</li><li>Improve separation and shielding</li><li>Check flying location for GPS signal obstructions and reflectors (nearby tall buildings)</li></ul></li><li>Loss of GPS</li></ul><p>Determining which of these is the primary cause requires a methodical approach to analysis of the EKF log data:</p><ul><li>Plot the velocity innovation test ratio - <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.vel_test_ratio</li><li>Plot the horizontal position innovation test ratio - <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.pos_test_ratio</li><li>Plot the height innovation test ratio - <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.hgt_test_ratio</li><li>Plot the magnetometer innovation test ratio - <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.mag_test_ratio</li><li>Plot the GPS receiver reported speed accuracy - <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/SensorGps.msg" target="_blank" rel="noreferrer">SensorGps.msg</a>.s_variance_m_s</li><li>Plot the IMU delta angle state estimates - <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.states[10], states[11] and states[12]</li><li>Plot the EKF internal high frequency vibration metrics: <ul><li>Delta angle coning vibration - <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.vibe[0]</li><li>High frequency delta angle vibration - <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.vibe[1]</li><li>High frequency delta velocity vibration - <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.vibe[2]</li></ul></li></ul><p>During normal operation, all the test ratios should remain below 0.5 with only occasional spikes above this as shown in the example below from a successful flight:</p><p><img src="`+t+'" alt="Position, Velocity, Height and Magnetometer Test Ratios"></p><p>The following plot shows the EKF vibration metrics for a multirotor with good isolation. The landing shock and the increased vibration during takeoff and landing can be seen. Insufficient data has been gathered with these metrics to provide specific advice on maximum thresholds.</p><p><img src="'+a+'" alt="Vibration metrics - successful"></p><p>The above vibration metrics are of limited value as the presence of vibration at a frequency close to the IMU sampling frequency (1 kHz for most boards) will cause offsets to appear in the data that do not show up in the high frequency vibration metrics. The only way to detect aliasing errors is in their effect on inertial navigation accuracy and the rise in innovation levels.</p><p>In addition to generating large position and velocity test ratios of &gt; 1.0, the different error mechanisms affect the other test ratios in different ways:</p><h3 id="determination-of-excessive-vibration" tabindex="-1">Determination of Excessive Vibration <a class="header-anchor" href="#determination-of-excessive-vibration" aria-label="Permalink to &quot;Determination of Excessive Vibration&quot;">​</a></h3><p>High vibration levels normally affect vertical position and velocity innovations as well as the horizontal components. Magnetometer test levels are only affected to a small extent.</p><p>(insert example plots showing bad vibration here)</p><h3 id="determination-of-excessive-gyro-bias" tabindex="-1">Determination of Excessive Gyro Bias <a class="header-anchor" href="#determination-of-excessive-gyro-bias" aria-label="Permalink to &quot;Determination of Excessive Gyro Bias&quot;">​</a></h3><p>Large gyro bias offsets are normally characterised by a change in the value of delta angle bias greater than 5E-4 during flight (equivalent to ~3 deg/sec) and can also cause a large increase in the magnetometer test ratio if the yaw axis is affected. Height is normally unaffected other than extreme cases. Switch on bias value of up to 5 deg/sec can be tolerated provided the filter is given time settle before flying. Pre-flight checks performed by the commander should prevent arming if the position is diverging.</p><p>(insert example plots showing bad gyro bias here)</p><h3 id="determination-of-poor-yaw-accuracy" tabindex="-1">Determination of Poor Yaw Accuracy <a class="header-anchor" href="#determination-of-poor-yaw-accuracy" aria-label="Permalink to &quot;Determination of Poor Yaw Accuracy&quot;">​</a></h3><p>Bad yaw alignment causes a velocity test ratio that increases rapidly when the vehicle starts moving due inconsistency in the direction of velocity calculated by the inertial nav and the GPS measurement. Magnetometer innovations are slightly affected. Height is normally unaffected.</p><p>(insert example plots showing bad yaw alignment here)</p><h3 id="determination-of-poor-gps-accuracy" tabindex="-1">Determination of Poor GPS Accuracy <a class="header-anchor" href="#determination-of-poor-gps-accuracy" aria-label="Permalink to &quot;Determination of Poor GPS Accuracy&quot;">​</a></h3><p>Poor GPS accuracy is normally accompanied by a rise in the reported velocity error of the receiver in conjunction with a rise in innovations. Transient errors due to multipath, obscuration and interference are more common causes. Here is an example of a temporary loss of GPS accuracy where the multi-rotor started drifting away from its loiter location and had to be corrected using the sticks. The rise in <a href="https://github.com/PX4/PX4-Autopilot/blob/main/msg/EstimatorStatus.msg" target="_blank" rel="noreferrer">EstimatorStatus</a>.vel_test_ratio to greater than 1 indicates the GPs velocity was inconsistent with other measurements and has been rejected.</p><p><img src="'+i+'" alt="GPS glitch - test ratios"></p><p>This is accompanied with rise in the GPS receivers reported velocity accuracy which indicates that it was likely a GPS error.</p><p><img src="'+o+'" alt="GPS Glitch - reported receiver accuracy"></p><p>If we also look at the GPS horizontal velocity innovations and innovation variances, we can see the large spike in North velocity innovation that accompanies this GPS &#39;glitch&#39; event.</p><p><img src="'+n+'" alt="GPS Glitch - velocity innovations"></p><h3 id="determination-of-gps-data-loss" tabindex="-1">Determination of GPS Data Loss <a class="header-anchor" href="#determination-of-gps-data-loss" aria-label="Permalink to &quot;Determination of GPS Data Loss&quot;">​</a></h3><p>Loss of GPS data will be shown by the velocity and position innovation test ratios &#39;flat-lining&#39;. If this occurs, check the other GPS status data in <code>vehicle_gps_position</code> for further information.</p><p>The following plot shows the NED GPS velocity innovations <code>ekf2_innovations_0.vel_pos_innov[0 ... 2]</code>, the GPS NE position innovations <code>ekf2_innovations_0.vel_pos_innov[3 ... 4]</code> and the Baro vertical position innovation <code>ekf2_innovations_0.vel_pos_innov[5]</code> generated from a simulated VTOL flight using SITL Gazebo.</p><p>The simulated GPS was made to lose lock at 73 seconds. Note the NED velocity innovations and NE position innovations &#39;flat-line&#39; after GPS is lost. Note that after 10 seconds without GPS data, the EKF reverts back to a static position mode using the last known position and the NE position innovations start to change again.</p><p><img src="'+r+'" alt="GPS Data Loss - in SITL"></p><h3 id="barometer-ground-effect-compensation" tabindex="-1">Barometer Ground Effect Compensation <a class="header-anchor" href="#barometer-ground-effect-compensation" aria-label="Permalink to &quot;Barometer Ground Effect Compensation&quot;">​</a></h3><p>If the vehicle has the tendency during landing to climb back into the air when close to the ground, the most likely cause is barometer ground effect.</p><p>This is caused when air pushed down by the propellers hits the ground and creates a high pressure zone below the drone. The result is a lower reading of pressure altitude, leading to an unwanted climb being commanded. The figure below shows a typical situation where the ground effect is present. Note how the barometer signal dips at the beginning and end of the flight.</p><p><img src="'+s+'" alt="Barometer ground effect"></p><p>You can enable <em>ground effect compensation</em> to fix this problem:</p><ul><li>From the plot estimate the magnitude of the barometer dip during takeoff or landing. In the plot above one can read a barometer dip of about 6 meters during landing.</li><li>Then set the parameter <a href="./../advanced_config/parameter_reference.html#EKF2_GND_EFF_DZ">EKF2_GND_EFF_DZ</a> to that value and add a 10 percent margin. Therefore, in this case a value of 6.6 meters would be a good starting point.</li></ul><p>If a terrain estimate is available (e.g. the vehicle is equipped with a range finder) then you can additionally specify <a href="./../advanced_config/parameter_reference.html#EKF2_GND_MAX_HGT">EKF2_GND_MAX_HGT</a>, the above ground-level altitude below which ground effect compensation should be activated. If no terrain estimate is available this parameter will have no effect and the system will use heuristics to determine if ground effect compensation should be activated.</p><h2 id="further-information" tabindex="-1">Further Information <a class="header-anchor" href="#further-information" aria-label="Permalink to &quot;Further Information&quot;">​</a></h2><ul><li><a href="https://youtu.be/HkYRJJoyBwQ" target="_blank" rel="noreferrer">PX4 State Estimation Overview</a>, <em>PX4 Developer Summit 2019</em>, Dr. Paul Riseborough): Overview of the estimator, and major changes from 2018/19, and the expected improvements through 2019/20.</li></ul>',226)]))}const F=l(m,[["render",f]]);export{y as __pageData,F as default};
