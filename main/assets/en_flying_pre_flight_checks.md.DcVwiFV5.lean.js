import{_ as r,a as o}from"./chunks/qgc_arming_checks_ui.B7KVsQ6b.js";import{_ as n,c as s,a8 as a,G as i,a as l,B as h,o as c}from"./chunks/framework.BDnHobkS.js";const y=JSON.parse('{"title":"QGroundControl Flight-Readiness Status","description":"","frontmatter":{},"headers":[],"relativePath":"en/flying/pre_flight_checks.md","filePath":"en/flying/pre_flight_checks.md"}'),d={name:"en/flying/pre_flight_checks.md"};function m(u,e,f,g,p,b){const t=h("Badge");return c(),s("div",null,[e[0]||(e[0]=a('<h1 id="qgroundcontrol-flight-readiness-status" tabindex="-1">QGroundControl Flight-Readiness Status <a class="header-anchor" href="#qgroundcontrol-flight-readiness-status" aria-label="Permalink to &quot;QGroundControl Flight-Readiness Status&quot;">​</a></h1><p>PX4 performs a number of preflight sensor quality and estimator checks to determine if, for example, there is a good enough position estimate to fly the vehicle in the current mode, and will block arming if the vehicle is not ready.</p><p>QGroundControl can be used to determine whether the vehicle is ready to fly, and more importantly what checks are failing.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>You can also get readiness notifications from the <a href="./../getting_started/led_meanings.html">vehicle status LEDs</a> and <a href="./../getting_started/tunes.html">warning tunes</a>. However QGC is the only way to determine the precise reasons why PX4 will not arm.</p></div><h2 id="flight-readiness-status" tabindex="-1">Flight Readiness Status <a class="header-anchor" href="#flight-readiness-status" aria-label="Permalink to &quot;Flight Readiness Status&quot;">​</a></h2><p>The overall &quot;readiness to fly&quot; is displayed in QGroundControl in the top left corner near the <strong>Q</strong> menu icon, as shown below:</p><p><img src="'+r+'" alt="QGC flight readiness indicators from top left corner"></p><p>The three states are:</p><ul><li>&quot;Ready to Fly&quot; (Green background): The vehicle is ready to fly in all modes, and can be armed.</li><li>&quot;Ready to Fly&quot; (Amber background): The vehicle is ready to fly in the current mode and can be armed, but some check is failing that means it will not be able to switch to some other mode.</li><li>&quot;Not Ready&quot; (Amber background): The vehicle is not ready to fly in the current mode, and cannot be armed.</li></ul><h2 id="qgc-arming-check-report" tabindex="-1">QGC Arming Check Report <a class="header-anchor" href="#qgc-arming-check-report" aria-label="Permalink to &quot;QGC Arming Check Report&quot;">​</a></h2>',10)),i(t,{type:"tip",text:"PX4 v1.14"}),e[1]||(e[1]=l()),i(t,{type:"tip",text:"QGC v4.2.0"}),e[2]||(e[2]=a('<p>You can find out what prearming checks are failing using the QGroundControl <a href="https://docs.qgroundcontrol.com/master/en/qgc-user-guide/fly_view/fly_view.html#arm" target="_blank" rel="noreferrer">Arming Check Report</a> in <em>Fly View</em>. To access this UI select the <a href="#flight-readiness-status">Flight Readiness Status</a> indicator in the top left corner of QGroundControl&#39;s Fly View.</p><p><img src="'+o+'" alt="QGC Arming Check Report"></p><p>The Arming Check Report will then pop up and list all current warnings, with a toggle on the right of each warning that expands each entry with additional information and possible solutions.</p><p>Once each issue is resolved it will disappear from the UI. When all issues blocking arming have been removed you can use the arm button to display the arming confirmation slider, and arm the vehicle (or you can just take off).</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>The QGC Arming Checks UI is available in the QGC Daily Build (QGC v4.2.0 and later), and works with PX4 v1.14 and later.</p></div><h2 id="flight-logs" tabindex="-1">Flight Logs <a class="header-anchor" href="#flight-logs" aria-label="Permalink to &quot;Flight Logs&quot;">​</a></h2><p>Preflight errors are also reported in <em>QGroundControl</em> as <code>PREFLIGHT FAIL</code> messages. The <code>estimator_status.gps_check_fail_flags</code> message <a href="./../getting_started/flight_reporting.html">in the logs</a> shows which GPS quality checks are failing.</p><p>Note that the <a href="#qgc-arming-check-report">Arming Check Report</a> is a much easier way to determine reasons for failure, but the logs may be useful in versions prior to PX4 v1.14.</p><h2 id="ekf-preflight-checks-errors" tabindex="-1">EKF Preflight Checks/Errors <a class="header-anchor" href="#ekf-preflight-checks-errors" aria-label="Permalink to &quot;EKF Preflight Checks/Errors&quot;">​</a></h2><p>This sections lists errors, with associated checks and parameters, that are reported by the <a href="./../advanced_config/tuning_the_ecl_ekf.html">EKF</a> (and propagate to <em>QGroundControl</em>). These are provided for information only (the QGC Arming Checks UI is the best way to get error and solution information).</p><h4 id="preflight-fail-ekf-high-imu-accel-bias" tabindex="-1">PREFLIGHT FAIL: EKF HIGH IMU ACCEL BIAS <a class="header-anchor" href="#preflight-fail-ekf-high-imu-accel-bias" aria-label="Permalink to &quot;PREFLIGHT FAIL: EKF HIGH IMU ACCEL BIAS&quot;">​</a></h4><p>The EKF IMU acceleration bias is the difference between the measured acceleration reported by the IMU sensor and the expected acceleration reported by the EKF2 estimator (which fuses position and/or velocity data from a number of sources, including the IMU, GNSS, flow sensors etc.). This bias may change when the sensor is turned on (&quot;turn-on bias&quot;) and over time due to noise and temperature differences (&quot;in-run bias&quot;). The number should generally be very small (near zero), indicating that measurements from different sources all agree on the acceleration.</p><p>The warning indicates that the bias is higher than some arbitrary threshold (the vehicle will not be allowed to take off). It is most likely a sign that accelerometer or thermal calibration are required:</p><ul><li>If you <em>sometimes</em> get the warning: <a href="./../config/accelerometer.html">re-calibrate the accelerometer</a>.</li><li>If you get <em>regularly</em> get the warning: Perform a <a href="./../advanced_config/sensor_thermal_calibration.html">thermal calibration</a>.</li><li>If you still get the warning after thermal calibration (or you can&#39;t perform thermal calibration): <ul><li>Verify that the issues do not come from the sensor or autopilot hardware: <ul><li>The easiest way to do this is to test the same frame/sensors with another autopilot.</li><li>Alternatively, <a href="./../dev_log/logging.html#configuration">log and compare</a> all accelerometers across a number of bench test runs with <code>6: Sensor comparison</code> enabled in <a href="./../advanced_config/parameter_reference.html#SDLOG_PROFILE">SDLOG_PROFILE</a>.</li></ul></li><li>Attempt to change the accelerometer bias learning tuning parameters.</li></ul></li></ul><p>Increasing the parameters will make the autopilot less likely to detect an anomaly and can modify the stability of the estimator. However it may be required if there are problems with the sensor that cannot be fixed by other means (i.e you can tune the EKF for better performance, but there is no way you can calibrate the accelerometer &quot;better&quot;).</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Tuning these parameters is a last resort. It should only be attempted if you have data showing it will improve the performance of the estimator.</p></div><table tabindex="0"><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><a id="EKF2_ABL_LIM"></a><a href="./../advanced_config/parameter_reference.html#EKF2_ABL_LIM">EKF2_ABL_LIM</a></td><td>The maximum bias value that the EKF is allowed to estimate (above this value the bias will be clipped and EKF will attempt to reset itself, possibly even switching to a more healthy EKF with a working IMU in a multi-EKF system). The autopilot will report a &quot;high accel bias&quot; if the estimated bias exceeds 75% of this parameter during a preflight check and prevent takeoff. The current value of 0.4m/s2 is already quite high and increasing it would make the autopilot less likely to detect an issue.</td></tr><tr><td><a id="EKF2_ABIAS_INIT"></a><a href="./../advanced_config/parameter_reference.html#EKF2_ABIAS_INIT">EKF2_ABIAS_INIT</a></td><td>Initial bias uncertainty (if perfectly calibrated, this is related to the &quot;turn-on bias&quot; of the sensor). Some users might want to reduce that value if they know that the sensor is well calibrated and that the turn-on bias is small.</td></tr><tr><td><a id="EKF2_ACC_B_NOISE"></a><a href="./../advanced_config/parameter_reference.html#EKF2_ACC_B_NOISE">EKF2_ACC_B_NOISE</a></td><td>The expected &quot;in-run bias&quot; of the accelerometer or &quot;how fast do we expect the bias to change per second&quot;. By default, this value is large enough to include the drift due to a temperature change. If the IMU is temperature calibrated, the user might want to reduce this parameter.</td></tr><tr><td><a id="EKF2_ABL_ACCLIM"></a><a href="./../advanced_config/parameter_reference.html#EKF2_ABL_ACCLIM">EKF2_ABL_ACCLIM</a></td><td>The maximum acceleration at which the estimator will try to learn an acceleration bias. This is to prevent the estimator from learning a bias due to non-linearity and scale factor errors. (Almost no user should need to change that parameter except if they really know what they are doing).</td></tr></tbody></table><h4 id="preflight-fail-ekf-high-imu-gyro-bias" tabindex="-1">PREFLIGHT FAIL: EKF HIGH IMU GYRO BIAS <a class="header-anchor" href="#preflight-fail-ekf-high-imu-gyro-bias" aria-label="Permalink to &quot;PREFLIGHT FAIL: EKF HIGH IMU GYRO BIAS&quot;">​</a></h4><ul><li>This error is produced when the IMU gyro bias estimated by the EKF is excessive.</li><li>Excessive in this case means that the bias estimate exceeds 10deg/s (half the configured limit, which is hardcoded to 20deg/s).</li></ul><h4 id="preflight-fail-accel-sensors-inconsistent-check-calibration" tabindex="-1">PREFLIGHT FAIL: ACCEL SENSORS INCONSISTENT - CHECK CALIBRATION <a class="header-anchor" href="#preflight-fail-accel-sensors-inconsistent-check-calibration" aria-label="Permalink to &quot;PREFLIGHT FAIL: ACCEL SENSORS INCONSISTENT - CHECK CALIBRATION&quot;">​</a></h4><ul><li>This error message is produced when the acceleration measurements from different IMU units are inconsistent.</li><li>This check only applies to boards with more than one IMU.</li><li>The check is controlled by the <a href="./../advanced_config/parameter_reference.html#COM_ARM_IMU_ACC">COM_ARM_IMU_ACC</a> parameter.</li></ul><h4 id="preflight-fail-gyro-sensors-inconsistent-check-calibration" tabindex="-1">PREFLIGHT FAIL: GYRO SENSORS INCONSISTENT - CHECK CALIBRATION <a class="header-anchor" href="#preflight-fail-gyro-sensors-inconsistent-check-calibration" aria-label="Permalink to &quot;PREFLIGHT FAIL: GYRO SENSORS INCONSISTENT - CHECK CALIBRATION&quot;">​</a></h4><ul><li>This error message is produced when the angular rate measurements from different IMU units are inconsistent.</li><li>This check only applies to boards with more than one IMU.</li><li>The check is controlled by the <a href="./../advanced_config/parameter_reference.html#COM_ARM_IMU_GYR">COM_ARM_IMU_GYR</a> parameter.</li></ul><h4 id="preflight-fail-compass-sensors-inconsistent-check-calibration" tabindex="-1">PREFLIGHT FAIL: COMPASS SENSORS INCONSISTENT - CHECK CALIBRATION <a class="header-anchor" href="#preflight-fail-compass-sensors-inconsistent-check-calibration" aria-label="Permalink to &quot;PREFLIGHT FAIL: COMPASS SENSORS INCONSISTENT - CHECK CALIBRATION&quot;">​</a></h4><ul><li>This error message is produced when the difference in measurements from different compass sensors is too great.</li><li>It indicates bad calibration, orientation or magnetic interference.</li><li>This check only applies to when more than one compass/magnetometer is connected.</li><li>The check is controlled by the <a href="./../advanced_config/parameter_reference.html#COM_ARM_MAG_ANG">COM_ARM_MAG_ANG</a> parameter.</li></ul><h4 id="preflight-fail-ekf-internal-checks" tabindex="-1">PREFLIGHT FAIL: EKF INTERNAL CHECKS <a class="header-anchor" href="#preflight-fail-ekf-internal-checks" aria-label="Permalink to &quot;PREFLIGHT FAIL: EKF INTERNAL CHECKS&quot;">​</a></h4><ul><li>This error message is generated if the innovation magnitudes of either the horizontal GPS velocity, magnetic yaw, vertical GPS velocity or vertical position sensor (Baro by default but could be range finder or GPS if non-standard parameters are being used) are excessive. Innovations are the difference between the value predicted by the inertial navigation calculation and measured by the sensor.</li><li>Users should check the innovation levels in the log file to determine the cause. These can be found under the <code>ekf2_innovations</code> message. Common problems/solutions include: <ul><li>IMU drift on warmup. May be resolved by restarting the autopilot. May require an IMU accel and gyro calibration.</li><li>Adjacent magnetic interference combined with vehicle movement. Resolve my moving vehicle and waiting or re-powering.</li><li>Bad magnetometer calibration combined with vehicle movement. Resolve by recalibrating.</li><li>Initial shock or rapid movement on startup that caused a bad inertial nav solution. Resolve by restarting the vehicle and minimising movement for the first 5 seconds.</li></ul></li></ul><h2 id="other-parameters" tabindex="-1">Other Parameters <a class="header-anchor" href="#other-parameters" aria-label="Permalink to &quot;Other Parameters&quot;">​</a></h2><p>The following parameters also affect preflight checks.</p><h4 id="com-arm-wo-gps" tabindex="-1">COM_ARM_WO_GPS <a class="header-anchor" href="#com-arm-wo-gps" aria-label="Permalink to &quot;COM_ARM_WO_GPS&quot;">​</a></h4><p>The <a href="./../advanced_config/parameter_reference.html#COM_ARM_WO_GPS">COM_ARM_WO_GPS</a> parameter controls whether or not arming is allowed without a global position estimate.</p><ul><li><code>1</code> (default): Arming <em>is</em> allowed without a position estimate for flight modes that do not require position information (only).</li><li><code>0</code>: Arming is allowed only if EKF is providing a global position estimate and EFK GPS quality checks are passing</li></ul>',32))])}const C=n(d,[["render",m]]);export{y as __pageData,C as default};
