import{_ as t,c as o,o as r,al as n}from"./chunks/framework.CT7RwC5a.js";const u=JSON.parse('{"title":"TensorFlow Lite Micro (TFLM)","description":"","frontmatter":{},"headers":[],"relativePath":"ko/advanced/tflm.md","filePath":"ko/advanced/tflm.md"}'),a={name:"ko/advanced/tflm.md"};function i(l,e,c,d,s,h){return r(),o("div",null,e[0]||(e[0]=[n('<h1 id="tensorflow-lite-micro-tflm" tabindex="-1">TensorFlow Lite Micro (TFLM) <a class="header-anchor" href="#tensorflow-lite-micro-tflm" aria-label="Permalink to &quot;TensorFlow Lite Micro (TFLM)&quot;">​</a></h1><p>The PX4 <a href="./../advanced/neural_networks.html">Multicopter Neural Network</a> module (<a href="./../modules/modules_controller.html#mc-nn-control">mc_nn_control</a>) integrates a neural network that uses the <a href="https://github.com/tensorflow/tflite-micro" target="_blank" rel="noreferrer">TensorFlow Lite Micro (TFLM)</a> inference library.</p><p>This is a mature inference library intended for use on embedded devices, and is hence a suitable choice for PX4.</p><p>This guide explains how the TFLM library is integrated into the <a href="./../modules/modules_controller.html#mc-nn-control">mc_nn_control</a> module, and the changes you would have to make to use it for your own neural network.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>For more information, see the <a href="https://ai.google.dev/edge/litert/microcontrollers/get_started" target="_blank" rel="noreferrer">TFLM guide</a>.</p></div><h2 id="tlmf-nn-formats" tabindex="-1">TLMF NN Formats <a class="header-anchor" href="#tlmf-nn-formats" aria-label="Permalink to &quot;TLMF NN Formats&quot;">​</a></h2><p>TFLM uses networks in its own <a href="https://ai.google.dev/edge/litert/models/convert" target="_blank" rel="noreferrer">tflite format</a>. However, since many microcontrollers do not have native filesystem support, a tflite file can be converted to a C++ source and header file.</p><p>This is what is done in <code>mc_nn_control</code>. The tflight neural network is represented in code by the files <a href="https://github.com/PX4/PX4-Autopilot/blob/main/src/modules/mc_nn_control/control_net.cpp" target="_blank" rel="noreferrer"><code>control_net.cpp</code></a> and <a href="https://github.com/PX4/PX4-Autopilot/blob/main/src/modules/mc_nn_control/control_net.hpp" target="_blank" rel="noreferrer"><code>control_net.hpp</code></a>.</p><h3 id="getting-a-network-in-tflite-format" tabindex="-1">Getting a Network in tflite Format <a class="header-anchor" href="#getting-a-network-in-tflite-format" aria-label="Permalink to &quot;Getting a Network in tflite Format&quot;">​</a></h3><p>There are many online resource for generating networks in the <code>.tflite</code> format.</p><p>For this example we trained the network in the open source <a href="https://ntnu-arl.github.io/aerial_gym_simulator/" target="_blank" rel="noreferrer">Aerial Gym Simulator</a>. Aerial Gym includes a guide, and supports RL both for control and vision-based navigation tasks.</p><p>The project includes conversion code for <code>PyTorch -&gt; TFLM</code> in the <a href="https://github.com/ntnu-arl/aerial_gym_simulator/tree/main/resources/conversion" target="_blank" rel="noreferrer">resources/conversion</a> folder.</p><h3 id="updating-mc-nn-control-with-your-own-nn" tabindex="-1">Updating <code>mc_nn_control</code> with your own NN <a class="header-anchor" href="#updating-mc-nn-control-with-your-own-nn" aria-label="Permalink to &quot;Updating `mc_nn_control` with your own NN&quot;">​</a></h3><p>You can convert a <code>.tflite</code> network into a <code>.cc</code> file in the ubuntu terminal with this command:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">xxd</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> converted_model.tflite</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> model_data.cc</span></span></code></pre></div><p>You will then have to modify the <code>control_net.hpp</code> and <code>control_net.cpp</code> to include the data from <code>model_data.cc</code>:</p><ul><li>Take the size of the network in the bottom of the <code>.cc</code> file and replace the size in <code>control_net.hpp</code>.</li><li>Take the data in the model array in the <code>cc</code> file, and replace the ones in <code>control_net.cpp</code>.</li></ul><p>You are now ready to run your own network.</p><h2 id="code-explanation" tabindex="-1">Code Explanation <a class="header-anchor" href="#code-explanation" aria-label="Permalink to &quot;Code Explanation&quot;">​</a></h2><p>This section explains the code used to integrate the NN in <code>control_net.cpp</code>.</p><h3 id="operations-and-resolver" tabindex="-1">Operations and Resolver <a class="header-anchor" href="#operations-and-resolver" aria-label="Permalink to &quot;Operations and Resolver&quot;">​</a></h3><p>Firstly we need to create the resolver and load the needed operators to run inference on the NN. This is done in the top of <code>mc_nn_control.cpp</code>. The number in <code>MicroMutableOpResolver&lt;3&gt;</code> represents how many operations you need to run the inference.</p><p>A full list of the operators can be found in the <a href="https://github.com/tensorflow/tflite-micro/blob/main/tensorflow/lite/micro/micro_mutable_op_resolver.h" target="_blank" rel="noreferrer">micro_mutable_op_resolver.h</a> file. There are quite a few supported operators, but you will not find the most advanced ones. In the control example the network is fully connected so we use <code>AddFullyConnected()</code>. Then the activation function is ReLU, and we <code>AddAdd()</code> for the bias on each neuron.</p><h3 id="interpreter" tabindex="-1">Interpreter <a class="header-anchor" href="#interpreter" aria-label="Permalink to &quot;Interpreter&quot;">​</a></h3><p>In the <code>InitializeNetwork()</code> we start by setting up the model that we loaded from the source and header file. Next is to set up the interpreter, this code is taken from the TFLM documentation and is thoroughly explained there. The end state is that the <code>_control_interpreter</code> is set up to later run inference with the <code>Invoke()</code> member function. The <code>_input_tensor</code> is also defined, it is fetched from <code>_control_interpreter-&gt;input(0)</code>.</p><h3 id="입력" tabindex="-1">입력 <a class="header-anchor" href="#입력" aria-label="Permalink to &quot;입력&quot;">​</a></h3><p>The <code>_input_tensor</code> is filled in the <code>PopulateInputTensor()</code> function. <code>_input_tensor</code> works by accessing the <code>-&gt;data.f</code> member array and fill in the required inputs for your network. The inputs used in the control network is covered in <a href="./../advanced/neural_networks.html">Neural Networks</a>.</p><h3 id="출력" tabindex="-1">출력 <a class="header-anchor" href="#출력" aria-label="Permalink to &quot;출력&quot;">​</a></h3><p>For the outputs the approach is fairly similar to the inputs. After setting the correct inputs, calling the <code>Invoke()</code> function the outputs can be found by getting <code>_control_interpreter-&gt;output(0)</code>. And from the output tensor you get the <code>-&gt;data.f</code> array.</p>',29)]))}const m=t(a,[["render",i]]);export{u as __pageData,m as default};
