import{_ as a,a as s}from"./chunks/PX4_High-Level_Flight-Stack.B3u3dIZd.js";import{_ as t,c as i,a8 as o,o as n}from"./chunks/framework.BDnHobkS.js";const g=JSON.parse('{"title":"PX4 系统架构概述","description":"","frontmatter":{},"headers":[],"relativePath":"zh/concept/architecture.md","filePath":"zh/concept/architecture.md"}'),r={name:"zh/concept/architecture.md"};function l(h,e,d,p,c,u){return n(),i("div",null,e[0]||(e[0]=[o('<h1 id="px4-系统架构概述" tabindex="-1">PX4 系统架构概述 <a class="header-anchor" href="#px4-系统架构概述" aria-label="Permalink to &quot;PX4 系统架构概述&quot;">​</a></h1><p>PX4 consists of two main layers: the <a href="#flight-stack">flight stack</a> is an estimation and flight control system, and the <a href="#middleware">middleware</a> is a general robotics layer that can support any type of autonomous robot, providing internal/external communications and hardware integration.</p><p>All PX4 <a href="./../airframes/">airframes</a> share a single codebase (this includes other robotic systems like boats, rovers, submarines etc.). The complete system design is <a href="http://www.reactivemanifesto.org" target="_blank" rel="noreferrer">reactive</a>, which means that:</p><ul><li>所有的功能都可以被分割成若干可替换、可重复使用的部件。</li><li>通过异步消息传递进行通信。</li><li>系统可以应对不同的工作负载。</li></ul><p><a id="architecture"></a></p><h2 id="顶层软件架构" tabindex="-1">顶层软件架构 <a class="header-anchor" href="#顶层软件架构" aria-label="Permalink to &quot;顶层软件架构&quot;">​</a></h2><p>下面的架构图对 PX4 的各个积木模块以及各模块之间的联系进行了一个详细的概述。 图的上半部分包括了中间件模块，而下半部分展示的则是飞行控制栈的组件。</p><p><img src="'+a+'" alt="PX4 Architecture"></p><p>The source code is split into self-contained modules/programs (shown in <code>monospace</code> in the diagram). 通常来说一个图中的积木块对应一个功能模块。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>At runtime, you can inspect which modules are executed with the <code>top</code> command in shell, and each module can be started/stopped individually via <code>&lt;module_name&gt; start/stop</code>. While <code>top</code> command is specific to NuttX shell, the other commands can be used in the SITL shell (pxh&gt;) as well. For more information about each of these modules see the <a href="./../modules/modules_main.html">Modules &amp; Commands Reference</a>.</p></div><p>The arrows show the information flow for the <em>most important</em> connections between the modules. 使用 发布-订阅 消息总线这个方案意味着：</p><p>Modules communicate with each other through a publish-subscribe message bus named <a href="./../middleware/uorb.html">uORB</a>. 它包括了为固定翼、旋翼和 VTOL 无人机设计的控制器，以及相应的姿态、位置估计器。</p><ul><li>系统是 “响应式” 的 — 系统异步运行，新数据抵达时系统立即进行更新。</li><li>系统所有的活动和通信都是完全并行的。</li><li>系统组件在任何地方都可以在保证线程安全的情况下使用数据。</li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>This architecture allows every single one of these blocks to be rapidly and easily replaced, even at runtime.</p></div><h3 id="飞行控制栈" tabindex="-1">飞行控制栈 <a class="header-anchor" href="#飞行控制栈" aria-label="Permalink to &quot;飞行控制栈&quot;">​</a></h3><p>The flight stack is a collection of guidance, navigation and control algorithms for autonomous drones. It includes controllers for fixed-wing, multirotor and VTOL airframes as well as estimators for attitude and position.</p><p>The following diagram shows an overview of the building blocks of the flight stack. It contains the full pipeline from sensors, RC input and autonomous flight control (Navigator), down to the motor or servo control (Actuators).</p><p><img src="'+s+`" alt="PX4 High-Level Flight Stack"></p><p>An <strong>estimator</strong> takes one or more sensor inputs, combines them, and computes a vehicle state (for example the attitude from IMU sensor data).</p><p>A <strong>controller</strong> is a component that takes a setpoint and a measurement or estimated state (process variable) as input. Its goal is to adjust the value of the process variable such that it matches the setpoint. The output is a correction to eventually reach that setpoint. For example the position controller takes position setpoints as inputs, the process variable is the currently estimated position, and the output is an attitude and thrust setpoint that move the vehicle towards the desired position.</p><p>A <strong>mixer</strong> takes force commands (such as &quot;turn right&quot;) and translates them into individual motor commands, while ensuring that some limits are not exceeded. This translation is specific for a vehicle type and depends on various factors, such as the motor arrangements with respect to the center of gravity, or the vehicle&#39;s rotational inertia.</p><p><a id="middleware"></a></p><h3 id="中间件" tabindex="-1">中间件 <a class="header-anchor" href="#中间件" aria-label="Permalink to &quot;中间件&quot;">​</a></h3><p>The <a href="./../middleware/">middleware</a> consists primarily of device drivers for embedded sensors, communication with the external world (companion computer, GCS, etc.) and the uORB publish-subscribe message bus.</p><p>In addition, the middleware includes a <a href="./../simulation/">simulation layer</a> that allows PX4 flight code to run on a desktop operating system and control a computer modeled vehicle in a simulated &quot;world&quot;.</p><h2 id="更新速率" tabindex="-1">更新速率 <a class="header-anchor" href="#更新速率" aria-label="Permalink to &quot;更新速率&quot;">​</a></h2><p>Since the modules wait for message updates, typically the drivers define how fast a module updates. Most of the IMU drivers sample the data at 1kHz, integrate it and publish with 250Hz. Other parts of the system, such as the <code>navigator</code>, don&#39;t need such a high update rate, and thus run considerably slower.</p><p>The message update rates can be <a href="./../middleware/uorb.html">inspected</a> in real-time on the system by running <code>uorb top</code>.</p><p><a id="runtime-environment"></a></p><h2 id="运行时的环境" tabindex="-1">运行时的环境 <a class="header-anchor" href="#运行时的环境" aria-label="Permalink to &quot;运行时的环境&quot;">​</a></h2><p>PX4 runs on various operating systems that provide a POSIX-API (such as Linux, macOS, NuttX or QuRT). It should also have some form of real-time scheduling (e.g. FIFO).</p><p>The inter-module communication (using <a href="./../middleware/uorb.html">uORB</a>) is based on shared memory. The whole PX4 middleware runs in a single address space, i.e. memory is shared between all modules.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The system is designed such that with minimal effort it would be possible to run each module in separate address space (parts that would need to be changed include <code>uORB</code>, <code>parameter interface</code>, <code>dataman</code> and <code>perf</code>).</p></div><p>There are 2 different ways that a module can be executed:</p><ul><li><p><strong>Tasks</strong>: The module runs in its own task with its own stack and process priority.</p></li><li><p><strong>Work queue tasks</strong>: The module runs on a shared work queue, sharing the same stack and work queue thread priority as other modules on the queue.</p><ul><li>All the tasks must behave co-operatively as they cannot interrupt each other.</li><li>Multiple <em>work queue tasks</em> can run on a queue, and there can be multiple queues.</li><li>A <em>work queue task</em> is scheduled by specifying a fixed time in the future, or via uORB topic update callback.</li></ul><p>The advantage of running modules on a work queue is that it uses less RAM, and potentially results in fewer task switches. The disadvantages are that <em>work queue tasks</em> are not allowed to sleep or poll on a message, or do blocking IO (such as reading from a file). Long-running tasks (doing heavy computation) should potentially also run in a separate task or at least a separate work queue.</p></li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Tasks running on a work queue do not show up in <a href="./../modules/modules_command.html#top"><code>top</code></a> (only the work queues themselves can be seen - e.g. as <code>wq:lp_default</code>). Use <a href="./../modules/modules_system.html#work-queue"><code>work_queue status</code></a> to display all active work queue items.</p></div><h3 id="后台任务" tabindex="-1">后台任务 <a class="header-anchor" href="#后台任务" aria-label="Permalink to &quot;后台任务&quot;">​</a></h3><p><code>px4_task_spawn_cmd()</code> is used to launch new tasks (NuttX) or threads (POSIX - Linux/macOS) that run independently from the calling (parent) task:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">independent_task </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> px4_task_spawn_cmd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;commander&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    // Process name</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SCHED_DEFAULT,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                  // Scheduling type (RR or FIFO)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SCHED_PRIORITY_DEFAULT </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 40</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Scheduling priority</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    3600</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                           // Stack size of the new task or thread</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    commander_thread_main,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // Task (or thread) main function</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">argv[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // Void pointer to pass to the new task</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                    // (here the commandline arguments).</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    );</span></span></code></pre></div><h3 id="操作系统相关的信息" tabindex="-1">操作系统相关的信息 <a class="header-anchor" href="#操作系统相关的信息" aria-label="Permalink to &quot;操作系统相关的信息&quot;">​</a></h3><h4 id="nuttx" tabindex="-1">NuttX <a class="header-anchor" href="#nuttx" aria-label="Permalink to &quot;NuttX&quot;">​</a></h4><p><a href="https://nuttx.apache.org//" target="_blank" rel="noreferrer">NuttX</a> is the primary RTOS for running PX4 on a flight-control board. It is open source (BSD license), light-weight, efficient and very stable.</p><p>Modules are executed as tasks: they have their own file descriptor lists, but they share a single address space. A task can still start one or more threads that share the file descriptor list.</p><p>Each task/thread has a fixed-size stack, and there is a periodic task which checks that all stacks have enough free space left (based on stack coloring).</p><h4 id="linux-macos" tabindex="-1">Linux/MacOS <a class="header-anchor" href="#linux-macos" aria-label="Permalink to &quot;Linux/MacOS&quot;">​</a></h4><p>On Linux or macOS, PX4 runs in a single process, and the modules run in their own threads (there is no distinction between tasks and threads as on NuttX).</p>`,46)]))}const f=t(r,[["render",l]]);export{g as __pageData,f as default};
