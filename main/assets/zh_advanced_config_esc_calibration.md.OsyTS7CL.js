import{_ as e,a as o,b as a,c as i}from"./chunks/esc_calibration_step_4.D5cu-0g4.js";import{_ as s,c as r,a8 as n,o as l}from"./chunks/framework.BDnHobkS.js";const C=JSON.parse('{"title":"电调（ESC）校准","description":"","frontmatter":{},"headers":[],"relativePath":"zh/advanced_config/esc_calibration.md","filePath":"zh/advanced_config/esc_calibration.md"}'),p={name:"zh/advanced_config/esc_calibration.md"};function c(h,t,u,d,m,f){return l(),r("div",null,t[0]||(t[0]=[n('<h1 id="电调-esc-校准" tabindex="-1">电调（ESC）校准 <a class="header-anchor" href="#电调-esc-校准" aria-label="Permalink to &quot;电调（ESC）校准&quot;">​</a></h1><div class="info custom-block"><p class="custom-block-title">INFO</p><p>These instructions are only relevant to <a href="./../peripherals/pwm_escs_and_servo.html">PWM ESCs</a> and <a href="./../peripherals/oneshot.html">OneShot ESCs</a>. <a href="./../peripherals/dshot.html">DShot</a> and <a href="./../can/">CAN</a> ESCs (<a href="./../dronecan/escs.html">DroneCAN</a>/Cyphal) do not require this kind of calibration.</p></div><p>电子速度控制器(ESC)根据飞行控制器的输入指令（FC）调节电机的速度（和方向）。 电调响应的输入范围是可配置的，甚至同一型号的不同电调之间的默认范围也是不同的。</p><p>此校准将使用来自飞行控制器的固定最大值 (2000us) 和最小值 (1000us) PWM 输入更新所有ESC。 因此，载具上的所有ESC/电机都将以同样的方式在整个输入范围内响应飞行控制器的输入。</p><p>建议使用此工具校准支持它的所有 PWM/OneShot ESC。</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Calibration is particularly important for low-cost ESC, as they commonly vary a lot in their response to input.</p><p>然而，也建议为高质量的控制员提供这种校准。 即使这些都是工厂校准的，而且都应以同样的方式作出反应，但实际上输入范围可能有所不同。 例如，如果控制器在离开工厂后被手动校准，它可能不再以同样的方式动作。</p></div><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>If you want to use an ESC that does not support this calibration, then it must be factory calibrated and respond consistently out of the box. This should be verified using <a href="./../config/actuators.html#actuator-testing">Actuator Testing</a>. Jump to the <a href="#actuatorconfig_step">actuator configuration step (7)</a> (which is still important).</p></div><p>OneShot ESCs should be <a href="./../peripherals/oneshot.html#px4-configuration">configured to use OneShot</a> before calibration. 您应该在更换ESC后校准，即使您先前已校准。</p><h2 id="操作前提" tabindex="-1">操作前提 <a class="header-anchor" href="#操作前提" aria-label="Permalink to &quot;操作前提&quot;">​</a></h2><p>校准序列要求您能够保持飞行控制器的供电，同时手动对ESC进行上电循环。</p><p>如果使用 Pixhawk 飞行控制器，推荐这样做的方式是通过USB单独为飞行控制器提供电力。 并在需要时连接/断开ESC的电池供电。 Flight control systems that can&#39;t power the autopilot via USB will need a <a href="#problem_power_module">different approach</a>.</p><p>如果电池通过电源模块连接，校准程序可以检测电池连接并用它来驱动校准序列。 如果无法检测到电池电力，则根据超时执行校准顺序。</p><h2 id="步骤" tabindex="-1">步骤 <a class="header-anchor" href="#步骤" aria-label="Permalink to &quot;步骤&quot;">​</a></h2><p>校准电调：</p><ol><li><p>卸下螺旋桨。</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Never attempt ESC calibration with propellers on!</p><p>The motors <em>should</em> not spin during ESC calibration. 但是，如果校准是在ESC 已经供电后开始的，或者如果ESC 对校准序列支持/检测不恰当， 它将响应PWM的输入，以最大速度运行电机。</p></div></li></ol><p>:::</p><ol start="2"><li><p>Map the ESCs you&#39;re calibrating as motors in the vehicle&#39;s <a href="./../config/actuators.html">Actuator Configuration</a>. 只有映射的驱动器才能获得输出，并且只有被映射为电机的ESC将被校准。</p></li><li><p>拔下电池，断开ESC电源。 飞行控制器必须保持供电，例如将USB连接到地面站。</p></li><li><p>Open the <em>QGroundControl</em> <strong>Settings &gt; Power</strong>, then press the <strong>Calibrate</strong> button.</p><p><img src="'+e+'" alt="ESC Calibration step 1"></p></li><li><p>启动校准序列后，在没有错误的情况下，直接给 ESC供电 (您应该被提示):</p><p><img src="'+o+'" alt="ESC Calibration step 2"></p><p>校准将自动开始:</p><p><img src="'+a+'" alt="ESC Calibration step 3"></p></li><li><p>在校准过程中，您应该听到来自ESC的特定模型不同的嘀音，它表明校准的各个步骤。</p><p>校准完成后会提示您。</p><p><a id="actuatorconfig_step"></a><img src="'+i+'" alt="ESC Calibration step 4"></p></li><li><p>Go back to the <a href="./../config/actuators.html">Actuator Configuration</a> section.</p><p>在ESC 校准后，所有具有相同(重新)校准的 ESC的电机对同样的输入应以同样的方式动作。 驱动器配置中默认的 PWM 输出设置现在应该能开箱即用。</p><p>你需要确认电机确实正常工作。 由于默认配置值已经设置为保守的设置，您可能也希望调整它们以适用于您的特定的 ESC。</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The steps below are similar to those described in <a href="./../config/actuators.html#motor-configuration">Actuator Configuration &gt; Motor Configuration</a>.</p></div></li></ol><p>:::</p><p>验证以下值：</p><ul><li><p>The minimum value for a motor (default: <code>1100us</code>) should make the motor spin slowly but reliably, and also spin up reliably after it was stopped.</p><p>You can confirm that a motor spins at minimum (still without propellers) in <a href="./../config/actuators.html#actuator-testing">Actuator Testing</a>, by enabling the sliders, and then moving the test output slider for the motor to the first snap position from the bottom. 当你将滑块从解锁到最小值时，正确的值应该使电机立即和可靠地旋转。</p><p>要找到“最佳”最小值，请将滑块移动到底部(禁用)。 Then increase the PWM output&#39;s <code>disarmed</code> setting in small increments (e.g. 1025us, 1050us, etc), until the motor starts to spin reliably (it is better to be a little too high than a little too low). Enter this value into the <code>minimum</code> setting for all the motor PWM outputs, and restore the <code>disarmed</code> output to <code>1100us</code>.</p></li><li><p>The maximum value for a motor (default: <code>1900us</code>) should be chosen such that increasing the value doesn&#39;t make the motor spin any faster.</p><p>You can confirm that the motor spins quickly at the maximum setting in <a href="./../config/actuators.html#actuator-testing">Actuator Testing</a>, by moving the associated test output slider to the top position.</p><p>To find the &quot;optimal&quot; maximum value, first move the slider to the bottom (disarmed). Then increase the PWM output&#39;s <code>disarmed</code> setting to near the default maximum (<code>1900</code>) - the motors should spin up. Listen to the tone of the motor as you increase the PWM maximum value for the output in increments (e.g. 1925us, 1950us, etc). The optimal value is found at the point when the sound of the motors does not change as you increase the value of the output. Enter this value into the <code>maximum</code> setting for all the motor PWM outputs, and restore the <code>disarmed</code> output to <code>1100us</code>.</p></li><li><p>The disarmed value for a motor (default: <code>1000us</code>) should make the motor stop and stay stopped.</p><p>You can confirm this in <a href="./../config/actuators.html#actuator-testing">Actuator Testing</a> by moving the test output slider to the snap position at the bottom of the slider and observing that the motor does not spin.</p><p>If the ESC spins with the default value of 1000us then the ESC is not properly calibrated. If using an ESC that can&#39;t be calibrated, you should reduce the PWM output value for the output to below where the motor does not spin anymore (such as 950us or 900us).</p></li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>VTOL and fixed-wing motors do not need any special PWM configuration. With the default PWM configuration they will automatically stop during flight when commanded by the autopilot.</p></div><h2 id="故障处理" tabindex="-1">故障处理 <a class="header-anchor" href="#故障处理" aria-label="Permalink to &quot;故障处理&quot;">​</a></h2><ol><li><p>Calibration can state that it has succeeded even when it has failed.</p><p>This happens if you do not power the ESC at the right time, or the ESCs don&#39;t support calibration. This occurs because PX4 has no feedback from the ESC to know whether or not calibration was successful. You have to rely on interpreting the beeps during the calibration and subsequent motor tests to know for sure that the calibration worked.</p><p><a id="problem_power_module"></a></p></li><li><p>Calibration cannot be started if you have a power module configured and connected (for safety reasons).</p><p>Unplug power to the ESCs first. If you&#39;re blocked because a power module is necessary to keep your flight controller alive, but you can (un)power the ESCs separately, you can temporarily disable the detection of the power module just for the ESC calibration using the parameters <a href="./../advanced_config/parameter_reference.html#BAT1_SOURCE">BATn_SOURCE</a>. Once the power module that&#39;s powering the autopilot is not detected as battery anymore a timing based calibration is possible.</p></li><li><p>PX4 will abort calibration (for safety reasons) if the system detects an increase in current consumption immediately after initiating calibration. This requires a power module.</p></li></ol>',23)]))}const S=s(p,[["render",c]]);export{C as __pageData,S as default};
