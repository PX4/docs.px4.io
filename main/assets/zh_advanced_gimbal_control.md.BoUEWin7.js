import{_ as a,a as t}from"./chunks/gimbal-simulation.BPY5-Iet.js";import{_ as i,c as o,a8 as s,o as l}from"./chunks/framework.BDnHobkS.js";const b=JSON.parse('{"title":"云台配置","description":"","frontmatter":{},"headers":[],"relativePath":"zh/advanced/gimbal_control.md","filePath":"zh/advanced/gimbal_control.md"}'),n={name:"zh/advanced/gimbal_control.md"};function r(h,e,c,d,p,m){return l(),o("div",null,e[0]||(e[0]=[s('<h1 id="云台配置" tabindex="-1">云台配置 <a class="header-anchor" href="#云台配置" aria-label="Permalink to &quot;云台配置&quot;">​</a></h1><p>本页面介绍了如何配置及控制一个安装了相机（或其他任务载荷）的云台。</p><h2 id="综述" tabindex="-1">综述 <a class="header-anchor" href="#综述" aria-label="Permalink to &quot;综述&quot;">​</a></h2><p>PX4 包含了一个通用的挂载设备/云台的控制驱动，它含有多种输入输出方式。</p><ul><li>输入方式定义控制云台挂在的协议，该协议由 PX4 管理。 该输入方式可能是一个遥控器，一个可以发送 MAVLink 的地面站，或者两者 — 两种可以自动切换。</li><li>输出方式定义了 PX4 如何与连接的云台通信。 推荐的协议是 MAVLink v2, 但您也可以直接连接到飞控的 PWM 输出端口。</li></ul><p>PX4 接收输入信号，并且将其路由/翻译后发送到输出。 任何输入方式都可以被选择来驱动任何输出。</p><p>输入和输出都使用参数进行配置。 The input is set using the parameter <a href="./../advanced_config/parameter_reference.html#MNT_MODE_IN">MNT_MODE_IN</a>. By default this is set to <code>Disabled (-1)</code> and the driver does not run. 在选择输入模式后，请重新启动飞行器以启动挂载驱动程序。</p><p>You should set <code>MNT_MODE_IN</code> to one of: <code>RC (1)</code>, <code>MAVlink gimbal protocol v2 (4)</code> or <code>Auto (0)</code> (the other options are deprecated). If you select <code>Auto (0)</code>, the gimbal will automatically select either RC or or MAVLink input based on the latest input. 请注意，从 MAVLink 到 RC 的自动切换需要一个大幅度地杆量操作！</p><p>The output is set using the <a href="./../advanced_config/parameter_reference.html#MNT_MODE_OUT">MNT_MODE_OUT</a> parameter. By default the output is set to a PXM port (<code>AUX (0)</code>). If the <a href="https://mavlink.io/en/services/gimbal_v2.html" target="_blank" rel="noreferrer">MAVLink Gimbal Protocol v2</a> is supported by your gimbal, you should instead select <code>MAVLink gimbal protocol v2 (2)</code>.</p><p>The full list of parameters for setting up the mount driver can be found in <a href="./../advanced_config/parameter_reference.html#mount">Parameter Reference &gt; Mount</a>. 下面介绍了一些通用的云台相关设置。</p><h2 id="mavlink-云台-mnt-mode-out-mavlink" tabindex="-1">MAVLink 云台 (MNT_MODE_OUT=MAVLINK) <a class="header-anchor" href="#mavlink-云台-mnt-mode-out-mavlink" aria-label="Permalink to &quot;MAVLink 云台 (MNT_MODE_OUT=MAVLINK)&quot;">​</a></h2><p>系统上的每个物理云台装置必须有自己的高级云台管理器， 地面站通过使用MAVLink云台协议发现它。 The ground station sends high level <a href="https://mavlink.io/en/services/gimbal_v2.html#gimbal-manager-messages" target="_blank" rel="noreferrer">MAVLink Gimbal Manager</a> commands to the manager of the gimbal it wants to control, and the manager will in turn send appropriate lower level &quot;gimbal device&quot; commands to control the gimbal.</p><p>PX4 can be configured as the gimbal manager to control a single gimbal device (which can either be physically connected or be a MAVLink gimbal that implements the <a href="https://mavlink.io/en/services/gimbal_v2.html#gimbal-device-messages" target="_blank" rel="noreferrer">gimbal device interface</a>).</p><p>To enable a MAVLink gimbal, first set parameter <a href="./../advanced_config/parameter_reference.html#MNT_MODE_IN">MNT_MODE_IN</a> to <code>MAVlink gimbal protocol v2</code> and <a href="./../advanced_config/parameter_reference.html#MNT_MODE_OUT">MNT_MODE_OUT</a> to <code>MAVLink gimbal protocol v2</code>.</p><p>The gimbal can be connected to <em>any free serial port</em> using the instructions in <a href="./../peripherals/mavlink_peripherals.html">MAVLink Peripherals (GCS/OSD/Companion)</a> (also see <a href="./../peripherals/serial_configuration.html#serial-port-configuration">Serial Port Configuration</a>). For example, if the <code>TELEM2</code> port on the flight controller is unused you can connect it to the gimbal and set the following PX4 parameters:</p><ul><li><a href="./../advanced_config/parameter_reference.html#MAV_1_CONFIG">MAV_1_CONFIG</a> to <strong>TELEM2</strong> (if <code>MAV_1_CONFIG</code> is already used for a companion computer (say), use <code>MAV_2_CONFIG</code>).</li><li><a href="./../advanced_config/parameter_reference.html#MAV_1_MODE">MAV_1_MODE</a> to <strong>NORMAL</strong></li><li><a href="./../advanced_config/parameter_reference.html#SER_TEL2_BAUD">SER_TEL2_BAUD</a> to manufacturer recommended baud rate.</li></ul><h3 id="多云台支持" tabindex="-1">多云台支持 <a class="header-anchor" href="#多云台支持" aria-label="Permalink to &quot;多云台支持&quot;">​</a></h3><p>PX4 可以自动为已连接的 PWM 云台或第一个在任何接口上检测到相同的系统 id 的 MAVLink 云台设备创建一个云台管理器。 它不会自动为它检测到的其他MAVLink云台设备创建云台管理器。</p><p>您可以支持额外的云台 ，但他们必须：</p><ul><li>implement the gimbal <em>manager</em> protocol</li><li>在 MAVLink 网络上对地面站和 PX4 可见。 这可能需要在PX4、GCS和云台之间配置流量转接。</li><li>每个云台必须有一个独特的ID。 对于已连接的 PWM 云台，这将是飞控系统的组件 ID</li></ul><h2 id="飞控-pwm-输出上的云台-mnt-mode-out-aux" tabindex="-1">飞控 PWM 输出上的云台 (MNT_MODE_OUT=AUX) <a class="header-anchor" href="#飞控-pwm-输出上的云台-mnt-mode-out-aux" aria-label="Permalink to &quot;飞控 PWM 输出上的云台 (MNT_MODE_OUT=AUX)&quot;">​</a></h2><p>The gimbal can also be controlled by connecting it to up to three flight controller PWM ports and setting the output mode to <code>MNT_MODE_OUT=AUX</code>.</p><p>The output pins that are used to control the gimbal are set in the <a href="./../config/actuators.html#actuator-outputs">Acuator Configuration &gt; Outputs</a> by selecting any three unused Actuator Outputs and assigning them the following output functions:</p><ul><li><code>Gimbal Roll</code>: Output controls gimbal roll.</li><li><code>Gimbal Pitch</code>: Output controls Gimbal pitch.</li><li><code>Gimbal Yaw</code>: Output controls Gimbal yaw.</li></ul><p>例如，您可能有以下设置可将云台滚动、俯仰和偏航分配给 AUX1-3 输出。</p><p><img src="'+a+'" alt="Gimbal Actuator config"></p><p>The PWM values to use for the disarmed, maximum and minimum values can be determined in the same way as other servo, using the <a href="./../config/actuators.html#actuator-testing">Actuator Test sliders</a> to confirm that each slider moves the appropriate axis, and changing the values so that the gimbal is in the appropriate position at the disarmed, low and high position in the slider. 这些数值也可以在云台文档中提供。</p><h2 id="sitl-software-in-the-loop" tabindex="-1">SITL (Software In The Loop) <a class="header-anchor" href="#sitl-software-in-the-loop" aria-label="Permalink to &quot;SITL (Software In The Loop)&quot;">​</a></h2><p>The <a href="./../sim_gazebo_classic/">Gazebo Classic</a> simulation <a href="./../sim_gazebo_classic/vehicles.html#typhoon-h480-hexrotor">Typhoon H480 model</a> comes with a preconfigured simulated gimbal.</p><p>要运行它，请使用：</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> px4_sitl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gazebo-classic_typhoon_h480</span></span></code></pre></div><p>To just test the <a href="./../modules/modules_driver.html#gimbal">gimbal driver</a> on other models or simulators, make sure the driver runs (using <code>gimbal start</code>), then configure its parameters.</p><h2 id="测试" tabindex="-1">测试 <a class="header-anchor" href="#测试" aria-label="Permalink to &quot;测试&quot;">​</a></h2><p>该驱动程序提供了一个简单的测试命令。 接下来描述了在 SITL 中的测试方式，但是这些指令也可以在真实的设备中使用。</p><p>使用下面这条指令开始仿真（不需要修改任何参数）：</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> px4_sitl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gazebo-classic_typhoon_h480</span></span></code></pre></div><p>Make sure it&#39;s armed, eg. with <code>commander takeoff</code>, then use the following command to control the gimbal (for example):</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gimbal</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> yaw</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 30</span></span></code></pre></div><p>Note that the simulated gimbal stabilizes itself, so if you send MAVLink commands, set the <code>stabilize</code> flags to <code>false</code>.</p><p><img src="'+t+'" alt="Gazebo Gimbal Simulation"></p>',40)]))}const f=i(n,[["render",r]]);export{b as __pageData,f as default};
