import{_ as t}from"./chunks/position_fixed.t2m3lWVy.js";import{_ as i}from"./chunks/rtl_cone.DihiHN2J.js";import{_ as a,c as n,a8 as o,o as r}from"./chunks/framework.BDnHobkS.js";const g=JSON.parse('{"title":"Return Mode (Generic Vehicle)","description":"","frontmatter":{},"headers":[],"relativePath":"en/flight_modes/return.md","filePath":"en/flight_modes/return.md"}'),l={name:"en/flight_modes/return.md"};function s(d,e,h,m,c,u){return r(),n("div",null,e[0]||(e[0]=[o('<h1 id="return-mode-generic-vehicle" tabindex="-1">Return Mode (Generic Vehicle) <a class="header-anchor" href="#return-mode-generic-vehicle" aria-label="Permalink to &quot;Return Mode (Generic Vehicle)&quot;">​</a></h1><img src="'+t+'" title="Position fix required (e.g. GPS)" width="30px"><p>The <em>Return</em> flight mode is used to <em>fly a vehicle to safety</em> on an unobstructed path to a safe destination, where it should land.</p><p>The following topics should be read first if you&#39;re using these vehicle types:</p><ul><li><a href="./../flight_modes_mc/return.html">Multicopter</a></li><li><a href="./../flight_modes_fw/return.html">Fixed-wing (Plane)</a></li><li><a href="./../flight_modes_vtol/return.html">VTOL</a></li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><ul><li>Mode is automatic - no user intervention is <em>required</em> to control the vehicle.</li><li>Mode requires a global 3d position estimate (from GPS or inferred from a <a href="./../ros/external_position_estimation.html#enabling-auto-modes-with-a-local-position">local position</a>). <ul><li>Flying vehicles can&#39;t switch to this mode without global position.</li><li>Flying vehicles will failsafe if they lose the position estimate.</li></ul></li><li>Mode requires home position is set.</li><li>Mode prevents arming (vehicle must be armed when switching to this mode).</li><li>RC control switches can be used to change flight modes on any vehicle.</li><li>RC stick movement in a multicopter (or VTOL in multicopter mode) will <a href="#COM_RC_OVERRIDE">by default</a> change the vehicle to <a href="./../flight_modes_mc/position.html">Position mode</a> unless handling a critical battery failsafe.</li><li>A VTOL will return as MC or FW based on its mode at the point the return mode was triggered. In MC mode it will respect multicopter parameters, such as the landing &quot;cone&quot;. In FW mode it will respect fixed-wing parameters (ignore the cone), but unless using a mission landing, will transition to MC mode and land at the destination after loitering at the descent altitude.</li></ul></div><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>PX4 provides several mechanisms for choosing a safe return path, destination and landing, including using home location, rally (&quot;safe&quot;) points, mission paths, and landing sequences defined in a mission.</p><p>All vehicles <em>nominally</em> support all of these mechanisms, but not all of them make as much sense for particular vehicles. For example, a multicopter can land virtually anywhere, so it doesn&#39;t make sense for it to use a landing sequence except in rare cases. Similarly, a fixed-wing vehicle needs to fly a safe landing path: it can use the home location as a return point, but it won&#39;t try and land on it by default.</p><p>This topic covers all the possible return types that any vehicle <em>might</em> be configured to use — the vehicle-specific return mode topics cover the default/recommended return type and configuration for each vehicle.</p><p>The following sections explain how to configure the <a href="#return_types">return type</a>, <a href="#minimum-return-altitude">minimum return altitude</a> and <a href="#loiter-landing-at-destination">landing/arrival behaviour</a>.</p><p><a id="return_types"></a></p><h2 id="return-types-rtl-type" tabindex="-1">Return Types (RTL_TYPE) <a class="header-anchor" href="#return-types-rtl-type" aria-label="Permalink to &quot;Return Types (RTL_TYPE)&quot;">​</a></h2><p>PX4 provides four alternative approaches for finding an unobstructed path to a safe destination and/or landing, which are set using the <a href="#RTL_TYPE">RTL_TYPE</a> parameter.</p><p>At high level these are:</p><ul><li><a href="#home_return">Home/rally point return</a> (<code>RTL_TYPE=0</code>): Ascend to safe altitude and return via a direct path to the closest rally point or home location.</li><li><a href="#mission-landing-rally-point-return-type-rtl-type-1">Mission landing/rally point return</a> (<code>RTL_TYPE=1</code>): Ascend to a safe altitude, fly direct to the closest destination <em>other than home</em>: rally point or start of mission landing. If no mission landing or rally points are defined, return home via direct path.</li><li><a href="#mission-path-return-type-rtl-type-2">Mission path return</a> (<code>RTL_TYPE=2</code>): Use mission path and fast-continue to mission landing (if defined). If no mission <em>landing</em> defined, fast-reverse mission to home. If no <em>mission</em> defined, return direct to home (rally points are ignored).</li><li><a href="#closest-safe-destination-return-type-rtl-type-3">Closest safe destination return</a> (<code>RTL_TYPE=3</code>): Ascend to a safe altitude and return via direct path to closest destination: home, start of mission landing pattern, or rally point. If the destination is a mission landing pattern, follow the pattern to land.</li></ul><p>More detailed explanations for each of the types are provided in the following sections.</p><p><a id="home_return"></a></p><h3 id="home-rally-point-return-type-rtl-type-0" tabindex="-1">Home/Rally Point Return Type (RTL_TYPE=0) <a class="header-anchor" href="#home-rally-point-return-type-rtl-type-0" aria-label="Permalink to &quot;Home/Rally Point Return Type (RTL_TYPE=0)&quot;">​</a></h3><p>This is the default return type for a <a href="./../flight_modes_mc/return.html">multicopter</a> (see topic for more information).</p><p>In this return type the vehicle:</p><ul><li>Ascends to a safe <a href="#minimum-return-altitude">minimum return altitude</a> (above any expected obstacles).</li><li>Flies via direct path to the home position or a rally point (whichever is closest)</li><li>On <a href="#loiter-landing-at-destination">arrival</a> descends to &quot;descent altitude&quot; and waits for a configurable time. This time may be used to deploy landing gear.</li><li>Lands or waits (this depends on landing parameters), By default an MC or VTOL in MC mode will land and a fixed-wing vehicle circles at the descent altitude. A VTOL in FW mode aligns its heading to the destination point, transitions to MC mode, and then lands.</li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>If no rally points are defined, this is the same as a <em>Return to Launch</em> (RTL)/<em>Return to Home</em> (RTH).</p></div><h3 id="mission-landing-rally-point-return-type-rtl-type-1" tabindex="-1">Mission Landing/Rally Point Return Type (RTL_TYPE=1) <a class="header-anchor" href="#mission-landing-rally-point-return-type-rtl-type-1" aria-label="Permalink to &quot;Mission Landing/Rally Point Return Type (RTL_TYPE=1)&quot;">​</a></h3><p>This is the default return type for a <a href="./../flight_modes_fw/return.html">fixed-wing</a> or <a href="./../flight_modes_vtol/return.html">VTOL</a> vehicle (see topics for more information).</p><p>In this return type the vehicle:</p><ul><li>Ascends to a safe <a href="#minimum-return-altitude">minimum return altitude</a> (above any expected obstacles) if needed. The vehicle maintains its initial altitude if that is higher than the minimum return altitude.</li><li>Flies via direct constant-altitude path to a rally point or the start of a <a href="#mission-landing-pattern">mission landing pattern</a> (whichever is closest). If no mission landing or rally points are defined the vehicle instead returns home via a direct path.</li><li>If the destination is a mission landing pattern it will follow the pattern to land.</li><li>If the destination is a rally point or home it will <a href="#loiter-landing-at-destination">land or wait</a> at descent altitude (depending on landing parameters). By default an MC or VTOL in MC mode will land, and a fixed-wing vehicle circles at the descent altitude. A VTOL in FW mode aligns its heading to the destination point, transitions to MC mode, and then lands.</li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Fixed wing vehicles commonly also set <a href="#MIS_TKO_LAND_REQ">MIS_TKO_LAND_REQ</a> to <em>require</em> a mission landing pattern.</p></div><h3 id="mission-path-return-type-rtl-type-2" tabindex="-1">Mission Path Return Type (RTL_TYPE=2) <a class="header-anchor" href="#mission-path-return-type-rtl-type-2" aria-label="Permalink to &quot;Mission Path Return Type (RTL_TYPE=2)&quot;">​</a></h3><p>This return type uses the mission (if defined) to provide a safe return <em>path</em>, and the <a href="#mission-landing-pattern">mission landing pattern</a> (if defined) to provide landing behaviour. If there is a mission but no mission landing pattern, the mission is flown <em>in reverse</em>. Rally points, if any, are ignored.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The behaviour is fairly complex because it depends on the flight mode, and whether a mission and mission landing are defined.</p></div><p>Mission <em>with</em> landing pattern:</p><ul><li><strong>Mission mode:</strong> Mission is continued in &quot;fast-forward mode&quot; (jumps, delay and any other non-position commands ignored, loiter and other position waypoints converted to simple waypoints) and then lands.</li><li><strong>Auto mode other than mission mode:</strong><ul><li>Ascend to a safe <a href="#minimum-return-altitude">minimum return altitude</a> above any expected obstacles.</li><li>Fly directly to closest waypoint (for FW not a landing WP) and descend to waypoint altitude.</li><li>Continue mission in fast forward mode from that waypoint.</li></ul></li><li><strong>Manual modes:</strong><ul><li>Ascend to a safe <a href="#minimum-return-altitude">minimum return altitude</a> above any expected obstacles.</li><li>Fly directly to landing sequence position and descend to waypoint altitude</li><li>Land using mission landing pattern</li></ul></li></ul><p>Mission <em>without</em> landing pattern defined:</p><ul><li><strong>Mission mode:</strong><ul><li>Mission flown &quot;fast-backward&quot; (in reverse) starting from the previous waypoint <ul><li>Jumps, delay and any other non-position commands ignored, loiter and other position waypoints converted to simple waypoints.</li><li>VTOL vehicles transition to FW mode (if needed) before flying the mission in reverse.</li></ul></li><li>On reaching waypoint 1, the vehicle ascends to the <a href="#minimum-return-altitude">minimum return altitude</a> and flies to the home position (where it <a href="#loiter-landing-at-destination">lands or waits</a>).</li></ul></li><li><strong>Auto mode other than mission mode:</strong><ul><li>Fly directly to closest waypoint (for FW not a landing WP) and descend to waypoint altitude.</li><li>Continue the mission in reverse, exactly as though Return mode was triggered in mission mode (above)</li></ul></li><li><strong>Manual modes:</strong> Fly directly to home location and land.</li></ul><p>If no mission is defined PX4 will fly directly to home location and land (rally points are ignored).</p><p>If the mission changes during return mode, then the behaviour is re-evaluated based on the new mission following the same rules as above (e.g. if the new mission has no landing sequence and you&#39;re in a mission, the mission is reversed).</p><h3 id="closest-safe-destination-return-type-rtl-type-3" tabindex="-1">Closest Safe Destination Return Type (RTL_TYPE=3) <a class="header-anchor" href="#closest-safe-destination-return-type-rtl-type-3" aria-label="Permalink to &quot;Closest Safe Destination Return Type (RTL_TYPE=3)&quot;">​</a></h3><p>In this return type the vehicle:</p><ul><li>Ascends to a safe <a href="#minimum-return-altitude">minimum return altitude</a> (above any expected obstacles).</li><li>Flies a direct path to the closest destination of: home location, mission landing pattern or rally point.</li><li>If the destination is a <a href="#mission-landing-pattern">mission landing pattern</a> the vehicle will follow the pattern to land.</li><li>If the destination is a home location or rally point, the vehicle will descend to the descent altitude (<a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a>) and then <a href="#loiter-landing-at-destination">lands or waits</a>. By default an MC or VTOL in MC mode will land, and a fixed-wing vehicle circles at the descent altitude. A VTOL in FW mode aligns its heading to the destination point, transitions to MC mode, and then lands.</li></ul><h2 id="minimum-return-altitude" tabindex="-1">Minimum Return Altitude <a class="header-anchor" href="#minimum-return-altitude" aria-label="Permalink to &quot;Minimum Return Altitude&quot;">​</a></h2><p>For most <a href="#return_types">return types</a> a vehicle will ascend to a <em>minimum safe altitude</em> before returning (unless already above that altitude), in order to avoid any obstacles between it and the destination.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The exception is when executing a <a href="#mission-path-return-type-rtl-type-2">mission path return</a> from <em>within a mission</em>. In this case the vehicle follows mission waypoints, which we assume are planned to avoid any obstacles.</p></div><p>The return altitude for a fixed-wing vehicle or a VTOL in fixed-wing mode is configured using the parameter <a href="#RTL_RETURN_ALT">RTL_RETURN_ALT</a> (does not use the code described in the next paragraph).</p><p>The return altitude for a multicopter or a VTOL vehicles in MC mode is configured using the parameters <a href="#RTL_RETURN_ALT">RTL_RETURN_ALT</a> and <a href="#RTL_CONE_ANG">RTL_CONE_ANG</a>, which define a half cone centered around the destination (home location or safety point).</p><p><img src="'+i+'" alt="Return mode cone"></p><p>If the vehicle is:</p><ul><li>Above <a href="#RTL_RETURN_ALT">RTL_RETURN_ALT</a> (1) it will return at its current altitude.</li><li>Below the cone it will return where it intersects the cone (2) or <a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a> (whichever is higher).</li><li>Outside the cone (3) it will first climb until it reaches <a href="#RTL_RETURN_ALT">RTL_RETURN_ALT</a>.</li><li>Inside the cone: <ul><li>Above <a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a> (4) it will return at its current altitude.</li><li>Below <a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a> (5) it will first ascend to <code>RTL_DESCEND_ALT</code>.</li></ul></li></ul><p>Note:</p><ul><li>If <a href="#RTL_CONE_ANG">RTL_CONE_ANG</a> is 0 degrees there is no &quot;cone&quot;: <ul><li>the vehicle returns at <code>RTL_RETURN_ALT</code> (or above).</li></ul></li><li>If <a href="#RTL_CONE_ANG">RTL_CONE_ANG</a> is 90 degrees the vehicle will return at the greater of <code>RTL_DESCEND_ALT</code> and the current altitude.</li><li>The vehicle will always ascend at least <a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a> for the return.</li></ul><h2 id="loiter-landing-at-destination" tabindex="-1">Loiter/Landing at Destination <a class="header-anchor" href="#loiter-landing-at-destination" aria-label="Permalink to &quot;Loiter/Landing at Destination&quot;">​</a></h2><p>Unless executing a <a href="#mission-landing-pattern">mission landing pattern</a> as part of the return mode, the vehicle will arrive at its destination, and rapidly descend to the <a href="#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a> altitude, where it will loiter for <a href="#RTL_LAND_DELAY">RTL_LAND_DELAY</a> before landing. If <code>RTL_LAND_DELAY=-1</code> it will loiter indefinitely.</p><p>The default landing configuration is vehicle dependent:</p><ul><li>Multicopters are configured to hover for a short while, deploying landing gear if needed, and then land.</li><li>Fixed-wing vehicles use a return mode with a <a href="#mission-landing-pattern">mission landing pattern</a>, as this enables automated landing. If not using a mission landing, the default configuration is to loiter indefinitely, so the user can take over and manually land.</li><li>VTOLs in MC mode fly and land exactly as a multicopter.</li><li>VTOLS in FW mode head towards the landing point, transition to MC mode, and then land on the destination.</li></ul><h2 id="mission-landing-pattern" tabindex="-1">Mission Landing Pattern <a class="header-anchor" href="#mission-landing-pattern" aria-label="Permalink to &quot;Mission Landing Pattern&quot;">​</a></h2><p>A mission landing pattern is a landing pattern defined as part of a mission plan. This consists of a <a href="https://mavlink.io/en/messages/common.html#MAV_CMD_DO_LAND_START" target="_blank" rel="noreferrer">MAV_CMD_DO_LAND_START</a>, one or more position waypoints, and a <a href="https://mavlink.io/en/messages/common.html#MAV_CMD_NAV_LAND" target="_blank" rel="noreferrer">MAV_CMD_NAV_LAND</a> (or <a href="https://mavlink.io/en/messages/common.html#MAV_CMD_NAV_VTOL_LAND" target="_blank" rel="noreferrer">MAV_CMD_NAV_VTOL_LAND</a> for a VTOL Vehicle).</p><p>Landing patterns defined in missions are the safest way to automatically land a <em>fixed-wing</em> vehicle on PX4. For this reason fixed-wing vehicles are configured to use <a href="#mission-landing-rally-point-return-type-rtl-type-1">Mission landing/really point return</a> by default.</p><h2 id="parameters" tabindex="-1">Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;Parameters&quot;">​</a></h2><p>The RTL parameters are listed in <a href="./../advanced_config/parameter_reference.html#return-mode">Parameter Reference &gt; Return Mode</a> (and summarised below).</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><a id="RTL_TYPE"></a><a href="./../advanced_config/parameter_reference.html#RTL_TYPE">RTL_TYPE</a></td><td>Return mechanism (path and destination).<br><code>0</code>: Return to a rally point or home (whichever is closest) via direct path.<br><code>1</code>: Return to a rally point or the mission landing pattern start point (whichever is closest), via direct path. If neither mission landing or rally points are defined return home via a direct path. If the destination is a mission landing pattern, follow the pattern to land.<br><code>2</code>: Use mission path fast-forward to landing if a landing pattern is defined, otherwise fast-reverse to home. Ignore rally points. Fly direct to home if no mission plan is defined.<br><code>3</code>: Return via direct path to closest destination: home, start of mission landing pattern or safe point. If the destination is a mission landing pattern, follow the pattern to land.</td></tr><tr><td><a id="RTL_RETURN_ALT"></a><a href="./../advanced_config/parameter_reference.html#RTL_RETURN_ALT">RTL_RETURN_ALT</a></td><td>Return altitude in meters (default: 60m) when <a href="./../advanced_config/parameter_reference.html#RTL_CONE_ANG">RTL_CONE_ANG</a> is 0. If already above this value the vehicle will return at its current altitude.</td></tr><tr><td><a id="RTL_DESCEND_ALT"></a><a href="./../advanced_config/parameter_reference.html#RTL_DESCEND_ALT">RTL_DESCEND_ALT</a></td><td>Minimum return altitude and altitude at which the vehicle will slow or stop its initial descent from a higher return altitude (default: 30m)</td></tr><tr><td><a id="RTL_LAND_DELAY"></a><a href="./../advanced_config/parameter_reference.html#RTL_LAND_DELAY">RTL_LAND_DELAY</a></td><td>Time to wait at <code>RTL_DESCEND_ALT</code> before landing (default: 0.5s) -by default this period is short so that the vehicle will simply slow and then land immediately. If set to -1 the system will loiter at <code>RTL_DESCEND_ALT</code> rather than landing. The delay is provided to allow you to configure time for landing gear to be deployed (triggered automatically).</td></tr><tr><td><a id="RTL_MIN_DIST"></a><a href="./../advanced_config/parameter_reference.html#RTL_MIN_DIST">RTL_MIN_DIST</a></td><td>Minimum horizontal distance from home position to trigger ascent to the return altitude specified by the &quot;cone&quot;. If the vehicle is horizontally closer than this distance to home, it will return at its current altitude or <code>RTL_DESCEND_ALT</code> (whichever is higher) instead of first ascending to RTL_RETURN_ALT.</td></tr><tr><td><a id="RTL_CONE_ANG"></a><a href="./../advanced_config/parameter_reference.html#RTL_CONE_ANG">RTL_CONE_ANG</a></td><td>Half-angle of the cone that defines the vehicle RTL return altitude. Values (in degrees): 0, 25, 45, 65, 80, 90. Note that 0 is &quot;no cone&quot; (always return at <code>RTL_RETURN_ALT</code> or higher), while 90 indicates that the vehicle must return at the current altitude or <code>RTL_DESCEND_ALT</code> (whichever is higher).</td></tr><tr><td><a id="COM_RC_OVERRIDE"></a><a href="./../advanced_config/parameter_reference.html#COM_RC_OVERRIDE">COM_RC_OVERRIDE</a></td><td>Controls whether stick movement on a multicopter (or VTOL in MC mode) causes a mode change to <a href="./../flight_modes_mc/position.html">Position mode</a> (except when vehicle is handling a critical battery failsafe). This can be separately enabled for auto modes and for offboard mode, and is enabled in auto modes by default.</td></tr><tr><td><a id="COM_RC_STICK_OV"></a><a href="./../advanced_config/parameter_reference.html#COM_RC_STICK_OV">COM_RC_STICK_OV</a></td><td>The amount of stick movement that causes a transition to <a href="./../flight_modes_mc/position.html">Position mode</a> (if <a href="#COM_RC_OVERRIDE">COM_RC_OVERRIDE</a> is enabled).</td></tr><tr><td><a id="RTL_LOITER_RAD"></a><a href="./../advanced_config/parameter_reference.html#RTL_LOITER_RAD">RTL_LOITER_RAD</a></td><td>[Fixed-wing Only] The radius of the loiter circle (at <a href="#RTL_LAND_DELAY">RTL_LAND_DELAY</a>).</td></tr><tr><td><a id="MIS_TKO_LAND_REQ"></a><a href="./../advanced_config/parameter_reference.html#MIS_TKO_LAND_REQ">MIS_TKO_LAND_REQ</a></td><td>Specify whether a mission landing or takeoff pattern is <em>required</em>. Generally fixed-wing vehicles set this to require a landing pattern but VTOL do not.</td></tr></tbody></table>',60)]))}const T=a(l,[["render",s]]);export{g as __pageData,T as default};
