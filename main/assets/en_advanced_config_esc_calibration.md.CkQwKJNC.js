import{_ as e,a as o,b as a,c as i}from"./chunks/esc_calibration_step_4.D5cu-0g4.js";import{_ as r,c as n,a8 as s,o as l}from"./chunks/framework.BDnHobkS.js";const y=JSON.parse('{"title":"ESC Calibration","description":"","frontmatter":{},"headers":[],"relativePath":"en/advanced_config/esc_calibration.md","filePath":"en/advanced_config/esc_calibration.md"}'),h={name:"en/advanced_config/esc_calibration.md"};function c(p,t,u,d,m,f){return l(),n("div",null,t[0]||(t[0]=[s('<h1 id="esc-calibration" tabindex="-1">ESC Calibration <a class="header-anchor" href="#esc-calibration" aria-label="Permalink to &quot;ESC Calibration&quot;">​</a></h1><div class="info custom-block"><p class="custom-block-title">INFO</p><p>These instructions are only relevant to <a href="./../peripherals/pwm_escs_and_servo.html">PWM ESCs</a> and <a href="./../peripherals/oneshot.html">OneShot ESCs</a>. <a href="./../peripherals/dshot.html">DShot</a> and <a href="./../can/">CAN</a> ESCs (<a href="./../dronecan/escs.html">DroneCAN</a>/Cyphal) do not require this kind of calibration.</p></div><p>Electronic Speed Controllers (ESCs) regulate motor speed (and direction) based on their input command from the flight controller (FC). The range of input commands to which an ESC will respond is often configurable, and the default range can differ even between ESCs of the same model.</p><p>This calibration updates all the ESCs with a fixed maximum (2000us) and minimum (1000us) PWM input from the flight controller. Subsequently all the ESCs/motors on the vehicle will respond to flight controller input in the same way, across the whole input range.</p><p>Calibration using this tool is recommended for all PWM/OneShot ESCs that support it.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Calibration is particularly important for low-cost ESC, as they commonly vary a lot in their response to input.</p><p>However it is also recommended for high-quality controllers. Even though these are factory calibrated and should all respond the same way, in practice the input range can differ. For example if a controller has been manually calibrated after leaving the factory it may no longer behave in the same way.</p></div><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>If you want to use an ESC that does not support this calibration, then it must be factory calibrated and respond consistently out of the box. This should be verified using <a href="./../config/actuators.html#actuator-testing">Actuator Testing</a>. Jump to the <a href="#actuatorconfig_step">actuator configuration step (7)</a> (which is still important).</p></div><p>OneShot ESCs should be <a href="./../peripherals/oneshot.html#px4-configuration">configured to use OneShot</a> before calibration. You should calibrate the ESCs after switching, even if you have previously calibrated.</p><h2 id="preconditions" tabindex="-1">Preconditions <a class="header-anchor" href="#preconditions" aria-label="Permalink to &quot;Preconditions&quot;">​</a></h2><p>The calibration sequence requires that you are able keep the flight controller powered while manually power-cyling the ESCs.</p><p>If using a Pixhawk flight controller, the recommended way to do this is to separately power the flight controller via USB, and connect/disconnect the battery to power the ESCs when needed. Flight control systems that can&#39;t power the autopilot via USB will need a <a href="#problem_power_module">different approach</a>.</p><p>If the battery is connected via a power module the calibration procedure can detect the battery connection and use it to drive the calibration sequence. If battery power can&#39;t be detected the calibration sequence is performed based on timeouts.</p><h2 id="steps" tabindex="-1">Steps <a class="header-anchor" href="#steps" aria-label="Permalink to &quot;Steps&quot;">​</a></h2><p>To calibrate the ESCs:</p><ol><li><p>Remove the propellers.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Never attempt ESC calibration with propellers on!</p><p>The motors <em>should</em> not spin during ESC calibration. However if calibration starts when the ESC is already powered, or if the ESC doesn&#39;t properly support/detect the calibration sequence, then it will respond to the PWM input by running the motor at maximum speed.</p></div></li><li><p>Map the ESCs you&#39;re calibrating as motors in the vehicle&#39;s <a href="./../config/actuators.html">Actuator Configuration</a>. Only mapped actuators get an output and only ESCs mapped as motors will be calibrated.</p></li><li><p>Unpower the ESCs by unplugging the battery. The flight controller must stay powered, for example, by keeping the USB connected to the ground station.</p></li><li><p>Open the <em>QGroundControl</em> <strong>Settings &gt; Power</strong>, then press the <strong>Calibrate</strong> button.</p><p><img src="'+e+'" alt="ESC Calibration step 1"></p></li><li><p>After starting the calibration sequence without error, directly power the ESCs (you should be prompted):</p><p><img src="'+o+'" alt="ESC Calibration step 2"></p><p>The calibration will begin automatically:</p><p><img src="'+a+'" alt="ESC Calibration step 3"></p></li><li><p>During the calibration you should hear model-specific beeping from the ESC, which indicates the individual steps of the calibration.</p><p>You will be prompted when the calibration completes.</p><p><a id="actuatorconfig_step"></a><img src="'+i+'" alt="ESC Calibration step 4"></p></li><li><p>Go back to the <a href="./../config/actuators.html">Actuator Configuration</a> section.</p><p>Following ESC calibration all motors with the same (re)calibrated ESCs should behave in the same way for the same inputs. The default PWM settings for motor outputs in the actuator configuration should now work out of the box.</p><p>You need to confirm that the motors do indeed work correctly. Since the default configuration values have been set conservatively, you may also wish to tune them for your particular ESCs.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The steps below are similar to those described in <a href="./../config/actuators.html#motor-configuration">Actuator Configuration &gt; Motor Configuration</a>.</p></div><p>Verify the following values:</p><ul><li><p>The minimum value for a motor (default: <code>1100us</code>) should make the motor spin slowly but reliably, and also spin up reliably after it was stopped.</p><p>You can confirm that a motor spins at minimum (still without propellers) in <a href="./../config/actuators.html#actuator-testing">Actuator Testing</a>, by enabling the sliders, and then moving the test output slider for the motor to the first snap position from the bottom. The correct value should make the motor spin immediately and reliably as you move the slider from disarmed to minimum.</p><p>To find the &quot;optimal&quot; minimum value, move the slider to the bottom (disarmed). Then increase the PWM output&#39;s <code>disarmed</code> setting in small increments (e.g. 1025us, 1050us, etc), until the motor starts to spin reliably (it is better to be a little too high than a little too low). Enter this value into the <code>minimum</code> setting for all the motor PWM outputs, and restore the <code>disarmed</code> output to <code>1100us</code>.</p></li><li><p>The maximum value for a motor (default: <code>1900us</code>) should be chosen such that increasing the value doesn&#39;t make the motor spin any faster.</p><p>You can confirm that the motor spins quickly at the maximum setting in <a href="./../config/actuators.html#actuator-testing">Actuator Testing</a>, by moving the associated test output slider to the top position.</p><p>To find the &quot;optimal&quot; maximum value, first move the slider to the bottom (disarmed). Then increase the PWM output&#39;s <code>disarmed</code> setting to near the default maximum (<code>1900</code>) - the motors should spin up. Listen to the tone of the motor as you increase the PWM maximum value for the output in increments (e.g. 1925us, 1950us, etc). The optimal value is found at the point when the sound of the motors does not change as you increase the value of the output. Enter this value into the <code>maximum</code> setting for all the motor PWM outputs, and restore the <code>disarmed</code> output to <code>1100us</code>.</p></li><li><p>The disarmed value for a motor (default: <code>1000us</code>) should make the motor stop and stay stopped.</p><p>You can confirm this in <a href="./../config/actuators.html#actuator-testing">Actuator Testing</a> by moving the test output slider to the snap position at the bottom of the slider and observing that the motor does not spin.</p><p>If the ESC spins with the default value of 1000us then the ESC is not properly calibrated. If using an ESC that can&#39;t be calibrated, you should reduce the PWM output value for the output to below where the motor does not spin anymore (such as 950us or 900us).</p></li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>VTOL and fixed-wing motors do not need any special PWM configuration. With the default PWM configuration they will automatically stop during flight when commanded by the autopilot.</p></div></li></ol><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h2><ol><li><p>Calibration can state that it has succeeded even when it has failed.</p><p>This happens if you do not power the ESC at the right time, or the ESCs don&#39;t support calibration. This occurs because PX4 has no feedback from the ESC to know whether or not calibration was successful. You have to rely on interpreting the beeps during the calibration and subsequent motor tests to know for sure that the calibration worked.</p><p><a id="problem_power_module"></a></p></li><li><p>Calibration cannot be started if you have a power module configured and connected (for safety reasons).</p><p>Unplug power to the ESCs first. If you&#39;re blocked because a power module is necessary to keep your flight controller alive, but you can (un)power the ESCs separately, you can temporarily disable the detection of the power module just for the ESC calibration using the parameters <a href="./../advanced_config/parameter_reference.html#BAT1_SOURCE">BATn_SOURCE</a>. Once the power module that&#39;s powering the autopilot is not detected as battery anymore a timing based calibration is possible.</p></li><li><p>PX4 will abort calibration (for safety reasons) if the system detects an increase in current consumption immediately after initiating calibration. This requires a power module.</p></li></ol>',17)]))}const w=r(h,[["render",c]]);export{y as __pageData,w as default};
