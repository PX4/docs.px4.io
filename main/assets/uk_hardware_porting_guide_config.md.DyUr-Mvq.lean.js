import{_ as a,a as n}from"./chunks/kconfig-guiconfig.DrvYOidL.js";import{_ as o,c as i,a8 as s,o as t}from"./chunks/framework.BDnHobkS.js";const m=JSON.parse('{"title":"PX4 Конфігурація плати (kconfig)","description":"","frontmatter":{},"headers":[],"relativePath":"uk/hardware/porting_guide_config.md","filePath":"uk/hardware/porting_guide_config.md"}'),c={name:"uk/hardware/porting_guide_config.md"};function l(p,e,r,d,u,f){return t(),i("div",null,e[0]||(e[0]=[s(`<h1 id="px4-конфігурація-плати-kconfig" tabindex="-1">PX4 Конфігурація плати (kconfig) <a class="header-anchor" href="#px4-конфігурація-плати-kconfig" aria-label="Permalink to &quot;PX4 Конфігурація плати (kconfig)&quot;">​</a></h1><p>The PX4 Autopilot firmware can be configured at build time to adapt it for specialized applications (fixed-wing, multicopter, rover or more), to enable new and experimental features (such as Cyphal) or to save flash &amp; RAM usage by disabling some drivers and subsystems. This configuration is handled through <em>Kconfig</em>, which is the same <a href="./../hardware/porting_guide_nuttx.html#nuttx-menuconfig-setup">configuration system used by NuttX</a>.</p><p>The configuration options (often referred as &quot;symbols&quot; by the <em>kconfig</em> language) are defined in <code>Kconfig</code> files under the <strong>/src</strong> directory.</p><h2 id="конвенція-наименування-символів-px4-kconfig" tabindex="-1">Конвенція найменування символів PX4 Kconfig <a class="header-anchor" href="#конвенція-наименування-символів-px4-kconfig" aria-label="Permalink to &quot;Конвенція найменування символів PX4 Kconfig&quot;">​</a></h2><p>За умовчанням символи для модулів/драйверів називаються на основі шляху папки модуля. For example, the symbol for the ADC driver at <code>src/drivers/adc/board_adc</code> must be named <code>DRIVERS_ADC_BOARD_ADC</code>.</p><p>Для додавання символів для параметрів, специфічних для драйвера/модуля, конвенція найменування полягає в тому, що за ім&#39;ям модуля слідує назва параметра. For example <code>UAVCAN_V1_GNSS_PUBLISHER</code> which is an option <code>GNSS_PUBLISHER</code> for the <code>UAVCAN_V1</code> module. The options have to be guarded behind an <code>if</code> statement to ensure that the options are only visible when the module itself is enabled.</p><p>Наприклад:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>menuconfig DRIVERS_UAVCAN_V1</span></span>
<span class="line"><span>    bool &quot;UAVCANv1&quot;</span></span>
<span class="line"><span>    default n</span></span>
<span class="line"><span>    ---help---</span></span>
<span class="line"><span>        Enable support for UAVCANv1</span></span>
<span class="line"><span></span></span>
<span class="line"><span>if DRIVERS_UAVCAN_V1</span></span>
<span class="line"><span>    config UAVCAN_V1_GNSS_PUBLISHER</span></span>
<span class="line"><span>        bool &quot;GNSS Publisher&quot;</span></span>
<span class="line"><span>        default n</span></span>
<span class="line"><span>endif #DRIVERS_UAVCAN_V1</span></span></code></pre></div><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Builds will silently ignore any missing or miss-spelled modules in the <code>*.px4board</code> configuration file.</p></div><h2 id="успадкування-мітки-px4-kconfig" tabindex="-1">Успадкування мітки PX4 Kconfig <a class="header-anchor" href="#успадкування-мітки-px4-kconfig" aria-label="Permalink to &quot;Успадкування мітки PX4 Kconfig&quot;">​</a></h2><p>Each PX4 board must have a <code>default.px4board</code> configuration and can have an optional <code>bootloader.px4board configuration</code>. However you can add also separate configurations under a different label e.g. <code>cyphal.px4board</code>. Note that by default the configuration of <code>cyphal.px4board</code> inherits all settings set in <code>default.px4board</code>. When changing the <code>cyphal.px4board</code> it only stores the delta of the Kconfig keys that are different compared to <code>default.px4board</code>, this is useful to simplify configurations management</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>When modifying a Kconfig key in <code>default.px4board</code> it will be modified in all derivative configurations of the same board that had the same config as well.</p></div><h2 id="налаштування-меню-px4-menuconfig" tabindex="-1">Налаштування меню PX4 Menuconfig <a class="header-anchor" href="#налаштування-меню-px4-menuconfig" aria-label="Permalink to &quot;Налаштування меню PX4 Menuconfig&quot;">​</a></h2><p>The <a href="https://pypi.org/project/kconfiglib/#menuconfig-interfaces" target="_blank" rel="noreferrer">menuconfig</a> tool is used to modify the PX4 board configuration, adding/removing modules, drivers, and other features.</p><p>Існують варіанти командного рядка та графічного інтерфейсу, які можна запустити за допомогою скорочень збірки PX4:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>make px4_fmu-v5_default boardconfig</span></span>
<span class="line"><span>make px4_fmu-v5_default boardguiconfig</span></span></code></pre></div><div class="info custom-block"><p class="custom-block-title">INFO</p><p><em>Kconfiglib</em> and <em>menuconfig</em> come with the <em>kconfiglib</em> python package, which is installed by the normal <a href="https://github.com/PX4/PX4-Autopilot/blob/main/Tools/setup/ubuntu.sh" target="_blank" rel="noreferrer">ubuntu.sh</a> installation script. If <em>kconfiglib</em> is not installed, you can do so using the command: <code>pip3 install kconfiglib</code></p></div><p>Інтерфейс командного рядка та графічного інтерфейсу показані нижче.</p><h3 id="інтерфеис-командного-рядка-menuconfig" tabindex="-1">Інтерфейс командного рядка menuconfig <a class="header-anchor" href="#інтерфеис-командного-рядка-menuconfig" aria-label="Permalink to &quot;Інтерфейс командного рядка menuconfig&quot;">​</a></h3><p><img src="`+a+'" alt="menuconfig command line interface"></p><h3 id="інтерфеис-користувача-menuconfig-gui" tabindex="-1">Інтерфейс користувача menuconfig GUI <a class="header-anchor" href="#інтерфеис-користувача-menuconfig-gui" aria-label="Permalink to &quot;Інтерфейс користувача menuconfig GUI&quot;">​</a></h3><p><img src="'+n+'" alt="menuconfig GUI interface"></p>',22)]))}const b=o(c,[["render",l]]);export{m as __pageData,b as default};
